*** Begin Patch
*** Update File: lib/features/rank/rank_screen.dart
@@
           ] else
             ...next.requirements.map(
               (req) => Padding(
                 padding: const EdgeInsets.only(bottom: 10),
                 child: Container(
@@
                       const SizedBox(width: 10),
                       Expanded(
                         child: Text(
-                          req,
+                          _localizedRequirement(req),
                           style: AppTypography.callout.copyWith(
                             fontSize: 14,
                             color: AppColors.secondaryLabel.withValues(
                               alpha: 0.85,
*** End Patch