*** Begin Patch
*** Update File: lib/features/profile/profile_screen.dart
@@
 class _ProfileScreenState extends State<ProfileScreen> {
+  static const Set<String> _availableLanguageCodes = {
+    'en',
+    'fr',
+    'zh',
+    'hi',
+    'es',
+  };
   static const List<_LanguageOption> _languages = [
     _LanguageOption(code: 'en', label: 'English', nativeLabel: 'English'),
     _LanguageOption(code: 'fr', label: 'French', nativeLabel: 'Français'),
     _LanguageOption(code: 'zh', label: 'Chinese', nativeLabel: '中文'),
     _LanguageOption(code: 'hi', label: 'Hindi', nativeLabel: 'हिन्दी'),
@@
   Future<void> _showLanguagePicker() async {
     final languageProvider = context.read<LanguageProvider>();
     final current = languageProvider.languageCode.toLowerCase();
     final selected = await _showSelectionSheet(
       title: _t('language'),
       subtitle: _t('language_picker_subtitle'),
       options: [
         for (final language in _languages)
           _SelectionOption(
             id: language.code,
             title: language.nativeLabel,
-            subtitle: language.label,
+            subtitle: _availableLanguageCodes.contains(language.code)
+                ? language.label
+                : '${language.label} • ${_t('language_not_available')}',
             selected: language.code == current,
+            disabled: !_availableLanguageCodes.contains(language.code),
           ),
       ],
     );
 
     if (selected == null || selected == current) return;
+    if (!_availableLanguageCodes.contains(selected)) return;
     setState(() => _isSettingsSaving = true);
     try {
       // Persist locally and to cloud in a single deterministic path.
       // This prevents cloud values from overriding local selection on relaunch.
@@
 class _SelectionOption {
   final String id;
   final String title;
   final String subtitle;
   final bool selected;
   final bool isPremium;
+  final bool disabled;
 
   const _SelectionOption({
     required this.id,
     required this.title,
     required this.subtitle,
     required this.selected,
     this.isPremium = false,
+    this.disabled = false,
   });
 }
*** End Patch