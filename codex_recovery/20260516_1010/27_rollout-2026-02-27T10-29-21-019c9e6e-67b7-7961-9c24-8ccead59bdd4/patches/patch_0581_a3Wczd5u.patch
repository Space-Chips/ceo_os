*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
   @override
   Widget build(BuildContext context) {
-    return Row(
-      children: [
-        _ShortcutCircle(
-          onTap: onOpenMenu,
-          icon: CupertinoIcons.person_fill,
-          iconColor: AppColors.secondaryLabel,
-          gradientColors: AppColors.floatingGlassGradient,
-        ),
-        const SizedBox(width: 10),
-        _ShortcutPill(
-          onTap: onOpenRank,
-          label: rankName,
-          textColor: AppColors.warning,
-        ),
-        const SizedBox(width: 10),
-        _ShortcutCircle(
-          onTap: onOpenFocus,
-          icon: CupertinoIcons.bolt_fill,
-          iconColor: CupertinoColors.white,
-          gradientColors: const [Color(0xFFFF8E5A), Color(0xFFFF664B)],
-          glowColor: const Color(0x55FF8A5B),
-        ),
-        const SizedBox(width: 10),
-        _ShortcutCircle(
-          onTap: onOpenNotes,
-          icon: CupertinoIcons.doc_text_fill,
-          iconColor: CupertinoColors.white,
-          gradientColors: const [Color(0xFF8E63FF), Color(0xFFB05CFF)],
-          glowColor: const Color(0x558E63FF),
-        ),
-        const SizedBox(width: 10),
-        _StreakPill(
-          onTap: onOpenStreak,
-          streakValue: winStreak,
-        ),
-      ],
+    return ClipRRect(
+      borderRadius: BorderRadius.circular(18),
+      child: BackdropFilter(
+        filter: ImageFilter.blur(sigmaX: 12, sigmaY: 12),
+        child: Container(
+          padding: const EdgeInsets.fromLTRB(10, 8, 10, 6),
+          decoration: BoxDecoration(
+            borderRadius: BorderRadius.circular(18),
+            gradient: LinearGradient(
+              begin: Alignment.topCenter,
+              end: Alignment.bottomCenter,
+              colors: [
+                AppColors.background.withValues(alpha: 0.14),
+                AppColors.background.withValues(alpha: 0.03),
+              ],
+            ),
+          ),
+          child: Row(
+            children: [
+              _ShortcutCircle(
+                onTap: onOpenMenu,
+                icon: CupertinoIcons.person_fill,
+                iconColor: AppColors.secondaryLabel,
+                glowColor: AppColors.edgeGlowSoft,
+              ),
+              const SizedBox(width: 10),
+              _ShortcutPill(
+                onTap: onOpenRank,
+                label: rankName,
+                levelText: 'LEVEL $rankLevel',
+              ),
+              const Spacer(),
+              _ActionCapsule(
+                onTap: onOpenFocus,
+                icon: CupertinoIcons.bolt_fill,
+                iconColor: AppColors.accent,
+              ),
+              const SizedBox(width: 8),
+              _ActionCapsule(
+                onTap: onOpenNotes,
+                icon: CupertinoIcons.doc_text_fill,
+                iconColor: AppColors.accent,
+              ),
+              const SizedBox(width: 8),
+              _StreakPill(
+                onTap: onOpenStreak,
+                streakValue: winStreak,
+              ),
+            ],
+          ),
+        ),
+      ),
     );
   }
 }
*** End Patch
