*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/stats_engine.dart
@@
   Future<void> backfillRecent({int days = 30}) async {
     final now = DateTime.now();
     final start = _day(now.subtract(Duration(days: days - 1)));
     for (var i = 0; i < days; i++) {
       final date = start.add(Duration(days: i));
       await recomputeForDate(date);
     }
   }
+
+  Future<void> hydrateHistoricalStats({int days = 90}) async {
+    final uid = _repository.currentUserId;
+    if (uid == null) return;
+
+    final end = _day(DateTime.now());
+    final start = end.subtract(Duration(days: days - 1));
+
+    await _syncFocusSessions(start: start, end: end);
+    await _syncCeoSessions(start: start, end: end);
+    await _syncHabitSchedule(start: start, end: end);
+    await _syncScreenTimeLogs(start: start, end: end);
+    await recordRankChangedFromSnapshot();
+    await backfillRecent(days: days);
+  }
+
+  Future<void> recordRankChangedFromSnapshot() async {
+    final snapshot = await _repository.getRankSnapshot();
+    if (snapshot == null) return;
+    final rankName = (snapshot['rank_name'] as String?)?.trim();
+    if (rankName == null || rankName.isEmpty) return;
+    await recordRankChanged(
+      rankName: rankName,
+      rankLevel: (snapshot['rank_level'] as num?)?.toInt(),
+      rankProgressPercent: _inferRankProgress(snapshot),
+    );
+  }
+
+  Future<void> _syncFocusSessions({
+    required DateTime start,
+    required DateTime end,
+  }) async {
+    final rows = await _repository.getSourceFocusSessions(
+      start: start,
+      end: end.add(const Duration(days: 1)),
+    );
+
+    for (final row in rows) {
+      final id = (row['id'] as String?)?.trim();
+      final startedAt = DateTime.tryParse((row['start_time'] ?? '').toString());
+      if (id == null || id.isEmpty || startedAt == null) continue;
+
+      final duration = _int(row['duration_minutes']);
+      await trackEvent(
+        StatsEventType.focusSessionStarted,
+        eventTime: startedAt,
+        recompute: false,
+        payload: {
+          'planned_duration_minutes': duration,
+          if (row['block_list_id'] != null) 'block_list_id': row['block_list_id'],
+        },
+        sourceKey: 'focus_started:$id',
+      );
+
+      final completed = _bool(row['completed'], fallback: true);
+      final endedAt =
+          DateTime.tryParse((row['end_time'] ?? '').toString()) ??
+          startedAt.add(Duration(minutes: duration));
+
+      await trackEvent(
+        completed
+            ? StatsEventType.focusSessionCompleted
+            : StatsEventType.focusSessionBroken,
+        eventTime: endedAt,
+        recompute: false,
+        payload: {
+          completed ? 'duration_minutes' : 'elapsed_minutes': duration,
+          if (row['block_list_id'] != null) 'block_list_id': row['block_list_id'],
+        },
+        sourceKey: 'focus_result:$id',
+      );
+    }
+  }
+
+  Future<void> _syncCeoSessions({
+    required DateTime start,
+    required DateTime end,
+  }) async {
+    final rows = await _repository.getSourceCeoSessions(
+      start: start,
+      end: end.add(const Duration(days: 1)),
+    );
+
+    for (final row in rows) {
+      final id = (row['id'] as String?)?.trim();
+      final startedAt = DateTime.tryParse((row['start_time'] ?? '').toString());
+      if (id == null || id.isEmpty || startedAt == null) continue;
+
+      final duration = _int(row['duration_minutes']);
+      await trackEvent(
+        StatsEventType.ceoSessionStarted,
+        eventTime: startedAt,
+        recompute: false,
+        payload: {
+          'planned_duration_minutes': duration,
+          'approved_apps_count': _int(row['approved_apps_count']),
+        },
+        sourceKey: 'ceo_started:$id',
+      );
+
+      final endedAt =
+          DateTime.tryParse((row['end_time'] ?? '').toString()) ??
+          startedAt.add(Duration(minutes: duration));
+      final completed =
+          row['end_time'] != null && _bool(row['active'], fallback: false) == false;
+
+      await trackEvent(
+        completed ? StatsEventType.ceoSessionCompleted : StatsEventType.ceoSessionBroken,
+        eventTime: endedAt,
+        recompute: false,
+        payload: {
+          completed ? 'duration_minutes' : 'elapsed_minutes': duration,
+          'approved_apps_count': _int(row['approved_apps_count']),
+        },
+        sourceKey: 'ceo_result:$id',
+      );
+    }
+  }
+
+  Future<void> _syncHabitSchedule({
+    required DateTime start,
+    required DateTime end,
+  }) async {
+    final habits = await _repository.getSourceHabits();
+    final completions = await _repository.getSourceHabitCompletions(start: start, end: end);
+
+    final completionByKey = <String, Map<String, dynamic>>{
+      for (final completion in completions)
+        '${completion['habit_id']}:${completion['date']}': completion,
+    };
+
+    for (final habit in habits) {
+      final habitId = (habit['id'] as String?)?.trim();
+      final createdAt = DateTime.tryParse((habit['created_at'] ?? '').toString());
+      if (habitId == null || habitId.isEmpty || createdAt == null) continue;
+
+      for (var cursor = start; !cursor.isAfter(end); cursor = cursor.add(const Duration(days: 1))) {
+        final day = _day(cursor);
+        if (day.isBefore(_day(createdAt))) continue;
+        if (!_isHabitScheduledOn(habit, day)) continue;
+
+        final dateKey = _fmtDay(day);
+        await trackEvent(
+          StatsEventType.habitPlanned,
+          eventTime: day,
+          recompute: false,
+          payload: {'habit_id': habitId},
+          sourceKey: 'habit_planned:$habitId:$dateKey',
+        );
+
+        final completion = completionByKey['$habitId:$dateKey'];
+        if (completion == null) continue;
+
+        await trackEvent(
+          _bool(completion['completed'], fallback: false)
+              ? StatsEventType.habitCompleted
+              : StatsEventType.habitMissed,
+          eventTime:
+              DateTime.tryParse((completion['checked_in_date'] ?? '').toString()) ??
+              day,
+          recompute: false,
+          payload: {'habit_id': habitId},
+          sourceKey: 'habit_result:${completion['id']}',
+        );
+      }
+    }
+  }
+
+  Future<void> _syncScreenTimeLogs({
+    required DateTime start,
+    required DateTime end,
+  }) async {
+    final logs = await _repository.getSourceScreenTimeLogs(start: start, end: end);
+    final totalByDay = <DateTime, int>{};
+    final distractingByDay = <DateTime, int>{};
+    final productiveByDay = <DateTime, int>{};
+
+    for (final log in logs) {
+      final date = _parseLooseDate(log['date']) ?? _day(DateTime.now());
+      final day = _day(date);
+      final minutes = ((_int(log['duration_seconds'])) / 60).round();
+      totalByDay[day] = (totalByDay[day] ?? 0) + minutes;
+
+      final entityType = (log['entity_type'] as String?)?.toLowerCase().trim() ?? '';
+      final entityName = (log['entity_name'] as String?)?.toLowerCase().trim() ?? '';
+      if (_looksProductive(entityType, entityName)) {
+        productiveByDay[day] = (productiveByDay[day] ?? 0) + minutes;
+      } else {
+        distractingByDay[day] = (distractingByDay[day] ?? 0) + minutes;
+      }
+    }
+
+    for (final entry in totalByDay.entries) {
+      final day = entry.key;
+      final key = _fmtDay(day);
+      await trackEvent(
+        StatsEventType.screenTimeRecorded,
+        eventTime: day,
+        recompute: false,
+        payload: {'total_minutes': entry.value, 'is_total': true},
+        sourceKey: 'screen_time_total:$key',
+      );
+
+      final productive = productiveByDay[day] ?? 0;
+      if (productive > 0) {
+        await trackEvent(
+          StatsEventType.productiveTimeRecorded,
+          eventTime: day,
+          recompute: false,
+          payload: {'minutes': productive, 'is_total': true},
+          sourceKey: 'screen_time_productive:$key',
+        );
+      }
+
+      final distracting = distractingByDay[day] ?? 0;
+      if (distracting > 0) {
+        await trackEvent(
+          StatsEventType.distractingTimeRecorded,
+          eventTime: day,
+          recompute: false,
+          payload: {'minutes': distracting, 'is_total': true},
+          sourceKey: 'screen_time_distracting:$key',
+        );
+      }
+    }
+  }
*** End Patch
