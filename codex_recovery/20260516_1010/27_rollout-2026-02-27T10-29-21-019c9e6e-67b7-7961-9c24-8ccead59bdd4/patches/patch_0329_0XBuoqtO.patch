*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/family_time_screen.dart
@@
                 child: CupertinoTextField(
                   controller: _newGroupCtrl,
                   placeholder: 'New group name',
+                  onSubmitted: (_) => _createGroup(),
                   style: AppTypography.mono.copyWith(fontSize: 12),
@@
                 padding: const EdgeInsets.symmetric(
                   horizontal: 12,
                   vertical: 10,
                 ),
-                color: Colors.white,
+                color: _primaryActionBg,
                 borderRadius: BorderRadius.circular(10),
                 onPressed: _saving ? null : _createGroup,
                 child: Text(
                   'Create',
                   style: AppTypography.mono.copyWith(
                     fontSize: 12,
                     fontWeight: FontWeight.w900,
-                    color: Colors.black,
+                    color: _primaryActionFg,
                   ),
                 ),
               ),
@@
                 child: CupertinoTextField(
                   controller: _joinTokenCtrl,
                   placeholder: 'Join with invite token',
                   textCapitalization: TextCapitalization.characters,
+                  onSubmitted: (_) => _joinByToken(),
                   style: AppTypography.mono.copyWith(fontSize: 12),
@@
               bundle.group.name,
               style: AppTypography.mono.copyWith(
                 fontSize: 12,
-                color: selected ? Colors.white : AppColors.secondaryLabel,
+                color: selected ? AppColors.label : AppColors.secondaryLabel,
                 fontWeight: FontWeight.w800,
               ),
             ),
@@
           CupertinoButton(
             padding: const EdgeInsets.symmetric(vertical: 10),
             color: _isAdmin
-                ? Colors.white
+                ? _primaryActionBg
                 : AppColors.backgroundLight.withValues(alpha: 0.6),
@@
               style: AppTypography.mono.copyWith(
                 fontSize: 12,
                 fontWeight: FontWeight.w900,
-                color: _isAdmin ? Colors.black : AppColors.tertiaryLabel,
+                color: _isAdmin ? _primaryActionFg : AppColors.tertiaryLabel,
               ),
             ),
           ),
@@
                 CupertinoButton(
                   padding: const EdgeInsets.symmetric(
                     horizontal: 10,
                     vertical: 9,
                   ),
-                  color: Colors.white,
+                  color: _primaryActionBg,
                   borderRadius: BorderRadius.circular(10),
@@
                   child: Text(
                     'Invite',
                     style: AppTypography.mono.copyWith(
                       fontSize: 12,
-                      color: Colors.black,
+                      color: _primaryActionFg,
                       fontWeight: FontWeight.w900,
                     ),
                   ),
                 ),
@@
               CupertinoButton(
                 padding: const EdgeInsets.symmetric(
                   horizontal: 10,
                   vertical: 9,
                 ),
-                color: _isAdmin ? Colors.white : AppColors.backgroundLight,
+                color: _isAdmin ? _primaryActionBg : AppColors.backgroundLight,
                 borderRadius: BorderRadius.circular(10),
@@
                 child: Text(
                   '+ App',
                   style: AppTypography.mono.copyWith(
                     fontSize: 12,
-                    color: _isAdmin ? Colors.black : AppColors.tertiaryLabel,
+                    color: _isAdmin ? _primaryActionFg : AppColors.tertiaryLabel,
                     fontWeight: FontWeight.w900,
                   ),
                 ),
               ),
@@
               CupertinoButton(
                 padding: const EdgeInsets.symmetric(
                   horizontal: 10,
                   vertical: 9,
                 ),
-                color: _isAdmin ? Colors.white : AppColors.backgroundLight,
+                color: _isAdmin ? _primaryActionBg : AppColors.backgroundLight,
                 borderRadius: BorderRadius.circular(10),
@@
                 child: Text(
                   '+ Site',
                   style: AppTypography.mono.copyWith(
                     fontSize: 12,
-                    color: _isAdmin ? Colors.black : AppColors.tertiaryLabel,
+                    color: _isAdmin ? _primaryActionFg : AppColors.tertiaryLabel,
                     fontWeight: FontWeight.w900,
                   ),
                 ),
               ),
*** End Patch
