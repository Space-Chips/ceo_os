*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/screen_time_manager_screen.dart
@@
         padding: const EdgeInsets.all(22),
         decoration: _moduleDecoration(
           radius: 22,
-          overlay: const Color(0xFFFFB450).withValues(alpha: 0.05),
+          overlay: AppColors.ambientTint.withValues(alpha: 0.08),
         ),
@@
                     style: AppTypography.callout.copyWith(
                       fontSize: 14,
-                      color: const Color(0xFFFFB347),
+                      color: AppColors.rankAccent,
                       fontWeight: FontWeight.w500,
                     ),
@@
               decoration: BoxDecoration(
                 shape: BoxShape.circle,
-                color: const Color(0xFFFFB450).withValues(alpha: 0.15),
+                color: AppColors.accentSurfaceSoft,
                 border: Border.all(
-                  color: const Color(0xFFFFB450).withValues(alpha: 0.35),
+                  color: AppColors.activeBorder,
                   width: 1,
                 ),
               ),
-              child: const Icon(
+              child: Icon(
                 CupertinoIcons.bolt,
-                color: Color(0xFFFFB347),
+                color: AppColors.accentIcon,
                 size: 28,
               ),
@@
                   label: 'TODAY',
                   value: '$todayMinutes',
                   unit: 'minutes',
-                  accent: const Color(0xFF60A5FA),
+                  accent: AppColors.scoreValue,
                   highlighted: true,
                 ),
@@
                   label: '7-DAY',
                   value: '$avg7dMinutes',
                   unit: 'minutes',
-                  accent: const Color(0xFFE2E8F0),
+                  accent: AppColors.secondaryLabel,
                 ),
@@
                   icon: CupertinoIcons.rosette,
-                  iconColor: const Color(0xFFFACC15),
+                  iconColor: AppColors.rankAccent,
                   label: 'RANK',
                   value: rankName,
-                  valueColor: const Color(0xFFFACC15),
+                  valueColor: AppColors.rankAccent,
                   onTap: () => context.push('/rank'),
                 ),
@@
         borderColor: highlighted
-            ? const Color(0xFF508CFF).withValues(alpha: 0.35)
-            : AppColors.white.withValues(alpha: 0.06),
+            ? AppColors.selectionOutline
+            : AppColors.border,
         overlay: highlighted
-            ? const Color(0xFF508CFF).withValues(alpha: 0.06)
+            ? AppColors.accentSurfaceSoft
             : null,
       ),
@@
-      gradient: const LinearGradient(
+      gradient: LinearGradient(
         begin: Alignment.topCenter,
         end: Alignment.bottomCenter,
-        colors: [Color(0xFF1A1A1A), Color(0xFF111111)],
+        colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
       ),
       border: Border.all(
-        color: borderColor ?? AppColors.white.withValues(alpha: 0.06),
+        color: borderColor ?? AppColors.border,
         width: 1,
       ),
*** Update File: /Users/timo/ceo_os/lib/features/focus/focus_screen.dart
@@
                           decoration: BoxDecoration(
-                            gradient: const LinearGradient(
+                            gradient: LinearGradient(
                               begin: Alignment.topCenter,
                               end: Alignment.bottomCenter,
-                              colors: [Color(0xFF1A1A1A), Color(0xFF111111)],
+                              colors: [
+                                AppColors.cardBackgroundAlt,
+                                AppColors.cardBase,
+                              ],
                             ),
                             borderRadius: BorderRadius.circular(18),
                             border: Border.all(
                               color: provider.focusDurationMinutes == preset
-                                  ? const Color(0xFF508CFF).withValues(
-                                      alpha: 0.45,
-                                    )
-                                  : AppColors.white.withValues(alpha: 0.06),
+                                  ? AppColors.selectionOutline
+                                  : AppColors.border,
                               width: 1,
                             ),
@@
           decoration: BoxDecoration(
-            color: AppColors.white.withValues(alpha: 0.04),
+            color: AppColors.cardBackgroundAlt.withValues(alpha: 0.92),
             borderRadius: BorderRadius.circular(16),
             border: Border.all(
-              color: AppColors.white.withValues(alpha: 0.08),
+              color: AppColors.borderStrong,
               width: 1,
             ),
           ),
@@
           decoration: BoxDecoration(
-            color: const Color(0xFFFF5050).withValues(alpha: 0.08),
+            color: AppColors.error.withValues(alpha: 0.08),
             borderRadius: BorderRadius.circular(16),
             border: Border.all(
-              color: const Color(0xFFFF5050).withValues(alpha: 0.35),
+              color: AppColors.error.withValues(alpha: 0.35),
               width: 1,
             ),
           ),
@@
-              const Icon(
+              Icon(
                 CupertinoIcons.exclamationmark_triangle,
-                color: Color(0xFFFF6B6B),
+                color: AppColors.error,
                 size: 22,
               ),
@@
                       style: AppTypography.callout.copyWith(
                         fontSize: 16,
-                        color: const Color(0xFFFF8A8A),
+                        color: AppColors.error.withValues(alpha: 0.88),
                         fontWeight: FontWeight.w700,
                       ),
@@
                   icon: CupertinoIcons.rosette,
-                  color: const Color(0xFFFACC15),
+                  color: AppColors.rankAccent,
                   value: '${_streak?.longestStreak ?? 0}',
                   label: 'RECORD',
                 ),
@@
                   icon: CupertinoIcons.arrow_up_right,
-                  color: const Color(0xFF6EE7B7),
+                  color: AppColors.success,
                   value: '$successRate%',
                   label: 'SUCCESS',
                 ),
@@
     final accentColor = isBreak
-        ? const Color(0xFF7ED6A5)
+        ? AppColors.success
         : AppColors.accent;
     final progressColor = isBreak
-        ? const Color(0x997ED6A5)
-        : AppColors.themeGlow.withValues(alpha: 0.6);
+        ? AppColors.success.withValues(alpha: 0.6)
+        : AppColors.focusPrimary.withValues(alpha: 0.6);
@@
                             gradient: LinearGradient(
                               begin: Alignment.topCenter,
                               end: Alignment.bottomCenter,
                               colors: [
-                                const Color(0xFF1A1A1A),
-                                const Color(0xFF111111),
+                                AppColors.cardBackgroundAlt,
+                                AppColors.cardBase,
                               ],
                             ),
@@
             decoration: BoxDecoration(
-              gradient: const LinearGradient(
+              gradient: LinearGradient(
                 begin: Alignment.topCenter,
                 end: Alignment.bottomCenter,
-                colors: [Color(0xFF1A1A1A), Color(0xFF111111)],
+                colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
               ),
               borderRadius: BorderRadius.circular(20),
               border: Border.all(
-                color: AppColors.white.withValues(alpha: 0.08),
+                color: AppColors.borderStrong,
                 width: 1,
               ),
*** End Patch
