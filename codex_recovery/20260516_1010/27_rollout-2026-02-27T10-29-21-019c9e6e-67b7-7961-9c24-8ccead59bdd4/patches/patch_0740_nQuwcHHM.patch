*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/stats_screen.dart
@@
     });
 
     try {
+      final now = DateTime.now();
+      final yearStart = DateTime(now.year, 1, 1);
+      final yearEnd = DateTime(now.year, 12, 31);
       final results = await Future.wait<dynamic>([
         _engine.buildSnapshot(),
         _insightsRepository.getDashboardSnapshot(),
+        _habitRepository.getHabits(),
+        _habitRepository.getCompletionsForDateRange(yearStart, yearEnd),
       ]);
       final snapshot = results[0] as AdvancedStatsSnapshot;
       final dashboardSnapshot = results[1] as DashboardSnapshot;
+      final habits = results[2] as List<Habit>;
+      final habitCompletions = results[3] as List<HabitCompletion>;
       if (!mounted) return;
       setState(() {
         _snapshot = snapshot;
         _dashboardSnapshot = dashboardSnapshot;
+        _habits = habits;
+        _habitYearCompletions = habitCompletions;
         _loading = false;
         _refreshing = false;
       });
@@
   Future<void> _refreshInBackground() async {
     try {
+      final now = DateTime.now();
+      final yearStart = DateTime(now.year, 1, 1);
+      final yearEnd = DateTime(now.year, 12, 31);
       await _engine.hydrateHistoricalStatsIfNeeded(days: 120);
-      final refreshed = await _engine.buildSnapshot();
-      final refreshedDashboard = await _insightsRepository
-          .getDashboardSnapshot();
+      final results = await Future.wait<dynamic>([
+        _engine.buildSnapshot(),
+        _insightsRepository.getDashboardSnapshot(),
+        _habitRepository.getHabits(),
+        _habitRepository.getCompletionsForDateRange(yearStart, yearEnd),
+      ]);
+      final refreshed = results[0] as AdvancedStatsSnapshot;
+      final refreshedDashboard = results[1] as DashboardSnapshot;
+      final refreshedHabits = results[2] as List<Habit>;
+      final refreshedHabitCompletions = results[3] as List<HabitCompletion>;
       if (!mounted) return;
       setState(() {
         _snapshot = refreshed;
         _dashboardSnapshot = refreshedDashboard;
+        _habits = refreshedHabits;
+        _habitYearCompletions = refreshedHabitCompletions;
       });
*** End Patch
