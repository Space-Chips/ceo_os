*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
         child: AnimatedContainer(
           duration: const Duration(milliseconds: 180),
           width: 22,
           height: 22,
-          decoration: const BoxDecoration(
+          decoration: BoxDecoration(
             shape: BoxShape.circle,
-            color: Color(0xFF55D9A5),
+            color: AppColors.accent,
           ),
-          child: const Icon(
+          child: Icon(
             CupertinoIcons.checkmark,
             size: 12,
-            color: CupertinoColors.black,
+            color: AppColors.onAccent,
           ),
         ),
       );
@@
                             borderRadius: BorderRadius.circular(14),
                             color: AppColors.white.withValues(
                               alpha: selected ? 0.1 : 0.04,
                             ),
                             border: Border.all(
                               color: selected
-                                  ? const Color(0xFF55D9A5)
+                                  ? AppColors.selectionOutline
                                   : AppColors.white.withValues(alpha: 0.08),
                               width: 1,
                             ),
                           ),
                           child: Icon(
                             _goalIconData(iconName),
                             size: 32,
                             color: selected
-                                ? const Color(0xFF55D9A5)
+                                ? AppColors.accentIcon
                                 : AppColors.secondaryLabel,
                           ),
                         ),
@@
                               child: Icon(
                                 _goalIconData(meta.icon),
                                 size: 22,
-                                color: const Color(0xFF55D9A5),
+                                color: AppColors.accentIcon,
                               ),
@@
                                     decoration: BoxDecoration(
                                       shape: BoxShape.circle,
                                       color: prov.isHabitCompletedToday(habit.id)
-                                          ? const Color(0xFF55D9A5)
-                                          : const Color(0x00000000),
+                                          ? AppColors.accent
+                                          : Colors.transparent,
                                       border: prov.isHabitCompletedToday(habit.id)
                                           ? null
                                           : Border.all(
@@
                                     child: prov.isHabitCompletedToday(habit.id)
-                                        ? const Icon(
+                                        ? Icon(
                                             CupertinoIcons.checkmark,
                                             size: 12,
-                                            color: CupertinoColors.black,
+                                            color: AppColors.onAccent,
                                           )
                                         : null,
                                   ),
@@
                       Text(
                         '${_weeklyScore?.successPercentage ?? 0}%',
                         style: AppTypography.title2.copyWith(
                           fontSize: 24,
                           fontWeight: FontWeight.w700,
-                          color: const Color(0xFF55D9A5),
+                          color: AppColors.scoreValue,
                         ),
                       ),
@@
                       child: Text(
                         'UNLOCK_EDIT',
                         style: AppTypography.callout.copyWith(
                           fontSize: 13,
-                          color: const Color(0xFF55D9A5),
+                          color: AppColors.accent,
                           fontWeight: FontWeight.w600,
                         ),
                       ),
@@
                       data: SliderThemeData(
                         trackHeight: 6,
-                        activeTrackColor: const Color(0xFF55D9A5),
+                        activeTrackColor: AppColors.accent,
                         inactiveTrackColor: AppColors.white.withValues(alpha: 0.12),
-                        thumbColor: CupertinoColors.white,
-                        overlayColor: const Color(
-                          0xFF55D9A5,
-                        ).withValues(alpha: 0.12),
+                        thumbColor: AppColors.white,
+                        overlayColor: AppColors.accent.withValues(alpha: 0.12),
                         thumbShape: const RoundSliderThumbShape(
                           enabledThumbRadius: 11,
                         ),
@@
   Widget _contractPill({
     required String label,
     required String value,
     required bool positive,
   }) {
-    final color = positive ? const Color(0xFF55D9A5) : AppColors.error;
+    final color = positive ? AppColors.accent : AppColors.error;
@@
     decoration: BoxDecoration(
       color: AppColors.white.withValues(alpha: 0.04),
       borderRadius: BorderRadius.circular(14),
       border: Border.all(
         color: AppColors.white.withValues(alpha: 0.08),
@@
     final decoration = BoxDecoration(
       gradient: dark
           ? null
-          : const LinearGradient(
+          : LinearGradient(
               begin: Alignment.topCenter,
               end: Alignment.bottomCenter,
-              colors: [Color(0xFFE5E5E5), Color(0xFFCFCFCF)],
+              colors: AppColors.buttonGradient,
             ),
       color: dark ? AppColors.white.withValues(alpha: 0.06) : null,
@@
-    final textColor = dark ? AppColors.label : AppColors.background;
+    final textColor = dark ? AppColors.label : AppColors.onAccent;
@@
     final decoration =
         widget.decoration ??
         BoxDecoration(
-          gradient: const LinearGradient(
+          gradient: LinearGradient(
             begin: Alignment.topCenter,
             end: Alignment.bottomCenter,
-            colors: [Color(0xFFE5E5E5), Color(0xFFCFCFCF)],
+            colors: AppColors.buttonGradient,
           ),
           borderRadius: BorderRadius.circular(widget.borderRadius),
           border: Border.all(
@@
               style: AppTypography.callout.copyWith(
                 fontSize: 16,
                 fontWeight: FontWeight.w600,
-                color: widget.textColor ?? AppColors.background,
+                color: widget.textColor ?? AppColors.onAccent,
               ),
             ),
           ),
@@
   return BoxDecoration(
-    gradient: const LinearGradient(
+    gradient: LinearGradient(
       begin: Alignment.topCenter,
       end: Alignment.bottomCenter,
-      colors: [Color(0xFF1A1A1A), Color(0xFF111111)],
+      colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
     ),
     borderRadius: BorderRadius.circular(radius),
     border: Border.all(
*** End Patch
