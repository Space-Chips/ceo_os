*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
           ...topFive.asMap().entries.map((entry) {
             final index = entry.key;
             final task = entry.value;
+            final widthFactor = (1 - (index * 0.03)).clamp(0.88, 1.0);
             return Padding(
               padding: const EdgeInsets.only(bottom: 10),
               child: _priorityCard(
                 rank: index + 1,
                 task: task,
+                widthFactor: widthFactor,
                 onTap: () => onComplete(task),
               ),
             );
           }),
*** End Patch
