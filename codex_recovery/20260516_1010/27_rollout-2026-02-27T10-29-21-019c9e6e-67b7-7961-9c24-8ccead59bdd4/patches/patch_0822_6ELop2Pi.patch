*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
   Set<String> _activeApps = {..._defaultActiveApps};
   Set<String> _enabledShortcuts = {..._defaultEnabledShortcuts};
   bool _hideNoModulesMessage = false;
   int _winStreak = 0;
+  String? _lastDataSignature;
@@
   void _handleSourceDataChanged() {
     _refreshDebounce?.cancel();
     _refreshDebounce = Timer(const Duration(milliseconds: 280), () {
       if (!mounted) return;
       _reloadSnapshot();
       unawaited(_loadWinStreak());
     });
   }
+
+  void _scheduleRefreshFromSignature(String signature) {
+    if (_lastDataSignature == signature) return;
+    _lastDataSignature = signature;
+    WidgetsBinding.instance.addPostFrameCallback((_) {
+      if (!mounted) return;
+      _handleSourceDataChanged();
+    });
+  }
@@
     final pendingTasks = taskProvider.tasks.where((t) => !t.completed).length;
+    final completedTasks = taskProvider.tasks.where((t) => t.completed).length;
     final todayEventsCount = taskProvider.events
         .where((event) => event.eventDate == _formatDateKey(DateTime.now()))
         .length;
+    final dataSignature =
+        '${taskProvider.tasks.length}:$completedTasks:${taskProvider.events.length}:'
+        '${habitProvider.habits.length}:${habitProvider.completedToday}:'
+        '${taskProvider.isLoading}:${habitProvider.isLoading}';
+    _scheduleRefreshFromSignature(dataSignature);
@@
         gradientColors: [
           AppColors.dashboardGradientStart,
           AppColors.dashboardGradientEnd.withValues(alpha: 0.992),
         ],
         border: Border.all(
-          color: AppColors.borderStrong.withValues(alpha: 0.2),
-          width: 0.9,
+          color: AppColors.borderStrong.withValues(alpha: 0.14),
+          width: 0.82,
         ),
*** End Patch
