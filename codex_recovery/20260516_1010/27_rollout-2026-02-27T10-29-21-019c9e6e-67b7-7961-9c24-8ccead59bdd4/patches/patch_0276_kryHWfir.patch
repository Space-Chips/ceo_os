*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
 class _PrimaryAppsGrid extends StatelessWidget {
   final List<_PrimaryAppCardData> cards;
 
   const _PrimaryAppsGrid({required this.cards});
 
   @override
   Widget build(BuildContext context) {
-    final top = cards.take(2).toList();
-    final bottom = cards.skip(2).take(2).toList();
+    if (cards.length == 1) {
+      return _PrimaryAppCard(data: cards.first);
+    }
 
-    return Column(
-      children: [
-        Row(
-          children: [
-            Expanded(child: _PrimaryAppCard(data: top[0])),
-            const SizedBox(width: 12),
-            Expanded(child: _PrimaryAppCard(data: top[1])),
-          ],
-        ),
-        const SizedBox(height: 12),
-        Row(
-          children: [
-            Expanded(child: _PrimaryAppCard(data: bottom[0])),
-            const SizedBox(width: 12),
-            Expanded(child: _PrimaryAppCard(data: bottom[1])),
-          ],
-        ),
-      ],
-    );
+    if (cards.length == 2) {
+      return Row(
+        children: [
+          Expanded(child: _PrimaryAppCard(data: cards[0])),
+          const SizedBox(width: 12),
+          Expanded(child: _PrimaryAppCard(data: cards[1])),
+        ],
+      );
+    }
+
+    if (cards.length == 3) {
+      return Column(
+        children: [
+          Row(
+            children: [
+              Expanded(child: _PrimaryAppCard(data: cards[0])),
+              const SizedBox(width: 12),
+              Expanded(child: _PrimaryAppCard(data: cards[1])),
+            ],
+          ),
+          const SizedBox(height: 12),
+          _PrimaryAppCard(data: cards[2]),
+        ],
+      );
+    }
+
+    final byId = <String, _PrimaryAppCardData>{
+      for (final card in cards) card.moduleId: card,
+    };
+    final rowA = [byId['Pareto'], byId['Habits']];
+    final rowB = [byId['Calendar'], byId['ScreenTimeManager']];
+
+    Widget buildRow(List<_PrimaryAppCardData?> row) {
+      final enabled = row.whereType<_PrimaryAppCardData>().toList();
+      if (enabled.isEmpty) return const SizedBox.shrink();
+      if (enabled.length == 1) return _PrimaryAppCard(data: enabled.first);
+      return Row(
+        children: [
+          Expanded(child: _PrimaryAppCard(data: enabled[0])),
+          const SizedBox(width: 12),
+          Expanded(child: _PrimaryAppCard(data: enabled[1])),
+        ],
+      );
+    }
+
+    return Column(
+      children: [
+        buildRow(rowA),
+        if (rowA.whereType<_PrimaryAppCardData>().isNotEmpty &&
+            rowB.whereType<_PrimaryAppCardData>().isNotEmpty)
+          const SizedBox(height: 12),
+        buildRow(rowB),
+      ],
+    );
   }
 }
*** End Patch
