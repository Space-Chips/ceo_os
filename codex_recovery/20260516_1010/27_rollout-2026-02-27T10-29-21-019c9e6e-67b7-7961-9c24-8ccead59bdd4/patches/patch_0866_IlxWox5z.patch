*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/repositories/premium_repository.dart
@@
         launchPremiumLabelsEnabled: defaults.launchPremiumLabelsEnabled,
         grandfatheringEnabled: defaults.grandfatheringEnabled,
         earlyDiscountEnabled: defaults.earlyDiscountEnabled,
+        billingProvider: defaults.billingProvider,
+        revenuecatIosApiKey: defaults.revenuecatIosApiKey,
+        revenuecatAndroidApiKey: defaults.revenuecatAndroidApiKey,
+        revenuecatEntitlementId: defaults.revenuecatEntitlementId,
+        revenuecatOfferingId: defaults.revenuecatOfferingId,
         earlyDiscountKind: defaults.earlyDiscountKind,
         earlyDiscountPercent: defaults.earlyDiscountPercent,
         earlyDiscountPriceId: defaults.earlyDiscountPriceId,
@@
   Future<PremiumCheckResult> canUseTheme(String themeId) async {
@@
     return const PremiumCheckResult.blocked(reason: 'themes');
   }
+
+  Future<bool> waitForBillingActivation({
+    Duration timeout = const Duration(seconds: 18),
+    Duration pollInterval = const Duration(seconds: 2),
+  }) async {
+    final deadline = DateTime.now().add(timeout);
+    while (DateTime.now().isBefore(deadline)) {
+      final status = (await _getSubscriptionStatus()).toLowerCase().trim();
+      if (status == 'active' || status == 'trialing') {
+        return true;
+      }
+      await Future<void>.delayed(pollInterval);
+    }
+    return false;
+  }
*** End Patch
