*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
 import 'package:flutter/services.dart';
 
+import '../utils/app_logger.dart';
+
 class FocusService {
@@
     try {
       final bool result = await _channel.invokeMethod('requestPermissions');
       return result;
     } on PlatformException catch (e) {
-      print("Failed to request permissions: '${e.message}'.");
+      AppLogger.error("Failed to request permissions: '${e.message}'.");
       return false;
     } on MissingPluginException {
-      print("Native permissions method missing. Rebuild required.");
+      AppLogger.warning('Native permissions method missing. Rebuild required.');
       return false;
     }
   }
@@
     try {
       final bool result = await _channel.invokeMethod('isAuthorized');
       return result;
     } on PlatformException catch (e) {
-      print("Failed to check authorization: '${e.message}'.");
+      AppLogger.error("Failed to check authorization: '${e.message}'.");
       return false;
     } on MissingPluginException {
       return false;
@@
       await _channel.invokeMethod('startShield', {
         'packages': packageNames,
         'categories': categories,
       });
     } on PlatformException catch (e) {
-      print("Failed to start shield: '${e.message}'.");
+      AppLogger.error("Failed to start shield: '${e.message}'.");
     } on MissingPluginException {
-      print("Native startShield method missing. Rebuild required.");
+      AppLogger.warning('Native startShield method missing. Rebuild required.');
     }
   }
@@
       await _channel.invokeMethod('cacheSelection', {
         'packages': packageNames,
         'categories': categories,
       });
     } on PlatformException catch (e) {
-      print("Failed to cache selection: '${e.message}'.");
+      AppLogger.error("Failed to cache selection: '${e.message}'.");
     } on MissingPluginException {
       // Ignore on unsupported runtimes.
     }
@@
     try {
       await _channel.invokeMethod('startCeoShield');
       return true;
     } on PlatformException catch (e) {
-      print("Failed to start CEO shield: '${e.message}'.");
+      AppLogger.error("Failed to start CEO shield: '${e.message}'.");
       return false;
     } on MissingPluginException {
       return false;
@@
     try {
       await _channel.invokeMethod('stopShield');
     } on PlatformException catch (e) {
-      print("Failed to stop shield: '${e.message}'.");
+      AppLogger.error("Failed to stop shield: '${e.message}'.");
     } on MissingPluginException {
-      print("Native stopShield method missing. Rebuild required.");
+      AppLogger.warning('Native stopShield method missing. Rebuild required.');
     }
   }
@@
     try {
       final bool result = await _channel.invokeMethod('isShieldActive');
       return result;
     } on PlatformException catch (e) {
-      print("Failed to check shield status: '${e.message}'.");
+      AppLogger.error("Failed to check shield status: '${e.message}'.");
       return false;
     } on MissingPluginException {
-      print("Native isShieldActive method missing. Rebuild required.");
+      AppLogger.warning(
+        'Native isShieldActive method missing. Rebuild required.',
+      );
       return false;
     }
   }
@@
       await _channel.invokeMethod('syncPlannedSessions', {
         'sessions': sessions,
       });
     } on PlatformException catch (e) {
-      print("Failed to sync planned sessions: '${e.message}'.");
+      AppLogger.error("Failed to sync planned sessions: '${e.message}'.");
     } on MissingPluginException {
       // Ignore on unsupported platforms/runtimes.
     }
@@
       final List<dynamic>? result = await _channel.invokeMethod(
         'openFamilyActivityPicker',
       );
       return result?.cast<String>();
     } on PlatformException catch (e) {
-      print("Failed to open picker: '${e.message}'.");
+      AppLogger.error("Failed to open picker: '${e.message}'.");
       return null;
     } on MissingPluginException catch (e) {
-      print(
-        "CRITICAL: Native focus plugin missing. Please STOP the app and run 'flutter run' again to apply native changes. Error: $e",
+      AppLogger.warning(
+        "Native focus plugin missing. Please stop the app and run 'flutter run' again to apply native changes. Error: $e",
       );
       return null;
     }
*** End Patch
