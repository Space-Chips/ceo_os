*** Begin Patch
*** Update File: /Users/timo/ceo_os/ios/FocusActivityMonitor/FocusActivityMonitorExtension.swift
@@
     static let classicSelectionStorageKey = "classic_selection_payloads_v1"
     static let classicEnabledStorageKey = "classic_shield_enabled_v1"
     static let classicPauseSelectionStorageKey = "classic_pause_selection_payloads_v1"
     static let classicPauseEnabledStorageKey = "classic_pause_enabled_v1"
     static let classicRestPeriodsStorageKey = "classic_rest_periods_v1"
+    static let classicDailyLimitEntriesStorageKey = "classic_daily_limit_entries_v1"
+    static let classicDailyLimitTriggeredIdsStorageKey = "classic_daily_limit_triggered_ids_v1"
 }
 
 private struct PlannedClassicRest: Codable {
     let id: String
     let startMillis: Int64
     let endMillis: Int64
 }
+
+private struct ClassicDailyLimitEntry: Codable {
+    let id: String
+    let selector: String
+    let kind: String
+    let limitMinutes: Int
+}
@@
     private func readClassicRestPeriods() -> [PlannedClassicRest] {
         guard let defaults = sharedDefaults(),
               let data = defaults.data(forKey: FocusMonitorShared.classicRestPeriodsStorageKey),
               let periods = try? JSONDecoder().decode([PlannedClassicRest].self, from: data) else {
             return []
         }
         return periods
     }
+
+    private func readClassicDailyLimitEntries() -> [ClassicDailyLimitEntry] {
+        guard let defaults = sharedDefaults(),
+              let data = defaults.data(forKey: FocusMonitorShared.classicDailyLimitEntriesStorageKey),
+              let entries = try? JSONDecoder().decode([ClassicDailyLimitEntry].self, from: data) else {
+            return []
+        }
+        return entries
+    }
+
+    private func readTriggeredDailyLimitIds() -> Set<String> {
+        Set(sharedDefaults()?.stringArray(forKey: FocusMonitorShared.classicDailyLimitTriggeredIdsStorageKey) ?? [])
+    }
+
+    private func saveTriggeredDailyLimitIds(_ ids: Set<String>) {
+        sharedDefaults()?.set(Array(ids), forKey: FocusMonitorShared.classicDailyLimitTriggeredIdsStorageKey)
+    }
+
+    private func payloadsForTriggeredDailyLimits() -> [String] {
+        let triggered = readTriggeredDailyLimitIds()
+        guard !triggered.isEmpty else { return [] }
+        return readClassicDailyLimitEntries()
+            .filter { triggered.contains($0.id) }
+            .map { $0.selector }
+    }
@@
         if defaults?.bool(forKey: FocusMonitorShared.classicPauseEnabledStorageKey) ?? false {
             payloads.append(contentsOf: defaults?.stringArray(forKey: FocusMonitorShared.classicPauseSelectionStorageKey) ?? [])
         }
+        payloads.append(contentsOf: payloadsForTriggeredDailyLimits())
@@
     override func intervalDidStart(for activity: DeviceActivityName) {
         super.intervalDidStart(for: activity)
         let defaults = sharedDefaults()
         if activity.rawValue.hasPrefix("focus_plan_activity_") || activity == .scheduledFocus {
             defaults?.set(true, forKey: FocusMonitorShared.scheduledFocusEnabledStorageKey)
             applyShield(includeFocus: true)
             return
         }
+        if activity.rawValue.hasPrefix("classic_daily_limit_activity_") {
+            let id = String(activity.rawValue.dropFirst("classic_daily_limit_activity_".count))
+            var triggered = readTriggeredDailyLimitIds()
+            triggered.remove(id)
+            saveTriggeredDailyLimitIds(triggered)
+            applyShield(includeFocus: defaults?.bool(forKey: FocusMonitorShared.scheduledFocusEnabledStorageKey) ?? false)
+            return
+        }
         if activity.rawValue.hasPrefix("classic_rest_activity_") {
             defaults?.set(classicPauseActiveNow(), forKey: FocusMonitorShared.classicPauseEnabledStorageKey)
             applyShield(includeFocus: defaults?.bool(forKey: FocusMonitorShared.scheduledFocusEnabledStorageKey) ?? false)
         }
     }
@@
         if activity.rawValue.hasPrefix("focus_plan_activity_") || activity == .scheduledFocus {
             defaults?.set(false, forKey: FocusMonitorShared.scheduledFocusEnabledStorageKey)
             applyShield(includeFocus: false)
             return
         }
+        if activity.rawValue.hasPrefix("classic_daily_limit_activity_") {
+            applyShield(includeFocus: defaults?.bool(forKey: FocusMonitorShared.scheduledFocusEnabledStorageKey) ?? false)
+            return
+        }
         if activity.rawValue.hasPrefix("classic_rest_activity_") {
             defaults?.set(classicPauseActiveNow(), forKey: FocusMonitorShared.classicPauseEnabledStorageKey)
             applyShield(includeFocus: defaults?.bool(forKey: FocusMonitorShared.scheduledFocusEnabledStorageKey) ?? false)
         }
     }
+
+    override func eventDidReachThreshold(_ event: DeviceActivityEvent.Name, activity: DeviceActivityName) {
+        super.eventDidReachThreshold(event, activity: activity)
+        guard event.rawValue.hasPrefix("classic_daily_limit_event_") else {
+            return
+        }
+        let id = String(event.rawValue.dropFirst("classic_daily_limit_event_".count))
+        var triggered = readTriggeredDailyLimitIds()
+        triggered.insert(id)
+        saveTriggeredDailyLimitIds(triggered)
+        let defaults = sharedDefaults()
+        applyShield(includeFocus: defaults?.bool(forKey: FocusMonitorShared.scheduledFocusEnabledStorageKey) ?? false)
+    }
 }
*** End Patch
