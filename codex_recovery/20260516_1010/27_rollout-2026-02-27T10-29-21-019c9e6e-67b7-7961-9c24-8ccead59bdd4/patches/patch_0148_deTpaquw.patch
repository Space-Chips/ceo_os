*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/leaderboard/leaderboard_screen.dart
@@
 import 'package:flutter/cupertino.dart';
 import 'package:flutter/services.dart';
 import 'package:go_router/go_router.dart';
+import 'package:provider/provider.dart';
 
 import '../../components/components.dart';
 import '../../core/models/user_models.dart';
+import '../../core/providers/language_provider.dart';
 import '../../core/repositories/user_repository.dart';
 import '../../core/theme/app_colors.dart';
 import '../../core/theme/app_typography.dart';
@@
   String _friendLabel(FriendConnection friend) {
     final name = (friend.friendName ?? '').trim();
     if (name.isNotEmpty) return name.toUpperCase();
     final email = (friend.friendEmail ?? '').trim();
     if (email.isNotEmpty) return email.split('@').first.toUpperCase();
-    return 'FRIEND';
+    return context.read<LanguageProvider>().t('friend');
   }
 
   String _identityLabel(LeaderboardEntry entry, {required bool mine}) {
-    if (mine) return 'YOU';
+    if (mine) return context.read<LanguageProvider>().t('you');
     final resolved = _identityLabels[entry.createdBy];
     if (resolved != null && resolved.trim().isNotEmpty) {
       return resolved.trim().toUpperCase();
@@
   Widget _buildMyPositionCard() {
     if (_myUserId == null) return const SizedBox.shrink();
+    final language = context.watch<LanguageProvider>();
     final myIndex = _entries.indexWhere(
       (row) => row.entry.createdBy == _myUserId,
     );
@@
                 Text(
-                  'YOUR_POSITION',
+                  language.t('your_position'),
                   style: AppTypography.mono.copyWith(
                     fontSize: 9,
                     color: AppColors.warning,
@@
                 Text(
-                  (mine.rankName ?? 'Starter').toUpperCase(),
+                  (mine.rankName ?? language.t('starter')).toUpperCase(),
                   style: AppTypography.mono.copyWith(
                     fontSize: 12,
                     fontWeight: FontWeight.bold,
@@
                 Text(
                   '#${myIndex + 1} • ${mine.winStreak ?? 0}d streak',
                   style: AppTypography.mono.copyWith(
@@
               Text(
-                'Top $topPercent%',
+                '${language.t('top')} $topPercent%',
                 style: AppTypography.mono.copyWith(
                   fontSize: 14,
                   fontWeight: FontWeight.bold,
@@
               Text(
-                'GLOBAL',
+                language.t('leaderboard_global'),
                 style: AppTypography.mono.copyWith(
                   fontSize: 9,
                   color: AppColors.tertiaryLabel,
@@
   String _friendErrorMessage(Object error) {
     if (error is ArgumentError) {
       final message = error.message?.toString();
       if (message != null && message.isNotEmpty) return message;
     }
-    return 'Please try again in a moment.';
+    return context.read<LanguageProvider>().t('please_try_again');
   }
@@
             isDefaultAction: true,
             onPressed: () => Navigator.pop(ctx),
-            child: const Text('OK'),
+            child: Text(context.read<LanguageProvider>().t('ok')),
           ),
         ],
       ),
@@
       await _loadFriendsOnly();
       await _showNotice(
-        'FRIEND_CONNECTED',
-        'Friend connection is now visible in your private ranking tab.',
+        context.read<LanguageProvider>().t('friend_connected'),
+        context.read<LanguageProvider>().t('friend_connected_message'),
       );
     } catch (e) {
       if (!mounted) return;
       setState(() => _friendsLoading = false);
-      await _showNotice('CONNECTION_FAILED', _friendErrorMessage(e));
+      await _showNotice(
+        context.read<LanguageProvider>().t('connection_failed'),
+        _friendErrorMessage(e),
+      );
     }
   }
@@
       context: context,
       builder: (ctx) => CupertinoAlertDialog(
-        title: const Text('Connect Friend'),
+        title: Text(context.read<LanguageProvider>().t('connect_friend_title')),
         content: Column(
           mainAxisSize: MainAxisSize.min,
           children: [
             const SizedBox(height: 8),
             CupertinoTextField(
               controller: email,
-              placeholder: 'Friend email',
+              placeholder: context.read<LanguageProvider>().t('friend_email'),
               keyboardType: TextInputType.emailAddress,
               autocorrect: false,
             ),
             const SizedBox(height: 8),
             CupertinoTextField(
               controller: name,
-              placeholder: 'Display name (optional)',
+              placeholder: context.read<LanguageProvider>().t(
+                'display_name_optional',
+              ),
             ),
           ],
         ),
         actions: [
           CupertinoDialogAction(
             onPressed: () => Navigator.pop(ctx),
-            child: const Text('Cancel'),
+            child: Text(context.read<LanguageProvider>().t('cancel')),
           ),
           CupertinoDialogAction(
             isDefaultAction: true,
@@
               if (!_isValidEmail(rawEmail)) {
                 await _showNotice(
-                  'INVALID_EMAIL',
-                  'Please provide a valid email address.',
+                  context.read<LanguageProvider>().t('invalid_email'),
+                  context.read<LanguageProvider>().t('provide_valid_email'),
                 );
                 return;
               }
@@
-            child: const Text('Connect'),
+            child: Text(context.read<LanguageProvider>().t('connect')),
           ),
         ],
       ),
@@
     } catch (_) {
       if (!mounted) return;
       setState(() => _friendsLoading = false);
-      await _showNotice('DELETE_FAILED', 'Unable to remove this friend now.');
+      await _showNotice(
+        context.read<LanguageProvider>().t('delete_failed'),
+        context.read<LanguageProvider>().t('unable_remove_friend'),
+      );
     }
   }
@@
       context: context,
       builder: (ctx) => CupertinoAlertDialog(
-        title: const Text('Remove Friend'),
-        content: Text('Remove ${_friendLabel(row.connection)} from this tab?'),
+        title: Text(context.read<LanguageProvider>().t('remove_friend')),
+        content: Text(
+          context.read<LanguageProvider>().t(
+            'remove_friend_confirm',
+          ).replaceAll('{friend}', _friendLabel(row.connection)),
+        ),
         actions: [
           CupertinoDialogAction(
             onPressed: () => Navigator.pop(ctx),
-            child: const Text('Cancel'),
+            child: Text(context.read<LanguageProvider>().t('cancel')),
           ),
           CupertinoDialogAction(
             isDestructiveAction: true,
             onPressed: () async {
               Navigator.pop(ctx);
               await _removeFriend(row);
             },
-            child: const Text('Remove'),
+            child: Text(context.read<LanguageProvider>().t('remove')),
           ),
         ],
       ),
@@
     await Clipboard.setData(ClipboardData(text: message));
     await _showNotice(
-      'INVITE_READY',
-      'Invitation copied. Share it with your peers.',
+      context.read<LanguageProvider>().t('invite_ready'),
+      context.read<LanguageProvider>().t('invitation_copied'),
     );
   }
@@
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     return CupertinoPageScaffold(
       backgroundColor: AppColors.background,
       navigationBar: CupertinoNavigationBar(
@@
-        middle: const NeoMonoText(
-          'LEADERBOARD',
+        middle: NeoMonoText(
+          language.t('leaderboard'),
           fontSize: 18,
           fontWeight: FontWeight.bold,
         ),
@@
   Widget _buildHeader() {
+    final language = context.watch<LanguageProvider>();
     return Column(
       children: [
@@
                   child: Text(
                     _activeTab == _LeaderboardTab.global
-                        ? 'Global ranking based on streak, level, and discipline index.'
-                        : 'Private friend ranking synced from your connection list.',
+                        ? language.t('global_ranking_description')
+                        : language.t('friends_ranking_description'),
                     style: AppTypography.mono.copyWith(
                       fontSize: 10,
                       color: AppColors.secondaryLabel,
@@
                   child: Text(
                     _activeTab == _LeaderboardTab.global
                         ? '${_entries.length}'
@@
                 child: Text(
-                  'GLOBAL',
+                  language.t('leaderboard_global'),
                   style: AppTypography.mono.copyWith(
                     fontSize: 10,
@@
                 child: Text(
-                  'FRIENDS',
+                  language.t('leaderboard_friends'),
                   style: AppTypography.mono.copyWith(
                     fontSize: 10,
@@
   List<Widget> _buildGlobalCards() {
+    final language = context.watch<LanguageProvider>();
     if (_entries.isEmpty) {
       return [
         GlassCard(
@@
           child: Text(
-            'No leaderboard data yet. Complete habits/tasks/focus to generate rankings.',
+            language.t('leaderboard_no_data'),
             textAlign: TextAlign.center,
             style: AppTypography.mono.copyWith(
@@
                 Text(
-                  entry.rankName ?? 'Starter',
+                  entry.rankName ?? language.t('starter'),
                   style: AppTypography.mono.copyWith(
                     fontSize: 13,
@@
   List<Widget> _buildFriendCards() {
+    final language = context.watch<LanguageProvider>();
     final widgets = <Widget>[
@@
               child: Text(
-                'Connect friends by email to track their rank progression here.',
+                language.t('connect_friends_hint'),
                 style: AppTypography.mono.copyWith(
                   fontSize: 10,
@@
               Text(
-                'SYNCING_FRIEND_CONNECTIONS',
+                language.t('syncing_friend_connections'),
                 style: AppTypography.mono.copyWith(
                   fontSize: 10,
@@
               Text(
-                'NO_FRIENDS_CONNECTED',
+                language.t('no_friends_connected'),
                 style: AppTypography.mono.copyWith(
                   fontSize: 10,
@@
               Text(
-                'Invite peers to compare streak and rank progression.',
+                language.t('invite_peers_hint'),
                 textAlign: TextAlign.center,
                 style: AppTypography.mono.copyWith(
@@
   Widget _buildFriendRow(_FriendRankedEntry row, int rank) {
+    final language = context.watch<LanguageProvider>();
     final friend = row.connection;
     final rankAccent = _rankAccentColor(rank);
@@
                   Text(
-                    email.isEmpty ? 'MANUAL_CONNECTION' : email,
+                    email.isEmpty ? language.t('manual_connection') : email,
                     maxLines: 1,
                     overflow: TextOverflow.ellipsis,
@@
                   Text(
-                    (friend.friendRankName ?? 'Starter').toUpperCase(),
+                    (friend.friendRankName ?? language.t('starter'))
+                        .toUpperCase(),
                     style: AppTypography.mono.copyWith(
                       fontSize: 9,
*** End Patch
