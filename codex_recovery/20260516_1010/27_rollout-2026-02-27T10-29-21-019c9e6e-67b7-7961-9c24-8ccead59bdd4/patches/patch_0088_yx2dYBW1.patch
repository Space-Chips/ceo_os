*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/calendar/calendar_screen.dart
@@
   @override
   Widget build(BuildContext context) {
     final currentDay = _selectedDate();
+    final selectedDayLabel = DateFormat('EEE d MMM').format(currentDay).toUpperCase();
@@
               child: ListView(
                 padding: const EdgeInsets.symmetric(
                   horizontal: 20,
                 ).copyWith(bottom: 120),
                 children: [
+                  _glowSurface(
+                    glowColor: AppColors.primaryOrange.withValues(alpha: 0.14),
+                    borderRadius: 20,
+                    child: GlassCard(
+                      padding: const EdgeInsets.all(14),
+                      borderRadius: 20,
+                      border: Border.all(
+                        color: AppColors.primaryOrange.withValues(alpha: 0.22),
+                        width: 0.7,
+                      ),
+                      child: Column(
+                        crossAxisAlignment: CrossAxisAlignment.start,
+                        children: [
+                          Row(
+                            children: [
+                              Icon(
+                                CupertinoIcons.calendar_today,
+                                size: 14,
+                                color: AppColors.primaryOrange,
+                              ),
+                              const SizedBox(width: 7),
+                              Text(
+                                'PLANNING_CONTROL',
+                                style: AppTypography.mono.copyWith(
+                                  fontSize: 9,
+                                  color: AppColors.primaryOrange,
+                                  fontWeight: FontWeight.bold,
+                                  letterSpacing: 1.2,
+                                ),
+                              ),
+                            ],
+                          ),
+                          const SizedBox(height: 8),
+                          Text(
+                            selectedDayLabel,
+                            style: AppTypography.mono.copyWith(
+                              fontSize: 14,
+                              color: AppColors.label,
+                              fontWeight: FontWeight.bold,
+                            ),
+                          ),
+                          const SizedBox(height: 4),
+                          Text(
+                            dayEvents.isEmpty
+                                ? 'No event scheduled on selected day.'
+                                : '${dayEvents.length} event(s) scheduled. Swipe for intelligence.',
+                            style: AppTypography.mono.copyWith(
+                              fontSize: 10,
+                              color: AppColors.secondaryLabel,
+                            ),
+                          ),
+                        ],
+                      ),
+                    ),
+                  ),
+                  const SizedBox(height: 12),
                   GlassCard(
                     padding: const EdgeInsets.all(12),
                     borderRadius: 16,
+                    border: Border.all(
+                      color: overloadInfo.overloaded
+                          ? AppColors.warning.withValues(alpha: 0.24)
+                          : AppColors.glassBorder,
+                      width: 0.55,
+                    ),
                     child: Row(
                       children: [
                         _metric('Today', '$todayCount'),
*** End Patch
