*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/setup/setup_flow_screen.dart
@@
     return _SetupStepScaffold(
       child: Column(
         crossAxisAlignment: CrossAxisAlignment.start,
         children: [
           Container(
             padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
             decoration: BoxDecoration(
               color: AppColors.glassSurfaceSoft,
               borderRadius: BorderRadius.circular(999),
               border: Border.all(color: AppColors.border),
             ),
             child: Text(
-              '3 quick steps',
+              'Setup • 3 steps',
               style: AppTypography.caption1.copyWith(
                 color: AppColors.secondaryLabel,
                 fontWeight: FontWeight.w600,
               ),
             ),
           ),
-          const SizedBox(height: 18),
-          Text('Set up Screen Time', style: AppTypography.largeTitle),
-          const SizedBox(height: 12),
-          Text(
-            'We’ll guide you through the permissions needed for instant, reliable blocking.',
-            style: AppTypography.body.copyWith(
-              color: AppColors.secondaryLabel,
-              height: 1.5,
-            ),
-          ),
-          const SizedBox(height: 22),
+          const SizedBox(height: 20),
+          Text('Screen Time setup', style: AppTypography.largeTitle),
+          const SizedBox(height: 10),
+          Text(
+            'A quick, guided setup to make blocking instant and reliable.',
+            style: AppTypography.body.copyWith(
+              color: AppColors.secondaryLabel,
+              height: 1.5,
+            ),
+          ),
+          const SizedBox(height: 20),
           GlassCard(
             level: GlassCardLevel.elevated,
             child: Padding(
-              padding: const EdgeInsets.all(18),
+              padding: const EdgeInsets.all(16),
               child: Column(
                 crossAxisAlignment: CrossAxisAlignment.start,
                 children: [
-                  Text('What this unlocks', style: AppTypography.title3),
-                  const SizedBox(height: 8),
+                  Text('You’ll enable', style: AppTypography.title3),
+                  const SizedBox(height: 10),
                   _BulletLine(
-                    text: 'Blocked apps stop instantly, not seconds later.',
+                    text: 'Instant blocking when an app opens.',
                   ),
                   _BulletLine(
-                    text: 'Usage limits stay accurate in the background.',
+                    text: 'Accurate usage limits in the background.',
                   ),
                   _BulletLine(
-                    text: 'Focus and blackout sessions hold in real time.',
+                    text: 'Focus and blackout sessions that hold.',
                   ),
                 ],
               ),
             ),
           ),
         ],
*** End Patch