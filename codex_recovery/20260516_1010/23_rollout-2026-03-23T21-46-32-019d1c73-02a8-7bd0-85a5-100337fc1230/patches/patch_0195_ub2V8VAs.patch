*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
 import '../screen_time_setup/screen_time_setup_controller.dart';
+import '../setup/setup_flow_controller.dart';
@@
   void _openScreenTimeEntry() {
-    final controller = context.read<ScreenTimeSetupController>();
-    unawaited(
-      controller.initialize().then((_) async {
-        await controller.refreshAndSync();
-        if (!mounted) return;
-        if (controller.isSetupRequired || !controller.isSetupComplete) {
-          context.push('/screen-time-setup');
-        } else {
-          context.push('/screen-time-manager');
-        }
-      }),
-    );
+    final isAndroid =
+        !kIsWeb && defaultTargetPlatform == TargetPlatform.android;
+    if (isAndroid) {
+      final setup = context.read<SetupFlowController>();
+      unawaited(
+        setup.initialize().then((_) async {
+          await setup.refreshPermissionStates();
+          if (!mounted) return;
+          if (setup.isSetupRequired || !setup.isSetupComplete) {
+            context.push('/setup');
+          } else {
+            context.push('/screen-time-manager');
+          }
+        }),
+      );
+      return;
+    }
+
+    final controller = context.read<ScreenTimeSetupController>();
+    unawaited(
+      controller.initialize().then((_) async {
+        await controller.refreshAndSync();
+        if (!mounted) return;
+        if (controller.isSetupRequired || !controller.isSetupComplete) {
+          context.push('/screen-time-setup');
+        } else {
+          context.push('/screen-time-manager');
+        }
+      }),
+    );
   }
*** End Patch