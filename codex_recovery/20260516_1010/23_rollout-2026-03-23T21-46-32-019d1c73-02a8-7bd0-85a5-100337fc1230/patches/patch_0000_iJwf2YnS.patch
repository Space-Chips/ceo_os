*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/profile/profile_screen.dart
@@
             child: Text(
-              'Theme preview changed, but cloud sync failed. Check Supabase schema/migration and retry.',
+              'Your appearance changed on this device, but we could not save it to your account right now. Please try again in a moment.',
               style: AppTypography.mono.copyWith(
                 fontSize: 12,
                 color: AppColors.secondaryLabel,
*** Update File: /Users/timo/ceo_os/lib/core/repositories/family_time_repository.dart
@@
   String _humanizeError(Object error) {
     if (error is PostgrestException) {
       if (error.code == '42P01') {
-        return 'Family Time backend not deployed. Run supabase_family_time_update.sql in Supabase SQL Editor.';
+        return 'Family Time is not available right now. Please try again later.';
       }
       if (error.code == '42501') {
-        return 'Family Time permissions are not configured. Verify RLS policies.';
+        return 'We could not access Family Time right now. Please try again later.';
       }
       if ((error.message).trim().isNotEmpty) {
         return error.message.trim();
       }
@@
     }
-    return 'Unexpected error. Please retry.';
+    return 'Something went wrong. Please try again.';
   }
*** Update File: /Users/timo/ceo_os/lib/core/providers/language_provider.dart
@@
-    'home_dashboard_retry_hint': 'Tap to retry after DB migration.',
+    'home_dashboard_retry_hint': 'Tap to try loading your dashboard again.',
@@
-        'Touchez pour réessayer après la migration de la base.',
+        'Touchez pour recharger votre tableau de bord.',
*** Update File: /Users/timo/ceo_os/lib/features/home/stats_screen.dart
@@
     if (normalized.contains('advanced_stats_events') ||
         normalized.contains('advanced_daily_stats') ||
         normalized.contains('advanced_weekly_stats') ||
         normalized.contains('advanced_monthly_stats') ||
         normalized.contains('advanced_lifetime_stats')) {
-      return 'Advanced stats tables are not installed in Supabase yet. Run `supabase_advanced_stats_update.sql`, then reopen this screen.';
+      return 'Advanced stats are not available right now. Please try again later.';
     }
     return raw;
   }
*** End Patch
