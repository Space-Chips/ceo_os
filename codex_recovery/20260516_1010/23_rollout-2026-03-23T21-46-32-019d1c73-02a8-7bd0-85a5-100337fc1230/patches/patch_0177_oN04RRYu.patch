*** Begin Patch
*** Update File: android/app/src/main/kotlin/com/wakeapp/ceoos/BlockingAccessibilityService.kt
@@
     private lateinit var browserBlockCoordinator: BrowserBlockCoordinator
     private lateinit var gateLauncher: GateLauncher
     private lateinit var blockingOverlay: BlockingOverlay
+    private lateinit var browserBlockingOverlay: BrowserBlockingOverlay
@@
         browserBlockCoordinator = BrowserBlockCoordinator()
         appBlockEngine = AppBlockEngine(browserBlockCoordinator)
         gateLauncher = GateLauncher()
         blockingOverlay = BlockingOverlay(this)
+        browserBlockingOverlay = BrowserBlockingOverlay(this)
@@
         if (isLauncher) {
             lastWasLauncher = true
             handleLauncherOverlay(now)
+            browserBlockingOverlay.hide()
             return
         }
@@
-        val isBrowser = foregroundAppDetector.isBrowserPackage(eventPackage)
-        val snapshot = foregroundAppDetector.createSnapshot(
+        val isBrowser = foregroundAppDetector.isBrowserPackage(eventPackage)
+        val snapshot = foregroundAppDetector.createSnapshot(
             event,
             if (isBrowser) rootInActiveWindow else null,
         )
 
         handleSnapshot(snapshot, startedAt)
     }
@@
     override fun onDestroy() {
@@
         launcherOverlayController.clear()
+        browserBlockingOverlay.hide()
         serviceScope.cancel()
         gateLauncher.dismiss(this)
     }
@@
     private fun evaluateCurrentState() {
         val root = rootInActiveWindow ?: return
         val rootPackage = root.packageName?.toString()?.trim()
         if (launcherPackageResolver.isLauncherPackage(rootPackage)) {
             handleLauncherOverlay(SystemClock.elapsedRealtime())
+            browserBlockingOverlay.hide()
             return
         }
@@
     private fun handleLauncherOverlay(nowElapsed: Long) {
-        if (nowElapsed - lastLauncherRefreshAt < 90L) return
+        if (nowElapsed - lastLauncherRefreshAt < 45L) return
         lastLauncherRefreshAt = nowElapsed
         val root = rootInActiveWindow ?: return
         val policyState = getPolicyStateFast()
         blockStateCache.update(policyState)
@@
         launcherOverlayController.updateOverlays(targets, blockedPackages)
     }
 
@@
         val decision = foregroundInterceptor.decisionForSnapshot(
@@
         val gateLaunched = when (decision) {
             BlockDecision.Allow -> {
                 blockingOverlay.hide()
+                browserBlockingOverlay.hide()
                 if (!BlockGateActivity.isVisible) {
                     blockingNavigator.dismiss(this)
                 }
                 false
             }
             BlockDecision.IgnoreTransient -> false
             is BlockDecision.BlockApp -> {
                 blockingOverlay.show()
+                browserBlockingOverlay.hide()
                 val launched = gateLauncher.launch(this, decision)
                 if (!launched) blockingOverlay.hide()
                 launched
             }
             is BlockDecision.BlockUrl -> {
-                blockingOverlay.show()
-                val launched = browserBlockCoordinator.handleBlockedUrl(this, decision, gateLauncher)
-                if (!launched) blockingOverlay.hide()
-                launched
+                blockingOverlay.hide()
+                val toolbarBounds = snapshot.browserToolbarBounds
+                browserBlockingOverlay.show(toolbarBounds, decision.target, decision.reason)
+                false
             }
         }
@@
         val listener = SharedPreferences.OnSharedPreferenceChangeListener { _, key ->
             if (key in observedPolicyKeys) {
                 pendingPolicyRefresh = true
                 launcherPackageResolver.refresh()
                 launcherIconTracker.refreshIndex()
+                val rootPackage = rootInActiveWindow?.packageName?.toString()?.trim()
+                if (launcherPackageResolver.isLauncherPackage(rootPackage)) {
+                    lastLauncherRefreshAt = 0L
+                    handleLauncherOverlay(SystemClock.elapsedRealtime())
+                }
+                requestImmediateEvaluation()
             }
         }
*** End Patch