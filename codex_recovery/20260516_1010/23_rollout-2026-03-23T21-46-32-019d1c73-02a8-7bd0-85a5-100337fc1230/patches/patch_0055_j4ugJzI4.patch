*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
 enum FocusPermissionState {
   unknown,
   approved,
   notDetermined,
   denied,
   unsupported,
 }
+
+enum PrivateScreenTimeSummaryStatus {
+  available,
+  requiresAuthorization,
+  requiresFullRelaunch,
+  unavailableOnSimulator,
+  unsupported,
+}
@@
-  Future<bool> supportsPrivateScreenTimeSummaryView() async {
+  Future<PrivateScreenTimeSummaryStatus> getPrivateScreenTimeSummaryStatus() async {
     const method = 'supportsPrivateScreenTimeSummaryView';
     try {
-      final bool? result = await _channel.invokeMethod<bool>(method);
-      return result ?? false;
+      final String? result = await _channel.invokeMethod<String>(method);
+      switch ((result ?? '').trim().toLowerCase()) {
+        case 'available':
+          return PrivateScreenTimeSummaryStatus.available;
+        case 'authorization_required':
+          return PrivateScreenTimeSummaryStatus.requiresAuthorization;
+        case 'simulator':
+          return PrivateScreenTimeSummaryStatus.unavailableOnSimulator;
+        case 'unsupported':
+          return PrivateScreenTimeSummaryStatus.unsupported;
+        default:
+          return PrivateScreenTimeSummaryStatus.requiresFullRelaunch;
+      }
     } on PlatformException {
-      return false;
+      return PrivateScreenTimeSummaryStatus.requiresFullRelaunch;
     } on MissingPluginException {
-      return false;
+      return PrivateScreenTimeSummaryStatus.requiresFullRelaunch;
     }
   }
+
+  Future<bool> supportsPrivateScreenTimeSummaryView() async {
+    final status = await getPrivateScreenTimeSummaryStatus();
+    return status == PrivateScreenTimeSummaryStatus.available;
+  }
*** End Patch
