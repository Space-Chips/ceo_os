*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/config/supabase_config.dart
@@
 class SupabaseConfig {
@@
   static String get googleWebClientId =>
       const String.fromEnvironment('GOOGLE_WEB_CLIENT_ID');
   static String get googleIosClientId =>
       const String.fromEnvironment('GOOGLE_IOS_CLIENT_ID');
+  static String get googleAndroidClientId =>
+      const String.fromEnvironment('GOOGLE_ANDROID_CLIENT_ID');
 
-  static bool get hasGoogleMobileClientConfig =>
-      googleWebClientId.isNotEmpty && googleIosClientId.isNotEmpty;
+  static bool get hasGoogleIosMobileClientConfig =>
+      googleWebClientId.isNotEmpty && googleIosClientId.isNotEmpty;
+
+  static bool get hasGoogleAndroidMobileClientConfig =>
+      googleWebClientId.isNotEmpty;
 
   static void validateConfiguration() {
*** Update File: /Users/timo/ceo_os/lib/core/providers/auth_provider.dart
@@
       // 1. Web and Desktop handled differently by Supabase,
       // but for Mobile we use google_sign_in package.
       if (!kIsWeb &&
           (defaultTargetPlatform == TargetPlatform.iOS ||
               defaultTargetPlatform == TargetPlatform.android)) {
-        if (!SupabaseConfig.hasGoogleMobileClientConfig) {
+        final hasPlatformConfig =
+            defaultTargetPlatform == TargetPlatform.iOS
+            ? SupabaseConfig.hasGoogleIosMobileClientConfig
+            : SupabaseConfig.hasGoogleAndroidMobileClientConfig;
+        if (!hasPlatformConfig) {
           throw StateError(
             'Google Sign-In is not available right now. Please use email sign-in instead.',
           );
         }
*** Update File: /Users/timo/ceo_os/lib/components/premium_gate_dialog.dart
@@
+import 'package:flutter/foundation.dart';
 import 'dart:ui';
@@
 String _footnoteText(PremiumRuntime runtime, bool isFr) {
+  final storeName = switch (defaultTargetPlatform) {
+    TargetPlatform.android => 'Google Play',
+    TargetPlatform.iOS => 'Apple',
+    _ => 'the store',
+  };
   if (runtime.resolved.discountEligible) {
@@
   }
 
   return isFr
-      ? 'Le paiement Premium est traité par Apple. L’accès Premium est activé après confirmation du store et synchronisation du compte.'
-      : 'Premium billing is processed by Apple. Premium access is activated after store confirmation and account sync.';
+      ? 'Le paiement Premium est traité par $storeName. L’accès Premium est activé après confirmation du store et synchronisation du compte.'
+      : 'Premium billing is processed by $storeName. Premium access is activated after store confirmation and account sync.';
 }
@@
 class _PremiumPaywallContentState extends State<_PremiumPaywallContent> {
@@
   bool _purchaseInFlight = false;
   bool _restoreInFlight = false;
+
+  bool get _isAndroid =>
+      !kIsWeb && defaultTargetPlatform == TargetPlatform.android;
+
+  String get _storeName => _isAndroid ? 'Google Play' : 'Apple';
+
+  String get _subscriptionSettingsLabel => _isAndroid
+      ? 'Google Play subscription settings'
+      : 'Apple ID settings';
@@
               _InfoCard(
                 text: widget.isFr
-                    ? 'Les offres App Store ne sont pas encore disponibles dans cet environnement. Le paiement restera désactivé tant que les produits ne sont pas configurés.'
-                    : 'App Store offers are not available in this environment yet. Purchasing stays disabled until products are configured.',
+                    ? 'Les offres $_storeName ne sont pas encore disponibles dans cet environnement. Le paiement restera désactivé tant que les produits ne sont pas configurés.'
+                    : '$_storeName offers are not available in this environment yet. Purchasing stays disabled until products are configured.',
               )
@@
                         ? 'CEO OS stocke les données nécessaires au fonctionnement du compte, aux fonctions de focus et aux achats Premium. Les achats sont traités par Apple et RevenueCat pour activer les droits Premium.'
-                        : 'CEO OS stores the data required to operate your account, focus features, and Premium purchases. Purchases are processed through Apple and RevenueCat to activate Premium access.',
+                        ? 'CEO OS stocke les données nécessaires au fonctionnement du compte, aux fonctions de focus et aux achats Premium. Les achats sont traités via $_storeName et RevenueCat pour activer les droits Premium.'
+                        : 'CEO OS stores the data required to operate your account, focus features, and Premium purchases. Purchases are processed through $_storeName and RevenueCat to activate Premium access.',
                   ),
                 ),
                 _LegalChip(
@@
                     title: widget.isFr ? 'Terms' : 'Terms',
                     body: widget.isFr
-                        ? 'Les abonnements se renouvellent automatiquement sauf annulation dans les réglages Apple ID. Toute portion inutilisée d’un essai gratuit est perdue lors de l’achat.'
-                        : 'Subscriptions renew automatically unless cancelled in Apple ID settings. Any unused portion of a free trial is forfeited after purchase.',
+                        ? 'Les abonnements se renouvellent automatiquement sauf annulation dans les réglages d’abonnement $_storeName. Toute portion inutilisée d’un essai gratuit est perdue lors de l’achat.'
+                        : 'Subscriptions renew automatically unless cancelled in $_subscriptionSettingsLabel. Any unused portion of a free trial is forfeited after purchase.',
                   ),
                 ),
@@
     if (selected == null) {
       return widget.isFr
-          ? 'Les achats sont gérés par Apple. Si aucune offre n’apparaît, le paiement reste désactivé jusqu’à configuration complète des produits.'
-          : 'Purchases are handled by Apple. If no offer appears, purchasing stays disabled until products are fully configured.';
+          ? 'Les achats sont gérés par $_storeName. Si aucune offre n’apparaît, le paiement reste désactivé jusqu’à configuration complète des produits.'
+          : 'Purchases are handled by $_storeName. If no offer appears, purchasing stays disabled until products are fully configured.';
     }
@@
       renewal,
       widget.isFr
-          ? 'L’abonnement peut être géré et annulé dans les réglages Apple ID après achat.'
-          : 'Subscriptions can be managed and cancelled in Apple ID settings after purchase.',
+          ? 'L’abonnement peut être géré et annulé dans les réglages d’abonnement $_storeName après achat.'
+          : 'Subscriptions can be managed and cancelled in $_subscriptionSettingsLabel after purchase.',
     ];
@@
         widget.isFr
-            ? 'L’achat a été confirmé par l’App Store. La synchronisation de ton accès Premium est encore en cours.'
-            : 'The App Store confirmed your purchase. Premium access is still syncing.',
+            ? 'L’achat a été confirmé par $_storeName. La synchronisation de ton accès Premium est encore en cours.'
+            : '$_storeName confirmed your purchase. Premium access is still syncing.',
       ),
@@
         widget.isFr
-            ? 'Les produits App Store ne sont pas prêts dans cet environnement.'
-            : 'App Store products are not ready in this environment yet.',
+            ? 'Les produits $_storeName ne sont pas prêts dans cet environnement.'
+            : '$_storeName products are not ready in this environment yet.',
       ),
       BillingPurchaseStatus.failed => (
         widget.isFr ? 'Achat impossible' : 'Purchase failed',
         result.message ??
             (widget.isFr
-                ? 'L’App Store n’a pas pu finaliser la transaction.'
-                : 'The App Store could not complete the transaction.'),
+                ? '$_storeName n’a pas pu finaliser la transaction.'
+                : '$_storeName could not complete the transaction.'),
       ),
@@
         widget.isFr
-            ? 'L’App Store a renvoyé tes achats. La synchronisation de l’accès Premium est encore en cours.'
-            : 'The App Store returned your purchases. Premium access is still syncing.',
+            ? '$_storeName a renvoyé tes achats. La synchronisation de l’accès Premium est encore en cours.'
+            : '$_storeName returned your purchases. Premium access is still syncing.',
       ),
@@
         widget.isFr
-            ? 'Les produits App Store ne sont pas prêts dans cet environnement.'
-            : 'App Store products are not ready in this environment yet.',
+            ? 'Les produits $_storeName ne sont pas prêts dans cet environnement.'
+            : '$_storeName products are not ready in this environment yet.',
       ),
       BillingPurchaseStatus.failed => (
         widget.isFr ? 'Restauration impossible' : 'Restore failed',
         result.message ??
             (widget.isFr
-                ? 'L’App Store n’a pas pu confirmer les achats.'
-                : 'The App Store could not confirm the purchases.'),
+                ? '$_storeName n’a pas pu confirmer les achats.'
+                : '$_storeName could not confirm the purchases.'),
       ),
*** Update File: /Users/timo/ceo_os/lib/features/profile/profile_screen.dart
@@
   Future<void> _openTermsOfUse() async {
+    final storeName = switch (defaultTargetPlatform) {
+      TargetPlatform.android => 'Google Play',
+      TargetPlatform.iOS => 'Apple',
+      _ => 'the store',
+    };
+    final subscriptionSettingsLabel = switch (defaultTargetPlatform) {
+      TargetPlatform.android => 'Google Play subscription settings',
+      TargetPlatform.iOS => 'Apple ID subscription settings',
+      _ => 'your store subscription settings',
+    };
     await _showDocumentSheet(
       title: 'TERMS_OF_USE',
-      sections: const [
+      sections: [
         _SettingsSection(
           'Acceptance',
           'Using this app means you accept the terms and operate within lawful use.',
@@
         ),
         _SettingsSection(
           'Subscriptions',
-          'Premium subscriptions and purchases are processed by Apple and may renew automatically unless canceled in your Apple ID subscription settings. Purchase restoration is available from the Premium paywall.',
+          'Premium subscriptions and purchases are processed by $storeName and may renew automatically unless canceled in your $subscriptionSettingsLabel. Purchase restoration is available from the Premium paywall.',
         ),
         _SettingsSection(
           'Termination',
*** End Patch
