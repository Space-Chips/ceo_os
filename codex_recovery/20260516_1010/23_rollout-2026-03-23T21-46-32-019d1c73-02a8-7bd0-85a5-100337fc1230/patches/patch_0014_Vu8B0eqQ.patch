*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/screen_time_manager_screen.dart
@@
             : Consumer<FocusProvider>(
                 builder: (context, focus, _) {
-                  final todayFromLogs = _todayMinutesFromLogs();
-                  final todayFromDailyStats = _todayMinutesFromDailyStats();
-                  final todayMinutes = todayFromLogs > 0
-                      ? todayFromLogs
-                      : (focus.screenTimeToday.round() > 0
-                            ? focus.screenTimeToday.round()
-                            : todayFromDailyStats);
-                  final avg7dFromLogs = _averageDailyMinutes(days: 7);
-                  final avg7d = avg7dFromLogs > 0
-                      ? avg7dFromLogs
-                      : _averageDailyMinutesFromDailyStats(days: 7);
-                  final rankNameRaw =
-                      (_rank?.rankName?.trim().isNotEmpty ?? false)
-                      ? _rank!.rankName!.trim()
-                      : 'Asleep';
-                  final rankName = RankArt.displayName(rankNameRaw);
-
                   return SafeArea(
                     child: ListView(
                       padding: const EdgeInsets.fromLTRB(14, 8, 14, 12),
                       children: [
                         _headerRow(),
                         const SizedBox(height: 10),
                         _focusEntryCard(),
                         const SizedBox(height: 12),
                         _statsClusterCard(
-                          todayMinutes: todayMinutes,
-                          avg7dMinutes: avg7d,
-                          rankName: rankName,
-                          streakValue: _streak?.currentStreak ?? 0,
+                          blockedTargets: _classicBlockedTargets,
+                          plannedPauseCount: _plannedPauseCount,
+                          activePauseCount: _activePauseCount,
+                          isAuthorized: focus.isAuthorized,
+                          focusActive: focus.isFocusModeActive,
                         ),
                         const SizedBox(height: 12),
                         _actionCard(
-                          icon: CupertinoIcons.rosette,
-                          iconColor: AppColors.accentIcon,
-                          title: 'Friends & Leaderboard',
-                          subtitle: 'Global rankings',
-                          onTap: () => context.push('/leaderboard'),
+                          icon: CupertinoIcons.briefcase_fill,
+                          iconColor: AppColors.rankAccent,
+                          title: 'CEO Mode',
+                          subtitle: 'Protected sprint with stricter controls',
+                          onTap: () => context.push('/ceo-mode'),
                         ),
                         const SizedBox(height: 8),
                         _actionCard(
-                          icon: CupertinoIcons.person_3_fill,
-                          iconColor: AppColors.rankAccent,
-                          title: 'Offline Together',
-                          subtitle: 'Shared no-phone sessions',
-                          onTap: () => context.push('/family-time'),
+                          icon: CupertinoIcons.nosign,
+                          iconColor: AppColors.accentIcon,
+                          title: 'Block Apps & Sites',
+                          subtitle:
+                              '$_classicBlockedTargets protected on this device',
+                          onTap: () => context.push('/screen-time'),
                         ),
-                        const SizedBox(height: 12),
+                        const SizedBox(height: 8),
                         _actionCard(
-                          icon: CupertinoIcons.nosign,
-                          iconColor: AppColors.rankAccent,
-                          title: 'Block Apps & Sites',
+                          icon: CupertinoIcons.clock_fill,
+                          iconColor: AppColors.streakAccent,
+                          title: 'Planned Pauses',
                           subtitle:
-                              '$_classicBlockedTargets blocked • limits & planned pauses',
+                              '$_plannedPauseCount scheduled • $_activePauseCount active now',
                           onTap: () => context.push('/screen-time'),
                         ),
                       ],
@@
   Widget _statsClusterCard({
-    required int todayMinutes,
-    required int avg7dMinutes,
-    required String rankName,
-    required int streakValue,
+    required int blockedTargets,
+    required int plannedPauseCount,
+    required int activePauseCount,
+    required bool isAuthorized,
+    required bool focusActive,
   }) {
     return Container(
@@
                 child: _statLargeTile(
-                  label: 'TODAY',
-                  value: '$todayMinutes',
-                  unit: 'minutes',
+                  label: 'PROTECTED',
+                  value: '$blockedTargets',
+                  unit: 'targets',
                   accent: AppColors.scoreValue,
                   highlighted: true,
                   borderColor: AppColors.screenTimeDailyAccentBorder,
@@
                 child: _statLargeTile(
-                  label: '7-DAY',
-                  value: '$avg7dMinutes',
-                  unit: 'minutes',
+                  label: 'PAUSES',
+                  value: '$plannedPauseCount',
+                  unit: 'scheduled',
                   accent: AppColors.secondaryLabel,
                 ),
               ),
@@
                 child: _statSmallTile(
-                  icon: CupertinoIcons.rosette,
-                  iconColor: AppColors.rankAccent,
-                  label: 'RANK',
-                  value: rankName,
-                  valueColor: AppColors.rankAccent,
-                  leading: RankArt(
-                    rankName: rankName,
-                    size: RankArtSize.sm,
-                    dimension: 28,
-                  ),
-                  onTap: () => context.push('/rank'),
+                  icon: CupertinoIcons.lock_shield_fill,
+                  iconColor: isAuthorized
+                      ? AppColors.success
+                      : AppColors.primaryOrange,
+                  label: 'ACCESS',
+                  value: isAuthorized ? 'READY' : 'ALLOW',
+                  valueColor: isAuthorized
+                      ? AppColors.success
+                      : AppColors.primaryOrange,
+                  onTap: () => context.push('/screen-time'),
                 ),
               ),
               const SizedBox(width: 10),
               Expanded(
                 child: _statSmallTile(
-                  icon: CupertinoIcons.flame,
-                  iconColor: AppColors.streakAccent,
-                  label: 'STREAK',
-                  value: '$streakValue',
-                  valueColor: AppColors.streakAccent,
-                  onTap: () => context.push('/win-streak'),
+                  icon: CupertinoIcons.bolt_fill,
+                  iconColor: focusActive
+                      ? AppColors.focusControlAccent
+                      : AppColors.secondaryLabel,
+                  label: 'STATUS',
+                  value: focusActive
+                      ? 'FOCUS'
+                      : (activePauseCount > 0 ? 'PAUSED' : 'IDLE'),
+                  valueColor: focusActive
+                      ? AppColors.focusControlAccent
+                      : AppColors.secondaryLabel,
+                  onTap: () => context.push('/focus'),
                 ),
               ),
*** End Patch
