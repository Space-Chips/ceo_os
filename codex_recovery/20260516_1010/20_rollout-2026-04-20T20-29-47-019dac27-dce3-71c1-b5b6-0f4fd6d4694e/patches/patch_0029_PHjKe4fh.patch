*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/classic_blocking_coordinator.dart
@@
     final scheduledPausePeriods = restPeriods
         .map(_toNativePausePeriod)
         .whereType<Map<String, dynamic>>()
         .toList(growable: false);
+    final enforceableAppCount = hydratedAppBindings.values
+        .where((item) => item.isEnforceable)
+        .length;
+    final enforceableWebsiteCount = websiteBindings.values
+        .where((item) => item.isEnforceable)
+        .length;
+    final unresolvedAppCount =
+        (apps.length - enforceableAppCount).clamp(0, apps.length).toInt();
+    final unresolvedWebsiteCount = (websites.length - enforceableWebsiteCount)
+        .clamp(0, websites.length)
+        .toInt();
 
     final summary = ClassicBlockingSyncResult(
       activePause: pauseActive,
-      enforceableAppCount: hydratedAppBindings.values
-          .where((item) => item.isEnforceable)
-          .length,
-      enforceableWebsiteCount: websiteBindings.values
-          .where((item) => item.isEnforceable)
-          .length,
+      enforceableAppCount: enforceableAppCount,
+      enforceableWebsiteCount: enforceableWebsiteCount,
       effectiveTargetCount: pauseActive
           ? pauseAppBindings.length + pauseWebsiteBindings.length
           : baseAppBindings.length + baseWebsiteBindings.length,
-      unresolvedAppCount: (apps.length - hydratedAppBindings.values
-              .where((item) => item.isEnforceable)
-              .length)
-          .clamp(0, apps.length),
-      unresolvedWebsiteCount: (websites.length - websiteBindings.values
-              .where((item) => item.isEnforceable)
-              .length)
-          .clamp(0, websites.length),
+      unresolvedAppCount: unresolvedAppCount,
+      unresolvedWebsiteCount: unresolvedWebsiteCount,
       pushedClassicPackageCount: baseClassicPackages.length,
*** End Patch
