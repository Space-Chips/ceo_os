*** Begin Patch
*** Update File: android/app/src/main/kotlin/com/wakeapp/ceoos/BlockingAccessibilityService.kt
@@
-    private var lastEventAtMillis: Long = 0L
+    private var lastEventAtElapsedMillis: Long = 0L
@@
-        val eventPackage = event.packageName?.toString()?.trim()
-        val rootPackage = rootInActiveWindow?.packageName?.toString()?.trim()
+        val eventPackage = event.packageName?.toString()?.trim()
+        val rootPackage = rootInActiveWindow?.packageName?.toString()?.trim()
+        val resolvedPackage = eventPackage ?: rootPackage
         val isLauncher = launcherPackageResolver.isLauncherPackage(eventPackage) ||
             launcherPackageResolver.isLauncherPackage(rootPackage)
@@
         if (event.eventType == AccessibilityEvent.TYPE_WINDOW_CONTENT_CHANGED ||
             event.eventType == AccessibilityEvent.TYPE_VIEW_TEXT_CHANGED ||
             event.eventType == AccessibilityEvent.TYPE_VIEW_TEXT_SELECTION_CHANGED ||
             event.eventType == AccessibilityEvent.TYPE_VIEW_FOCUSED
         ) {
-            if (!foregroundAppDetector.isBrowserPackage(eventPackage)) {
+            val isBrowserEvent = foregroundAppDetector.isBrowserPackage(eventPackage) ||
+                foregroundAppDetector.isBrowserPackage(rootPackage)
+            if (!isBrowserEvent) {
                 return
             }
-            if (now - lastEventAtMillis < 120L) {
+            if (now - lastEventAtElapsedMillis < 120L) {
                 return
             }
         }
 
-        val startedAt = System.currentTimeMillis()
-        if (eventPackage != null) {
-            val lastPackage = lastForegroundPackage
-            val lastAt = lastEventAtMillis
-            if (lastPackage == eventPackage &&
+        val startedAtWall = System.currentTimeMillis()
+        val startedAtElapsed = now
+        if (resolvedPackage != null) {
+            val lastPackage = lastForegroundPackage
+            val lastAt = lastEventAtElapsedMillis
+            if (lastPackage == resolvedPackage &&
                 event.eventType != AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED
             ) {
                 val cooldown = if (event.eventType == AccessibilityEvent.TYPE_WINDOW_CONTENT_CHANGED) {
                     120L
                 } else {
                     220L
                 }
-                if (startedAt - lastAt < cooldown) {
+                if (startedAtElapsed - lastAt < cooldown) {
                     return
                 }
             }
-            lastForegroundPackage = eventPackage
-            lastEventAtMillis = startedAt
+            lastForegroundPackage = resolvedPackage
+            lastEventAtElapsedMillis = startedAtElapsed
         }
 
-        val isBrowser = foregroundAppDetector.isBrowserPackage(eventPackage)
+        val isBrowser = foregroundAppDetector.isBrowserPackage(resolvedPackage)
         val snapshot = foregroundAppDetector.createSnapshot(
             event,
             if (isBrowser) rootInActiveWindow else null,
         )
 
-        handleSnapshot(snapshot, startedAt)
+        handleSnapshot(snapshot, startedAtWall)
*** End Patch
