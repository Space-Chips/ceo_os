*** Begin Patch
*** Update File: android/app/src/main/kotlin/com/wakeapp/ceoos/BlockingAccessibilityService.kt
@@
 import android.content.SharedPreferences
 import android.os.Process
-import android.os.Handler
-import android.os.Looper
 import android.provider.Settings
 import android.os.SystemClock
 import android.util.Log
 import android.view.accessibility.AccessibilityEvent
@@
 
-    private val mainHandler = Handler(Looper.getMainLooper())
-
     private var shield: InterceptionShield? = null
@@
-        val startedAtWall = System.currentTimeMillis()
-        val startedAtElapsed = now
+        val startedAtWall = System.currentTimeMillis()
+        val startedAtElapsed = now
+        val startedAtUptime = SystemClock.uptimeMillis()
@@
-        val stabilizedSnapshot = stabilizeSnapshotIfNeeded(snapshot, startedAtElapsed)
-        handleSnapshot(stabilizedSnapshot, startedAtWall, startedAtElapsed, event.eventTime)
+        val stabilizedSnapshot = stabilizeSnapshotIfNeeded(snapshot, startedAtElapsed)
+        handleSnapshot(
+            snapshot = stabilizedSnapshot,
+            startedAt = startedAtWall,
+            startedAtElapsed = startedAtElapsed,
+            startedAtUptime = startedAtUptime,
+            eventUptimeMillis = event.eventTime,
+        )
@@
             handleSnapshot(
                 snapshot = snapshot,
                 startedAt = System.currentTimeMillis(),
                 startedAtElapsed = SystemClock.elapsedRealtime(),
-                eventTimeMillis = syntheticEvent.eventTime,
+                startedAtUptime = SystemClock.uptimeMillis(),
+                eventUptimeMillis = syntheticEvent.eventTime,
             )
         } finally {
             syntheticEvent.recycle()
         }
     }
 
     private fun handleSnapshot(
         snapshot: ForegroundSnapshot,
         startedAt: Long,
         startedAtElapsed: Long,
-        eventTimeMillis: Long,
+        startedAtUptime: Long,
+        eventUptimeMillis: Long,
     ) {
@@
-            val eventDelay = if (eventTimeMillis > 0L) {
-                (startedAtElapsed - eventTimeMillis).coerceAtLeast(0L)
+            // AccessibilityEvent.eventTime is based on uptimeMillis, not elapsedRealtime.
+            val eventDelay = if (eventUptimeMillis > 0L) {
+                (startedAtUptime - eventUptimeMillis).coerceAtLeast(0L)
             } else {
                 0L
             }
+            val decisionLabel = decisionLabel(effectiveDecision)
             BlockingLogger.record(
                 eventType = snapshot.eventType,
                 packageName = snapshot.packageName,
                 className = snapshot.className,
                 detectedUrl = snapshot.detectedBrowserUrl,
-                decision = effectiveDecision.javaClass.simpleName + when (effectiveDecision) {
-                    is BlockDecision.BlockApp -> ":${effectiveDecision.reason}"
-                    is BlockDecision.BlockUrl -> ":${effectiveDecision.reason}"
-                    else -> ""
-                },
+                decision = decisionLabel,
                 gateLaunched = gateLaunched,
                 gateFailureReason = gateFailureReason,
                 latencyMillis = (System.currentTimeMillis() - startedAt).coerceAtLeast(0L),
                 eventDelayMillis = eventDelay,
                 mode = policyState.currentMode.name.lowercase(),
             )
         }
     }
@@
     private fun openBrowserHome(browserPackage: String?, blockedDomain: String?) {
@@
-        try {
-            if (packageManager.resolveActivity(blankIntent, 0) != null) {
-                startActivity(blankIntent)
-                return
-            }
-            if (packageManager.resolveActivity(intent, 0) != null) {
-                startActivity(intent)
-                return
-            }
-            if (launchIntent != null) {
-                startActivity(launchIntent)
-                return
-            }
-        } catch (_: Exception) {
-        }
+        // Important: don't use resolveActivity() here; package visibility restrictions can make it lie.
+        // Just try the intents, and fall back on exceptions.
+        try {
+            if (pkg == "com.android.chrome" || pkg == "com.brave.browser") {
+                try {
+                    startActivity(
+                        Intent(Intent.ACTION_VIEW, Uri.parse("chrome://newtab")).apply {
+                            `package` = pkg
+                            addFlags(
+                                Intent.FLAG_ACTIVITY_NEW_TASK or
+                                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
+                                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
+                                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
+                            )
+                        },
+                    )
+                    return
+                } catch (_: Exception) {
+                }
+            }
+            try {
+                startActivity(blankIntent)
+                return
+            } catch (_: Exception) {
+                try {
+                    startActivity(intent)
+                    return
+                } catch (_: Exception) {
+                    if (launchIntent != null) {
+                        startActivity(launchIntent)
+                        return
+                    }
+                }
+            }
+        } catch (_: Exception) {
+        }
 
         performGlobalAction(GLOBAL_ACTION_HOME)
     }
@@
     private fun resolveForegroundFromUsageStats(): String? {
@@
     }
+
+    private fun decisionLabel(decision: BlockDecision): String {
+        return when (decision) {
+            BlockDecision.Allow -> "allow"
+            BlockDecision.IgnoreTransient -> "ignore_transient"
+            is BlockDecision.BlockApp -> "block_app:${decision.reason}"
+            is BlockDecision.BlockUrl -> "block_url:${decision.reason}"
+        }
+    }
 }
*** End Patch