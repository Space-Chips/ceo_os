*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/BlockingAccessibilityService.kt
@@
 import android.content.ComponentName
 import android.content.Context
 import android.content.Intent
-import android.net.Uri
 import android.os.Build
@@
     @Volatile
     private var lastPermissionReturnAt: Long = 0L
-
-    @Volatile
-    private var browserHomeGracePackage: String? = null
-
-    @Volatile
-    private var browserHomeGraceUntilElapsed: Long = 0L
-
-    @Volatile
-    private var browserHomeGraceDomain: String? = null
@@
-        fun onBrowserHomeRequested(browserPackage: String?) {
-            instance?.registerBrowserHomeRedirect(browserPackage, null)
-        }
-
-        fun onBrowserHomeRequested(browserPackage: String?, blockedDomain: String?) {
-            instance?.registerBrowserHomeRedirect(browserPackage, blockedDomain)
-        }
-
         fun debugSnapshot(): Map<String, Any?> = BlockingLogger.snapshot()
     }
@@
         serviceScope.cancel()
         gateLauncher.dismiss(this)
-        browserHomeGracePackage = null
-        browserHomeGraceUntilElapsed = 0L
-        browserHomeGraceDomain = null
         shield?.hide()
         shield = null
@@
-        val effectiveDecision = when (decision) {
-            is BlockDecision.BlockUrl -> {
-                if (shouldIgnoreDuringBrowserHomeGrace(decision, snapshot, startedAtElapsed)) {
-                    BlockDecision.IgnoreTransient
-                } else {
-                    decision
-                }
-            }
-            else -> decision
-        }
+        val effectiveDecision = decision
@@
             is BlockDecision.BlockUrl -> {
-                openBrowserHome(decision.browserPackage, decision.target)
+                performGlobalAction(GLOBAL_ACTION_HOME)
             }
             else -> Unit
         }
     }
-
-    private fun openBrowserHome(browserPackage: String?, blockedDomain: String?) {
-        val pkg = browserPackage?.trim().orEmpty()
-        if (pkg.isEmpty()) {
-            performGlobalAction(GLOBAL_ACTION_HOME)
-            return
-        }
-
-        registerBrowserHomeRedirect(pkg, blockedDomain)
-
-        val safeUrlIntent = Intent(Intent.ACTION_VIEW, Uri.parse("https://www.google.com/")).apply {
-            `package` = pkg
-            addFlags(
-                Intent.FLAG_ACTIVITY_NEW_TASK or
-                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
-                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
-                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
-            )
-        }
-
-        val blankIntent = Intent(Intent.ACTION_VIEW, Uri.parse("about:blank")).apply {
-            `package` = pkg
-            addFlags(
-                Intent.FLAG_ACTIVITY_NEW_TASK or
-                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
-                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
-                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
-            )
-        }
-
-        val intent = Intent(Intent.ACTION_MAIN).apply {
-            addCategory(Intent.CATEGORY_APP_BROWSER)
-            `package` = pkg
-            addFlags(
-                Intent.FLAG_ACTIVITY_NEW_TASK or
-                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
-                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
-                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
-            )
-        }
-        val launchIntent = packageManager.getLaunchIntentForPackage(pkg)?.apply {
-            addFlags(
-                Intent.FLAG_ACTIVITY_NEW_TASK or
-                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
-                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
-                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
-            )
-        }
-
-        // Important: don't use resolveActivity() here; package visibility restrictions can make it lie.
-        // Just try the intents, and fall back on exceptions.
-        try {
-            try {
-                startActivity(safeUrlIntent)
-                return
-            } catch (_: Exception) {
-            }
-            if (pkg == "com.android.chrome" || pkg == "com.brave.browser") {
-                try {
-                    startActivity(
-                        Intent(Intent.ACTION_VIEW, Uri.parse("chrome://newtab")).apply {
-                            `package` = pkg
-                            addFlags(
-                                Intent.FLAG_ACTIVITY_NEW_TASK or
-                                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
-                                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
-                                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
-                            )
-                        },
-                    )
-                    return
-                } catch (_: Exception) {
-                }
-            }
-            try {
-                startActivity(blankIntent)
-                return
-            } catch (_: Exception) {
-                try {
-                    startActivity(intent)
-                    return
-                } catch (_: Exception) {
-                    if (launchIntent != null) {
-                        startActivity(launchIntent)
-                        return
-                    }
-                }
-            }
-        } catch (_: Exception) {
-        }
-
-        performGlobalAction(GLOBAL_ACTION_HOME)
-    }
@@
-    private fun registerBrowserHomeRedirect(browserPackage: String?) {
-        val pkg = browserPackage?.trim().orEmpty()
-        if (pkg.isEmpty()) return
-        foregroundAppDetector.clearRecentDomain(pkg)
-        browserHomeGracePackage = pkg
-        browserHomeGraceUntilElapsed = SystemClock.elapsedRealtime() + 1_200L
-        browserHomeGraceDomain = null
-    }
-
-    private fun registerBrowserHomeRedirect(browserPackage: String?, blockedDomain: String?) {
-        val pkg = browserPackage?.trim().orEmpty()
-        if (pkg.isEmpty()) return
-        foregroundAppDetector.clearRecentDomain(pkg)
-        browserHomeGracePackage = pkg
-        browserHomeGraceUntilElapsed = SystemClock.elapsedRealtime() + 250L
-        browserHomeGraceDomain = blockedDomain?.trim()?.ifEmpty { null }
-    }
-
-    private fun isBrowserHomeGraceActive(
-        browserPackage: String?,
-        nowElapsed: Long,
-    ): Boolean {
-        val pkg = browserPackage?.trim().orEmpty()
-        if (pkg.isEmpty()) return false
-        val gracePackage = browserHomeGracePackage ?: return false
-        if (gracePackage != pkg) return false
-        val active = nowElapsed <= browserHomeGraceUntilElapsed
-        if (!active) {
-            browserHomeGracePackage = null
-            browserHomeGraceUntilElapsed = 0L
-            browserHomeGraceDomain = null
-        }
-        return active
-    }
-
-    private fun shouldIgnoreDuringBrowserHomeGrace(
-        decision: BlockDecision.BlockUrl,
-        snapshot: ForegroundSnapshot,
-        nowElapsed: Long,
-    ): Boolean {
-        if (!isBrowserHomeGraceActive(snapshot.packageName, nowElapsed)) return false
-        val graceDomain = browserHomeGraceDomain ?: return false
-        if (decision.target != graceDomain) return false
-        return snapshot.detectedBrowserUrl == null
-    }
-
*** End Patch
