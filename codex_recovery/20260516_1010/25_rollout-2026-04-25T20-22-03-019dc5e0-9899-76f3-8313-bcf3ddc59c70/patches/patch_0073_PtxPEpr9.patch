*** Begin Patch
*** Update File: lib/features/control_center_setup/widgets/dashboard_widgets_screen.dart
@@
 class DashboardWidgetToggle extends StatelessWidget {
@@
 }
+
+class DashboardWidgetSelector extends StatelessWidget {
+  final ControlCenterSetupState state;
+  final void Function(DashboardWidget widget) onToggle;
+
+  const DashboardWidgetSelector({
+    super.key,
+    required this.state,
+    required this.onToggle,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    return ListView(
+      physics: const BouncingScrollPhysics(),
+      padding: EdgeInsets.zero,
+      children: [
+        DashboardWidgetToggle(
+          label: 'Focus',
+          icon: CupertinoIcons.bolt_fill,
+          value: state.enabledDashboardWidgets.contains(DashboardWidget.focus),
+          onChanged: (_) => onToggle(DashboardWidget.focus),
+        ),
+        DashboardWidgetToggle(
+          label: 'Notes',
+          icon: CupertinoIcons.doc_text_fill,
+          value: state.enabledDashboardWidgets.contains(DashboardWidget.notes),
+          onChanged: (_) => onToggle(DashboardWidget.notes),
+        ),
+        DashboardWidgetToggle(
+          label: 'Win streak',
+          icon: CupertinoIcons.flame_fill,
+          value: state.enabledDashboardWidgets.contains(
+            DashboardWidget.winStreak,
+          ),
+          onChanged: (_) => onToggle(DashboardWidget.winStreak),
+        ),
+        DashboardWidgetToggle(
+          label: 'Rank',
+          icon: CupertinoIcons.star_fill,
+          value: state.enabledDashboardWidgets.contains(DashboardWidget.rank),
+          onChanged: (_) => onToggle(DashboardWidget.rank),
+        ),
+        DashboardWidgetToggle(
+          label: 'Blackout button',
+          icon: CupertinoIcons.moon_fill,
+          value: state.enabledDashboardWidgets.contains(
+            DashboardWidget.blackoutButton,
+          ),
+          onChanged: (_) => onToggle(DashboardWidget.blackoutButton),
+        ),
+      ],
+    );
+  }
+}
 
*** End Patch