*** Begin Patch
*** Update File: lib/features/home/home_screen.dart
@@
                     child: _TopShortcutsBar(
                       rankLevel: rankLevel,
                       rankName: rankName,
                       rankLabel: localizedRankName,
                       winStreak: _winStreak,
+                      showRank: _enabledDashboardWidgets.contains(
+                        DashboardWidget.rank,
+                      ),
+                      showFocus: _enabledDashboardWidgets.contains(
+                        DashboardWidget.focus,
+                      ),
+                      showNotes: _enabledDashboardWidgets.contains(
+                        DashboardWidget.notes,
+                      ),
+                      showWinStreak: _enabledDashboardWidgets.contains(
+                        DashboardWidget.winStreak,
+                      ),
                       onOpenMenu: _openSecondaryMenu,
                       onOpenRank: () => context.push('/rank'),
                       onOpenFocus: _openQuickFocusSession,
                       onOpenNotes: () => context.push('/notes'),
                       onOpenStreak: () => context.push('/win-streak'),
                     ),
@@
 class _TopShortcutsBar extends StatelessWidget {
   final int rankLevel;
   final String rankName;
   final String rankLabel;
   final int winStreak;
+  final bool showRank;
+  final bool showFocus;
+  final bool showNotes;
+  final bool showWinStreak;
   final VoidCallback onOpenMenu;
   final VoidCallback onOpenRank;
   final VoidCallback onOpenFocus;
   final VoidCallback onOpenNotes;
   final VoidCallback onOpenStreak;
 
   const _TopShortcutsBar({
     required this.rankLevel,
     required this.rankName,
     required this.rankLabel,
     required this.winStreak,
+    required this.showRank,
+    required this.showFocus,
+    required this.showNotes,
+    required this.showWinStreak,
     required this.onOpenMenu,
     required this.onOpenRank,
     required this.onOpenFocus,
     required this.onOpenNotes,
     required this.onOpenStreak,
   });
*** End Patch