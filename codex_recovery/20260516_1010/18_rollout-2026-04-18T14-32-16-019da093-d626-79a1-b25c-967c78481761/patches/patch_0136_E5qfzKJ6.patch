*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/focus/focus_screen.dart
@@
   Widget _buildActive(FocusProvider provider) {
+    final language = context.watch<LanguageProvider>();
     final isExitPending = provider.state == FocusState.exitPending;
@@
-    final timerLabel = isExitPending ? 'EXIT WINDOW' : 'DEEP WORK';
+    final timerLabel = isExitPending
+        ? language.t('focus_exit_window')
+        : language.t('focus_deep_work');
     final controlTitle = switch (provider.state) {
-      FocusState.focusing => 'Focus Session',
-      FocusState.exitPending => 'Exit Request',
-      FocusState.shortBreak => 'Focus Session',
-      FocusState.longBreak => 'Focus Session',
-      FocusState.requestingBreak => 'Focus Session',
-      FocusState.breakOptionsMenu => 'Focus Session',
-      FocusState.idle => 'Focus Session',
+      FocusState.focusing => language.t('focus_session'),
+      FocusState.exitPending => language.t('focus_exit_request'),
+      FocusState.shortBreak => language.t('focus_session'),
+      FocusState.longBreak => language.t('focus_session'),
+      FocusState.requestingBreak => language.t('focus_session'),
+      FocusState.breakOptionsMenu => language.t('focus_session'),
+      FocusState.idle => language.t('focus_session'),
     };
@@
-          _header(title: 'Focus'),
+          _header(title: language.t('focus')),
@@
                 if (provider.state == FocusState.focusing)
                   _FocusSecondaryButton(
-                    label: 'REQUEST EXIT (100s)',
+                    label: language.t('focus_request_exit'),
                     onTap: provider.requestEarlyExit,
                   )
                 else if (provider.state == FocusState.exitPending) ...[
@@
                     child: Text(
                       provider.canFinalizeEarlyExit
-                          ? 'You can now leave Focus Mode. This will break your streak.'
-                          : 'Hold for ${provider.exitCountdownLabel} before quitting. You can return anytime.',
+                          ? language.t('focus_exit_ready_message')
+                          : language
+                                .t('focus_hold_before_quit')
+                                .replaceAll(
+                                  '{countdown}',
+                                  provider.exitCountdownLabel,
+                                ),
@@
                   _FocusSecondaryButton(
-                    label: 'RETURN TO SESSION',
+                    label: language.t('focus_return_to_session'),
                     onTap: provider.returnToSession,
                   ),
                   const SizedBox(height: 10),
                   if (provider.canFinalizeEarlyExit)
                     _FocusSecondaryButton(
-                      label: 'QUIT NOW (RESET STREAK)',
+                      label: language.t('focus_quit_now_reset_streak'),
                       onTap: () => provider.finalizeEarlyExit(),
                     )
                   else
@@
                       child: Text(
-                        'QUIT AVAILABLE IN ${provider.exitCountdownLabel}',
+                        language
+                            .t('focus_quit_available_in')
+                            .replaceAll(
+                              '{countdown}',
+                              provider.exitCountdownLabel,
+                            ),
@@
                 ] else
                   _FocusSecondaryButton(
-                    label: 'RETURN TO HOME',
+                    label: language.t('focus_return_home'),
                     onTap: () => context.go('/home'),
                   ),
*** End Patch
