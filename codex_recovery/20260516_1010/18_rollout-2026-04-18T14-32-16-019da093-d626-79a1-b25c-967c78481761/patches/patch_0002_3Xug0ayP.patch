*** Begin Patch
*** Update File: /Users/timo/ceo_os/ios/FocusActivityMonitor/FocusActivityMonitorExtension.swift
@@
     static let classicSelectionStorageKey = "classic_selection_payloads_v1"
     static let classicEnabledStorageKey = "classic_shield_enabled_v1"
     static let classicPauseSelectionStorageKey = "classic_pause_selection_payloads_v1"
     static let classicPauseEnabledStorageKey = "classic_pause_enabled_v1"
+    static let ceoEnabledStorageKey = "ceo_shield_enabled_v1"
     static let classicRestPeriodsStorageKey = "classic_rest_periods_v1"
     static let classicDailyLimitEntriesStorageKey = "classic_daily_limit_entries_v1"
     static let classicDailyLimitTriggeredIdsStorageKey = "classic_daily_limit_triggered_ids_v1"
     static let classicWebsiteDomainsStorageKey = "classic_website_domains_v1"
     static let classicPauseWebsiteDomainsStorageKey =
         "classic_pause_website_domains_v1"
+    static let ceoAlwaysAllowedBundleIds: Set<String> = [
+        "com.apple.mobilephone",
+        "com.apple.MobileSMS",
+        "com.apple.mobilecal"
+    ]
 }
@@
     private func selfApplicationToken() -> ApplicationToken? {
         Application(bundleIdentifier: FocusMonitorShared.hostAppBundleId).token
     }
+
+    private func applicationTokens(for bundleIds: Set<String>) -> Set<ApplicationToken> {
+        var tokens = Set<ApplicationToken>()
+        for raw in bundleIds {
+            let bundleId = raw.trimmingCharacters(in: .whitespacesAndNewlines)
+            guard !bundleId.isEmpty, let token = Application(bundleIdentifier: bundleId).token else {
+                continue
+            }
+            tokens.insert(token)
+        }
+        return tokens
+    }
+
+    private func ceoAllowedApplicationTokens() -> Set<ApplicationToken> {
+        var tokens = applicationTokens(for: FocusMonitorShared.ceoAlwaysAllowedBundleIds)
+        if let selfToken = selfApplicationToken() {
+            tokens.insert(selfToken)
+        }
+        return tokens
+    }
@@
     private func applyShield(includeFocus: Bool) {
         let defaults = sharedDefaults()
+        let ceoEnabled = defaults?.bool(forKey: FocusMonitorShared.ceoEnabledStorageKey) ?? false
+        if ceoEnabled {
+            let ceoAllowedTokens = ceoAllowedApplicationTokens()
+            store.shield.applications = nil
+            store.shield.applicationCategories = .all(except: ceoAllowedTokens)
+            if #available(iOS 16.0, *) {
+                store.shield.webDomainCategories = .all(except: Set())
+                store.webContent.blockedByFilter = .all(except: Set())
+            }
+            store.shield.webDomains = nil
+            return
+        }
 
         var payloads: [String] = []
         let shouldIncludeFocus = includeFocus || (defaults?.bool(forKey: FocusMonitorShared.focusEnabledStorageKey) ?? false)
*** End Patch
