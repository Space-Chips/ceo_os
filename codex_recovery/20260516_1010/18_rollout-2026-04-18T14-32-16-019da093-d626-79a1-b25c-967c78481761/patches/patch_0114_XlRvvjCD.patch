*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/providers/focus_provider.dart
@@
   Future<void> syncPlannedFocusSessions(List<CalendarEvent> events) async {
@@
     await _focusService.syncPlannedSessions(payload);
   }
+
+  Future<void> _restoreSessionFromPrefs() async {
+    final prefs = await SharedPreferences.getInstance();
+    final rawState = prefs.getString(_prefsState);
+    if (rawState == null || rawState == FocusState.idle.name) {
+      return;
+    }
+
+    final startAtMs = prefs.getInt(_prefsStartAtMs);
+    final endAtMs = prefs.getInt(_prefsEndAtMs);
+    if (startAtMs == null || endAtMs == null) {
+      await _clearPersistedSessionState();
+      return;
+    }
+
+    final restoredState = rawState == FocusState.exitPending.name
+        ? FocusState.exitPending
+        : FocusState.focusing;
+    final restoredStart = DateTime.fromMillisecondsSinceEpoch(startAtMs);
+    final restoredEnd = DateTime.fromMillisecondsSinceEpoch(endAtMs);
+    final restoredExitReadyAtMs = prefs.getInt(_prefsExitReadyAtMs);
+    final now = DateTime.now();
+    sessionTitle = prefs.getString(_prefsSessionTitle);
+    linkedTaskId = prefs.getString(_prefsLinkedTaskId);
+
+    if (!restoredEnd.isAfter(now)) {
+      _state = restoredState;
+      _isFocusModeActive = true;
+      _sessionStartTime = restoredStart;
+      _sessionEndTime = restoredEnd;
+      _remainingSeconds = 0;
+      _focusExitReadyAt = null;
+
+      try {
+        await _logSession(completed: true);
+      } catch (error) {
+        AppLogger.warning(
+          'Failed to log recovered Focus session completion. $error',
+        );
+      }
+      await _syncClassicBaselineFromBlockedAppsSites();
+      _focusShieldOverrideActive = false;
+
+      _state = FocusState.idle;
+      _isFocusModeActive = false;
+      _sessionStartTime = null;
+      _sessionEndTime = null;
+      _focusExitReadyAt = null;
+      _remainingSeconds = focusDurationMinutes * 60;
+      await _clearPersistedSessionState();
+      notifyListeners();
+      return;
+    }
+
+    _state = restoredState;
+    _isFocusModeActive = true;
+    _sessionStartTime = restoredStart;
+    _sessionEndTime = restoredEnd;
+    _remainingSeconds = restoredEnd
+        .difference(now)
+        .inSeconds
+        .clamp(0, focusDurationMinutes * 60);
+    _focusExitReadyAt = restoredState == FocusState.exitPending
+        ? (restoredExitReadyAtMs != null
+              ? DateTime.fromMillisecondsSinceEpoch(restoredExitReadyAtMs)
+              : now.add(
+                  const Duration(seconds: _focusExitCountdownSeconds),
+                ))
+        : null;
+
+    final overrideReady = await _applyFocusShieldOverrideFromBlockedAppsSites();
+    if (!overrideReady) {
+      _state = FocusState.idle;
+      _isFocusModeActive = false;
+      _sessionStartTime = null;
+      _sessionEndTime = null;
+      _focusExitReadyAt = null;
+      _remainingSeconds = focusDurationMinutes * 60;
+      await _clearPersistedSessionState();
+      notifyListeners();
+      return;
+    }
+
+    _startTimer();
+    notifyListeners();
+  }
+
+  bool get _isPersistableState =>
+      _state == FocusState.focusing || _state == FocusState.exitPending;
+
+  Future<void> _persistSessionState() async {
+    final prefs = await SharedPreferences.getInstance();
+    if (!_isPersistableState ||
+        !_isFocusModeActive ||
+        _sessionStartTime == null ||
+        _sessionEndTime == null) {
+      await _clearPersistedSessionState();
+      return;
+    }
+
+    await prefs.setString(_prefsState, _state.name);
+    await prefs.setInt(_prefsStartAtMs, _sessionStartTime!.millisecondsSinceEpoch);
+    await prefs.setInt(_prefsEndAtMs, _sessionEndTime!.millisecondsSinceEpoch);
+
+    if (_focusExitReadyAt != null) {
+      await prefs.setInt(
+        _prefsExitReadyAtMs,
+        _focusExitReadyAt!.millisecondsSinceEpoch,
+      );
+    } else {
+      await prefs.remove(_prefsExitReadyAtMs);
+    }
+
+    if (sessionTitle != null && sessionTitle!.trim().isNotEmpty) {
+      await prefs.setString(_prefsSessionTitle, sessionTitle!.trim());
+    } else {
+      await prefs.remove(_prefsSessionTitle);
+    }
+
+    if (linkedTaskId != null && linkedTaskId!.trim().isNotEmpty) {
+      await prefs.setString(_prefsLinkedTaskId, linkedTaskId!.trim());
+    } else {
+      await prefs.remove(_prefsLinkedTaskId);
+    }
+  }
+
+  Future<void> _clearPersistedSessionState() async {
+    final prefs = await SharedPreferences.getInstance();
+    await prefs.setString(_prefsState, FocusState.idle.name);
+    await prefs.remove(_prefsStartAtMs);
+    await prefs.remove(_prefsEndAtMs);
+    await prefs.remove(_prefsExitReadyAtMs);
+    await prefs.remove(_prefsSessionTitle);
+    await prefs.remove(_prefsLinkedTaskId);
+  }
*** End Patch
