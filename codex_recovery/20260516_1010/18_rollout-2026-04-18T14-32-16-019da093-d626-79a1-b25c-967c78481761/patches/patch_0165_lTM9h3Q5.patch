*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
 class _CeoModeCard extends StatelessWidget {
   final String statusLabel;
+  final String title;
+  final String maxFocusLabel;
+  final String sessionPrefix;
+  final String exitPrefix;
   final VoidCallback onOpenCeoMode;
 
-  const _CeoModeCard({required this.statusLabel, required this.onOpenCeoMode});
+  const _CeoModeCard({
+    required this.statusLabel,
+    required this.title,
+    required this.maxFocusLabel,
+    required this.sessionPrefix,
+    required this.exitPrefix,
+    required this.onOpenCeoMode,
+  });
 
   String get _displayStatusLabel {
     if (statusLabel == 'MAXIMUM FOCUS') {
-      return 'Max focus';
+      return maxFocusLabel;
     }
     if (statusLabel.startsWith('IN SESSION ')) {
-      return statusLabel.replaceFirst('IN SESSION ', 'Session ');
+      return statusLabel.replaceFirst('IN SESSION ', '$sessionPrefix ');
     }
     if (statusLabel.startsWith('EXIT WINDOW ')) {
-      return statusLabel.replaceFirst('EXIT WINDOW ', 'Exit ');
+      return statusLabel.replaceFirst('EXIT WINDOW ', '$exitPrefix ');
     }
     return statusLabel;
   }
@@
                   Text(
-                    'Blackout Mode',
+                    title,
                     maxLines: 2,
                     overflow: TextOverflow.ellipsis,
*** End Patch
