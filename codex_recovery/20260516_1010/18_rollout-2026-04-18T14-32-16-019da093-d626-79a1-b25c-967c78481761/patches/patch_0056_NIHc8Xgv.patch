*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/stats_screen.dart
@@
         if (snapshot.todayCard != null) ...[
           const SizedBox(height: 10),
           _ShareCardContainer(
             title: 'Today card',
+            child: Builder(
+              builder: (_) {
+                final todayCard = snapshot.todayCard;
+                if (todayCard == null) return const SizedBox.shrink();
+                return _TodayCard(
+                  card: todayCard,
+                  score: wakeScore > 0 ? wakeScore : todayCard.attentionScore,
+                );
+              },
+            ),
-            child: _TodayCard(
-              card: snapshot.todayCard!,
-              score: wakeScore > 0
-                  ? wakeScore
-                  : snapshot.todayCard!.attentionScore,
-            ),
           ),
         ],
@@
         if (snapshot.todayCard != null) ...[
           const SizedBox(height: 4),
           _ShareCardContainer(
             title: 'Screenshot summary',
-            child: _TodayCard(
-              card: snapshot.todayCard!,
-              score: wakeScore > 0
-                  ? wakeScore
-                  : snapshot.todayCard!.attentionScore,
-            ),
+            child: Builder(
+              builder: (_) {
+                final todayCard = snapshot.todayCard;
+                if (todayCard == null) return const SizedBox.shrink();
+                return _TodayCard(
+                  card: todayCard,
+                  score: wakeScore > 0 ? wakeScore : todayCard.attentionScore,
+                );
+              },
+            ),
           ),
         ],
@@
   final isScheduled =
       !normalizedDay.isBefore(createdDay) &&
       (habit.isDaily ||
-          (habit.specificDays != null && habit.specificDays!.contains(dayNum)));
+          (habit.specificDays?.contains(dayNum) ?? false));
*** End Patch
