*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/auth/login_screen.dart
@@
   Future<void> _showError(dynamic error) async {
     if (!mounted) return;
     await showAuthDialog(
       context,
-      title: 'Sign-in error',
+      title: _t('auth_signin_error_title'),
       message: humanizeAuthError(error),
     );
   }
@@
     if (!isValidEmail(email)) {
       await showAuthDialog(
         context,
-        title: 'Invalid email',
-        message: 'Please enter a valid email address.',
+        title: _t('auth_invalid_email_title'),
+        message: _t('auth_invalid_email_message'),
       );
       return;
     }
     if (password.isEmpty) {
       await showAuthDialog(
         context,
-        title: 'Password required',
-        message: 'Please enter your password to continue.',
+        title: _t('auth_password_required_title'),
+        message: _t('auth_password_required_message'),
       );
       return;
     }
@@
     if (!isValidEmail(email)) {
       await showAuthDialog(
         context,
-        title: 'Reset password',
-        message: 'Enter your account email first, then try again.',
+        title: _t('auth_reset_password_title'),
+        message: _t('auth_reset_password_enter_email_first'),
       );
       return;
     }
@@
       if (!mounted) return;
       await showAuthDialog(
         context,
-        title: 'Check your email',
-        message:
-            'If an account exists for $email, a password reset email has been sent.',
+        title: _t('auth_check_email_title'),
+        message: _t(
+          'auth_check_email_reset_message',
+        ).replaceAll('{email}', email),
       );
*** End Patch
