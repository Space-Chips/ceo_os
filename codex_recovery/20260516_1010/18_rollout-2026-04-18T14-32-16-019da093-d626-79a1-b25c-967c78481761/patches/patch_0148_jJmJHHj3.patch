*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
 import '../../core/models/habit_models.dart';
 import '../../core/models/task_models.dart';
 import '../../core/providers/habit_provider.dart';
+import '../../core/providers/language_provider.dart';
 import '../../core/repositories/feature_repository.dart';
@@
 class _HabitsScreenState extends State<HabitsScreen> {
@@
   void _onTopActionPressed() {
@@
   String _swipeHint() {
+    final language = context.read<LanguageProvider>();
     if (_activeTab == 0) {
-      return 'Swipe right for Goals & Contract →';
+      return language.t('habits_swipe_right_goals');
     }
-    return '← Swipe left for Habits Grid';
+    return language.t('habits_swipe_left_grid');
   }
 
   Widget _topRow() {
-    final actionLabel = _activeTab == 0 ? '+ Habit' : '+ Goal';
+    final language = context.watch<LanguageProvider>();
+    final actionLabel = _activeTab == 0
+        ? language.t('habits_add_habit')
+        : language.t('habits_add_goal');
@@
                   Text(
-                  'Home',
+                  language.t('home'),
@@
   Widget _titleBlock() {
+    final language = context.watch<LanguageProvider>();
     return Padding(
@@
           Text(
-            'Habits',
+            language.t('habits'),
*** End Patch
