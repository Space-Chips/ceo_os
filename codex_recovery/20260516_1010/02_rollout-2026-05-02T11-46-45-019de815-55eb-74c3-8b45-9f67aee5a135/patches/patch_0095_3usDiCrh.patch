*** Begin Patch
*** Update File: lib/core/services/home_widget_service.dart
@@
   @override
   Widget build(BuildContext context) {
     return _CeoBaseCard(
       headerTitle: title,
       headerSubtitle: dateLabel,
       child: LayoutBuilder(
         builder: (context, constraints) {
           final isLarge = constraints.maxWidth >= 300;
+          final isEmptyDay = tasksTotal == 0 && habitsTotal == 0 && eventsCount == 0;
+          if (isEmptyDay) {
+            return Column(
+              crossAxisAlignment: CrossAxisAlignment.start,
+              children: [
+                const SizedBox(height: 10),
+                _CeoEmptyState(
+                  title: noEventsLabel.isEmpty ? 'Your day is empty' : ' ',
+                  subtitle: 'Add tasks, habits, or events in WakeApp',
+                ),
+              ],
+            );
+          }
           if (!isLarge) {
             return Column(
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
*** End Patch