*** Begin Patch
*** Update File: lib/core/repositories/premium_repository.dart
@@
   Future<PremiumRuntime> getRuntime() async {
     final config = await getConfig();
     await _syncLaunchEntitlementsIfNeeded();
-    final isTestFlight = await AppEnvironmentService.isTestFlight();
-    await _syncTestFlightPremiumOverrideIfNeeded(isTestFlight: isTestFlight);
+    final isTestFlight = await AppEnvironmentService.isTestFlight();
     final subscriptionStatus = await _getSubscriptionStatus();
     final entitlements = await getUserEntitlements();
     final graceActive = await _isClientGraceActive();
     final resolved = _resolveEntitlements(
       config: config,
@@
     );
 
     return PremiumRuntime(
@@
     );
   }
+
+  /// Grant Premium permanently to accounts created from a TestFlight install.
+  /// This is the ONLY automatic premium condition requested: if the account
+  /// is created via TestFlight, it becomes premium and stays premium.
+  Future<void> grantPremiumForTestFlightAccountIfEligible() async {
+    final uid = _currentUserId;
+    if (uid == null) return;
+
+    final isTestFlight = await AppEnvironmentService.isTestFlight();
+    if (!TesterConfig.testFlightPremiumEnabled || !isTestFlight) return;
+
+    final betaKey = TesterConfig.testFlightBetaPremiumKey.trim();
+    if (betaKey.isEmpty) return;
+
+    try {
+      await _diag.log('testflight_account_grant_start');
+      final response = await _client.functions.invoke(
+        'grant-beta-premium',
+        headers: {'x-beta-premium-key': betaKey},
+      );
+      await _diag.log(
+        'testflight_account_grant_done',
+        data: {'status': response.status},
+      );
+    } catch (e) {
+      await _diag.log(
+        'testflight_account_grant_failed',
+        data: {'error': e.toString()},
+      );
+    }
+  }
@@
-  Future<void> _syncTestFlightPremiumOverrideIfNeeded({
-    required bool isTestFlight,
-  }) async {
-    final uid = _currentUserId;
-    if (uid == null) return;
-    if (!TesterConfig.testFlightPremiumEnabled || !isTestFlight) return;
-
-    final betaKey = TesterConfig.testFlightBetaPremiumKey.trim();
-    if (betaKey.isEmpty) return;
-
-    try {
-      final prefs = await SharedPreferences.getInstance();
-      final lastMillis = prefs.getInt('$_testflightSyncKeyPrefix$uid');
-      final last = lastMillis == null
-          ? null
-          : DateTime.fromMillisecondsSinceEpoch(lastMillis);
-      if (last != null &&
-          DateTime.now().difference(last) < _testflightSyncInterval) {
-        return;
-      }
-
-      await _diag.log('testflight_override_sync_start');
-      final response = await _client.functions.invoke(
-        'grant-beta-premium',
-        headers: {'x-beta-premium-key': betaKey},
-      );
-
-      await prefs.setInt(
-        '$_testflightSyncKeyPrefix$uid',
-        DateTime.now().millisecondsSinceEpoch,
-      );
-      await _diag.log(
-        'testflight_override_sync_done',
-        data: {'status': response.status},
-      );
-    } catch (e) {
-      await _diag.log(
-        'testflight_override_sync_failed',
-        data: {'error': e.toString()},
-      );
-    }
-  }
+  // NOTE: intentionally no periodic TestFlight syncing.
+  // Premium must depend on *account creation channel*, not current install.
 
   Future<void> _syncLaunchEntitlementsIfNeeded() async {
*** End Patch