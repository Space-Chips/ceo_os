*** Begin Patch
*** Update File: lib/core/services/premium_onboarding_prompt_service.dart
@@
     if (runtime.resolved.isPremiumUser) {
       await prefs.setBool(key, true);
       return;
     }
 
     await prefs.setBool(key, true);
+    if (!context.mounted) return;
     await showPremiumPaywallSheet(
       context: context,
       runtime: runtime,
       reason: 'onboarding',
     );
   }
 }
*** End Patch