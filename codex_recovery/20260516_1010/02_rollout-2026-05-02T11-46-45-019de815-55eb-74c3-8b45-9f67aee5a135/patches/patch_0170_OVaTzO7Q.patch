*** Begin Patch
*** Update File: lib/core/widgets/home_widgets/widget_design_tokens.dart
@@
   static const double radiusInner = 20;
   static const double radiusCard = 18;
 
-  // Keep outer background visible but minimal: maximize usable widget space.
-  static const EdgeInsets padSquare = EdgeInsets.fromLTRB(12, 12, 12, 10);
-  static const EdgeInsets padRect = EdgeInsets.fromLTRB(12, 12, 12, 10);
+  // Safe internal padding that never touches rounded corners.
+  // small widgets: 14–16 pt, medium widgets: 18–20 pt (per spec).
+  static const EdgeInsets padSquare = EdgeInsets.fromLTRB(15, 15, 15, 14);
+  static const EdgeInsets padRect = EdgeInsets.fromLTRB(19, 18, 19, 16);
*** End Patch