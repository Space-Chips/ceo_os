*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
       for (final selected in picked) {
         final payload = selected.payload.trim();
         if (payload.isEmpty || existingPayloads.contains(payload)) {
           continue;
         }
-        final label = _resolvedAppLabel(selected);
+        var effectiveSelection = selected;
+        if (_isGenericAppName(effectiveSelection.preferredLabel)) {
+          final described = await _focusService.describeAppSelectionPayload(
+            payload,
+          );
+          if (described != null) {
+            effectiveSelection = IosFamilyActivitySelection(
+              payload: payload,
+              appName: described.appName,
+              bundleIdentifier: described.bundleIdentifier,
+            );
+          }
+        }
+        final label = _resolvedAppLabel(effectiveSelection);
         final record = await _repo.createBlockedAppRecord(label);
         if (record == null) continue;
*** End Patch
