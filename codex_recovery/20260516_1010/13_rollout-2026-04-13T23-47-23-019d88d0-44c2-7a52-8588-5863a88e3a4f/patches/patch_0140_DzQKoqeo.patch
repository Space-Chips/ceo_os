*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/focus/block_list_sheet.dart
@@
       if (!provider.isAuthorized) {
         final granted = await provider.requestPermissions();
@@
           return;
         }
       }
-
-      final tokens = await provider.selectAppsNative();
-      if (tokens != null) {
-        setState(() {
-          _blockedPackages = tokens;
-        });
-        return;
-      }
-
       if (mounted) {
         await showCupertinoDialog<void>(
           context: context,
           builder: (ctx) => CupertinoAlertDialog(
-            title: const Text('Unable to Open Apple Picker'),
-            content: const Text(
-              'WakeApp could not open Apple\'s Screen Time app picker right now. Close the app completely, reopen it, and try again.',
+            title: const Text('Use Screen Time Control'),
+            content: const Text(
+              'On iPhone, app and website selection is now managed from Screen Time Control with search suggestions. This legacy editor no longer opens the Apple picker.',
             ),
             actions: [
               CupertinoDialogAction(
                 child: const Text('OK'),
*** End Patch
