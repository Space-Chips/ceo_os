*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
-  Future<void> _addBlockedIosAppSuggestion(_IosAppSuggestion suggestion) async {
-    if (_saving || !Platform.isIOS) return;
-
-    final authorized = await _focusService.isAuthorized();
-    if (!authorized) {
-      final granted = await _focusService.requestPermissions();
-      if (!granted) {
-        await _showNotice(
-          title: 'Permissions required',
-          message:
-              'Allow Screen Time / Family Controls first so selected apps can be blocked during your focus sessions on this device.',
-        );
-        return;
-      }
-    }
-
-    var encoded = _iosAppSelectionByBundle[suggestion.bundleIdentifier];
-    encoded ??= await _focusService.encodeApplicationBundleSelection(
-      suggestion.bundleIdentifier,
-    );
-    final payload = encoded?.payload.trim();
-    if (payload == null || payload.isEmpty) {
-      await _showNotice(
-        title: 'App unavailable',
-        message:
-            '${suggestion.label} cannot be blocked on this iPhone by the current Screen Time API.',
-      );
-      return;
-    }
-
-    final existingPayloads = _blockedApps
-        .map((app) => _appBindings[app.id]?.nativePayload?.trim())
-        .whereType<String>()
-        .where((value) => value.isNotEmpty)
-        .toSet();
-    final existingBundles = _appBindings.values
-        .map((binding) => binding.nativeIdentifier?.trim())
-        .whereType<String>()
-        .where((value) => value.isNotEmpty)
-        .toSet();
-    final resolvedBundle = encoded?.bundleIdentifier?.trim().isNotEmpty == true
-        ? encoded!.bundleIdentifier!.trim()
-        : suggestion.bundleIdentifier;
-    if (existingPayloads.contains(payload) ||
-        existingBundles.contains(resolvedBundle)) {
-      await _showNotice(
-        title: 'Already blocked',
-        message: '${suggestion.label} is already in your blocked apps.',
-      );
-      return;
-    }
-
-    await _runMutation(() async {
-      final existingLabels = _blockedApps
-          .map((app) => (app.appName ?? '').trim())
-          .where((name) => name.isNotEmpty)
-          .toSet();
-      final label = _resolvedAppLabel(
-        IosFamilyActivitySelection(
-          payload: payload,
-          appName: encoded?.appName ?? suggestion.label,
-          bundleIdentifier: resolvedBundle,
-        ),
-        existingLabels,
-      );
-      final record = await _repo.createBlockedAppRecord(label);
-      if (record == null) return;
-      await _classicLocalStore.saveAppBinding(
-        rowId: record.id,
-        nativeIdentifier: resolvedBundle,
-        nativePayload: payload,
-      );
-    });
-  }
+  Future<void> _addBlockedIosAppsFromPicker() async {
+    if (_saving || !Platform.isIOS) return;
+
+    final authorized = await _focusService.isAuthorized();
+    if (!authorized) {
+      final granted = await _focusService.requestPermissions();
+      if (!granted) {
+        await _showNotice(
+          title: 'Permissions required',
+          message:
+              'Allow Screen Time / Family Controls first so selected apps can be blocked during your focus sessions on this device.',
+        );
+        return;
+      }
+    }
+
+    final selections = await _focusService.openFamilyActivityPickerWithMetadata();
+    final picked = (selections ?? [])
+        .where((item) => item.payload.trim().isNotEmpty)
+        .toList(growable: false);
+    if (picked.isEmpty) return;
+
+    await _runMutation(() async {
+      final existingLabels = _blockedApps
+          .map((app) => (app.appName ?? '').trim())
+          .where((name) => name.isNotEmpty)
+          .toSet();
+      final existingPayloads = _blockedApps
+          .map((app) => _appBindings[app.id]?.nativePayload?.trim())
+          .whereType<String>()
+          .where((payload) => payload.isNotEmpty)
+          .toSet();
+
+      for (final selected in picked) {
+        final payload = selected.payload.trim();
+        if (payload.isEmpty || existingPayloads.contains(payload)) {
+          continue;
+        }
+        final label = _resolvedAppLabel(selected, existingLabels);
+        existingLabels.add(label);
+        final record = await _repo.createBlockedAppRecord(label);
+        if (record == null) continue;
+        await _classicLocalStore.saveAppBinding(
+          rowId: record.id,
+          nativeIdentifier: selected.bundleIdentifier,
+          nativePayload: payload,
+        );
+        existingPayloads.add(payload);
+      }
+    });
+  }
 
   Future<void> _addBlockedWebsite({String? overrideDomain}) async {
     if (_saving) return;
-    final value = (overrideDomain ?? _siteCtrl.text).trim();
+    if (Platform.isIOS) {
+      await _addBlockedIosWebsitesFromPicker();
+      return;
+    }
+    final value = (overrideDomain ?? _siteCtrl.text).trim();
@@
-    if (Platform.isIOS) {
-      if (_iosWebsitePayloadByDomain.isNotEmpty &&
-          !_iosWebsitePayloadByDomain.containsKey(normalizedDomain)) {
-        await _showNotice(
-          title: 'Website unavailable',
-          message:
-              '$normalizedDomain is not currently supported by the Screen Time website API on this iPhone.',
-        );
-        return;
-      }
-      final authorized = await _focusService.isAuthorized();
-      if (!authorized) {
-        final granted = await _focusService.requestPermissions();
-        if (!granted) {
-          await _showNotice(
-            title: 'Permissions required',
-            message:
-                'Allow Screen Time / Family Controls first so selected websites can be blocked during your focus sessions on this device.',
-          );
-          return;
-        }
-      }
-      var websitePayload = _iosWebsitePayloadByDomain[normalizedDomain];
-      websitePayload ??= await _focusService.encodeWebsiteDomainSelection(
-        normalizedDomain,
-      );
-      websitePayload = websitePayload?.trim();
-      if (websitePayload == null || websitePayload.isEmpty) {
-        await _showNotice(
-          title: 'Website unavailable',
-          message:
-              '$normalizedDomain cannot be blocked on this iPhone by the current Screen Time API.',
-        );
-        return;
-      }
-      await _runMutation(() async {
-        final record = await _repo.createBlockedWebsiteRecord(normalizedDomain);
-        if (record == null) return;
-        await _classicLocalStore.saveWebsiteBinding(
-          rowId: record.id,
-          nativeIdentifier: normalizedDomain,
-          nativePayload: websitePayload,
-        );
-      });
-    } else {
-      await _runMutation(() async {
-        final record = await _repo.createBlockedWebsiteRecord(normalizedDomain);
-        if (record == null) return;
-        await _classicLocalStore.saveWebsiteBinding(
-          rowId: record.id,
-          nativeIdentifier: normalizedDomain,
-        );
-      });
-    }
+    await _runMutation(() async {
+      final record = await _repo.createBlockedWebsiteRecord(normalizedDomain);
+      if (record == null) return;
+      await _classicLocalStore.saveWebsiteBinding(
+        rowId: record.id,
+        nativeIdentifier: normalizedDomain,
+      );
+    });
@@
   }
+
+  Future<void> _addBlockedIosWebsitesFromPicker() async {
+    if (_saving || !Platform.isIOS) return;
+
+    final authorized = await _focusService.isAuthorized();
+    if (!authorized) {
+      final granted = await _focusService.requestPermissions();
+      if (!granted) {
+        await _showNotice(
+          title: 'Permissions required',
+          message:
+              'Allow Screen Time / Family Controls first so selected websites can be blocked during your focus sessions on this device.',
+        );
+        return;
+      }
+    }
+
+    final selections = await _focusService.openFamilyActivityWebsitePickerWithMetadata();
+    final picked = (selections ?? [])
+        .where((item) => item.payload.trim().isNotEmpty)
+        .toList(growable: false);
+    if (picked.isEmpty) return;
+
+    await _runMutation(() async {
+      final existingPayloads = _blockedWebsites
+          .map((site) => (site.id))
+          .map((id) => (id, _siteBindingFor(id)))
+          .map((pair) => pair.$2?.nativePayload?.trim())
+          .whereType<String>()
+          .where((payload) => payload.isNotEmpty)
+          .toSet();
+      for (final selected in picked) {
+        final payload = selected.payload.trim();
+        if (payload.isEmpty || existingPayloads.contains(payload)) continue;
+        final resolved = selected.domain?.trim();
+        final domain = (resolved != null && resolved.isNotEmpty)
+            ? normalizeDomainInput(resolved) ?? resolved.toLowerCase()
+            : null;
+        final label = (domain != null && domain.isNotEmpty)
+            ? domain
+            : 'selected-website-${DateTime.now().microsecondsSinceEpoch}';
+        final record = await _repo.createBlockedWebsiteRecord(label);
+        if (record == null) continue;
+        await _classicLocalStore.saveWebsiteBinding(
+          rowId: record.id,
+          nativeIdentifier: domain,
+          nativePayload: payload,
+        );
+        existingPayloads.add(payload);
+      }
+    });
+  }
+
+  ClassicBlockBinding? _siteBindingFor(String rowId) {
+    // best effort read from in-memory map after load/sync
+    // website bindings are persisted in dedicated store and refreshed via _load()
+    return null;
+  }
*** End Patch
