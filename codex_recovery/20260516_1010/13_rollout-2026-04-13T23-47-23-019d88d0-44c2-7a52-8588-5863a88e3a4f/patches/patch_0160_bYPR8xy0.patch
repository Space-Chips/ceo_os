*** Begin Patch
*** Update File: /Users/timo/ceo_os/ios/Runner/AppDelegate.swift
@@
-    private func encodeApplicationBundleSelection(args: [String: Any]?, result: FlutterResult) {
-        guard let rawBundleId = args?["bundleIdentifier"] as? String else {
-            result(
-                FlutterError(
-                    code: "INVALID_BUNDLE_ID",
-                    message: "A bundle identifier string is required.",
-                    details: nil
-                )
-            )
-            return
-        }
-        let bundleId = rawBundleId.trimmingCharacters(in: .whitespacesAndNewlines)
-        guard !bundleId.isEmpty else {
-            result(
-                FlutterError(
-                    code: "INVALID_BUNDLE_ID",
-                    message: "Provide a valid bundle identifier.",
-                    details: nil
-                )
-            )
-            return
-        }
-        guard let token = Application(bundleIdentifier: bundleId).token else {
-            result(
-                FlutterError(
-                    code: "APP_TOKEN_UNAVAILABLE",
-                    message: "This app cannot be converted to a Screen Time token on this iPhone.",
-                    details: nil
-                )
-            )
-            return
-        }
-        var selection = FamilyActivitySelection()
-        selection.applicationTokens = Set([token])
-        guard let encoded = encodeSelection(selection) else {
-            result(
-                FlutterError(
-                    code: "ENCODE_ERROR",
-                    message: "Failed to encode app selection.",
-                    details: nil
-                )
-            )
-            return
-        }
-
-        let app = Application(token: token)
-        let appName = app.localizedDisplayName?.trimmingCharacters(in: .whitespacesAndNewlines)
-        let resolvedBundle = app.bundleIdentifier?.trimmingCharacters(in: .whitespacesAndNewlines)
-        result([
-            "payload": encoded,
-            "appName": appName ?? "",
-            "bundleIdentifier": (resolvedBundle?.isEmpty == false ? resolvedBundle! : bundleId)
-        ])
-    }
-
@@
-        case "encodeApplicationBundleSelection":
-            encodeApplicationBundleSelection(args: args, result: result)
         case "describeAppSelectionPayload":
             describeAppSelectionPayload(args: args, result: result)
*** End Patch
