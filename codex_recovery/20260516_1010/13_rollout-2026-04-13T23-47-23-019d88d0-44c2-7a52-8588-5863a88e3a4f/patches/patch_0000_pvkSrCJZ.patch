*** Begin Patch
*** Update File: android/app/src/main/kotlin/com/wakeapp/ceoos/ForegroundAppDetector.kt
@@
     private fun detectBrowserToolbarBounds(
         event: AccessibilityEvent,
         root: AccessibilityNodeInfo?,
     ): Rect? {
         val bounds = Rect()
         val source = event.source
         if (source != null && isUrlBarNode(source)) {
             source.getBoundsInScreen(bounds)
             if (!bounds.isEmpty) return Rect(bounds)
         }
 
         if (root == null) return null
         val visited = mutableSetOf<Int>()
-        var best: Rect? = null
+        var best: Rect? = null
+        val rootBounds = Rect()
+        root.getBoundsInScreen(rootBounds)
+        val rootWidth = rootBounds.width().takeIf { it > 0 } ?: 0
+        val rootHeight = rootBounds.height().takeIf { it > 0 } ?: 0
+        var bestScore = Float.MIN_VALUE
 
         fun visit(node: AccessibilityNodeInfo?, depth: Int) {
             if (node == null || depth > 8) return
             val identity = System.identityHashCode(node)
             if (!visited.add(identity) || visited.size > 260) return
 
             if (isUrlBarNode(node)) {
                 node.getBoundsInScreen(bounds)
                 if (!bounds.isEmpty) {
-                    val candidate = Rect(bounds)
-                    if (best == null || candidate.bottom < best!!.bottom) {
-                        best = candidate
-                    }
+                    val candidate = Rect(bounds)
+                    val widthRatio = if (rootWidth > 0) {
+                        candidate.width().toFloat() / rootWidth.toFloat()
+                    } else {
+                        0f
+                    }
+                    val heightRatio = if (rootHeight > 0) {
+                        candidate.height().toFloat() / rootHeight.toFloat()
+                    } else {
+                        0f
+                    }
+                    val verticalBias = if (rootHeight > 0) {
+                        val mid = candidate.centerY().toFloat() / rootHeight.toFloat()
+                        if (mid < 0.25f || mid > 0.75f) 0.18f else 0f
+                    } else {
+                        0f
+                    }
+                    val score = (widthRatio * 2.2f) - (heightRatio * 0.6f) + verticalBias
+                    if (score > bestScore) {
+                        bestScore = score
+                        best = candidate
+                    }
                 }
             }
 
             for (index in 0 until node.childCount) {
                 visit(node.getChild(index), depth + 1)
*** End Patch