*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
-                        if (_blockedApps.isNotEmpty)
-                          ..._blockedApps.map(
-                            (app) => Padding(
-                              padding: const EdgeInsets.only(bottom: 8),
-                              child: _blockedAppCard(app),
-                            ),
-                          )
+                        if (_blockedApps.isNotEmpty)
+                          ..._blockedApps.asMap().entries.map(
+                            (entry) => Padding(
+                              padding: const EdgeInsets.only(bottom: 8),
+                              child: _blockedAppCard(
+                                entry.value,
+                                fallbackOrdinal: entry.key + 1,
+                              ),
+                            ),
+                          )
@@
-  Widget _blockedAppCard(BlockedApp app) {
+  Widget _blockedAppCard(BlockedApp app, {required int fallbackOrdinal}) {
     final limit = app.timeLimitMinutes ?? 0;
-    final title = _displayNameForBlockedApp(app);
+    var title = _displayNameForBlockedApp(app);
+    if (_isGenericAppName(title)) {
+      title = 'Selected app $fallbackOrdinal';
+    }
*** End Patch
