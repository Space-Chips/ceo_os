*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
 class AndroidLaunchableApp {
@@
   }
 }
 
+class IosFamilyActivitySelection {
+  final String payload;
+  final String? appName;
+  final String? bundleIdentifier;
+
+  const IosFamilyActivitySelection({
+    required this.payload,
+    this.appName,
+    this.bundleIdentifier,
+  });
+}
+
 class FocusService {
@@
   Future<List<String>?> openFamilyActivityPicker() async {
+    final detailed = await openFamilyActivityPickerWithMetadata();
+    if (detailed == null) return null;
+    return detailed.map((entry) => entry.payload).toList(growable: false);
+  }
+
+  Future<List<IosFamilyActivitySelection>?> openFamilyActivityPickerWithMetadata() async {
     const method = 'openFamilyActivityPicker';
     if (_shouldSkipMissingMethod(method)) return null;
     try {
-      // Returns a list of opaque tokens representing selected apps/categories
       final List<dynamic>? result = await _channel.invokeMethod(method);
-      return result?.cast<String>();
+      if (result == null) return null;
+
+      final selections = <IosFamilyActivitySelection>[];
+      for (final item in result) {
+        if (item is String) {
+          final payload = item.trim();
+          if (payload.isEmpty) continue;
+          selections.add(IosFamilyActivitySelection(payload: payload));
+          continue;
+        }
+        if (item is Map) {
+          final payload = (item['payload'] as String?)?.trim();
+          if (payload == null || payload.isEmpty) continue;
+          final appName = (item['appName'] as String?)?.trim();
+          final bundleIdentifier = (item['bundleIdentifier'] as String?)?.trim();
+          selections.add(
+            IosFamilyActivitySelection(
+              payload: payload,
+              appName: appName?.isNotEmpty == true ? appName : null,
+              bundleIdentifier: bundleIdentifier?.isNotEmpty == true
+                  ? bundleIdentifier
+                  : null,
+            ),
+          );
+        }
+      }
+      return selections;
     } on PlatformException catch (e) {
       AppLogger.error("Failed to open picker: '${e.message}'.");
       return null;
     } on MissingPluginException {
       _markMissingMethod(method);
@@
   Future<List<String>?> openFamilyActivityWebsitePicker() async {
@@
   }
+
+  Future<String?> encodeWebsiteDomainSelection(String domain) async {
+    const method = 'encodeWebsiteDomainSelection';
+    if (_shouldSkipMissingMethod(method)) return null;
+    try {
+      final String? encoded = await _channel.invokeMethod<String>(method, {
+        'domain': domain,
+      });
+      if (encoded == null || encoded.trim().isEmpty) return null;
+      return encoded.trim();
+    } on PlatformException catch (e) {
+      AppLogger.error("Failed to encode website selection: '${e.message}'.");
+      return null;
+    } on MissingPluginException {
+      _markMissingMethod(method);
+      return null;
+    }
+  }
*** End Patch
