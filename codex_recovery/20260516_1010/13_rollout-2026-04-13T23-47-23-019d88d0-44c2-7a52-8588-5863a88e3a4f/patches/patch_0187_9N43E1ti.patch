*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
 class FocusService {
   static const MethodChannel _channel = MethodChannel('com.ceoos.app/focus');
   static final Set<String> _missingNativeMethods = <String>{};
+  String? _lastIosAppPickerError;
+  String? _lastIosWebsitePickerError;
@@
   Future<List<IosFamilyActivitySelection>?>
   openFamilyActivityPickerWithMetadata() async {
@@
     try {
       final List<dynamic>? result = await _channel.invokeMethod(method);
       if (result == null) return null;
+      _lastIosAppPickerError = null;
@@
       return selections;
     } on PlatformException catch (e) {
+      _lastIosAppPickerError =
+          (e.message?.trim().isNotEmpty ?? false)
+          ? e.message!.trim()
+          : 'Unable to open the Apple app picker.';
       AppLogger.error("Failed to open picker: '${e.message}'.");
       return null;
     } on MissingPluginException {
       _markMissingMethod(method);
+      _lastIosAppPickerError =
+          'Native app picker is unavailable. Restart the app fully.';
       return null;
     }
   }
@@
   Future<List<IosWebsiteSelection>?>
   openFamilyActivityWebsitePickerWithMetadata() async {
@@
     try {
       final List<dynamic>? result = await _channel.invokeMethod(method);
       if (result == null) return null;
+      _lastIosWebsitePickerError = null;
       final selections = <IosWebsiteSelection>[];
@@
       return selections;
     } on PlatformException catch (e) {
+      _lastIosWebsitePickerError =
+          (e.message?.trim().isNotEmpty ?? false)
+          ? e.message!.trim()
+          : 'Unable to open the Apple website picker.';
       AppLogger.error("Failed to open website picker: '${e.message}'.");
       return null;
     } on MissingPluginException {
       _markMissingMethod(method);
+      _lastIosWebsitePickerError =
+          'Native website picker is unavailable. Restart the app fully.';
       return null;
     }
   }
+
+  String? takeLastIosAppPickerError() {
+    final value = _lastIosAppPickerError;
+    _lastIosAppPickerError = null;
+    return value;
+  }
+
+  String? takeLastIosWebsitePickerError() {
+    final value = _lastIosWebsitePickerError;
+    _lastIosWebsitePickerError = null;
+    return value;
+  }
*** End Patch
