*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
   Widget _blockedWebsiteCard(BlockedWebsite site) {
     final limit = site.timeLimitMinutes ?? 0;
     final binding = _websiteBindings[site.id];
-    final sitePayload = binding?.nativePayload?.trim();
-    final showNativeWebsiteLabel =
-        Platform.isIOS && sitePayload != null && sitePayload.isNotEmpty;
     final bindingDomain = binding?.nativeIdentifier?.trim();
     final title = site.urlDomain?.trim().isNotEmpty == true
         ? site.urlDomain!.trim()
         : (bindingDomain != null && bindingDomain.isNotEmpty
               ? bindingDomain
@@
             Row(
               children: [
-                Expanded(
-                  child: showNativeWebsiteLabel
-                      ? SizedBox(
-                          height: 36,
-                          child: UiKitView(
-                            viewType:
-                                'com.ceoos.app/blocked_website_token_label',
-                            creationParams: <String, dynamic>{
-                              'payload': sitePayload,
-                              'fallbackTitle': title,
-                              'textColorHex': _colorToArgbHex(AppColors.label),
-                            },
-                            creationParamsCodec: StandardMessageCodec(),
-                          ),
-                        )
-                      : Row(
-                          children: [
-                            _entityBadge(
-                              fallbackIcon: CupertinoIcons.globe,
-                              seed: title,
-                              accent: const Color(0xFF4A9EFF),
-                            ),
-                            const SizedBox(width: 14),
-                            Expanded(
-                              child: Text(
-                                title,
-                                maxLines: 2,
-                                overflow: TextOverflow.ellipsis,
-                                style: AppTypography.headline.copyWith(
-                                  fontSize: 16,
-                                  color: AppColors.label,
-                                ),
-                              ),
-                            ),
-                          ],
-                        ),
-                ),
+                _entityBadge(
+                  fallbackIcon: CupertinoIcons.globe,
+                  seed: title,
+                  accent: const Color(0xFF4A9EFF),
+                ),
+                const SizedBox(width: 14),
+                Expanded(
+                  child: Text(
+                    title,
+                    maxLines: 2,
+                    overflow: TextOverflow.ellipsis,
+                    style: AppTypography.headline.copyWith(
+                      fontSize: 16,
+                      color: AppColors.label,
+                    ),
+                  ),
+                ),
                 const SizedBox(width: 10),
                 _deleteChip(
*** End Patch
