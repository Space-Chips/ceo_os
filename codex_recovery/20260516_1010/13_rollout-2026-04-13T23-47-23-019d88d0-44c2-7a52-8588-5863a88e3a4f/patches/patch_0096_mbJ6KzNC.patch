*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
-  Future<void> _addBlockedWebsite() async {
+  Future<void> _addBlockedWebsite({String? overrideDomain}) async {
     if (_saving) return;
-    final value = _siteCtrl.text.trim();
+    final value = (overrideDomain ?? _siteCtrl.text).trim();
     if (value.isEmpty) return;
@@
-      final selection = await _focusService.openFamilyActivityWebsitePicker();
-      final payload = _firstNonEmpty(selection);
-      if (payload == null) {
-        await _showNotice(
-          title: 'Selection canceled',
-          message:
-              'Select at least one app or website, then tap Done to save your block.',
-        );
-        return;
-      }
-
       await _runMutation(() async {
         final record = await _repo.createBlockedWebsiteRecord(normalizedDomain);
         if (record == null) return;
         await _classicLocalStore.saveWebsiteBinding(
           rowId: record.id,
-          nativePayload: payload,
+          nativeIdentifier: normalizedDomain,
         );
       });
*** End Patch