*** Begin Patch
*** Update File: lib/features/premium/premium_comparison_table.dart
@@
 class PremiumComparisonTable extends StatelessWidget {
   final bool isFr;
 
   const PremiumComparisonTable({super.key, required this.isFr});
 
+  ({Color primary, Color secondary, Color tertiary, Color accent})
+  _textPaletteForSurface(Color surface) {
+    final isLightSurface = surface.computeLuminance() > 0.58;
+    if (!isLightSurface) {
+      return (
+        primary: AppColors.label,
+        secondary: AppColors.secondaryLabel,
+        tertiary: AppColors.tertiaryLabel,
+        accent: AppColors.accentText,
+      );
+    }
+    return (
+      primary: const Color(0xFF0B0B0F),
+      secondary: const Color(0xFF2B3138),
+      tertiary: const Color(0xFF5A616B),
+      accent: AppColors.accentDeep,
+    );
+  }
+
   @override
   Widget build(BuildContext context) {
+    final surface = AppColors.cardBackgroundAlt;
+    final palette = _textPaletteForSurface(surface);
     final headerFree = isFr ? 'Gratuit' : 'Free';
     final headerPremium = isFr ? 'Premium' : 'Premium';
@@
     return Container(
       decoration: BoxDecoration(
-        color: AppColors.cardBackgroundAlt.withValues(alpha: 0.92),
+        color: surface.withValues(alpha: 0.92),
         borderRadius: BorderRadius.circular(20),
         border: Border.all(color: AppColors.borderStrong, width: 0.9),
@@
             Row(
               children: [
-                Expanded(child: _headerCell(isFr ? 'Fonctionnalité' : 'Feature')),
+                Expanded(
+                  child: _headerCell(
+                    isFr ? 'Fonctionnalité' : 'Feature',
+                    palette.secondary,
+                  ),
+                ),
                 const SizedBox(width: 10),
-                Expanded(child: _headerCell(headerFree)),
+                Expanded(child: _headerCell(headerFree, palette.secondary)),
                 const SizedBox(width: 10),
-                Expanded(child: _headerCell(headerPremium)),
+                Expanded(child: _headerCell(headerPremium, palette.secondary)),
               ],
             ),
@@
                   Expanded(
                     child: Text(
                       row.title,
                       style: AppTypography.subhead.copyWith(
-                        color: AppColors.label,
+                        color: palette.primary,
                         fontWeight: FontWeight.w700,
                       ),
                     ),
                   ),
                   const SizedBox(width: 10),
-                  Expanded(child: _valueCell(row.free)),
+                  Expanded(child: _valueCell(row.free, palette.secondary)),
                   const SizedBox(width: 10),
-                  Expanded(child: _valueCell(row.premium, emphasize: true)),
+                  Expanded(
+                    child: _valueCell(
+                      row.premium,
+                      palette.secondary,
+                      emphasize: true,
+                      primaryColor: palette.primary,
+                    ),
+                  ),
                 ],
               ),
               const SizedBox(height: 10),
@@
               child: Text(
                 coffeeLine,
                 textAlign: TextAlign.center,
                 style: AppTypography.footnote.copyWith(
-                  color: AppColors.label.withValues(alpha: 0.92),
+                  color: palette.primary.withValues(alpha: 0.92),
                   fontWeight: FontWeight.w700,
                   height: 1.25,
                 ),
               ),
             ),
           ],
         ),
       ),
     );
   }
@@
-  Widget _headerCell(String text) => Text(
+  Widget _headerCell(String text, Color secondary) => Text(
         text,
         style: AppTypography.overline.copyWith(
-          color: AppColors.secondaryLabel.withValues(alpha: 0.75),
+          color: secondary.withValues(alpha: 0.75),
           fontWeight: FontWeight.w800,
           letterSpacing: 1.4,
         ),
       );
 
-  Widget _valueCell(String text, {bool emphasize = false}) => Text(
+  Widget _valueCell(
+    String text,
+    Color secondary, {
+    bool emphasize = false,
+    Color? primaryColor,
+  }) =>
+      Text(
         text,
         style: AppTypography.footnote.copyWith(
           color: emphasize
-              ? AppColors.label
-              : AppColors.secondaryLabel.withValues(alpha: 0.9),
+              ? (primaryColor ?? AppColors.label)
+              : secondary.withValues(alpha: 0.9),
           fontWeight: emphasize ? FontWeight.w800 : FontWeight.w600,
           height: 1.25,
         ),
       );
 }
*** End Patch