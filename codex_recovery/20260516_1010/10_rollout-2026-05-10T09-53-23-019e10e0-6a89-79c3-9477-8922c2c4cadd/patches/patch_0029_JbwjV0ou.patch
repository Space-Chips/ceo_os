*** Begin Patch
*** Update File: lib/features/home/home_screen.dart
@@
-  /// When a module or shortcut is toggled in the lateral menu, mirror the
-  /// change back into the control-center config slotItems so the home screen
-  /// grid stays in sync. If setup was not completed yet, this promotes the
-  /// configuration to setupCompleted so user changes apply immediately.
-  Future<void> _syncToggleToConfig({
-    required String id,
-    required bool enabled,
-    required bool isModule,
-  }) async {
-    final slots = _normalizeControlCenterSlots(
-      _controlCenterConfiguration.slotItems,
-    );
-
-    final slotId = isModule
-        ? (_appNameToSlotId[id] ?? '')
-        : (_shortcutIdToSlotId[id] ?? '');
-    if (slotId.isEmpty) return;
-
-    if (!enabled) {
-      for (int i = 0; i < slots.length; i++) {
-        if (slots[i] == slotId) slots[i] = '';
-      }
-    } else {
-      if (!slots.contains(slotId)) {
-        final emptyIdx = slots.indexWhere((s) => s.isEmpty);
-        if (emptyIdx >= 0) slots[emptyIdx] = slotId;
-      }
-    }
-
-    final newConfig = ControlCenterConfiguration(
-      slotItems: slots,
-      enabledWidgets: _controlCenterConfiguration.enabledWidgets,
-      selectedThemeId: _controlCenterConfiguration.selectedThemeId,
-      setupCompleted: true,
-    );
-    if (!mounted) return;
-    setState(() => _controlCenterConfiguration = newConfig);
-    final store = await ControlCenterSetupStore.create();
-    await store.save(newConfig);
-  }
-
   Future<void> _loadBaseData() async {
*** End Patch