*** Begin Patch
*** Update File: lib/components/premium_paywall.dart
@@
 class _MicroProofRow extends StatelessWidget {
   final bool isFr;
   final Color textColor;
+  final bool showRestore;
   const _MicroProofRow({required this.isFr, required this.textColor});
@@
   Widget build(BuildContext context) {
     return Row(
       children: [
         Expanded(
           child: _MicroPill(
             text: isFr ? 'Activation instantanée' : 'Instant activation',
             textColor: textColor,
           ),
         ),
-        const SizedBox(width: 10),
-        Expanded(
-          child: _MicroPill(
-            text: isFr ? 'Restaurable à tout moment' : 'Restore anytime',
-            textColor: textColor,
-          ),
-        ),
+        if (showRestore) ...[
+          const SizedBox(width: 10),
+          Expanded(
+            child: _MicroPill(
+              text: isFr ? 'Restaurable à tout moment' : 'Restore anytime',
+              textColor: textColor,
+            ),
+          ),
+        ] else ...[
+          const SizedBox(width: 10),
+          Expanded(
+            child: _MicroPill(
+              text: isFr ? 'Annulable à tout moment' : 'Cancel anytime',
+              textColor: textColor,
+            ),
+          ),
+        ],
       ],
     );
   }
 }
*** End Patch