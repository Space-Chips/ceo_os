*** Begin Patch
*** Update File: lib/core/repositories/premium_repository.dart
@@
   Future<PremiumRuntime> getRuntime() async {
     final config = await getConfig();
     // Never block UI-critical paths on network syncing.
     // (We avoid `unawaited(...)` for compatibility with older Flutter SDKs.)
     // ignore: discarded_futures
     _syncLaunchEntitlementsIfNeeded();
     final entitlements = await getUserEntitlements();
-    const subscriptionStatus = 'disabled';
-    const graceActive = false;
+    final subscriptionStatus = await _getSubscriptionStatus();
+    final graceActive = await _isClientGraceActive();
     final resolved = _resolveEntitlements(
       config: config,
       entitlements: entitlements,
       subscriptionStatus: subscriptionStatus,
       clientGraceActive: graceActive,
     );
@@
   ResolvedEntitlements _resolveEntitlements({
     required PremiumConfig config,
     required UserEntitlements entitlements,
     required String subscriptionStatus,
     required bool clientGraceActive,
   }) {
     final now = DateTime.now();
     final overrideActive =
         entitlements.premiumOverride &&
         (entitlements.premiumOverrideExpiresAt == null ||
             (entitlements.premiumOverrideExpiresAt?.isAfter(now) ?? false));
-    // WakeApp rule (as requested): Premium depends ONLY on the allowlist-backed
-    // server override stored in `user_entitlements.premium_override`.
-    final launchPremiumActive = false;
-    final hasPaidSubscription = false;
-    final isTrialing = false;
-    final isPremiumUser = overrideActive;
+    final status = subscriptionStatus.toLowerCase().trim();
+    final hasPaidSubscription = status == 'active' || status == 'trialing';
+    final isTrialing = status == 'trialing';
+    final launchPremiumActive = config.launchModeEnabled &&
+        (config.launchStartedAt == null || now.isAfter(config.launchStartedAt!)) &&
+        (config.launchEndsAt == null || now.isBefore(config.launchEndsAt!));
+    final isPremiumUser =
+        overrideActive || hasPaidSubscription || clientGraceActive || launchPremiumActive;
     final canUsePremiumThemes =
         isPremiumUser || entitlements.themeBundleGranted;
     final discountKind = entitlements.discountKind ?? config.earlyDiscountKind;
     final discountPercent =
         entitlements.discountPercent ?? config.earlyDiscountPercent;
@@
       launchPremiumActive: launchPremiumActive,
       hasPaidSubscription: hasPaidSubscription,
       isTrialing: isTrialing,
       premiumOverrideActive: overrideActive,
       clientGraceActive: clientGraceActive,
       isPremiumUser: isPremiumUser,
       canUsePremiumThemes: canUsePremiumThemes,
*** End Patch