*** Begin Patch
*** Update File: lib/core/repositories/premium_repository.dart
@@
+import 'dart:convert';
+
 import 'package:intl/intl.dart';
+import 'package:flutter/foundation.dart';
 import 'package:shared_preferences/shared_preferences.dart';
 import 'package:supabase_flutter/supabase_flutter.dart';
@@
 class PremiumRepository {
@@
   static const Duration _clientGraceWindow = Duration(hours: 2);
   static const String _clientGraceKeyPrefix = 'premium_grace_until_v1_';
+  static const String _cachedConfigKey = 'premium_cached_config_v1';
+  static const String _cachedEntitlementsKeyPrefix =
+      'premium_cached_entitlements_v1_';
+  static const Duration _networkTimeout = Duration(seconds: 2);
@@
   Future<PremiumRuntime> getRuntime() async {
     final config = await getConfig();
-    await _syncLaunchEntitlementsIfNeeded();
-    final entitlements = await getUserEntitlements();
+    // Never block UI-critical paths on network syncing.
+    unawaited(_syncLaunchEntitlementsIfNeeded());
+    final entitlements = await getUserEntitlements();
     const subscriptionStatus = 'disabled';
     const graceActive = false;
@@
   Future<PremiumConfig> getConfig() async {
     try {
-      final premiumRow = await _client
+      final premiumRow = await _client
           .from('app_configs')
           .select('config_value')
           .eq('config_key', 'premium')
-          .maybeSingle();
+          .maybeSingle()
+          .timeout(_networkTimeout);
 
       final premiumConfig = premiumRow?['config_value'];
       if (premiumConfig is Map) {
+        // Cache last known good config for offline usage.
+        try {
+          final prefs = await SharedPreferences.getInstance();
+          await prefs.setString(
+            _cachedConfigKey,
+            jsonEncode(Map<String, dynamic>.from(premiumConfig)),
+          );
+        } catch (_) {}
         return PremiumConfig.fromAppConfig(
           Map<String, dynamic>.from(premiumConfig),
         );
       }
 
       final legacyLaunchMode = await _getLegacyFreeLaunchMode();
       final defaults = PremiumConfig.defaults();
       return PremiumConfig(
         launchModeEnabled: legacyLaunchMode,
@@
         premiumThemes: defaults.premiumThemes,
       );
     } catch (_) {
+      // Offline fallback: use cached premium config if available.
+      try {
+        final prefs = await SharedPreferences.getInstance();
+        final raw = prefs.getString(_cachedConfigKey);
+        if (raw != null && raw.isNotEmpty) {
+          final decoded = jsonDecode(raw);
+          if (decoded is Map) {
+            return PremiumConfig.fromAppConfig(
+              Map<String, dynamic>.from(decoded as Map),
+            );
+          }
+        }
+      } catch (_) {}
       return PremiumConfig.defaults();
     }
   }
 
   Future<UserEntitlements> getUserEntitlements() async {
     final uid = _currentUserId;
     if (uid == null) return UserEntitlements.empty();
 
     try {
-      final row = await _client
+      final row = await _client
           .from('user_entitlements')
           .select()
           .eq('created_by', uid)
-          .maybeSingle();
+          .maybeSingle()
+          .timeout(_networkTimeout);
+      // Cache last known good entitlements for offline usage.
+      try {
+        final prefs = await SharedPreferences.getInstance();
+        await prefs.setString(
+          '$_cachedEntitlementsKeyPrefix$uid',
+          jsonEncode(row ?? <String, dynamic>{}),
+        );
+      } catch (_) {}
       return UserEntitlements.fromJson(row);
     } catch (_) {
+      // Offline fallback: use cached entitlements if available.
+      try {
+        final prefs = await SharedPreferences.getInstance();
+        final raw = prefs.getString('$_cachedEntitlementsKeyPrefix$uid');
+        if (raw != null && raw.isNotEmpty) {
+          final decoded = jsonDecode(raw);
+          if (decoded is Map) {
+            return UserEntitlements.fromJson(
+              Map<String, dynamic>.from(decoded as Map),
+            );
+          }
+        }
+      } catch (_) {}
       return UserEntitlements.empty();
     }
   }
*** End Patch