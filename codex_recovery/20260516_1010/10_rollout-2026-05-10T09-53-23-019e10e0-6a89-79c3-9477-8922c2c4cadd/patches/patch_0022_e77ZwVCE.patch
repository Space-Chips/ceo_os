*** Begin Patch
*** Update File: lib/components/premium_paywall.dart
@@
 class _PremiumPaywallContentState extends State<PremiumPaywallContent> {
@@
   BillingPackageOption? _preferredHeroOption(
     List<BillingPackageOption> options,
   ) {
@@
   }
+
+  ({Color primary, Color secondary, Color tertiary, Color accent})
+  _textPaletteForSurface(Color surface) {
+    final isLightSurface = surface.computeLuminance() > 0.58;
+    if (!isLightSurface) {
+      return (
+        primary: AppColors.label,
+        secondary: AppColors.secondaryLabel,
+        tertiary: AppColors.tertiaryLabel,
+        accent: AppColors.accentText,
+      );
+    }
+    // When a theme uses light card surfaces, the global text tokens can be too
+    // light (optimized for dark surfaces). Override locally for readability.
+    return (
+      primary: const Color(0xFF0B0B0F),
+      secondary: const Color(0xFF2B3138),
+      tertiary: const Color(0xFF5A616B),
+      accent: AppColors.accentDeep,
+    );
+  }
@@
   @override
   Widget build(BuildContext context) {
+    final palette = _textPaletteForSurface(AppColors.cardBackgroundAlt);
     return FutureBuilder<List<BillingPackageOption>>(
       future: _packageOptionsFuture,
       builder: (context, snapshot) {
@@
             Text(
               'WakeApp Premium',
               style: AppTypography.overline.copyWith(
-                color: AppColors.accentText.withValues(alpha: 0.9),
+                color: palette.accent.withValues(alpha: 0.9),
                 letterSpacing: 1.1,
               ),
             ),
@@
               widget.message.title,
               style: AppTypography.title1.copyWith(
                 fontSize: 28,
                 fontWeight: FontWeight.w700,
-                color: AppColors.label,
+                color: palette.primary,
               ),
             ),
@@
               widget.message.description,
               style: AppTypography.body.copyWith(
                 fontSize: 15,
-                color: AppColors.secondaryLabel.withValues(alpha: 0.92),
+                color: palette.secondary.withValues(alpha: 0.92),
                 height: 1.42,
               ),
             ),
@@
                         widget.isFr ? 'Utilisation actuelle' : 'Current usage',
                         style: AppTypography.subhead.copyWith(
-                          color: AppColors.secondaryLabel,
+                          color: palette.secondary,
                         ),
                       ),
                     ),
                     Text(
                       '${widget.current} / ${widget.limit}',
                       style: AppTypography.headline.copyWith(
-                        color: AppColors.label,
+                        color: palette.primary,
                         fontWeight: FontWeight.w700,
                       ),
                     ),
@@
                         CupertinoIcons.check_mark_circled_solid,
                         size: 16,
-                        color: AppColors.accentText.withValues(alpha: 0.95),
+                        color: palette.accent.withValues(alpha: 0.95),
                       ),
                     ),
                     const SizedBox(width: 10),
                     Expanded(
                       child: Text(
                         benefit,
                         style: AppTypography.callout.copyWith(
-                          color: AppColors.secondaryLabel.withValues(
+                          color: palette.secondary.withValues(
                             alpha: 0.92,
                           ),
                           height: 1.35,
                         ),
*** End Patch