*** Begin Patch
*** Update File: lib/core/repositories/premium_repository.dart
@@
   Future<String> _getSubscriptionStatus() async {
     final uid = _currentUserId;
     if (uid == null) return 'none';
 
     try {
       final row = await _client
           .from('billing_subscriptions')
           .select('status')
           .eq('created_by', uid)
-          .maybeSingle();
+          .maybeSingle()
+          .timeout(_networkTimeout);
 
       final status = (row?['status'] as String?)?.trim();
       if (status == null || status.isEmpty) return 'none';
       return status;
     } catch (_) {
       // If table isn't present yet, stay in free mode.
       return 'none';
     }
   }
 }
*** End Patch