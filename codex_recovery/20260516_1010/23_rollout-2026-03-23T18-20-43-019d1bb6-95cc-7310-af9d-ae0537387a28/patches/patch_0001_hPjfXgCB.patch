*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/repositories/settings_repository.dart
@@
-  Future<void> _safeDeleteByCreatedBy(String tableName) async {
-    try {
-      await _client.from(tableName).delete().eq('created_by', _currentUserId);
-    } catch (_) {
-      // Ignore missing/non-critical table errors in fallback mode.
-    }
-  }
-
-  Future<void> _fallbackDeleteMyAccountData() async {
-    const tables = [
-      'habit_completions',
-      'habit_logs',
-      'habits',
-      'calendar_events',
-      'pareto_tasks',
-      'task_groups',
-      'focus_sessions',
-      'block_lists',
-      'blocked_apps',
-      'notes',
-      'objectives',
-      'screen_time_logs',
-      'event_types',
-      'leaderboard_entries',
-      'friend_connections',
-      'weekly_habit_scores',
-      'weekly_contracts',
-      'user_ranks',
-      'win_streaks',
-      'user_entitlements',
-      'billing_subscriptions',
-      'app_settings',
-    ];
-
-    for (final table in tables) {
-      await _safeDeleteByCreatedBy(table);
-    }
-
-    try {
-      await _client.from('profiles').delete().eq('id', _currentUserId);
-    } catch (_) {
-      // Ignore fallback profile delete errors.
-    }
-  }
-
   Future<void> deleteMyAccountData() async {
     try {
       await _client.functions.invoke('delete-account');
*** End Patch
