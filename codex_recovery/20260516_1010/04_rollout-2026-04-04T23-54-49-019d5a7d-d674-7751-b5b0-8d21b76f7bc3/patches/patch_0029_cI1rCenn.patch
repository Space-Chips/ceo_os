*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
                     const SizedBox(height: 18),
                     _sectionHeader(
                       key: _appsSectionKey,
-                      title: 'BLOCKED APPS',
-                      subtitle: '',
-                      trailing: _pressScale(
-                        onPressed: () {
-                          setState(() => _showAddApp = !_showAddApp);
-                        },
-                        child: Container(
-                          width: 36,
-                          height: 36,
-                          decoration: BoxDecoration(
-                            color: AppColors.white.withValues(alpha: 0.05),
-                            borderRadius: BorderRadius.circular(12),
-                          ),
-                          alignment: Alignment.center,
-                          child: Icon(
-                            _showAddApp
-                                ? CupertinoIcons.minus
-                                : CupertinoIcons.add,
-                            size: 18,
-                            color: AppColors.label.withValues(alpha: 0.8),
-                          ),
-                        ),
-                      ),
+                      title: 'Blocked apps',
+                      subtitle: 'Search your installed apps and keep the list up to date.',
+                      trailing: Row(
+                        mainAxisSize: MainAxisSize.min,
+                        children: [
+                          _countPill(
+                            _blockedApps.isEmpty
+                                ? 'None'
+                                : _blockedApps.length == 1
+                                ? '1 app'
+                                : '${_blockedApps.length} apps',
+                          ),
+                          const SizedBox(width: 8),
+                          _iconPill(
+                            icon: _isAndroid
+                                ? CupertinoIcons.search
+                                : (_showAddApp
+                                      ? CupertinoIcons.minus
+                                      : CupertinoIcons.add),
+                            onTap: _isAndroid
+                                ? _openAndroidBlockedAppsEditor
+                                : () {
+                                    setState(() => _showAddApp = !_showAddApp);
+                                  },
+                          ),
+                        ],
+                      ),
                     ),
-                    if (_showAddApp) ...[
+                    if (_isAndroid) ...[
+                      _actionLauncherCard(
+                        icon: CupertinoIcons.search,
+                        title: 'Search installed apps',
+                        subtitle:
+                            'Live results appear as you type. Add or remove blocked apps from one picker.',
+                        actionLabel: 'Open app picker',
+                        onTap: _openAndroidBlockedAppsEditor,
+                      ),
+                      const SizedBox(height: 8),
+                    ],
+                    if (!_isAndroid && _showAddApp) ...[
                       Container(
-                        padding: const EdgeInsets.all(12),
+                        padding: const EdgeInsets.all(14),
                         decoration: _moduleDecoration(),
                         child: Column(
+                          crossAxisAlignment: CrossAxisAlignment.start,
                           children: [
-                            CupertinoTextField(
+                            Text(
+                              'Give the selection a label, then choose the app in Family Controls.',
+                              style: AppTypography.footnote.copyWith(
+                                fontSize: 13,
+                                height: 1.35,
+                                color: AppColors.secondaryLabel.withValues(
+                                  alpha: 0.82,
+                                ),
+                              ),
+                            ),
+                            const SizedBox(height: 12),
+                            CupertinoSearchTextField(
                               controller: _appCtrl,
-                              placeholder: 'App name (Instagram, TikTok, ...)',
-                              style: AppTypography.mono.copyWith(fontSize: 11),
-                              decoration: BoxDecoration(
-                                color: AppColors.backgroundLight,
-                                borderRadius: BorderRadius.circular(10),
-                                border: Border.all(
-                                  color: AppColors.glassBorder,
-                                  width: 0.5,
-                                ),
+                              placeholder: 'Instagram, TikTok, YouTube…',
+                              backgroundColor: AppColors.backgroundLight,
+                              borderRadius: BorderRadius.circular(14),
+                              style: AppTypography.body.copyWith(
+                                fontSize: 15,
+                                color: AppColors.label,
+                              ),
+                              placeholderStyle: AppTypography.body.copyWith(
+                                fontSize: 15,
+                                color: AppColors.tertiaryLabel,
                               ),
                             ),
-                            const SizedBox(height: 10),
-                            SizedBox(
-                              width: double.infinity,
-                              child: LiquidButton(
-                                label: 'ADD_APP',
-                                onPressed: _saving
-                                    ? null
-                                    : () {
-                                        _addBlockedApp();
-                                      },
-                              ),
+                            const SizedBox(height: 12),
+                            LiquidButton(
+                              label: 'Choose apps',
+                              fullWidth: true,
+                              onPressed: _saving ? null : _addBlockedApp,
                             ),
                           ],
                         ),
                       ),
                       const SizedBox(height: 8),
                     ],
                     if (_blockedApps.isNotEmpty)
                       ..._blockedApps.map(
                         (app) => Padding(
                           padding: const EdgeInsets.only(bottom: 8),
-                          child: _blockedItemCard(
-                            title: (app.appName ?? 'Unknown app').toUpperCase(),
-                            subtitle: (app.timeLimitMinutes ?? 0) > 0
-                                ? 'Daily limit ${(app.timeLimitMinutes ?? 0)}m'
-                                : 'Fully blocked',
-                            onSetLimit: _saving
-                                ? null
-                                : () => _editDailyLimit(
-                                    title: app.appName ?? 'App',
-                                    currentMinutes: app.timeLimitMinutes ?? 0,
-                                    onSave: (minutes) => _repo
-                                        .setBlockedAppTime(app.id, minutes),
-                                  ),
-                            onDelete: _saving
-                                ? null
-                                : () => _runMutation(() async {
-                                    await _repo.deleteBlockedApp(app.id);
-                                    await _classicLocalStore.removeAppBinding(
-                                      app.id,
-                                    );
-                                  }),
-                            onTap: () {
-                              final limit = app.timeLimitMinutes ?? 0;
-                              if (limit <= 0) {
-                                _showNotice(
-                                  title: app.appName ?? 'App',
-                                  message:
-                                      'This app is fully blocked (no daily allowance).',
-                                );
-                                return;
-                              }
-                              _showLocalLimitInfo(
-                                title: app.appName ?? 'App',
-                                limitMinutes: limit,
-                              );
-                            },
-                          ),
+                          child: _blockedAppCard(app),
                         ),
-                      ),
+                      )
+                    else
+                      _emptyStateCard(
+                        icon: CupertinoIcons.app_badge,
+                        title: 'No blocked apps yet',
+                        message: _isAndroid
+                            ? 'Open the app picker to choose apps from this device.'
+                            : 'Add a label, then choose apps from Family Controls.',
+                      ),
                     const SizedBox(height: 18),
                     _sectionHeader(
                       key: _sitesSectionKey,
-                      title: 'BLOCKED WEBSITES',
-                      subtitle: '',
-                      trailing: _pressScale(
-                        onPressed: () {
-                          setState(() => _showAddWebsite = !_showAddWebsite);
-                        },
-                        child: Container(
-                          width: 36,
-                          height: 36,
-                          decoration: BoxDecoration(
-                            color: AppColors.white.withValues(alpha: 0.05),
-                            borderRadius: BorderRadius.circular(12),
-                          ),
-                          alignment: Alignment.center,
-                          child: Icon(
-                            _showAddWebsite
-                                ? CupertinoIcons.minus
-                                : CupertinoIcons.add,
-                            size: 18,
-                            color: AppColors.label.withValues(alpha: 0.8),
-                          ),
-                        ),
-                      ),
+                      title: 'Blocked websites',
+                      subtitle:
+                          'Block distracting domains and optionally add daily limits.',
+                      trailing: Row(
+                        mainAxisSize: MainAxisSize.min,
+                        children: [
+                          _countPill(
+                            _blockedWebsites.isEmpty
+                                ? 'None'
+                                : _blockedWebsites.length == 1
+                                ? '1 site'
+                                : '${_blockedWebsites.length} sites',
+                          ),
+                          const SizedBox(width: 8),
+                          _iconPill(
+                            icon: _showAddWebsite
+                                ? CupertinoIcons.minus
+                                : CupertinoIcons.add,
+                            onTap: () {
+                              setState(
+                                () => _showAddWebsite = !_showAddWebsite,
+                              );
+                            },
+                          ),
+                        ],
+                      ),
                     ),
                     Container(
                       padding: const EdgeInsets.all(16),
                       decoration: BoxDecoration(
@@
                           const SizedBox(width: 8),
                           Expanded(
                             child: Text(
-                              'Block porn websites (global NSFW shield)',
-                              style: AppTypography.footnote.copyWith(
-                                fontSize: 13,
-                                color: AppColors.secondaryLabel.withValues(
-                                  alpha: 0.82,
-                                ),
+                              'Global NSFW shield',
+                              style: AppTypography.callout.copyWith(
+                                fontSize: 14,
+                                color: AppColors.label,
+                                fontWeight: FontWeight.w600,
                               ),
                             ),
                           ),
@@
                     const SizedBox(height: 8),
                     if (_showAddWebsite) ...[
                       Container(
-                        padding: const EdgeInsets.all(12),
+                        padding: const EdgeInsets.all(14),
                         decoration: _moduleDecoration(),
                         child: Column(
+                          crossAxisAlignment: CrossAxisAlignment.start,
                           children: [
-                            CupertinoTextField(
+                            Text(
+                              'Add one domain at a time. Example: youtube.com or reddit.com',
+                              style: AppTypography.footnote.copyWith(
+                                fontSize: 13,
+                                height: 1.35,
+                                color: AppColors.secondaryLabel.withValues(
+                                  alpha: 0.82,
+                                ),
+                              ),
+                            ),
+                            const SizedBox(height: 12),
+                            CupertinoSearchTextField(
                               controller: _siteCtrl,
-                              placeholder: 'Domain (x.com, youtube.com, ...)',
-                              style: AppTypography.mono.copyWith(fontSize: 11),
-                              decoration: BoxDecoration(
-                                color: AppColors.backgroundLight,
-                                borderRadius: BorderRadius.circular(10),
-                                border: Border.all(
-                                  color: AppColors.glassBorder,
-                                  width: 0.5,
-                                ),
+                              placeholder: 'Search or paste a domain',
+                              backgroundColor: AppColors.backgroundLight,
+                              borderRadius: BorderRadius.circular(14),
+                              style: AppTypography.body.copyWith(
+                                fontSize: 15,
+                                color: AppColors.label,
+                              ),
+                              placeholderStyle: AppTypography.body.copyWith(
+                                fontSize: 15,
+                                color: AppColors.tertiaryLabel,
                               ),
                             ),
-                            const SizedBox(height: 10),
-                            SizedBox(
-                              width: double.infinity,
-                              child: LiquidButton(
-                                label: 'ADD_WEBSITE',
-                                onPressed: _saving
-                                    ? null
-                                    : () {
-                                        _addBlockedWebsite();
-                                      },
-                              ),
+                            const SizedBox(height: 12),
+                            LiquidButton(
+                              label: 'Add website',
+                              fullWidth: true,
+                              onPressed: _saving ? null : _addBlockedWebsite,
                             ),
                           ],
                         ),
@@
                     if (_blockedWebsites.isNotEmpty)
                       ..._blockedWebsites.map(
                         (site) => Padding(
                           padding: const EdgeInsets.only(bottom: 8),
-                          child: _blockedItemCard(
-                            title: (site.urlDomain ?? 'Unknown website')
-                                .toUpperCase(),
-                            subtitle: (site.timeLimitMinutes ?? 0) > 0
-                                ? 'Daily limit ${(site.timeLimitMinutes ?? 0)}m'
-                                : 'Fully blocked',
-                            onSetLimit: _saving
-                                ? null
-                                : () => _editDailyLimit(
-                                    title: site.urlDomain ?? 'Website',
-                                    currentMinutes: site.timeLimitMinutes ?? 0,
-                                    onSave: (minutes) =>
-                                        _repo.setBlockedWebsiteTime(
-                                          site.id,
-                                          minutes,
-                                        ),
-                                  ),
-                            onDelete: _saving
-                                ? null
-                                : () => _runMutation(() async {
-                                    await _repo.deleteBlockedWebsite(site.id);
-                                    await _classicLocalStore
-                                        .removeWebsiteBinding(site.id);
-                                  }),
-                            onTap: () {
-                              final limit = site.timeLimitMinutes ?? 0;
-                              if (limit <= 0) {
-                                _showNotice(
-                                  title: site.urlDomain ?? 'Website',
-                                  message:
-                                      'This website is fully blocked (no daily allowance).',
-                                );
-                                return;
-                              }
-                              _showLocalLimitInfo(
-                                title: site.urlDomain ?? 'Website',
-                                limitMinutes: limit,
-                              );
-                            },
-                          ),
+                          child: _blockedWebsiteCard(site),
                         ),
-                      ),
+                      )
+                    else
+                      _emptyStateCard(
+                        icon: CupertinoIcons.globe,
+                        title: 'No blocked websites yet',
+                        message:
+                            'Add a domain to keep distracting sites out of reach.',
+                      ),
                     const SizedBox(height: 18),
                     _sectionHeader(
                       key: _pausesSectionKey,
-                      title: 'PAUSE',
-                      subtitle: '',
+                      title: 'Pause',
+                      subtitle: 'Schedule breaks ahead of time so protections lift predictably.',
                       trailing: null,
                     ),
*** End Patch
