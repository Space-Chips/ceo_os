*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/AppBlockingService.kt
@@
 class AppBlockingService : AccessibilityService() {
     private val foregroundMonitorIntervalMillis = 120L
-    private val blockRedirectCooldownMillis = 700L
@@
     private var ceoShieldActive = false
 
     private var currentPackageName: String? = null
     private var currentVisibleDomain: String? = null
-    private var lastForcedRedirectSignature: String? = null
-    private var lastForcedRedirectAtMillis: Long = 0L
@@
         fun setCeoShieldActive(active: Boolean) {
             instance?.ceoShieldActive = active
             if (active) {
                 instance?.refreshFocusBlockListFromPrefs()
             }
             instance?.applyShieldState()
             instance?.enforceCurrentForegroundIfNeeded()
         }
+
+        fun onBlockGateHidden() {
+            instance?.currentBlockSignature = null
+        }
+
+        fun navigateBackFromBlockGate() {
+            instance?.navigateAwayFromBlockedWebsite()
+        }
@@
     private data class BlockPresentation(
         val kicker: String,
         val title: String,
         val subtitle: String,
+        val primaryAction: String,
+        val primaryLabel: String,
     )
*** End Patch
