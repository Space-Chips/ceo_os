*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/MainActivity.kt
@@
-    private fun prefs() = getSharedPreferences("FlutterSharedPreferences", Context.MODE_PRIVATE)
+    private fun prefs() = getSharedPreferences("FlutterSharedPreferences", Context.MODE_PRIVATE)
+
+    private fun persistFocusSelection(
+        packages: List<String>,
+        categories: List<String> = emptyList(),
+    ) {
+        val payload = org.json.JSONObject().apply {
+            put("blocked_package_names", org.json.JSONArray(packages))
+            put("blocked_categories", org.json.JSONArray(categories))
+        }
+        prefs().edit().putString("flutter.active_block_list", payload.toString()).apply()
+    }
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/MainActivity.kt
@@
                 "startShield" -> {
                     val packages = call.argument<List<String>>("packages") ?: listOf()
+                    val categories = call.argument<List<String>>("categories") ?: listOf()
+                    persistFocusSelection(packages, categories)
                     prefs().edit().putBoolean("focus_shield_active", true).apply()
                     AppBlockingService.updateBlockList(packages)
                     AppBlockingService.setShieldActive(true)
                     result.success(null)
                 }
@@
                 "startCeoShield" -> {
                     // Android fallback: enable shielding with currently configured block list.
                     // If Flutter passes packages, prefer them.
                     val packages = call.argument<List<String>>("packages")
+                    val categories = call.argument<List<String>>("categories") ?: listOf()
                     if (packages != null && packages.isNotEmpty()) {
+                        persistFocusSelection(packages, categories)
                         AppBlockingService.updateBlockList(packages)
                     }
                     prefs().edit().putBoolean("ceo_shield_active", true).apply()
                     AppBlockingService.setCeoShieldActive(true)
                     result.success(null)
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/AppBlockingService.kt
@@
 import android.accessibilityservice.AccessibilityService
 import android.app.usage.UsageStatsManager
 import android.content.Context
 import android.content.Intent
+import android.content.pm.PackageManager
 import android.graphics.Color
@@
     private fun appLabelForPackage(packageName: String): String {
         return try {
             val appInfo = packageManager.getApplicationInfo(packageName, 0)
             packageManager.getApplicationLabel(appInfo)?.toString()?.trim().orEmpty()
                 .ifEmpty { packageName.substringAfterLast('.') }
         } catch (_: Exception) {
             packageName.substringAfterLast('.')
         }
     }
+
+    private fun homePackageNames(): Set<String> {
+        return try {
+            val intent = Intent(Intent.ACTION_MAIN).apply {
+                addCategory(Intent.CATEGORY_HOME)
+            }
+            packageManager.queryIntentActivities(intent, PackageManager.MATCH_DEFAULT_ONLY)
+                .mapNotNull { it.activityInfo?.packageName?.trim() }
+                .filter { it.isNotEmpty() }
+                .toSet()
+        } catch (_: Exception) {
+            emptySet()
+        }
+    }
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/AppBlockingService.kt
@@
     private fun enforceCurrentForegroundIfNeeded() {
-        val candidatePackage = currentPackageName ?: rootInActiveWindow?.packageName?.toString()
+        val rootPackage = rootInActiveWindow?.packageName?.toString()?.trim().orEmpty()
+        val candidatePackage = when {
+            rootPackage.isNotEmpty() -> rootPackage
+            !currentPackageName.isNullOrBlank() -> currentPackageName!!.trim()
+            else -> null
+        }
         if (candidatePackage == null) {
             if (
                 !focusShieldActive &&
@@
             return
         }
 
+        if (candidatePackage.isEmpty()) {
+            removeBlockOverlay()
+            return
+        }
+
         if (candidatePackage == packageName && BlockGateActivity.isVisible) {
             return
         }
+
+        if (homePackageNames().contains(candidatePackage)) {
+            currentPackageName = candidatePackage
+            currentVisibleDomain = null
+            removeBlockOverlay()
+            return
+        }
 
         if (effectiveBlockedPackages().contains(candidatePackage)) {
+            currentPackageName = candidatePackage
             showBlockOverlay()
             return
         }
@@
         tickWebsiteUsage()
 
         val packageFromEvent = event.packageName?.toString()
-        if (!packageFromEvent.isNullOrBlank()) {
-            currentPackageName = packageFromEvent
-        } else if (currentPackageName == null) {
-            currentPackageName = rootInActiveWindow?.packageName?.toString()
-        }
+        val rootPackage = rootInActiveWindow?.packageName?.toString()
+        currentPackageName = when {
+            !rootPackage.isNullOrBlank() -> rootPackage
+            !packageFromEvent.isNullOrBlank() -> packageFromEvent
+            else -> currentPackageName
+        }
*** End Patch
