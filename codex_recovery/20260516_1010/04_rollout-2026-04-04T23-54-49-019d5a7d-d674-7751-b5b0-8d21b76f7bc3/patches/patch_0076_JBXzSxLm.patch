*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/AppBlockingService.kt
@@
     private fun presentBlockGate(presentation: BlockPresentation) {
-        val signature = "${presentation.kicker}|${presentation.title}|${presentation.subtitle}"
+        val signature =
+            "${presentation.kicker}|${presentation.title}|${presentation.subtitle}|${presentation.primaryAction}|${presentation.primaryLabel}"
         if (signature == currentBlockSignature) return
         currentBlockSignature = signature
 
         try {
             val intent = Intent(this, BlockGateActivity::class.java).apply {
@@
                 )
                 putExtra(BlockGateActivity.EXTRA_KICKER, presentation.kicker)
                 putExtra(BlockGateActivity.EXTRA_TITLE, presentation.title)
                 putExtra(BlockGateActivity.EXTRA_SUBTITLE, presentation.subtitle)
+                putExtra(BlockGateActivity.EXTRA_PRIMARY_ACTION, presentation.primaryAction)
+                putExtra(BlockGateActivity.EXTRA_PRIMARY_LABEL, presentation.primaryLabel)
             }
             startActivity(intent)
         } catch (e: Exception) {
             Log.e("AppBlockingService", "Error presenting block gate", e)
         }
     }
 
-    private fun launchHomeSurface() {
+    private fun launchHomeSurface() {
         try {
-            if (!performGlobalAction(GLOBAL_ACTION_HOME)) {
+            if (!performGlobalAction(GLOBAL_ACTION_HOME)) {
                 val intent = Intent(Intent.ACTION_MAIN).apply {
                     addCategory(Intent.CATEGORY_HOME)
                     addFlags(Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_NO_ANIMATION)
                 }
                 startActivity(intent)
@@
             Log.e("AppBlockingService", "Error returning home", e)
         }
     }
 
-    private fun redirectBlockedForeground(presentation: BlockPresentation) {
-        val signature = "${presentation.kicker}|${presentation.title}|${presentation.subtitle}"
-        val now = System.currentTimeMillis()
-        val shouldForceHome =
-            signature != lastForcedRedirectSignature ||
-                now - lastForcedRedirectAtMillis >= blockRedirectCooldownMillis
-
-        if (shouldForceHome) {
-            lastForcedRedirectSignature = signature
-            lastForcedRedirectAtMillis = now
-            launchHomeSurface()
-            foregroundMonitorHandler.postDelayed({
-                presentBlockGate(presentation)
-            }, 32L)
-            return
-        }
-
-        presentBlockGate(presentation)
+    private fun navigateAwayFromBlockedWebsite() {
+        try {
+            if (!performGlobalAction(GLOBAL_ACTION_BACK)) {
+                launchHomeSurface()
+            }
+        } catch (e: Exception) {
+            Log.e("AppBlockingService", "Error navigating away from blocked website", e)
+        }
     }
@@
                 exceededDailyLimitPackages.contains(activePackage) -> BlockPresentation(
                     kicker = "Daily limit reached",
                     title = "$appName is unavailable for now",
                     subtitle = "You have reached today's allowed time for this app. WakeApp will unlock it again tomorrow.",
+                    primaryAction = "go_home",
+                    primaryLabel = "Return to Home",
                 )
                 ceoShieldActive && focusBlockedPackages.contains(activePackage) -> BlockPresentation(
                     kicker = "Blackout Mode",
                     title = "$appName is blocked",
                     subtitle = "This app stays locked while Blackout Mode is active so you can stay fully off distractions.",
+                    primaryAction = "go_home",
+                    primaryLabel = "Return to Home",
                 )
                 focusShieldActive && focusBlockedPackages.contains(activePackage) -> BlockPresentation(
                     kicker = "Focus Mode",
                     title = "$appName is blocked",
                     subtitle = "This app is unavailable during your current Focus session.",
+                    primaryAction = "go_home",
+                    primaryLabel = "Return to Home",
                 )
                 classicPauseShieldActive && classicPauseBlockedPackages.contains(activePackage) -> BlockPresentation(
                     kicker = "Scheduled pause",
                     title = "$appName is blocked",
                     subtitle = "This app is unavailable during the pause window you planned in WakeApp.",
+                    primaryAction = "go_home",
+                    primaryLabel = "Return to Home",
                 )
                 else -> BlockPresentation(
                     kicker = "Blocked app",
                     title = "$appName is blocked",
                     subtitle = "This app is on your blocked list in WakeApp. Open WakeApp to change the selection.",
+                    primaryAction = "go_home",
+                    primaryLabel = "Return to Home",
                 )
             }
         }
@@
                 exceededDailyLimitWebsites.contains(blockedDomain) -> BlockPresentation(
                     kicker = "Daily limit reached",
                     title = "$blockedDomain is unavailable for now",
                     subtitle = "You have reached today's allowed time for this website in supported browsers.",
+                    primaryAction = "go_back",
+                    primaryLabel = "Back to Browser",
                 )
                 classicPauseShieldActive && classicPauseBlockedWebsites.contains(blockedDomain) -> BlockPresentation(
                     kicker = "Scheduled pause",
                     title = "$blockedDomain is blocked",
                     subtitle = "This website is unavailable during the pause window you planned in WakeApp.",
+                    primaryAction = "go_back",
+                    primaryLabel = "Back to Browser",
                 )
                 else -> BlockPresentation(
                     kicker = "Blocked website",
                     title = "$blockedDomain is blocked",
                     subtitle = "This website is on your blocked list in WakeApp and is unavailable in supported browsers.",
+                    primaryAction = "go_back",
+                    primaryLabel = "Back to Browser",
                 )
             }
         }
@@
     private fun showBlockOverlay() {
         val presentation = currentBlockPresentation() ?: return
-        redirectBlockedForeground(presentation)
+        presentBlockGate(presentation)
     }
 
     private fun removeBlockOverlay() {
-        lastForcedRedirectSignature = null
+        currentBlockSignature = null
         dismissBlockGate()
     }
*** End Patch
