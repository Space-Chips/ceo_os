*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/MainActivity.kt
@@
 import android.app.AppOpsManager
+import android.app.admin.DevicePolicyManager
 import android.content.ComponentName
 import android.content.Context
 import android.content.Intent
 import android.content.pm.ApplicationInfo
@@
     private fun prefs() = getSharedPreferences("FlutterSharedPreferences", Context.MODE_PRIVATE)
+
+    private fun openDeviceAdminSettings(): Boolean {
+        val intent = Intent(DevicePolicyManager.ACTION_ADD_DEVICE_ADMIN).apply {
+            putExtra(
+                DevicePolicyManager.EXTRA_DEVICE_ADMIN,
+                ComponentName(this@MainActivity, WakeDeviceAdminReceiver::class.java),
+            )
+            putExtra(
+                DevicePolicyManager.EXTRA_ADD_EXPLANATION,
+                "WakeApp can use Android device management to hard-block selected apps when supported on this device.",
+            )
+            addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
+        }
+        startActivity(intent)
+        return true
+    }
@@
                 "listLaunchableApps" -> {
                     listLaunchableApps(result)
                 }
+                "getHardBlockStatus" -> {
+                    result.success(HardAppBlockManager.status(this))
+                }
+                "openDeviceAdminSettings" -> {
+                    result.success(openDeviceAdminSettings())
+                }
                 "startShield" -> {
                     val packages = call.argument<List<String>>("packages") ?: listOf()
                     val categories = call.argument<List<String>>("categories") ?: listOf()
*** End Patch
