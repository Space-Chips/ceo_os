*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
 class _CeoModeCard extends StatelessWidget {
   final String statusLabel;
   final VoidCallback onOpenCeoMode;
 
   const _CeoModeCard({required this.statusLabel, required this.onOpenCeoMode});
+
+  String get _displayStatusLabel {
+    if (statusLabel == 'MAXIMUM FOCUS') {
+      return 'MAX FOCUS';
+    }
+    if (statusLabel.startsWith('IN SESSION ')) {
+      return statusLabel.replaceFirst('IN SESSION ', 'SESSION ');
+    }
+    if (statusLabel.startsWith('EXIT WINDOW ')) {
+      return statusLabel.replaceFirst('EXIT WINDOW ', 'EXIT ');
+    }
+    return statusLabel;
+  }
 
   @override
   Widget build(BuildContext context) {
     return GestureDetector(
@@
             ),
             const SizedBox(width: 12),
             Expanded(
               child: Text(
-                'Blackout Mode',
+                'Blackout\nMode',
+                maxLines: 2,
                 style: AppTypography.title3.copyWith(
                   fontSize: 19,
                   fontWeight: FontWeight.w700,
                   color: AppColors.label,
+                  height: 1.02,
                 ),
               ),
             ),
-            const SizedBox(width: 10),
+            const SizedBox(width: 8),
             GestureDetector(
               onTap: onOpenCeoMode,
               child: Container(
+                constraints: const BoxConstraints(minWidth: 94, maxWidth: 112),
                 padding: const EdgeInsets.symmetric(
-                  horizontal: 24,
+                  horizontal: 14,
                   vertical: 13,
                 ),
                 decoration: BoxDecoration(
                   borderRadius: BorderRadius.circular(12),
                   border: Border.all(color: AppColors.borderStrong, width: 1),
                   color: AppColors.surface.withValues(alpha: 0.58),
                 ),
                 child: Text(
-                  statusLabel,
+                  _displayStatusLabel,
+                  maxLines: 1,
+                  overflow: TextOverflow.ellipsis,
+                  textAlign: TextAlign.center,
                   style: AppTypography.overline.copyWith(
-                    fontSize: 11,
+                    fontSize: 10,
                     color: AppColors.secondaryLabel,
                   ),
                 ),
               ),
*** End Patch
