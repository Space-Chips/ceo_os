*** Begin Patch
*** Update File: lib/features/calendar/calendar_screen.dart
@@
   static const List<String> _secondaryKeywords = <String>[
     'optional',
     'maybe',
     'tentative',
     'coffee',
     'lunch',
   ];
 
-  String _t(String key) => context.read<LanguageProvider>().t(key);
-  String _localeCode() => context.read<LanguageProvider>().languageCode;
+  String _t(String key) {
+    try {
+      return context.read<LanguageProvider>().t(key);
+    } catch (_) {
+      return key;
+    }
+  }
+
+  String _localeCode() {
+    try {
+      return context.read<LanguageProvider>().languageCode;
+    } catch (_) {
+      return 'en';
+    }
+  }
@@
   @override
   Widget build(BuildContext context) {
     try {
       // Rebuild when language changes so localized date labels refresh.
-      context.watch<LanguageProvider>().languageCode;
+      try {
+        context.watch<LanguageProvider>().languageCode;
+      } catch (_) {}
       return CupertinoPageScaffold(
         backgroundColor: AppColors.background,
         child: AmbientBackdrop(
           child: SafeArea(
             child: Consumer<TaskProvider>(
@@
     } catch (error, stack) {
       debugPrint('CalendarScreen build failed: $error');
       debugPrintStack(stackTrace: stack);
-      final message =
-          kDebugMode ? 'CalendarScreen error:\n$error' : _t('retry');
+      final message = kDebugMode ? 'CalendarScreen error:\n$error' : 'Retry';
       return CupertinoPageScaffold(
         backgroundColor: AppColors.background,
         child: SafeArea(
           child: Center(
*** End Patch