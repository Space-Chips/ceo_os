*** Begin Patch
*** Update File: lib/features/ceo_mode/ceo_mode_screen.dart
@@
 class _CeoModeScreenState extends State<CeoModeScreen> {
@@
   bool get _isAndroid =>
       !kIsWeb && defaultTargetPlatform == TargetPlatform.android;
 
+  String _t(String key) => context.watch<LanguageProvider>().t(key);
+
   @override
   void initState() {
     super.initState();
     WidgetsBinding.instance.addPostFrameCallback((_) {
       context.read<CeoModeProvider>().initialize();
@@
       final issue = provider.lastStartIssue;
       final (title, message) = switch (issue) {
         'permissions_denied' => (
           _isAndroid
-              ? 'Turn On Android Protection Access'
-              : 'Turn On Screen Time Access',
+              ? _t('blackout_error_permissions_title_android')
+              : _t('blackout_error_permissions_title_ios'),
           _isAndroid
-              ? 'Blackout Mode can enforce blocking on this Android device, but Accessibility or Usage Access is currently off. Re-enable Android protection access before starting a protected session.'
-              : 'Blackout Mode can enforce blocking on this iPhone, but Screen Time / Family Controls access is currently off. Re-enable it in iOS Settings before starting a protected session.',
+              ? _t('blackout_error_permissions_message_android')
+              : _t('blackout_error_permissions_message_ios'),
         ),
         'unsupported' => (
-          'Unsupported Runtime',
+          _t('blackout_error_unsupported_title'),
           _isAndroid
-              ? 'This build does not currently expose the Android protection services required for Blackout Mode, so the session cannot be enforced here.'
-              : 'This build does not currently expose Apple Screen Time / Family Controls protection, so Blackout Mode cannot enforce the session here.',
+              ? _t('blackout_error_unsupported_message_android')
+              : _t('blackout_error_unsupported_message_ios'),
         ),
         'activation_failed' => (
-          'Blackout Mode Unavailable',
+          _t('blackout_error_activation_failed_title'),
           _isAndroid
-              ? 'WakeApp could not start the Android Blackout protection on this device. Check the required protection permissions, then try again.'
-              : 'WakeApp could not start the protected Blackout session on this device. Check Screen Time access, then try again.',
+              ? _t('blackout_error_activation_failed_message_android')
+              : _t('blackout_error_activation_failed_message_ios'),
         ),
         _ => (
-          'Setup Required',
+          _t('blackout_error_setup_required_title'),
           _isAndroid
-              ? 'Enable Android protection permissions to enforce Blackout Mode protections.'
-              : 'Enable Screen Time / Family Controls permissions to enforce Blackout Mode protections.',
+              ? _t('blackout_error_setup_required_message_android')
+              : _t('blackout_error_setup_required_message_ios'),
         ),
       };
       await showCupertinoDialog<void>(
         context: context,
@@
   String _protectionStatusMessage(CeoModeProvider provider) {
     final status = provider.protectionStatus;
     if (provider.isAuthorized) {
-      return 'Protection access granted';
+      return _t('blackout_protection_granted');
     }
     if (!status.isSupported) {
-      return _isAndroid
-          ? 'Android protection is unavailable on this runtime'
-          : 'Apple Screen Time protection is unavailable on this runtime';
+      return _isAndroid
+          ? _t('blackout_protection_unavailable_android')
+          : _t('blackout_protection_unavailable_ios');
     }
     if (status.shouldOpenSettings) {
-      return _isAndroid
-          ? 'Android protection access is off. Re-enable it in Android settings for enforced Blackout Mode'
-          : 'Screen Time access is off. Re-enable it in iOS Settings for enforced Blackout Mode';
+      return _isAndroid
+          ? _t('blackout_protection_off_android')
+          : _t('blackout_protection_off_ios');
     }
-    return _isAndroid
-        ? 'Grant Android protection permissions to enable protected Blackout Mode sessions'
-        : 'Grant Screen Time permissions to enable protected Blackout Mode sessions';
+    return _isAndroid
+        ? _t('blackout_protection_grant_android')
+        : _t('blackout_protection_grant_ios');
   }
 
   String _protectionActionLabel(CeoModeProvider provider) {
-    if (provider.isAuthorized) return 'Refresh';
-    if (!provider.protectionStatus.isSupported) return 'Unavailable';
+    if (provider.isAuthorized) return _t('blackout_action_refresh');
+    if (!provider.protectionStatus.isSupported) {
+      return _t('blackout_action_unavailable');
+    }
     return provider.protectionStatus.shouldOpenSettings
-        ? 'Open Settings'
-        : 'Enable';
+        ? _t('blackout_action_open_settings')
+        : _t('blackout_action_enable');
   }
@@
                     Text(
-                      'Activate Blackout Mode?',
+                      _t('blackout_confirm_title'),
                       textAlign: TextAlign.center,
                       style: AppTypography.title2.copyWith(
                         fontSize: 20,
                         fontWeight: FontWeight.w700,
                         color: AppColors.label,
@@
                     Text(
-                      'All apps except Phone, Messages, and Calendar will be unavailable until the session ends.',
+                      _t('blackout_confirm_body'),
                       textAlign: TextAlign.center,
                       style: AppTypography.body.copyWith(
                         fontSize: 14,
                         height: 1.4,
                         color: AppColors.secondaryLabel.withValues(alpha: 0.78),
@@
                         Text(
-                          'Session duration',
+                          _t('focus_session_duration'),
                           textAlign: TextAlign.center,
                           style: AppTypography.subhead.copyWith(
                             fontSize: 14,
                             color: AppColors.secondaryLabel.withValues(
                               alpha: 0.74,
@@
                         Text(
-                          _confirmationDurationLabel(
-                            provider.selectedDurationMinutes,
-                          ),
+                          _durationLabel(provider.selectedDurationMinutes),
                           textAlign: TextAlign.center,
                           style: AppTypography.title3.copyWith(
                             fontSize: 16,
                             fontWeight: FontWeight.w600,
                             color: AppColors.label,
@@
                     CupertinoButton(
                       padding: EdgeInsets.zero,
                       onPressed: () => Navigator.of(ctx).pop(false),
                       child: Text(
-                        'Cancel',
+                        _t('cancel'),
                         style: AppTypography.callout.copyWith(
                           color: AppColors.label.withValues(alpha: 0.7),
                           fontWeight: FontWeight.w600,
                         ),
                       ),
@@
                           alignment: Alignment.center,
                           child: Text(
-                            'Start Session',
+                            _t('blackout_confirm_start'),
                             style: AppTypography.callout.copyWith(
                               fontWeight: FontWeight.w700,
                               color: AppColors.background,
                             ),
                           ),
@@
   }
 
-  String _confirmationDurationLabel(int minutes) {
-    if (minutes >= 60) {
-      final hours = minutes ~/ 60;
-      final remainingMinutes = minutes % 60;
-      if (remainingMinutes == 0) {
-        final value = hours;
-        return value == 1 ? '1 hour' : '$value hours';
-      }
-      return '$hours hour${hours > 1 ? 's' : ''} ${remainingMinutes.toString().padLeft(2, '0')}';
-    }
-    return minutes == 1 ? '1 minute' : '$minutes minutes';
-  }
-
   @override
   Widget build(BuildContext context) {
     return Consumer<CeoModeProvider>(
       builder: (context, ceo, _) {
@@
                 Text(
-                  'Home',
+                  _t('home'),
                   style: AppTypography.callout.copyWith(
                     fontSize: 16,
                     color: AppColors.secondaryLabel,
                     fontWeight: FontWeight.w600,
@@
               child: Text(
-                'LOCKED',
+                _t('blackout_locked'),
                 style: AppTypography.caption1.copyWith(
                   fontSize: 10,
                   color: AppColors.primaryOrange,
                   fontWeight: FontWeight.bold,
                   letterSpacing: 1.2,
@@
           child: Text(
-            'Blackout Mode',
+            _t('blackout_mode'),
             style: AppTypography.largeTitle.copyWith(
               fontSize: 42,
               fontWeight: FontWeight.w800,
               letterSpacing: 0.3,
@@
           child: Text(
-            'Maximum focus. Once activated, distractions are locked until the session ends.',
+            _t('blackout_description'),
             style: AppTypography.body.copyWith(
               fontSize: 15,
               height: 1.5,
               color: AppColors.secondaryLabel.withValues(alpha: 0.82),
             ),
@@
                 Text(
-                  'Duration',
+                  _t('duration'),
                   style: AppTypography.overline.copyWith(
                     fontSize: 12,
                     color: AppColors.accent,
                     fontWeight: FontWeight.w700,
                     letterSpacing: 1.2,
@@
               Text(
-                'Essential Apps (3)',
+                '${_t('blackout_essential_apps')} (3)',
                 style: AppTypography.overline.copyWith(
                   fontSize: 12,
                   color: AppColors.secondaryLabel.withValues(alpha: 0.7),
                   fontWeight: FontWeight.w700,
                   letterSpacing: 1.1,
                 ),
               ),
               const SizedBox(height: 12),
-              _approvedAppItem(CupertinoIcons.phone, 'Phone'),
+              _approvedAppItem(CupertinoIcons.phone, _t('blackout_phone')),
               const SizedBox(height: 10),
-              _approvedAppItem(CupertinoIcons.chat_bubble_2, 'Messages'),
+              _approvedAppItem(
+                CupertinoIcons.chat_bubble_2,
+                _t('blackout_messages'),
+              ),
               const SizedBox(height: 10),
-              _approvedAppItem(CupertinoIcons.calendar, 'Calendar'),
+              _approvedAppItem(CupertinoIcons.calendar, _t('calendar')),
             ],
           ),
         ),
@@
         const SizedBox(height: 12),
         Text(
-          'Blackout Mode blocks everything except the essential apps shown above.',
+          _t('blackout_essential_note'),
           textAlign: TextAlign.center,
           style: AppTypography.body.copyWith(
             fontSize: 13,
             color: AppColors.secondaryLabel.withValues(alpha: 0.55),
           ),
@@
                 child: ceo.isBusy
                     ? CupertinoActivityIndicator(color: AppColors.onAccent)
                     : Text(
-                        'Start CEO Session',
+                        _t('blackout_start_session'),
                         style: AppTypography.callout.copyWith(
                           fontSize: 16,
                           fontWeight: FontWeight.w700,
                           color: AppColors.onAccent,
@@
                 Text(
-                  'BLACKOUT ACTIVE',
+                  _t('blackout_active'),
                   style: AppTypography.caption1.copyWith(
                     fontSize: 12,
                     color: AppColors.primaryOrange,
                     fontWeight: FontWeight.w700,
                     letterSpacing: 1.6,
@@
                 Text(
-                  'All apps and websites are locked until the session ends.',
+                  _t('blackout_active_description'),
                   textAlign: TextAlign.center,
                   style: AppTypography.subhead.copyWith(
                     fontSize: 13,
                     color: AppColors.secondaryLabel,
                   ),
@@
         LiquidButton(
-          label: 'REQUEST EXIT (10 MIN)',
+          label: _t('blackout_request_exit').replaceAll('{minutes}', '10'),
           fullWidth: true,
           onPressed: ceo.requestExit,
         ),
@@
                                     Text(
-                                      'EXIT COUNTDOWN',
+                                      _t('blackout_exit_countdown'),
                                       textAlign: TextAlign.center,
                                       style: AppTypography.overline.copyWith(
                                         fontSize: 12,
                                         color: AppColors.warning,
@@
                       child: Text(
-                        'If your screen turns off, countdown restarts to 10:00.',
+                        _t('blackout_exit_hint'),
                         textAlign: TextAlign.center,
                         style: AppTypography.footnote.copyWith(
                           fontSize: 13,
                           height: 1.42,
@@
                     LiquidButton(
-                      label: 'RETURN TO SESSION',
+                      label: _t('blackout_return_to_session'),
                       fullWidth: true,
                       onPressed: ceo.returnToSession,
                     ),
@@
                       child: LiquidButton(
                         label: ceo.canFinalizeExit
-                            ? 'QUIT NOW'
-                            : 'WAIT FOR COUNTDOWN',
+                            ? _t('blackout_quit_now')
+                            : _t('blackout_wait_for_countdown'),
                         fullWidth: true,
                         onPressed: ceo.canFinalizeExit
                             ? ceo.finalizeExit
                             : null,
@@
               Text(
-                'remaining',
+                _t('blackout_remaining'),
                 style: AppTypography.caption1.copyWith(
                   fontSize: 11,
                   color: AppColors.secondaryLabel,
                   letterSpacing: 1.2,
*** End Patch