*** Begin Patch
*** Update File: android/app/src/main/kotlin/com/wakeapp/ceoos/GateLauncher.kt
@@
         return when (decision) {
             is BlockDecision.BlockApp -> {
                 val appName = appLabelForPackage(packageManager, decision.packageName)
                 when (decision.reason) {
@@
                 }
             }
-            is BlockDecision.BlockUrl -> null
             else -> null
         }
     }
*** End Patch