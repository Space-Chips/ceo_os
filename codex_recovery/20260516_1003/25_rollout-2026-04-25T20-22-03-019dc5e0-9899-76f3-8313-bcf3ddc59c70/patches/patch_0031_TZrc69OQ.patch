*** Begin Patch
*** Update File: lib/features/home/home_screen.dart
@@
               final visibleShortcutCards = shortcutCards
                   .where((card) => _enabledShortcuts.contains(card.moduleId))
                   .toList(growable: false);
-              final visibleCards = [
-                ...visibleAppCards,
-                ...visibleShortcutCards,
-              ].take(4).toList(growable: false);
-              final shouldShowNoModulesMessage =
-                  visibleCards.isEmpty && !_hideNoModulesMessage;
+              Future<void> openCustomizer() async {
+                await context.push('/control-center-setup?edit=true');
+                if (!mounted) return;
+                await _loadControlCenterConfiguration();
+                if (!mounted) return;
+                _handleSourceDataChanged();
+              }
+
+              final controlSlots = _normalizeControlCenterSlots(
+                _controlCenterConfiguration.slotItems,
+              );
+              final cardById = <String, _PrimaryAppCardData>{
+                'module_todo': _PrimaryAppCardData(
+                  moduleId: 'module_todo',
+                  title: 'To-Do',
+                  icon: CupertinoIcons.check_mark_circled,
+                  active: true,
+                  badgeValue: pendingTasks,
+                  onTap: () => unawaited(_pushAndRefresh('/tasks')),
+                  onQuickAdd: _openQuickAddTask,
+                ),
+                'module_habits': _PrimaryAppCardData(
+                  moduleId: 'module_habits',
+                  title: 'Habits',
+                  icon: CupertinoIcons.flame_fill,
+                  active: true,
+                  badgeValue: habitsCount,
+                  onTap: () => unawaited(_pushAndRefresh('/habits')),
+                  onQuickAdd: _openQuickAddHabit,
+                ),
+                'module_schedule': _PrimaryAppCardData(
+                  moduleId: 'module_schedule',
+                  title: 'Schedule',
+                  icon: CupertinoIcons.calendar,
+                  active: true,
+                  badgeValue: todayEventsCount,
+                  onTap: () => unawaited(_pushAndRefresh('/calendar')),
+                  onQuickAdd: _openQuickAddEvent,
+                ),
+                'module_screen_time': _PrimaryAppCardData(
+                  moduleId: 'module_screen_time',
+                  title: 'Screen Time',
+                  icon: CupertinoIcons.shield,
+                  active: true,
+                  onTap: _openScreenTimeEntry,
+                ),
+                'shortcut_blackout': _PrimaryAppCardData(
+                  moduleId: 'shortcut_blackout',
+                  title: 'Blackout',
+                  icon: CupertinoIcons.moon_fill,
+                  active: true,
+                  onTap: () => unawaited(_pushAndRefresh('/ceo-mode')),
+                ),
+                'shortcut_quick_focus': _PrimaryAppCardData(
+                  moduleId: 'shortcut_quick_focus',
+                  title: 'Focus rapide',
+                  icon: CupertinoIcons.bolt_fill,
+                  active: true,
+                  onTap: _openQuickFocusSession,
+                ),
+                'shortcut_controlled_pause': _PrimaryAppCardData(
+                  moduleId: 'shortcut_controlled_pause',
+                  title: 'Pause contrôlée',
+                  icon: CupertinoIcons.pause_circle_fill,
+                  active: true,
+                  onTap: () => unawaited(_pushAndRefresh('/focus')),
+                ),
+                'shortcut_block_now': _PrimaryAppCardData(
+                  moduleId: 'shortcut_block_now',
+                  title: 'Bloquer maintenant',
+                  icon: CupertinoIcons.hand_raised_fill,
+                  active: true,
+                  onTap: _openScreenTimeEntry,
+                ),
+                'shortcut_add_rule': _PrimaryAppCardData(
+                  moduleId: 'shortcut_add_rule',
+                  title: 'Ajouter une règle',
+                  icon: CupertinoIcons.plus_circle_fill,
+                  active: true,
+                  onTap: _openScreenTimeEntry,
+                ),
+                'shortcut_stats': _PrimaryAppCardData(
+                  moduleId: 'shortcut_stats',
+                  title: 'Statistiques',
+                  icon: CupertinoIcons.chart_bar_alt_fill,
+                  active: true,
+                  onTap: () => unawaited(_pushAndRefresh('/dashboard')),
+                ),
+              };
+
+              _PrimaryAppCardData placeholderCard(int index) {
+                return _PrimaryAppCardData(
+                  moduleId: 'empty_slot_$index',
+                  title: 'Ajouter',
+                  icon: CupertinoIcons.plus,
+                  active: false,
+                  onTap: openCustomizer,
+                  isPlaceholder: true,
+                );
+              }
+
+              final slotCards = <_PrimaryAppCardData>[
+                for (int i = 0; i < 4; i++)
+                  (controlSlots[i].isEmpty
+                      ? placeholderCard(i)
+                      : (cardById[controlSlots[i]] ?? placeholderCard(i))),
+              ];
+
+              final shouldShowNoModulesMessage =
+                  controlSlots.every((id) => id.isEmpty) &&
+                  !_hideNoModulesMessage;
 
               return Stack(
                 children: [
                   ListView(
@@
                       ),
                       const SizedBox(height: 14),
-                      if (visibleCards.isNotEmpty)
-                        _PrimaryAppsGrid(cards: visibleCards),
-                      if (visibleCards.isNotEmpty && shouldShowNoModulesMessage)
-                        const SizedBox(height: 14),
+                      _PrimaryAppsGrid(cards: slotCards),
+                      if (shouldShowNoModulesMessage) const SizedBox(height: 14),
                       if (shouldShowNoModulesMessage)
                         _NoModulesMessageCard(
                           message: language.t('no_active_modules'),
                           onDismiss: _dismissNoModulesMessage,
@@
-                      const SizedBox(height: 16),
-                      _CeoModeCard(
-                        statusLabel: ceoProvider.homeCardLabel,
-                        title: language.t('blackout_mode'),
-                        maxFocusLabel: language.t('max_focus'),
-                        sessionPrefix: language.t('session_prefix'),
-                        exitPrefix: language.t('exit_prefix'),
-                        onOpenCeoMode: () => context.push('/ceo-mode'),
-                      ),
+                      if (_enabledDashboardWidgets.contains(
+                        DashboardWidget.blackoutButton,
+                      )) ...[
+                        const SizedBox(height: 16),
+                        _CeoModeCard(
+                          statusLabel: ceoProvider.homeCardLabel,
+                          title: language.t('blackout_mode'),
+                          maxFocusLabel: language.t('max_focus'),
+                          sessionPrefix: language.t('session_prefix'),
+                          exitPrefix: language.t('exit_prefix'),
+                          onOpenCeoMode: () => context.push('/ceo-mode'),
+                        ),
+                      ],
*** End Patch