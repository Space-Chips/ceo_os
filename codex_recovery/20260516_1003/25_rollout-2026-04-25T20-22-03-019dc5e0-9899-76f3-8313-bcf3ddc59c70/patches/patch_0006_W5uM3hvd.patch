*** Begin Patch
*** Add File: lib/features/control_center_setup/widgets/control_center_slot.dart
+import 'dart:ui';
+
+import 'package:flutter/cupertino.dart';
+
+import '../../../components/glass_card.dart';
+import '../../../core/theme/app_colors.dart';
+import '../../../core/theme/app_typography.dart';
+import '../control_center_setup_models.dart';
+
+class ControlCenterSlot extends StatelessWidget {
+  final ControlCenterItem? item;
+  final VoidCallback? onTap;
+  final VoidCallback? onRemove;
+  final bool showRemove;
+  final double appearProgress; // 0..1
+  final bool pulse;
+
+  const ControlCenterSlot({
+    super.key,
+    required this.item,
+    this.onTap,
+    this.onRemove,
+    this.showRemove = false,
+    this.appearProgress = 1,
+    this.pulse = false,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    final p = appearProgress.clamp(0.0, 1.0);
+    final scale = lerpDouble(0.92, 1, Curves.easeOutCubic.transform(p))!;
+    final opacity = Curves.easeOutCubic.transform(p);
+
+    final base = GlassCard(
+      level: GlassCardLevel.elevated,
+      padding: const EdgeInsets.all(12),
+      borderRadius: 18,
+      showEdgeGlow: true,
+      glowColor: AppColors.edgeGlowSoft,
+      gradientColors: [
+        AppColors.cardBackgroundStrong,
+        AppColors.cardBase,
+      ],
+      border: Border.all(
+        color: AppColors.white.withValues(alpha: 0.08),
+        width: 0.9,
+      ),
+      child: Stack(
+        children: [
+          Positioned.fill(
+            child: AnimatedOpacity(
+              duration: const Duration(milliseconds: 240),
+              curve: Curves.easeOutCubic,
+              opacity: pulse ? 1 : 0,
+              child: Container(
+                decoration: BoxDecoration(
+                  borderRadius: BorderRadius.circular(18),
+                  gradient: LinearGradient(
+                    begin: Alignment.topLeft,
+                    end: Alignment.bottomRight,
+                    colors: [
+                      AppColors.accent.withValues(alpha: 0.14),
+                      AppColors.themeGlow.withValues(alpha: 0.0),
+                    ],
+                  ),
+                ),
+              ),
+            ),
+          ),
+          Center(
+            child: item == null
+                ? Column(
+                    mainAxisSize: MainAxisSize.min,
+                    children: [
+                      Icon(
+                        CupertinoIcons.plus,
+                        size: 18,
+                        color: AppColors.secondaryLabel.withValues(alpha: 0.8),
+                      ),
+                      const SizedBox(height: 6),
+                      Text(
+                        'Ajouter',
+                        style: AppTypography.caption1.copyWith(
+                          color: AppColors.tertiaryLabel.withValues(alpha: 0.7),
+                          fontWeight: FontWeight.w600,
+                        ),
+                      ),
+                    ],
+                  )
+                : Column(
+                    mainAxisSize: MainAxisSize.min,
+                    children: [
+                      Container(
+                        width: 42,
+                        height: 42,
+                        decoration: BoxDecoration(
+                          borderRadius: BorderRadius.circular(14),
+                          color: AppColors.white.withValues(alpha: 0.05),
+                          border: Border.all(
+                            color: AppColors.white.withValues(alpha: 0.08),
+                          ),
+                        ),
+                        child: Icon(
+                          item!.icon,
+                          color: AppColors.accentIcon,
+                          size: 22,
+                        ),
+                      ),
+                      const SizedBox(height: 10),
+                      Text(
+                        item!.title,
+                        textAlign: TextAlign.center,
+                        maxLines: 2,
+                        overflow: TextOverflow.ellipsis,
+                        style: AppTypography.footnote.copyWith(
+                          color: AppColors.label,
+                          fontWeight: FontWeight.w700,
+                          height: 1.1,
+                        ),
+                      ),
+                    ],
+                  ),
+          ),
+          if (item != null && showRemove && onRemove != null)
+            Positioned(
+              right: 8,
+              top: 8,
+              child: GestureDetector(
+                onTap: onRemove,
+                child: Container(
+                  width: 28,
+                  height: 28,
+                  decoration: BoxDecoration(
+                    borderRadius: BorderRadius.circular(10),
+                    color: AppColors.white.withValues(alpha: 0.06),
+                    border: Border.all(
+                      color: AppColors.white.withValues(alpha: 0.08),
+                    ),
+                  ),
+                  child: Icon(
+                    CupertinoIcons.xmark,
+                    size: 14,
+                    color: AppColors.secondaryLabel,
+                  ),
+                ),
+              ),
+            ),
+        ],
+      ),
+    );
+
+    return Opacity(
+      opacity: opacity,
+      child: Transform.scale(
+        scale: scale,
+        child: GestureDetector(onTap: onTap, child: base),
+      ),
+    );
+  }
+}
+
*** End Patch