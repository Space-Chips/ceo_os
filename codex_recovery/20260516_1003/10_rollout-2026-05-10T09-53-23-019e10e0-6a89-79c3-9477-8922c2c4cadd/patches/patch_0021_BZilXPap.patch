*** Begin Patch
*** Update File: lib/features/home/home_screen.dart
@@
   List<String> _normalizeControlCenterSlots(List<String> raw) {
     final trimmed = raw.take(4).map((id) => id.trim()).toList(growable: true);
     while (trimmed.length < 4) {
       trimmed.add('');
     }
     return trimmed;
   }
+
+  List<String> _buildControlCenterSlotsFromSelections({
+    required Set<String> activeApps,
+    required Set<String> enabledShortcuts,
+  }) {
+    final slots = <String>[];
+    const moduleOrder = ['Pareto', 'Habits', 'Calendar', 'ScreenTimeManager'];
+    for (final moduleId in moduleOrder) {
+      if (!activeApps.contains(moduleId)) continue;
+      final slotId = _appNameToSlotId[moduleId];
+      if (slotId == null || slotId.isEmpty) continue;
+      if (!slots.contains(slotId)) slots.add(slotId);
+    }
+    for (final shortcutId in _shortcutOrder) {
+      if (!enabledShortcuts.contains(shortcutId)) continue;
+      final slotId = _shortcutIdToSlotId[shortcutId];
+      if (slotId == null || slotId.isEmpty) continue;
+      if (!slots.contains(slotId)) slots.add(slotId);
+    }
+    while (slots.length < 4) {
+      slots.add('');
+    }
+    return slots.take(4).toList(growable: false);
+  }
+
+  Future<void> _persistControlCenterSelections({
+    required Set<String> activeApps,
+    required Set<String> enabledShortcuts,
+  }) async {
+    final slots = _buildControlCenterSlotsFromSelections(
+      activeApps: activeApps,
+      enabledShortcuts: enabledShortcuts,
+    );
+    final newConfig = ControlCenterConfiguration(
+      slotItems: slots,
+      enabledWidgets: _controlCenterConfiguration.enabledWidgets,
+      selectedThemeId: _controlCenterConfiguration.selectedThemeId,
+      setupCompleted: true,
+    );
+    if (!mounted) return;
+    setState(() => _controlCenterConfiguration = newConfig);
+    final store = await ControlCenterSetupStore.create();
+    await store.save(newConfig);
+  }
@@
   Future<bool> _togglePrimaryModule(String moduleId, bool enabled) async {
     final previous = Set<String>.from(_activeApps);
     final previousShortcuts = Set<String>.from(_enabledShortcuts);
     final next = Set<String>.from(_activeApps);
@@
     setState(() {
       _activeApps = next;
       _enabledShortcuts = normalizedShortcuts;
     });
     try {
       final id = await _featureRepository.saveActiveAppsFast(
         next.toList(),
         settingsId: _settingsId,
       );
       _settingsId = id;
       final prefs = await SharedPreferences.getInstance();
       await prefs.setStringList(
         _prefsKey('enabled_shortcuts_v2'),
         normalizedShortcuts.toList(growable: false),
       );
-      // Keep the visible home grid in sync: if this fails, treat as a failure
-      // so the toggle cannot silently desync from what the user sees.
-      await _syncToggleToConfig(id: moduleId, enabled: enabled, isModule: true);
+      // Persist a fully rebuilt slot layout so module toggles cannot desync
+      // the home grid from shortcut capacity changes.
+      await _persistControlCenterSelections(
+        activeApps: next,
+        enabledShortcuts: normalizedShortcuts,
+      );
       return true;
     } catch (_) {
       if (mounted) {
         setState(() {
           _activeApps = previous;
           _enabledShortcuts = previousShortcuts;
         });
       }
       return false;
     }
   }
 
   Future<void> _toggleShortcut(String shortcutId, bool enabled) async {
     final previous = Set<String>.from(_enabledShortcuts);
     final next = Set<String>.from(_enabledShortcuts);
@@
-    setState(() => _enabledShortcuts = next);
+    final normalizedNext = _normalizeEnabledShortcuts(next, _activeApps);
+    setState(() => _enabledShortcuts = normalizedNext);
 
     final prefs = await SharedPreferences.getInstance();
     try {
       await prefs.setStringList(
         _prefsKey('enabled_shortcuts_v2'),
-        next.toList(growable: false),
+        normalizedNext.toList(growable: false),
       );
-      await _syncToggleToConfig(id: shortcutId, enabled: enabled, isModule: false);
+      await _persistControlCenterSelections(
+        activeApps: _activeApps,
+        enabledShortcuts: normalizedNext,
+      );
     } catch (_) {
       if (!mounted) return;
       setState(() => _enabledShortcuts = previous);
     }
   }
*** End Patch