*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
   Future<void> cacheSelection(
     List<String> packageNames,
     List<String> categories,
   ) async {
@@
     }
   }
+
+  Future<void> syncClassicShieldConfig(
+    List<String> packageNames,
+    List<String> categories,
+  ) async {
+    try {
+      await _channel.invokeMethod('syncClassicShieldConfig', {
+        'packages': packageNames,
+        'categories': categories,
+      });
+    } on PlatformException catch (e) {
+      AppLogger.error("Failed to sync classic shield config: '${e.message}'.");
+    } on MissingPluginException {
+      AppLogger.warning(
+        'Native syncClassicShieldConfig method missing. Rebuild required.',
+      );
+    }
+  }
+
+  Future<void> setClassicShieldEnabled(bool enabled) async {
+    try {
+      await _channel.invokeMethod('setClassicShieldEnabled', {
+        'enabled': enabled,
+      });
+    } on PlatformException catch (e) {
+      AppLogger.error("Failed to toggle classic shield: '${e.message}'.");
+    } on MissingPluginException {
+      AppLogger.warning(
+        'Native setClassicShieldEnabled method missing. Rebuild required.',
+      );
+    }
+  }
*** End Patch
