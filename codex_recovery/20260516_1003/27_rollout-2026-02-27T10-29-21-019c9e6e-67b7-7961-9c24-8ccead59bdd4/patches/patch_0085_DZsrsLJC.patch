*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
   @override
   Widget build(BuildContext context) {
+    final tone = () {
+      final key = title.toLowerCase();
+      if (key.contains('quick + important')) return AppColors.primaryOrange;
+      if (key.contains('slow + important')) return AppColors.accentSecondary;
+      if (key.contains('quick + not')) return AppColors.warning;
+      return AppColors.tertiaryLabel;
+    }();
+
     return GlassCard(
       padding: const EdgeInsets.all(14),
       borderRadius: 14,
+      border: Border.all(color: tone.withValues(alpha: 0.24), width: 0.6),
       child: Column(
         crossAxisAlignment: CrossAxisAlignment.start,
         children: [
@@
                   title.toUpperCase(),
                   style: AppTypography.mono.copyWith(
                     fontSize: 11,
-                    color: AppColors.label,
+                    color: tone == AppColors.tertiaryLabel
+                        ? AppColors.label
+                        : tone,
                     fontWeight: FontWeight.bold,
                   ),
                 ),
               ),
               Text(
                 '${tasks.length}',
                 style: AppTypography.mono.copyWith(
                   fontSize: 11,
-                  color: AppColors.primaryOrange,
+                  color: tone == AppColors.tertiaryLabel
+                      ? AppColors.secondaryLabel
+                      : tone,
                   fontWeight: FontWeight.bold,
                 ),
               ),
@@
                 .take(4)
                 .map(
                   (task) => GestureDetector(
                     onTap: () => onOpen(task),
                     child: Padding(
                       padding: const EdgeInsets.only(bottom: 6),
-                      child: Text(
-                        '• ${task.title}',
-                        maxLines: 1,
-                        overflow: TextOverflow.ellipsis,
-                        style: AppTypography.mono.copyWith(
-                          fontSize: 10,
-                          color: AppColors.secondaryLabel,
+                      child: Container(
+                        padding: const EdgeInsets.symmetric(
+                          horizontal: 8,
+                          vertical: 6,
+                        ),
+                        decoration: BoxDecoration(
+                          borderRadius: BorderRadius.circular(8),
+                          color: AppColors.backgroundLight.withValues(
+                            alpha: 0.5,
+                          ),
+                          border: Border.all(
+                            color: AppColors.glassBorder,
+                            width: 0.4,
+                          ),
+                        ),
+                        child: Text(
+                          task.title,
+                          maxLines: 1,
+                          overflow: TextOverflow.ellipsis,
+                          style: AppTypography.mono.copyWith(
+                            fontSize: 10,
+                            color: AppColors.secondaryLabel,
+                          ),
                         ),
                       ),
                     ),
                   ),
*** End Patch
