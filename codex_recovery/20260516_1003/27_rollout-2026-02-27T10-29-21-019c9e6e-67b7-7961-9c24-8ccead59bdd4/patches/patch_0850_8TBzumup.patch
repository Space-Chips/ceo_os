*** Begin Patch
*** Add File: /Users/timo/ceo_os/supabase_premium_foundation_v2.sql
+begin;
+
+create unique index if not exists app_configs_config_key_uidx
+  on public.app_configs (config_key);
+
+insert into public.app_configs (config_key, config_value)
+values (
+  'premium',
+  jsonb_build_object(
+    'launch_mode_enabled', true,
+    'launch_started_at', '2026-03-20T00:00:00+01:00',
+    'launch_ends_at', '2026-04-20T00:00:00+02:00',
+    'paywall_enabled', false,
+    'launch_premium_labels_enabled', true,
+    'grandfathering_enabled', true,
+    'early_discount_enabled', true,
+    'tasks_free_limit', 5,
+    'habits_free_limit', 3,
+    'notes_free_limit', 50,
+    'focus_free_daily_limit', 1,
+    'focus_free_max_duration_minutes', 30,
+    'ceo_free_weekly_limit', 1,
+    'ceo_free_max_duration_minutes', 60,
+    'free_themes', jsonb_build_array(
+      'carbon_system',
+      'ember_protocol',
+      'cloud_studio'
+    ),
+    'premium_themes', jsonb_build_array(
+      'modern_desert',
+      'royal_violet'
+    )
+  )
+)
+on conflict (config_key) do update
+set config_value = coalesce(public.app_configs.config_value, '{}'::jsonb) || excluded.config_value;
+
+create table if not exists public.user_entitlements (
+  id uuid default gen_random_uuid() primary key,
+  created_at timestamptz default now() not null,
+  updated_at timestamptz default now() not null,
+  created_by uuid references auth.users not null unique,
+  early_launch_user boolean default false not null,
+  early_launch_qualified_at timestamptz,
+  is_grandfathered boolean default false not null,
+  grandfather_reason text,
+  theme_bundle_granted boolean default false not null,
+  theme_bundle_source text,
+  discount_eligible boolean default false not null,
+  discount_kind text,
+  discount_percent integer,
+  discount_price_id text,
+  badge_code text,
+  premium_override boolean default false not null,
+  premium_override_reason text,
+  premium_override_expires_at timestamptz
+);
+
+alter table public.user_entitlements enable row level security;
+
+drop policy if exists "user_entitlements_own" on public.user_entitlements;
+create policy "user_entitlements_own"
+  on public.user_entitlements
+  for all
+  using (auth.uid() = created_by)
+  with check (auth.uid() = created_by);
+
+do $$
+begin
+  if not exists (
+    select 1
+    from pg_constraint
+    where conname = 'user_entitlements_discount_percent_check'
+  ) then
+    alter table public.user_entitlements
+      add constraint user_entitlements_discount_percent_check
+      check (
+        discount_percent is null
+        or (discount_percent >= 0 and discount_percent <= 100)
+      );
+  end if;
+
+  if not exists (
+    select 1
+    from pg_constraint
+    where conname = 'user_entitlements_discount_kind_check'
+  ) then
+    alter table public.user_entitlements
+      add constraint user_entitlements_discount_kind_check
+      check (
+        discount_kind is null
+        or discount_kind in ('percentage', 'price_id', 'founding_price')
+      );
+  end if;
+
+  if not exists (
+    select 1
+    from pg_constraint
+    where conname = 'user_entitlements_badge_code_check'
+  ) then
+    alter table public.user_entitlements
+      add constraint user_entitlements_badge_code_check
+      check (
+        badge_code is null
+        or badge_code in ('early_user', 'founding_member')
+      );
+  end if;
+end $$;
+
+create index if not exists user_entitlements_created_by_idx
+  on public.user_entitlements (created_by);
+
+create or replace function public.set_user_entitlements_updated_at()
+returns trigger
+language plpgsql
+as $$
+begin
+  new.updated_at = now();
+  return new;
+end;
+$$;
+
+drop trigger if exists trg_user_entitlements_updated_at on public.user_entitlements;
+create trigger trg_user_entitlements_updated_at
+before update on public.user_entitlements
+for each row
+execute function public.set_user_entitlements_updated_at();
+
+commit;
*** Update File: /Users/timo/ceo_os/lib/core/models/premium_models.dart
@@
 class PremiumLimits {
   static const int focusSessionsPerDay = 1;
   static const int focusMaxDurationMinutes = 30;
   static const int ceoSessionsPerWeek = 1;
   static const int ceoMaxDurationMinutes = 60;
   static const int maxActiveHabits = 3;
   static const int maxActiveTasks = 5;
+  static const int maxNotes = 50;
 }
@@
 class PremiumRuntime {
   final bool isPremiumUser;
-  final bool freeLaunchMode;
+  final bool freeLaunchMode;
   final String subscriptionStatus;
+  final PremiumConfig config;
+  final UserEntitlements entitlements;
+  final ResolvedEntitlements resolved;
 
   const PremiumRuntime({
     required this.isPremiumUser,
     required this.freeLaunchMode,
     required this.subscriptionStatus,
+    required this.config,
+    required this.entitlements,
+    required this.resolved,
   });
 }
+
+class PremiumConfig {
+  final bool launchModeEnabled;
+  final DateTime? launchStartedAt;
+  final DateTime? launchEndsAt;
+  final bool paywallEnabled;
+  final bool launchPremiumLabelsEnabled;
+  final bool grandfatheringEnabled;
+  final bool earlyDiscountEnabled;
+  final int tasksFreeLimit;
+  final int habitsFreeLimit;
+  final int notesFreeLimit;
+  final int focusFreeDailyLimit;
+  final int focusFreeMaxDurationMinutes;
+  final int ceoFreeWeeklyLimit;
+  final int ceoFreeMaxDurationMinutes;
+  final List<String> freeThemes;
+  final List<String> premiumThemes;
+
+  const PremiumConfig({
+    required this.launchModeEnabled,
+    required this.launchStartedAt,
+    required this.launchEndsAt,
+    required this.paywallEnabled,
+    required this.launchPremiumLabelsEnabled,
+    required this.grandfatheringEnabled,
+    required this.earlyDiscountEnabled,
+    required this.tasksFreeLimit,
+    required this.habitsFreeLimit,
+    required this.notesFreeLimit,
+    required this.focusFreeDailyLimit,
+    required this.focusFreeMaxDurationMinutes,
+    required this.ceoFreeWeeklyLimit,
+    required this.ceoFreeMaxDurationMinutes,
+    required this.freeThemes,
+    required this.premiumThemes,
+  });
+
+  factory PremiumConfig.defaults() => const PremiumConfig(
+    launchModeEnabled: true,
+    launchStartedAt: null,
+    launchEndsAt: null,
+    paywallEnabled: false,
+    launchPremiumLabelsEnabled: true,
+    grandfatheringEnabled: true,
+    earlyDiscountEnabled: true,
+    tasksFreeLimit: PremiumLimits.maxActiveTasks,
+    habitsFreeLimit: PremiumLimits.maxActiveHabits,
+    notesFreeLimit: PremiumLimits.maxNotes,
+    focusFreeDailyLimit: PremiumLimits.focusSessionsPerDay,
+    focusFreeMaxDurationMinutes: PremiumLimits.focusMaxDurationMinutes,
+    ceoFreeWeeklyLimit: PremiumLimits.ceoSessionsPerWeek,
+    ceoFreeMaxDurationMinutes: PremiumLimits.ceoMaxDurationMinutes,
+    freeThemes: ['carbon_system', 'ember_protocol', 'cloud_studio'],
+    premiumThemes: ['modern_desert', 'royal_violet'],
+  );
+
+  factory PremiumConfig.fromAppConfig(Map<String, dynamic>? config) {
+    final source = config ?? const <String, dynamic>{};
+    return PremiumConfig(
+      launchModeEnabled: _readBool(source['launch_mode_enabled'], fallback: true),
+      launchStartedAt: _readDateTime(source['launch_started_at']),
+      launchEndsAt: _readDateTime(source['launch_ends_at']),
+      paywallEnabled: _readBool(source['paywall_enabled'], fallback: false),
+      launchPremiumLabelsEnabled: _readBool(
+        source['launch_premium_labels_enabled'],
+        fallback: true,
+      ),
+      grandfatheringEnabled: _readBool(
+        source['grandfathering_enabled'],
+        fallback: true,
+      ),
+      earlyDiscountEnabled: _readBool(
+        source['early_discount_enabled'],
+        fallback: true,
+      ),
+      tasksFreeLimit: _readInt(
+        source['tasks_free_limit'],
+        fallback: PremiumLimits.maxActiveTasks,
+      ),
+      habitsFreeLimit: _readInt(
+        source['habits_free_limit'],
+        fallback: PremiumLimits.maxActiveHabits,
+      ),
+      notesFreeLimit: _readInt(
+        source['notes_free_limit'],
+        fallback: PremiumLimits.maxNotes,
+      ),
+      focusFreeDailyLimit: _readInt(
+        source['focus_free_daily_limit'],
+        fallback: PremiumLimits.focusSessionsPerDay,
+      ),
+      focusFreeMaxDurationMinutes: _readInt(
+        source['focus_free_max_duration_minutes'],
+        fallback: PremiumLimits.focusMaxDurationMinutes,
+      ),
+      ceoFreeWeeklyLimit: _readInt(
+        source['ceo_free_weekly_limit'],
+        fallback: PremiumLimits.ceoSessionsPerWeek,
+      ),
+      ceoFreeMaxDurationMinutes: _readInt(
+        source['ceo_free_max_duration_minutes'],
+        fallback: PremiumLimits.ceoMaxDurationMinutes,
+      ),
+      freeThemes: _readStringList(
+        source['free_themes'],
+        fallback: const ['carbon_system', 'ember_protocol', 'cloud_studio'],
+      ),
+      premiumThemes: _readStringList(
+        source['premium_themes'],
+        fallback: const ['modern_desert', 'royal_violet'],
+      ),
+    );
+  }
+}
+
+class UserEntitlements {
+  final bool earlyLaunchUser;
+  final DateTime? earlyLaunchQualifiedAt;
+  final bool isGrandfathered;
+  final String? grandfatherReason;
+  final bool themeBundleGranted;
+  final String? themeBundleSource;
+  final bool discountEligible;
+  final String? discountKind;
+  final int? discountPercent;
+  final String? discountPriceId;
+  final String? badgeCode;
+  final bool premiumOverride;
+  final String? premiumOverrideReason;
+  final DateTime? premiumOverrideExpiresAt;
+
+  const UserEntitlements({
+    required this.earlyLaunchUser,
+    required this.earlyLaunchQualifiedAt,
+    required this.isGrandfathered,
+    required this.grandfatherReason,
+    required this.themeBundleGranted,
+    required this.themeBundleSource,
+    required this.discountEligible,
+    required this.discountKind,
+    required this.discountPercent,
+    required this.discountPriceId,
+    required this.badgeCode,
+    required this.premiumOverride,
+    required this.premiumOverrideReason,
+    required this.premiumOverrideExpiresAt,
+  });
+
+  factory UserEntitlements.empty() => const UserEntitlements(
+    earlyLaunchUser: false,
+    earlyLaunchQualifiedAt: null,
+    isGrandfathered: false,
+    grandfatherReason: null,
+    themeBundleGranted: false,
+    themeBundleSource: null,
+    discountEligible: false,
+    discountKind: null,
+    discountPercent: null,
+    discountPriceId: null,
+    badgeCode: null,
+    premiumOverride: false,
+    premiumOverrideReason: null,
+    premiumOverrideExpiresAt: null,
+  );
+
+  factory UserEntitlements.fromJson(Map<String, dynamic>? json) {
+    final source = json ?? const <String, dynamic>{};
+    return UserEntitlements(
+      earlyLaunchUser: _readBool(source['early_launch_user']),
+      earlyLaunchQualifiedAt: _readDateTime(source['early_launch_qualified_at']),
+      isGrandfathered: _readBool(source['is_grandfathered']),
+      grandfatherReason: source['grandfather_reason'] as String?,
+      themeBundleGranted: _readBool(source['theme_bundle_granted']),
+      themeBundleSource: source['theme_bundle_source'] as String?,
+      discountEligible: _readBool(source['discount_eligible']),
+      discountKind: source['discount_kind'] as String?,
+      discountPercent: _readNullableInt(source['discount_percent']),
+      discountPriceId: source['discount_price_id'] as String?,
+      badgeCode: source['badge_code'] as String?,
+      premiumOverride: _readBool(source['premium_override']),
+      premiumOverrideReason: source['premium_override_reason'] as String?,
+      premiumOverrideExpiresAt: _readDateTime(
+        source['premium_override_expires_at'],
+      ),
+    );
+  }
+}
+
+class ResolvedEntitlements {
+  final bool launchPremiumActive;
+  final bool hasPaidSubscription;
+  final bool isTrialing;
+  final bool premiumOverrideActive;
+  final bool isPremiumUser;
+  final bool canUsePremiumThemes;
+  final bool canCreateUnlimitedTasks;
+  final bool canCreateUnlimitedHabits;
+  final bool canCreateUnlimitedNotes;
+  final bool canUseExtendedFocus;
+  final bool canUseExtendedCeo;
+  final bool canAccessReports;
+  final bool canAccessAdvancedCalendar;
+  final bool discountEligible;
+  final String? badgeCode;
+
+  const ResolvedEntitlements({
+    required this.launchPremiumActive,
+    required this.hasPaidSubscription,
+    required this.isTrialing,
+    required this.premiumOverrideActive,
+    required this.isPremiumUser,
+    required this.canUsePremiumThemes,
+    required this.canCreateUnlimitedTasks,
+    required this.canCreateUnlimitedHabits,
+    required this.canCreateUnlimitedNotes,
+    required this.canUseExtendedFocus,
+    required this.canUseExtendedCeo,
+    required this.canAccessReports,
+    required this.canAccessAdvancedCalendar,
+    required this.discountEligible,
+    required this.badgeCode,
+  });
+}
@@
     case 'tasks':
       return PremiumMessage(
         title: isFr ? 'Limite de tâches atteinte' : 'Task Limit Reached',
         description: isFr
             ? 'La version gratuite permet 5 tâches actives maximum. Passe en Premium pour une capacité illimitée.'
             : 'Free users can keep up to 5 active tasks. Upgrade for unlimited active tasks.',
       );
+    case 'notes':
+      return PremiumMessage(
+        title: isFr ? 'Limite de notes atteinte' : 'Note Limit Reached',
+        description: isFr
+            ? 'La version gratuite permet jusqu’à 50 notes. Passe en Premium pour des notes illimitées.'
+            : 'Free users can keep up to 50 notes. Upgrade for unlimited notes.',
+      );
+    case 'themes':
+      return PremiumMessage(
+        title: isFr ? 'Thème Premium' : 'Premium Theme',
+        description: isFr
+            ? 'Ce thème est réservé aux utilisateurs Premium.'
+            : 'This theme is reserved for Premium users.',
+      );
     case 'reports':
       return PremiumMessage(
         title: isFr ? 'Rapports Premium' : 'Premium Reports',
@@
       );
   }
 }
+
+bool _readBool(dynamic raw, {bool fallback = false}) {
+  if (raw is bool) return raw;
+  if (raw is String) return raw.toLowerCase().trim() == 'true';
+  return fallback;
+}
+
+int _readInt(dynamic raw, {required int fallback}) {
+  if (raw is int) return raw;
+  if (raw is num) return raw.toInt();
+  if (raw is String) return int.tryParse(raw.trim()) ?? fallback;
+  return fallback;
+}
+
+int? _readNullableInt(dynamic raw) {
+  if (raw == null) return null;
+  if (raw is int) return raw;
+  if (raw is num) return raw.toInt();
+  if (raw is String) return int.tryParse(raw.trim());
+  return null;
+}
+
+DateTime? _readDateTime(dynamic raw) {
+  if (raw == null) return null;
+  if (raw is DateTime) return raw;
+  if (raw is String) return DateTime.tryParse(raw);
+  return null;
+}
+
+List<String> _readStringList(dynamic raw, {required List<String> fallback}) {
+  if (raw is List) {
+    return raw.map((e) => e.toString().trim()).where((e) => e.isNotEmpty).toList();
+  }
+  return fallback;
+}
*** Update File: /Users/timo/ceo_os/lib/core/repositories/feature_repository.dart
@@
   Future<List<Note>> getNotes() async {
     final response = await _client
         .from('notes')
         .select()
         .eq('created_by', _currentUserId)
         .order('updated_date', ascending: false);
     return (response as List).map((e) => Note.fromJson(e)).toList();
   }
+
+  Future<int> countNotes() async {
+    final response = await _client
+        .from('notes')
+        .select('id')
+        .eq('created_by', _currentUserId);
+    return (response as List).length;
+  }
*** Update File: /Users/timo/ceo_os/lib/core/repositories/premium_repository.dart
@@
 import 'package:intl/intl.dart';
 import 'package:supabase_flutter/supabase_flutter.dart';
 
 import '../models/premium_models.dart';
+import 'feature_repository.dart';
 import '../services/supabase_service.dart';
 
 class PremiumRepository {
   final SupabaseService _supabaseService;
+  late final FeatureRepository _featureRepository = FeatureRepository(
+    supabaseService: _supabaseService,
+  );
 
   PremiumRepository({SupabaseService? supabaseService})
     : _supabaseService = supabaseService ?? SupabaseService();
@@
   String? get _currentUserId => _client.auth.currentUser?.id;
 
   Future<PremiumRuntime> getRuntime() async {
-    final freeLaunchMode = await _getFreeLaunchMode();
+    final config = await getConfig();
     final subscriptionStatus = await _getSubscriptionStatus();
-    final premiumStatuses = {'active', 'trialing'};
-    final isPremiumUser =
-        freeLaunchMode ||
-        premiumStatuses.contains(subscriptionStatus.toLowerCase().trim());
+    final entitlements = await getUserEntitlements();
+    final resolved = _resolveEntitlements(
+      config: config,
+      entitlements: entitlements,
+      subscriptionStatus: subscriptionStatus,
+    );
 
     return PremiumRuntime(
-      isPremiumUser: isPremiumUser,
-      freeLaunchMode: freeLaunchMode,
+      isPremiumUser: resolved.isPremiumUser,
+      freeLaunchMode: resolved.launchPremiumActive,
       subscriptionStatus: subscriptionStatus,
+      config: config,
+      entitlements: entitlements,
+      resolved: resolved,
     );
   }
+
+  Future<PremiumConfig> getConfig() async {
+    try {
+      final premiumRow = await _client
+          .from('app_configs')
+          .select('config_value')
+          .eq('config_key', 'premium')
+          .maybeSingle();
+
+      final premiumConfig = premiumRow?['config_value'];
+      if (premiumConfig is Map<String, dynamic>) {
+        return PremiumConfig.fromAppConfig(premiumConfig);
+      }
+
+      final legacyLaunchMode = await _getLegacyFreeLaunchMode();
+      final defaults = PremiumConfig.defaults();
+      return PremiumConfig(
+        launchModeEnabled: legacyLaunchMode,
+        launchStartedAt: defaults.launchStartedAt,
+        launchEndsAt: defaults.launchEndsAt,
+        paywallEnabled: defaults.paywallEnabled,
+        launchPremiumLabelsEnabled: defaults.launchPremiumLabelsEnabled,
+        grandfatheringEnabled: defaults.grandfatheringEnabled,
+        earlyDiscountEnabled: defaults.earlyDiscountEnabled,
+        tasksFreeLimit: defaults.tasksFreeLimit,
+        habitsFreeLimit: defaults.habitsFreeLimit,
+        notesFreeLimit: defaults.notesFreeLimit,
+        focusFreeDailyLimit: defaults.focusFreeDailyLimit,
+        focusFreeMaxDurationMinutes: defaults.focusFreeMaxDurationMinutes,
+        ceoFreeWeeklyLimit: defaults.ceoFreeWeeklyLimit,
+        ceoFreeMaxDurationMinutes: defaults.ceoFreeMaxDurationMinutes,
+        freeThemes: defaults.freeThemes,
+        premiumThemes: defaults.premiumThemes,
+      );
+    } catch (_) {
+      return PremiumConfig.defaults();
+    }
+  }
+
+  Future<UserEntitlements> getUserEntitlements() async {
+    final uid = _currentUserId;
+    if (uid == null) return UserEntitlements.empty();
+
+    try {
+      final row = await _client
+          .from('user_entitlements')
+          .select()
+          .eq('created_by', uid)
+          .maybeSingle();
+      return UserEntitlements.fromJson(row);
+    } catch (_) {
+      return UserEntitlements.empty();
+    }
+  }
+
+  Future<ResolvedEntitlements> getResolvedEntitlements() async {
+    final runtime = await getRuntime();
+    return runtime.resolved;
+  }
 
   Future<PremiumCheckResult> canCreateTask() async {
     final runtime = await getRuntime();
-    if (runtime.isPremiumUser) {
+    if (runtime.resolved.canCreateUnlimitedTasks) {
       return const PremiumCheckResult.allowed();
     }
 
     final activeCount = await _countRows(
       table: 'pareto_tasks',
       filters: (query) => query.eq('completed', false),
     );
 
-    if (activeCount >= PremiumLimits.maxActiveTasks) {
+    if (activeCount >= runtime.config.tasksFreeLimit) {
       return PremiumCheckResult.blocked(
         reason: 'tasks',
-        limit: PremiumLimits.maxActiveTasks,
+        limit: runtime.config.tasksFreeLimit,
         current: activeCount,
       );
     }
     return const PremiumCheckResult.allowed();
   }
 
   Future<PremiumCheckResult> canCreateHabit() async {
     final runtime = await getRuntime();
-    if (runtime.isPremiumUser) {
+    if (runtime.resolved.canCreateUnlimitedHabits) {
       return const PremiumCheckResult.allowed();
     }
 
     final activeCount = await _countRows(
       table: 'habits',
       filters: (query) => query.eq('archived', false),
     );
 
-    if (activeCount >= PremiumLimits.maxActiveHabits) {
+    if (activeCount >= runtime.config.habitsFreeLimit) {
       return PremiumCheckResult.blocked(
         reason: 'habits',
-        limit: PremiumLimits.maxActiveHabits,
+        limit: runtime.config.habitsFreeLimit,
         current: activeCount,
       );
     }
     return const PremiumCheckResult.allowed();
   }
+
+  Future<PremiumCheckResult> canCreateNote() async {
+    final runtime = await getRuntime();
+    if (runtime.resolved.canCreateUnlimitedNotes) {
+      return const PremiumCheckResult.allowed();
+    }
+
+    final totalNotes = await _featureRepository.countNotes();
+    if (totalNotes >= runtime.config.notesFreeLimit) {
+      return PremiumCheckResult.blocked(
+        reason: 'notes',
+        limit: runtime.config.notesFreeLimit,
+        current: totalNotes,
+      );
+    }
+
+    return const PremiumCheckResult.allowed();
+  }
 
   Future<PremiumCheckResult> canStartFocusSession(int durationMinutes) async {
     final runtime = await getRuntime();
-    if (runtime.isPremiumUser) {
+    if (runtime.resolved.canUseExtendedFocus) {
       return const PremiumCheckResult.allowed();
     }
 
-    if (durationMinutes > PremiumLimits.focusMaxDurationMinutes) {
+    if (durationMinutes > runtime.config.focusFreeMaxDurationMinutes) {
       return PremiumCheckResult.blocked(
         reason: 'focus_duration',
-        limit: PremiumLimits.focusMaxDurationMinutes,
+        limit: runtime.config.focusFreeMaxDurationMinutes,
         current: durationMinutes,
       );
     }
@@
-    if (countToday >= PremiumLimits.focusSessionsPerDay) {
+    if (countToday >= runtime.config.focusFreeDailyLimit) {
       return PremiumCheckResult.blocked(
         reason: 'focus_daily',
-        limit: PremiumLimits.focusSessionsPerDay,
+        limit: runtime.config.focusFreeDailyLimit,
         current: countToday,
       );
     }
@@
   Future<PremiumCheckResult> canStartCeoSession(int durationMinutes) async {
     final runtime = await getRuntime();
-    if (runtime.isPremiumUser) {
+    if (runtime.resolved.canUseExtendedCeo) {
       return const PremiumCheckResult.allowed();
     }
 
-    if (durationMinutes > PremiumLimits.ceoMaxDurationMinutes) {
+    if (durationMinutes > runtime.config.ceoFreeMaxDurationMinutes) {
       return PremiumCheckResult.blocked(
         reason: 'ceo_duration',
-        limit: PremiumLimits.ceoMaxDurationMinutes,
+        limit: runtime.config.ceoFreeMaxDurationMinutes,
         current: durationMinutes,
       );
     }
@@
-    if (countThisWeek >= PremiumLimits.ceoSessionsPerWeek) {
+    if (countThisWeek >= runtime.config.ceoFreeWeeklyLimit) {
       return PremiumCheckResult.blocked(
         reason: 'ceo_weekly',
-        limit: PremiumLimits.ceoSessionsPerWeek,
+        limit: runtime.config.ceoFreeWeeklyLimit,
         current: countThisWeek,
       );
     }
@@
   Future<PremiumCheckResult> canAccessReports() async {
     final runtime = await getRuntime();
-    if (runtime.isPremiumUser) {
+    if (runtime.resolved.canAccessReports) {
       return const PremiumCheckResult.allowed();
     }
 
     return const PremiumCheckResult.blocked(reason: 'reports');
   }
 
   Future<PremiumCheckResult> canAccessAdvancedCalendar() async {
     final runtime = await getRuntime();
-    if (runtime.isPremiumUser) {
+    if (runtime.resolved.canAccessAdvancedCalendar) {
       return const PremiumCheckResult.allowed();
     }
 
     return const PremiumCheckResult.blocked(reason: 'calendar_advanced');
   }
+
+  Future<PremiumCheckResult> canUseTheme(String themeId) async {
+    final runtime = await getRuntime();
+    final normalizedThemeId = themeId.trim().toLowerCase();
+    final isPremiumTheme = runtime.config.premiumThemes.contains(normalizedThemeId);
+    if (!isPremiumTheme || runtime.resolved.canUsePremiumThemes) {
+      return const PremiumCheckResult.allowed();
+    }
+    return const PremiumCheckResult.blocked(reason: 'themes');
+  }
@@
-  Future<bool> _getFreeLaunchMode() async {
+  ResolvedEntitlements _resolveEntitlements({
+    required PremiumConfig config,
+    required UserEntitlements entitlements,
+    required String subscriptionStatus,
+  }) {
+    final normalizedStatus = subscriptionStatus.toLowerCase().trim();
+    final hasPaidSubscription = normalizedStatus == 'active';
+    final isTrialing = normalizedStatus == 'trialing';
+    final now = DateTime.now();
+    final overrideActive =
+        entitlements.premiumOverride &&
+        (entitlements.premiumOverrideExpiresAt == null ||
+            entitlements.premiumOverrideExpiresAt!.isAfter(now));
+    final launchStartedOk =
+        config.launchStartedAt == null || !now.isBefore(config.launchStartedAt!);
+    final launchNotEnded =
+        config.launchEndsAt == null || now.isBefore(config.launchEndsAt!);
+    final launchPremiumActive =
+        config.launchModeEnabled && launchStartedOk && launchNotEnded;
+    final isPremiumUser =
+        launchPremiumActive || hasPaidSubscription || isTrialing || overrideActive;
+    final canUsePremiumThemes =
+        isPremiumUser || entitlements.themeBundleGranted;
+
+    return ResolvedEntitlements(
+      launchPremiumActive: launchPremiumActive,
+      hasPaidSubscription: hasPaidSubscription,
+      isTrialing: isTrialing,
+      premiumOverrideActive: overrideActive,
+      isPremiumUser: isPremiumUser,
+      canUsePremiumThemes: canUsePremiumThemes,
+      canCreateUnlimitedTasks: isPremiumUser,
+      canCreateUnlimitedHabits: isPremiumUser,
+      canCreateUnlimitedNotes: isPremiumUser,
+      canUseExtendedFocus: isPremiumUser,
+      canUseExtendedCeo: isPremiumUser,
+      canAccessReports: isPremiumUser,
+      canAccessAdvancedCalendar: isPremiumUser,
+      discountEligible: entitlements.discountEligible,
+      badgeCode: entitlements.badgeCode,
+    );
+  }
+
+  Future<bool> _getLegacyFreeLaunchMode() async {
     try {
       final row = await _client
           .from('app_configs')
@@
-      if (row == null) return true;
+      if (row == null) return true;
@@
       return true;
     } catch (_) {
       // Safe default aligned with CEOV1 launch logic.
       return true;
*** End Patch
