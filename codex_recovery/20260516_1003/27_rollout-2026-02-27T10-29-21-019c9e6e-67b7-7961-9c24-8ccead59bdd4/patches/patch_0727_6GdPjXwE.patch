*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/task_importance_theme.dart
@@
       case 'essential':
       case 'high':
         return TaskImportanceBadgeStyle(
           background: _essentialOrange.withValues(
-            alpha: AppColors.isDark ? 0.13 : 0.09,
+            alpha: AppColors.isDark ? 0.1 : 0.06,
           ),
           border: _essentialOrange.withValues(
-            alpha: AppColors.isDark ? 0.34 : 0.24,
+            alpha: AppColors.isDark ? 0.24 : 0.16,
           ),
           text: _essentialOrange,
           glow: _essentialOrange.withValues(
-            alpha: AppColors.isDark ? 0.14 : 0.08,
+            alpha: AppColors.isDark ? 0.08 : 0.04,
           ),
         );
       case 'average':
       case 'medium':
         return TaskImportanceBadgeStyle(
           background: _averageYellow.withValues(
-            alpha: AppColors.isDark ? 0.13 : 0.09,
+            alpha: AppColors.isDark ? 0.095 : 0.055,
           ),
           border: _averageYellow.withValues(
-            alpha: AppColors.isDark ? 0.3 : 0.22,
+            alpha: AppColors.isDark ? 0.2 : 0.14,
           ),
           text: _averageYellow,
           glow: _averageYellow.withValues(
-            alpha: AppColors.isDark ? 0.12 : 0.07,
+            alpha: AppColors.isDark ? 0.07 : 0.035,
           ),
         );
@@
       case 'essential':
       case 'high':
         return TaskImportanceCardStyle(
           border: badgeStyle.border.withValues(
-            alpha: AppColors.isDark ? 0.58 : 0.42,
+            alpha: AppColors.isDark ? 0.42 : 0.28,
           ),
           start: Color.alphaBlend(
-            badgeStyle.text.withValues(alpha: AppColors.isDark ? 0.09 : 0.035),
+            badgeStyle.text.withValues(alpha: AppColors.isDark ? 0.055 : 0.022),
             AppColors.cardBackgroundAlt,
           ),
           end: AppColors.cardBase,
           badge: badgeStyle,
         );
@@
       case 'average':
       case 'medium':
         return TaskImportanceCardStyle(
           border: badgeStyle.border.withValues(
-            alpha: AppColors.isDark ? 0.52 : 0.38,
+            alpha: AppColors.isDark ? 0.36 : 0.24,
           ),
           start: Color.alphaBlend(
-            badgeStyle.text.withValues(alpha: AppColors.isDark ? 0.075 : 0.03),
+            badgeStyle.text.withValues(alpha: AppColors.isDark ? 0.045 : 0.018),
             AppColors.cardBackgroundAlt,
           ),
           end: AppColors.cardBase,
           badge: badgeStyle,
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
   double _taskCardBorderWidth(String? importanceRaw) {
     switch ((importanceRaw ?? '').toLowerCase()) {
       case 'essential':
       case 'high':
-        return AppColors.isDark ? 0.64 : 0.74;
+        return AppColors.isDark ? 0.42 : 0.5;
       case 'average':
       case 'medium':
-        return AppColors.isDark ? 0.6 : 0.7;
+        return AppColors.isDark ? 0.4 : 0.48;
       default:
         return AppColors.isDark ? 1 : 1.1;
     }
   }
+
+  double _importanceBadgeBorderWidth(String? importanceRaw) {
+    switch ((importanceRaw ?? '').toLowerCase()) {
+      case 'essential':
+      case 'high':
+        return AppColors.isDark ? 0.82 : 0.88;
+      case 'average':
+      case 'medium':
+        return AppColors.isDark ? 0.8 : 0.86;
+      default:
+        return AppColors.isDark ? 1 : 1.05;
+    }
+  }
@@
         color: palette.badgeBg,
         border: Border.all(
           color: palette.badgeBorder,
-          width: AppColors.isDark ? 1 : 1.05,
+          width: _importanceBadgeBorderWidth(task.importanceLevel),
         ),
       ),
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/screen_time_manager_screen.dart
@@
         padding: const EdgeInsets.all(22),
         decoration: _moduleDecoration(
           radius: 22,
           borderColor: AppColors.isDark
-              ? AppColors.activeBorder
+              ? (AppColors.isCarbonSystem
+                    ? AppColors.focusControlAccent.withValues(alpha: 0.44)
+                    : AppColors.activeBorder)
               : AppColors.selectionOutline.withValues(alpha: 0.98),
-          borderWidth: AppColors.isDark ? 1.1 : 1.25,
+          borderWidth: AppColors.isDark ? 1.15 : 1.25,
           overlay: AppColors.ambientTint.withValues(
             alpha: AppColors.isDark ? 0.08 : 0.045,
           ),
@@
                       Text(
                         'Deep work environment',
                         style: AppTypography.callout.copyWith(
                           fontSize: 14,
-                          color: AppColors.rankAccent,
+                          color: AppColors.focusControlAccent,
                           fontWeight: FontWeight.w500,
                         ),
                       ),
@@
                   decoration: BoxDecoration(
                     shape: BoxShape.circle,
-                    color: AppColors.accentSurfaceSoft,
-                    border: Border.all(color: AppColors.activeBorder, width: 1),
+                    color: Color.alphaBlend(
+                      AppColors.focusControlAccent.withValues(
+                        alpha: AppColors.isCarbonSystem ? 0.12 : 0.08,
+                      ),
+                      AppColors.accentSurfaceSoft,
+                    ),
+                    border: Border.all(
+                      color: AppColors.isCarbonSystem
+                          ? AppColors.focusControlAccent.withValues(alpha: 0.52)
+                          : AppColors.activeBorder,
+                      width: 1,
+                    ),
                   ),
                   child: Icon(
                     CupertinoIcons.bolt,
-                    color: AppColors.accentIcon,
+                    color: AppColors.focusControlAccent,
                     size: 28,
                   ),
                 ),
@@
                 child: _statLargeTile(
                   label: 'TODAY',
                   value: '$todayMinutes',
                   unit: 'minutes',
                   accent: AppColors.scoreValue,
                   highlighted: true,
+                  borderColor: AppColors.screenTimeDailyAccentBorder,
                 ),
@@
                   icon: CupertinoIcons.flame,
-                  iconColor: AppColors.primaryOrange,
+                  iconColor: AppColors.streakAccent,
                   label: 'STREAK',
                   value: '$streakValue',
-                  valueColor: AppColors.primaryOrange,
+                  valueColor: AppColors.streakAccent,
                   onTap: () => context.push('/win-streak'),
                 ),
               ),
@@
   Widget _statLargeTile({
     required String label,
     required String value,
     required String unit,
     required Color accent,
     bool highlighted = false,
+    Color? borderColor,
   }) {
     return Container(
@@
       decoration: _moduleDecoration(
         radius: 18,
         borderColor: highlighted
-            ? AppColors.selectionOutline
+            ? (borderColor ?? AppColors.selectionOutline)
             : AppColors.border,
         overlay: highlighted ? AppColors.accentSurfaceSoft : null,
       ),
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/family_time_screen.dart
@@
               const SizedBox(width: 10),
               _actionButton(
                 label: 'Create',
                 onTap: _saving ? null : _createGroup,
+                primaryColor: AppColors.familyCreateAccent,
               ),
@@
 Widget _actionButton({
   required String label,
   required VoidCallback? onTap,
   bool primary = true,
+  Color? primaryColor,
 }) {
+  final resolvedPrimary = primaryColor ?? AppColors.accent;
   return _PressScale(
     onTap: onTap,
     scale: 0.98,
@@
         decoration: BoxDecoration(
           borderRadius: BorderRadius.circular(12),
           color: primary
-            ? AppColors.accent
+            ? resolvedPrimary
             : AppColors.pillBackground,
         border: Border.all(
           color: primary
-              ? AppColors.accent.withValues(alpha: 0.35)
+              ? resolvedPrimary.withValues(alpha: 0.35)
               : AppColors.pillBorder,
         ),
       ),
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
               decoration: _habitModuleCardDecoration(
                 radius: 20,
                 borderColor: AppColors.white.withValues(alpha: 0.06),
               ),
@@
                       '$scorePercent%',
                       style: AppTypography.heroNumber.copyWith(
                         fontSize: 48,
                         fontWeight: FontWeight.w700,
-                        color: AppColors.scoreValue,
+                        color: AppColors.habitsWeekAccent,
                       ),
                     ),
@@
                           return LinearProgressIndicator(
                             value: value,
                             backgroundColor: AppColors.white.withValues(alpha: 0.08),
                             valueColor: AlwaysStoppedAnimation<Color>(
-                              AppColors.scoreValue,
+                              AppColors.habitsWeekAccent,
                             ),
                           );
                         },
@@
           decoration: BoxDecoration(
             shape: BoxShape.circle,
-            color: AppColors.accent,
+            color: AppColors.habitsCompletionAccent,
           ),
@@
           border: Border.all(
-            color: AppColors.primaryOrange.withValues(alpha: 0.6),
+            color: AppColors.habitsTodayRingAccent,
             width: 1.5,
           ),
@@
         color: AppColors.label.withValues(alpha: 0),
         border: Border.all(
-          color: AppColors.white.withValues(alpha: 0.25),
+          color: AppColors.habitsOpenRingAccent,
           width: 1,
         ),
       ),
*** End Patch
