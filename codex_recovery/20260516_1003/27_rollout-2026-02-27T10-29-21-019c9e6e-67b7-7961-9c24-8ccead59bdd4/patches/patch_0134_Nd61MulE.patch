*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/screen_time_manager_screen.dart
@@
                 final todayFromLogs = _todayMinutesFromLogs();
                 final todayMinutes = todayFromLogs > 0
                     ? todayFromLogs
                     : focus.screenTimeToday.round();
                 final avg7d = _averageDailyMinutes(days: 7);
                 final blockedTargets = _blockedTargets(focus);
+                final usageGood = todayMinutes <= 120;
+                final usageModerate = todayMinutes > 120 && todayMinutes <= 240;
+                final todayTone = usageGood
+                    ? const Color(0xFF4B8BFF)
+                    : usageModerate
+                    ? const Color(0xFF4A96D8)
+                    : AppColors.error;
 
                 return SafeArea(
                   child: ListView(
                     padding: const EdgeInsets.all(20),
                     children: [
@@
                       GlassCard(
                         padding: const EdgeInsets.all(18),
                         borderRadius: 16,
-                        child: Row(
+                        border: Border.all(
+                          color: AppColors.primaryOrange.withValues(alpha: 0.14),
+                          width: 0.55,
+                        ),
+                        child: Column(
+                          crossAxisAlignment: CrossAxisAlignment.start,
                           children: [
-                            _metric('TODAY', '${todayMinutes}m'),
-                            _metric('AVG_7D', '${avg7d}m'),
-                            _metric(
-                              'STREAK',
-                              '${_streak?.currentStreak ?? 0}d',
-                            ),
-                            _metric('RANK', 'L${_rank?.rankLevel ?? 1}'),
+                            Text(
+                              'PERFORMANCE_CLUSTER',
+                              style: AppTypography.mono.copyWith(
+                                fontSize: 9,
+                                color: AppColors.tertiaryLabel,
+                                letterSpacing: 1.2,
+                              ),
+                            ),
+                            const SizedBox(height: 10),
+                            Row(
+                              children: [
+                                Expanded(
+                                  child: _clusterTile(
+                                    label: 'TODAY',
+                                    value: '${todayMinutes}m',
+                                    tone: todayTone,
+                                  ),
+                                ),
+                                const SizedBox(width: 8),
+                                Expanded(
+                                  child: _clusterTile(
+                                    label: 'AVG_7D',
+                                    value: '${avg7d}m',
+                                    tone: AppColors.secondaryLabel,
+                                  ),
+                                ),
+                              ],
+                            ),
+                            const SizedBox(height: 8),
+                            Row(
+                              children: [
+                                Expanded(
+                                  child: _clusterTile(
+                                    label: 'RANK',
+                                    value:
+                                        (_rank?.rankName ?? 'Starter')
+                                            .toUpperCase(),
+                                    tone: AppColors.warning,
+                                  ),
+                                ),
+                                const SizedBox(width: 8),
+                                Expanded(
+                                  child: _clusterTile(
+                                    label: 'STREAK',
+                                    value: '${_streak?.currentStreak ?? 0}d',
+                                    tone: AppColors.primaryOrange,
+                                  ),
+                                ),
+                              ],
+                            ),
                           ],
                         ),
                       ),
@@
                       ),
                       const SizedBox(height: 12),
-                      Row(
-                        children: [
-                          Expanded(
-                            child: _miniRouteButton(
-                              label: 'LOGS',
-                              icon: CupertinoIcons.chart_bar_fill,
-                              onTap: () => context.push('/screen-time'),
-                            ),
-                          ),
-                          const SizedBox(width: 8),
-                          Expanded(
-                            child: _miniRouteButton(
-                              label: 'RANK',
-                              icon: CupertinoIcons.star_fill,
-                              onTap: () => context.push('/rank'),
-                            ),
-                          ),
-                          const SizedBox(width: 8),
-                          Expanded(
-                            child: _miniRouteButton(
-                              label: 'LEADERBOARD',
-                              icon: CupertinoIcons.person_3_fill,
-                              onTap: () => context.push('/leaderboard'),
-                            ),
-                          ),
-                        ],
+                      _secondaryActionCard(
+                        title: 'BLOCK_APPS_AND_SITES',
+                        subtitle: '$blockedTargets targets currently protected',
+                        icon: CupertinoIcons.nosign,
+                        onTap: () => _openBlockListEditor(focus),
+                      ),
+                      const SizedBox(height: 8),
+                      _secondaryActionCard(
+                        title: 'LEADERBOARD',
+                        subtitle: 'Global rankings and friends progress',
+                        icon: CupertinoIcons.star_fill,
+                        onTap: () => context.push('/leaderboard'),
+                      ),
+                      const SizedBox(height: 8),
+                      _secondaryActionCard(
+                        title: 'SCREEN_TIME_LOGS',
+                        subtitle: 'View detailed usage records',
+                        icon: CupertinoIcons.chart_bar_fill,
+                        onTap: () => context.push('/screen-time'),
                       ),
                       const SizedBox(height: 16),
                       Text(
@@
     );
   }
 
-  Widget _metric(String label, String value) {
-    return Expanded(
-      child: Column(
-        crossAxisAlignment: CrossAxisAlignment.start,
-        children: [
-          Text(
-            label,
-            style: AppTypography.mono.copyWith(
-              fontSize: 9,
-              color: AppColors.tertiaryLabel,
-            ),
-          ),
-          const SizedBox(height: 4),
-          Text(
-            value,
-            style: AppTypography.mono.copyWith(
-              fontSize: 13,
-              fontWeight: FontWeight.bold,
-            ),
-          ),
-        ],
+  Widget _clusterTile({
+    required String label,
+    required String value,
+    required Color tone,
+  }) {
+    return Container(
+      padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 10),
+      decoration: BoxDecoration(
+        borderRadius: BorderRadius.circular(12),
+        color: AppColors.backgroundLight.withValues(alpha: 0.65),
+        border: Border.all(color: tone.withValues(alpha: 0.28), width: 0.6),
+      ),
+      child: Column(
+        crossAxisAlignment: CrossAxisAlignment.start,
+        children: [
+          Text(
+            label,
+            style: AppTypography.mono.copyWith(
+              fontSize: 8,
+              color: AppColors.tertiaryLabel,
+              letterSpacing: 0.9,
+            ),
+          ),
+          const SizedBox(height: 4),
+          Text(
+            value,
+            maxLines: 1,
+            overflow: TextOverflow.ellipsis,
+            style: AppTypography.mono.copyWith(
+              fontSize: 12,
+              fontWeight: FontWeight.bold,
+              color: tone,
+            ),
+          ),
+        ],
       ),
     );
   }
 
-  Widget _miniRouteButton({
-    required String label,
+  Widget _secondaryActionCard({
+    required String title,
+    required String subtitle,
     required IconData icon,
     required VoidCallback onTap,
   }) {
     return CupertinoButton(
       padding: EdgeInsets.zero,
       onPressed: onTap,
       child: GlassCard(
-        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 10),
+        padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 12),
         borderRadius: 12,
-        child: Center(
-          child: Column(
-            children: [
-              Icon(icon, size: 13, color: AppColors.primaryOrange),
-              const SizedBox(height: 4),
-              Text(
-                label,
-                textAlign: TextAlign.center,
-                style: AppTypography.mono.copyWith(
-                  fontSize: 8,
-                  fontWeight: FontWeight.bold,
-                  color: AppColors.primaryOrange,
-                ),
+        border: Border.all(
+          color: AppColors.primaryOrange.withValues(alpha: 0.14),
+          width: 0.5,
+        ),
+        child: Row(
+          children: [
+            Container(
+              width: 34,
+              height: 34,
+              decoration: BoxDecoration(
+                borderRadius: BorderRadius.circular(10),
+                color: AppColors.backgroundLight.withValues(alpha: 0.72),
+                border: Border.all(
+                  color: AppColors.primaryOrange.withValues(alpha: 0.25),
+                  width: 0.5,
+                ),
+              ),
+              child: Icon(icon, size: 17, color: AppColors.primaryOrange),
+            ),
+            const SizedBox(width: 10),
+            Expanded(
+              child: Column(
+                crossAxisAlignment: CrossAxisAlignment.start,
+                children: [
+                  Text(
+                    title,
+                    style: AppTypography.mono.copyWith(
+                      fontSize: 10,
+                      fontWeight: FontWeight.bold,
+                    ),
+                  ),
+                  const SizedBox(height: 2),
+                  Text(
+                    subtitle,
+                    maxLines: 1,
+                    overflow: TextOverflow.ellipsis,
+                    style: AppTypography.mono.copyWith(
+                      fontSize: 9,
+                      color: AppColors.secondaryLabel,
+                    ),
+                  ),
+                ],
               ),
-            ],
-          ),
+            ),
+            Icon(
+              CupertinoIcons.chevron_right,
+              size: 14,
+              color: AppColors.tertiaryLabel,
+            ),
+          ],
         ),
       ),
     );
   }
 }
*** End Patch
