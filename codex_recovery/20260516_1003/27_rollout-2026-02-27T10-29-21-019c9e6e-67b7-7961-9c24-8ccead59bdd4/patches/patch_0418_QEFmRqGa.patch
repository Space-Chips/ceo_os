*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/screen_time_manager_screen.dart
@@
                         _statsClusterCard(
                           todayMinutes: todayMinutes,
                           avg7dMinutes: avg7d,
                           rankName: rankName,
                           streakValue: _streak?.currentStreak ?? 0,
                         ),
                         const SizedBox(height: 12),
-                        _actionCard(
-                          icon: CupertinoIcons.nosign,
-                          iconColor: const Color(0xFF3B82F6),
-                          title: 'Block Apps & Sites',
-                          subtitle: '$blockedTargets blocked • limits & pauses',
-                          onTap: () => context.push('/screen-time'),
-                        ),
-                        const SizedBox(height: 8),
                         _actionCard(
                           icon: CupertinoIcons.rosette,
                           iconColor: const Color(0xFF3B82F6),
                           title: 'Leaderboard',
                           subtitle: 'Global rankings',
                           onTap: () => context.push('/leaderboard'),
                         ),
                         const SizedBox(height: 8),
                         _actionCard(
                           icon: CupertinoIcons.person_3_fill,
                           iconColor: const Color(0xFF8B5CF6),
                           title: 'Family Time',
                           subtitle: 'Shared no-phone sessions',
                           onTap: () => context.push('/family-time'),
                         ),
+                        const SizedBox(height: 12),
+                        _actionCard(
+                          icon: CupertinoIcons.nosign,
+                          iconColor: const Color(0xFF3B82F6),
+                          title: 'Block Apps & Sites',
+                          subtitle:
+                              '$blockedTargets blocked • limits & planned pauses',
+                          featured: true,
+                          onTap: () => context.push('/screen-time'),
+                        ),
                       ],
                     ),
                   );
                 },
               ),
@@
   Widget _actionCard({
     required IconData icon,
     required Color iconColor,
     required String title,
     required String subtitle,
+    bool featured = false,
     required VoidCallback onTap,
   }) {
-    return CupertinoButton(
+    final card = CupertinoButton(
       padding: EdgeInsets.zero,
       onPressed: onTap,
       child: GlassCard(
-        padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 14),
+        padding: EdgeInsets.symmetric(
+          horizontal: featured ? 16 : 14,
+          vertical: featured ? 16 : 14,
+        ),
         borderRadius: 20,
-        level: GlassCardLevel.standard,
+        level: featured ? GlassCardLevel.elevated : GlassCardLevel.standard,
         showEdgeGlow: true,
         border: Border.all(
-          color: AppColors.glassBorder.withValues(alpha: 0.72),
-          width: 0.7,
+          color: featured
+              ? iconColor.withValues(alpha: 0.72)
+              : AppColors.glassBorder.withValues(alpha: 0.72),
+          width: featured ? 0.95 : 0.7,
+        ),
+        gradientColors: featured
+            ? [
+                AppColors.floatingGlassGradient.first.withValues(alpha: 0.92),
+                iconColor.withValues(alpha: 0.18),
+              ]
+            : null,
+        child: ConstrainedBox(
+          constraints: BoxConstraints(minHeight: featured ? 84 : 0),
+          child: Row(
+            children: [
+              Container(
+                width: featured ? 56 : 48,
+                height: featured ? 56 : 48,
+                decoration: BoxDecoration(
+                  borderRadius: BorderRadius.circular(14),
+                  color: AppColors.backgroundLight.withValues(alpha: 0.7),
+                  border: Border.all(
+                    color: AppColors.glassBorder.withValues(alpha: 0.55),
+                    width: 0.7,
+                  ),
+                ),
+                child: Icon(icon, color: iconColor, size: featured ? 28 : 25),
+              ),
+              const SizedBox(width: 12),
+              Expanded(
+                child: Column(
+                  mainAxisAlignment: MainAxisAlignment.center,
+                  crossAxisAlignment: CrossAxisAlignment.start,
+                  children: [
+                    Text(
+                      title,
+                      style: AppTypography.mono.copyWith(
+                        fontSize: featured ? 18 : 16,
+                        fontWeight: FontWeight.w900,
+                        color: AppColors.label,
+                      ),
+                    ),
+                    const SizedBox(height: 3),
+                    Text(
+                      subtitle,
+                      style: AppTypography.mono.copyWith(
+                        fontSize: featured ? 13 : 12,
+                        color: AppColors.secondaryLabel,
+                      ),
+                    ),
+                  ],
+                ),
+              ),
+              Icon(
+                CupertinoIcons.arrow_right,
+                color: AppColors.tertiaryLabel,
+                size: featured ? 20 : 18,
+              ),
+            ],
+          ),
         ),
-        child: Row(
-          children: [
-            Container(
-              width: 48,
-              height: 48,
-              decoration: BoxDecoration(
-                borderRadius: BorderRadius.circular(14),
-                color: AppColors.backgroundLight.withValues(alpha: 0.7),
-                border: Border.all(
-                  color: AppColors.glassBorder.withValues(alpha: 0.55),
-                  width: 0.7,
-                ),
-              ),
-              child: Icon(icon, color: iconColor, size: 25),
-            ),
-            const SizedBox(width: 12),
-            Expanded(
-              child: Column(
-                crossAxisAlignment: CrossAxisAlignment.start,
-                children: [
-                  Text(
-                    title,
-                    style: AppTypography.mono.copyWith(
-                      fontSize: 16,
-                      fontWeight: FontWeight.w900,
-                      color: AppColors.label,
-                    ),
-                  ),
-                  const SizedBox(height: 3),
-                  Text(
-                    subtitle,
-                    style: AppTypography.mono.copyWith(
-                      fontSize: 12,
-                      color: AppColors.secondaryLabel,
-                    ),
-                  ),
-                ],
-              ),
-            ),
-            Icon(
-              CupertinoIcons.arrow_right,
-              color: AppColors.tertiaryLabel,
-              size: 18,
-            ),
-          ],
-        ),
       ),
     );
+    if (!featured) return card;
+    return _glowSurface(
+      glowColor: iconColor.withValues(alpha: 0.3),
+      borderRadius: 20,
+      child: card,
+    );
   }
*** End Patch
