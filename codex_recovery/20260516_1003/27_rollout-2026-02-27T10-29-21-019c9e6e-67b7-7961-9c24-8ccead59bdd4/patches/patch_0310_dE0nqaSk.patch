*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/calendar/add_event_sheet.dart
@@
   void _adjustDuration(int delta) {
     setState(() {
       _durationMinutes = (_durationMinutes + delta).clamp(15, 480).toInt();
     });
   }
+
+  String _timeLabel() {
+    final time = _selectedTime;
+    if (time == null) return 'ALL DAY';
+    final dt = DateTime(2000, 1, 1, time.hour, time.minute);
+    return DateFormat('HH:mm').format(dt);
+  }
@@
                                     Text(
-                                      (_selectedTime != null
-                                              ? _selectedTime!.format(context)
-                                              : 'ALL DAY')
-                                          .toUpperCase(),
+                                      _timeLabel(),
                                       style: AppTypography.mono.copyWith(
                                         fontSize: 12,
                                         color: _selectedTime != null
*** End Patch
