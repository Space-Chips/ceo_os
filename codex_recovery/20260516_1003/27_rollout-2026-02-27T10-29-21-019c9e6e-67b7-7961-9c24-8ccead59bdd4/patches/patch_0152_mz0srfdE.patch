*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/rank/rank_screen.dart
@@
   Widget _tierRow(_RankTier tier, {required int currentLevel}) {
+    final language = context.watch<LanguageProvider>();
     final isCurrent = tier.level == currentLevel;
     final isUnlocked = tier.level < currentLevel;
     final statusColor = isCurrent
         ? AppColors.primaryOrange
         : isUnlocked
         ? AppColors.success
         : AppColors.tertiaryLabel;
 
     final badge = isCurrent
-        ? context.watch<LanguageProvider>().t('current')
+        ? language.t('current')
         : isUnlocked
-        ? context.watch<LanguageProvider>().t('unlocked')
-        : context.watch<LanguageProvider>().t('locked');
+        ? language.t('unlocked')
+        : language.t('locked');
*** End Patch
