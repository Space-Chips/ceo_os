*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/calendar/calendar_screen.dart
@@
                       boxShadow: AppColors.isDark
                           ? const []
                           : [
                               BoxShadow(
                                 color: AppColors.glassShadowSoft.withValues(
-                                  alpha: 0.045,
+                                  alpha: 0.055,
                                 ),
-                                blurRadius: 7,
-                                offset: const Offset(0, 2),
+                                blurRadius: 8,
+                                offset: const Offset(0, 3),
                                 spreadRadius: -5,
                               ),
                             ],
                     ),
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
       case 'essential':
       case 'high':
+        final isLightTheme = !AppColors.isDark;
         return _TaskPalette(
-          border: AppColors.warning.withValues(alpha: 0.4),
-          start: AppColors.warning.withValues(alpha: 0.12),
-          end: AppColors.warning.withValues(alpha: 0.05),
-          badgeBg: AppColors.warning.withValues(alpha: 0.12),
-          badgeBorder: AppColors.warning.withValues(alpha: 0.35),
+          border: isLightTheme
+              ? AppColors.warning.withValues(alpha: 0.26)
+              : AppColors.warning.withValues(alpha: 0.4),
+          start: isLightTheme
+              ? Color.alphaBlend(
+                  AppColors.warning.withValues(alpha: 0.035),
+                  AppColors.cardBackgroundAlt,
+                )
+              : AppColors.warning.withValues(alpha: 0.12),
+          end: isLightTheme
+              ? AppColors.cardBase
+              : AppColors.warning.withValues(alpha: 0.05),
+          badgeBg: isLightTheme
+              ? AppColors.warning.withValues(alpha: 0.08)
+              : AppColors.warning.withValues(alpha: 0.12),
+          badgeBorder: isLightTheme
+              ? AppColors.warning.withValues(alpha: 0.24)
+              : AppColors.warning.withValues(alpha: 0.35),
           badgeText: AppColors.warning,
         );
@@
             decoration: BoxDecoration(
               borderRadius: BorderRadius.circular(18),
               gradient: LinearGradient(
                 begin: Alignment.topCenter,
                 end: Alignment.bottomCenter,
                 colors: [palette.start, palette.end],
               ),
-              border: Border.all(color: palette.border, width: 1),
+              border: Border.all(
+                color: palette.border,
+                width: AppColors.isDark ? 1 : 1.1,
+              ),
               boxShadow: [
                 BoxShadow(
                   color: AppColors.glassShadow.withValues(
                     alpha: AppColors.isDark ? 0.18 : 0.1,
@@
         decoration: BoxDecoration(
           borderRadius: BorderRadius.circular(18),
           gradient: LinearGradient(
             begin: Alignment.topCenter,
             end: Alignment.bottomCenter,
             colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
           ),
           border: Border.all(
             color: AppColors.border,
-            width: 1,
+            width: AppColors.isDark ? 1 : 1.08,
           ),
@@
       decoration: BoxDecoration(
         borderRadius: BorderRadius.circular(8),
         color: palette.badgeBg,
-        border: Border.all(color: palette.badgeBorder, width: 1),
+        border: Border.all(
+          color: palette.badgeBorder,
+          width: AppColors.isDark ? 1 : 1.05,
+        ),
       ),
*** End Patch
