*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/leaderboard/leaderboard_screen.dart
@@
 class _LeaderboardScreenState extends State<LeaderboardScreen> {
   final UserRepository _repository = UserRepository();
   List<_RankedEntry> _entries = [];
   List<_FriendRankedEntry> _friendEntries = [];
+  Map<String, String> _identityLabels = {};
   String? _myUserId;
   bool _loading = true;
   bool _friendsLoading = false;
   _LeaderboardTab _activeTab = _LeaderboardTab.global;
@@
   Future<void> _load() async {
     setState(() => _loading = true);
     await _repository.refreshLeaderboardForMe();
 
     final entries = await _repository.getLeaderboard();
@@
     final ranked =
         entries
             .where((e) => e.optedIn)
             .map((e) => _RankedEntry(entry: e, score: _computeScore(e)))
             .toList()
           ..sort((a, b) => b.score.compareTo(a.score));
+    final identityLabels = await _repository.getPublicIdentityLabelsByUserIds(
+      ranked.map((r) => r.entry.createdBy),
+    );
@@
       _entries = ranked;
       _friendEntries = friendRanked;
+      _identityLabels = identityLabels;
       _myUserId = me;
       _loading = false;
       _friendsLoading = false;
     });
   }
@@
   Future<void> _loadFriendsOnly() async {
     final friends = await _repository.getFriendConnections();
@@
       _friendsLoading = false;
     });
   }
+
+  Future<void> _syncAndLoadFriends() async {
+    await _repository.syncFriendConnections();
+    await _loadFriendsOnly();
+  }
@@
   String _friendLabel(FriendConnection friend) {
@@
     return 'FRIEND';
   }
+
+  String _identityLabel(LeaderboardEntry entry, {required bool mine}) {
+    if (mine) return 'YOU';
+    final resolved = _identityLabels[entry.createdBy];
+    if (resolved != null && resolved.trim().isNotEmpty) {
+      return resolved.trim().toUpperCase();
+    }
+    return entry.createdBy.substring(0, 8).toUpperCase();
+  }
@@
                 Text(
-                  mine ? 'YOU' : entry.createdBy.substring(0, 8),
+                  _identityLabel(entry, mine: mine),
                   style: AppTypography.mono.copyWith(
                     fontSize: 10,
                     color: AppColors.tertiaryLabel,
@@
               onPressed: _friendsLoading
                   ? null
                   : () {
                       setState(() => _friendsLoading = true);
-                      _loadFriendsOnly();
+                      _syncAndLoadFriends();
                     },
*** End Patch
