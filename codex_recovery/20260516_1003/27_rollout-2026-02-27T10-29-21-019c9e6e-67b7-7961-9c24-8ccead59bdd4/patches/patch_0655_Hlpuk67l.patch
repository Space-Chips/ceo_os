*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/components/ambient_backdrop.dart
@@
         Positioned.fill(
           child: DecoratedBox(
             decoration: BoxDecoration(
               gradient: LinearGradient(
                 begin: const Alignment(-1, -0.6),
                 end: const Alignment(1, 1),
                 colors: [
                   AppColors.ambientTint.withValues(
-                    alpha: AppColors.isDark ? 0.09 : 0.014,
+                    alpha: AppColors.isDark ? 0.09 : 0.007,
                   ),
                   AppColors.overlayScrim.withValues(alpha: 0),
                   AppColors.sectionBackground.withValues(
-                    alpha: AppColors.isDark ? 0.22 : 0.05,
+                    alpha: AppColors.isDark ? 0.22 : 0.02,
                   ),
                 ],
                 stops: const [0, 0.48, 1],
               ),
             ),
           ),
         ),
         Positioned(
-          top: -170,
-          right: -140,
+          top: AppColors.isDark ? -170 : -150,
+          right: AppColors.isDark ? -140 : null,
+          left: AppColors.isDark ? null : -40,
           child: _SoftAura(
-            size: 320,
+            size: AppColors.isDark ? 320 : 380,
             color: AppColors.ambientTint.withValues(
-              alpha: AppColors.isDark ? 0.1 : 0.02,
+              alpha: AppColors.isDark ? 0.1 : 0.012,
             ),
           ),
         ),
         Positioned(
-          bottom: -190,
-          left: -120,
+          bottom: AppColors.isDark ? -190 : -170,
+          left: AppColors.isDark ? -120 : -50,
+          right: AppColors.isDark ? null : -50,
           child: _SoftAura(
-            size: 280,
+            size: AppColors.isDark ? 280 : 360,
             color: AppColors.cardBackgroundAlt.withValues(
-              alpha: AppColors.isDark ? 0.08 : 0.02,
+              alpha: AppColors.isDark ? 0.08 : 0.012,
             ),
           ),
         ),
@@
                   colors: [
                     AppColors.overlayScrim.withValues(
                       alpha: AppColors.isDark ? 0.16 : 0.035,
                     ),
                     AppColors.overlayScrim.withValues(alpha: 0),
                     AppColors.overlayScrim.withValues(
-                      alpha: AppColors.isDark ? 0.36 : 0.06,
+                      alpha: AppColors.isDark ? 0.36 : 0.035,
                     ),
                   ],
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/screen_time_manager_screen.dart
@@
         decoration: _moduleDecoration(
           radius: 22,
           borderColor: AppColors.isDark
               ? AppColors.activeBorder
-              : AppColors.borderStrong.withValues(alpha: 0.92),
-          overlay: AppColors.ambientTint.withValues(alpha: 0.08),
+              : AppColors.selectionOutline.withValues(alpha: 0.98),
+          borderWidth: AppColors.isDark ? 1.1 : 1.25,
+          overlay: AppColors.ambientTint.withValues(alpha: AppColors.isDark ? 0.08 : 0.045),
         ),
         child: Stack(
           children: [
             Positioned(
               top: 0,
@@
                   decoration: BoxDecoration(
                     borderRadius: BorderRadius.circular(999),
                     color: AppColors.glassHighlight.withValues(
-                      alpha: AppColors.isDark ? 0.2 : 0.24,
+                      alpha: AppColors.isDark ? 0.22 : 0.28,
                     ),
                   ),
                 ),
               ),
             ),
@@
   BoxDecoration _moduleDecoration({
     double radius = 18,
     Color? borderColor,
+    double borderWidth = 1,
     Color? overlay,
   }) {
     return BoxDecoration(
       borderRadius: BorderRadius.circular(radius),
       gradient: LinearGradient(
@@
       ),
       border: Border.all(
         color: borderColor ?? AppColors.border,
-        width: 1,
+        width: borderWidth,
       ),
       boxShadow: [
         BoxShadow(
-          color: AppColors.glassShadow.withValues(alpha: 0.18),
-          blurRadius: 18,
-          offset: const Offset(0, 8),
+          color: AppColors.glassShadow.withValues(
+            alpha: AppColors.isDark ? 0.18 : 0.1,
+          ),
+          blurRadius: AppColors.isDark ? 18 : 14,
+          offset: Offset(0, AppColors.isDark ? 8 : 6),
         ),
       ],
       color: overlay,
       backgroundBlendMode: overlay == null ? null : BlendMode.srcOver,
     );
   }
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
           border: Border.all(color: accent, width: 1),
           boxShadow: [
             BoxShadow(
-              color: AppColors.black.withValues(alpha: 0.5),
-              blurRadius: 30,
-              offset: const Offset(0, 10),
+              color: AppColors.glassShadow.withValues(
+                alpha: AppColors.isDark ? 0.18 : 0.1,
+              ),
+              blurRadius: AppColors.isDark ? 18 : 14,
+              offset: Offset(0, AppColors.isDark ? 8 : 6),
             ),
           ],
         ),
@@
               boxShadow: [
                 BoxShadow(
-                  color: AppColors.black.withValues(alpha: 0.5),
-                  blurRadius: 20,
-                  offset: const Offset(0, 8),
+                  color: AppColors.glassShadow.withValues(
+                    alpha: AppColors.isDark ? 0.16 : 0.09,
+                  ),
+                  blurRadius: AppColors.isDark ? 14 : 12,
+                  offset: Offset(0, AppColors.isDark ? 7 : 6),
                 ),
               ],
             ),
@@
             boxShadow: [
               BoxShadow(
-                color: AppColors.black.withValues(alpha: 0.5),
-                blurRadius: 20,
-                offset: const Offset(0, 8),
+                color: AppColors.glassShadow.withValues(
+                  alpha: AppColors.isDark ? 0.16 : 0.09,
+                ),
+                blurRadius: AppColors.isDark ? 14 : 12,
+                offset: Offset(0, AppColors.isDark ? 7 : 6),
               ),
             ],
           ),
@@
           boxShadow: [
             BoxShadow(
-              color: AppColors.black.withValues(alpha: 0.45),
-              blurRadius: 18,
-              offset: const Offset(0, 8),
+              color: AppColors.glassShadow.withValues(
+                alpha: AppColors.isDark ? 0.18 : 0.1,
+              ),
+              blurRadius: AppColors.isDark ? 16 : 12,
+              offset: Offset(0, AppColors.isDark ? 8 : 6),
             ),
           ],
         ),
*** End Patch
