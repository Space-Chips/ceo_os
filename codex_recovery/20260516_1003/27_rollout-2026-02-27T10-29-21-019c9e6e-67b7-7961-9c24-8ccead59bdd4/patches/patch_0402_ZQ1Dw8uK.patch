*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
     return _InteractiveLift(
       onTap: onOpenDashboard,
       borderRadius: 36,
-      hoverScale: 1.016,
-      pressedScale: 1.026,
-      glowColor: AppColors.accentSecondary.withValues(alpha: 0.76),
+      hoverScale: 1.03,
+      pressedScale: 1.045,
+      glowColor: AppColors.accentSecondary.withValues(alpha: 0.9),
       child: GlassCard(
@@
     return _InteractiveLift(
       onTap: data.onTap,
       borderRadius: 28,
-      hoverScale: 1.02,
-      pressedScale: 1.03,
-      glowColor: glowColor.withValues(alpha: 0.92),
+      hoverScale: 1.032,
+      pressedScale: 1.047,
+      glowColor: glowColor.withValues(alpha: 1.0),
       child: Stack(
@@
 class _InteractiveLift extends StatefulWidget {
@@
   const _InteractiveLift({
@@
     required this.borderRadius,
     required this.glowColor,
-    this.hoverScale = 1.014,
-    this.pressedScale = 1.024,
+    this.hoverScale = 1.022,
+    this.pressedScale = 1.036,
   });
@@
         onTapDown: _interactive
             ? (_) => setState(() {
                 _isPressed = true;
-                _isHovered = true;
               })
             : null,
@@
                 BoxShadow(
                   color: widget.glowColor.withValues(
-                    alpha: _isPressed ? 0.4 : (_isHovered ? 0.62 : 0.26),
+                    alpha: _isPressed ? 0.82 : (_isHovered ? 0.72 : 0.3),
                   ),
-                  blurRadius: _isPressed ? 28 : (_isHovered ? 44 : 20),
-                  offset: Offset(0, _isPressed ? 8 : (_isHovered ? 14 : 8)),
-                  spreadRadius: _isPressed ? -6 : (_isHovered ? 0 : -10),
+                  blurRadius: _isPressed ? 64 : (_isHovered ? 54 : 22),
+                  offset: Offset(0, _isPressed ? 12 : (_isHovered ? 14 : 8)),
+                  spreadRadius: _isPressed ? 3 : (_isHovered ? 1 : -9),
                 ),
                 BoxShadow(
                   color: AppColors.glassShadow.withValues(
-                    alpha: _isPressed ? 0.2 : (_isHovered ? 0.34 : 0.18),
+                    alpha: _isPressed ? 0.28 : (_isHovered ? 0.38 : 0.2),
                   ),
-                  blurRadius: _isPressed ? 10 : (_isHovered ? 18 : 12),
-                  offset: Offset(0, _isPressed ? 4 : (_isHovered ? 10 : 6)),
+                  blurRadius: _isPressed ? 14 : (_isHovered ? 20 : 12),
+                  offset: Offset(0, _isPressed ? 6 : (_isHovered ? 10 : 6)),
                   spreadRadius: -8,
                 ),
               ],
*** End Patch
