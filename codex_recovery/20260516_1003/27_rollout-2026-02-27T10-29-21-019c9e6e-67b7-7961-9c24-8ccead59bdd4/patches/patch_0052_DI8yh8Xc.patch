*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
   _DueInfo _dueInfo(DateTime? deadline) {
@@
     return _DueInfo(
       label: DateFormat('EEE d MMM').format(deadline),
       color: AppColors.secondaryLabel,
       isOverdue: false,
     );
   }
+
+  void _goToNextTab() {
+    if (_activeTab == _TaskTab.list) {
+      setState(() => _activeTab = _TaskTab.matrix);
+      return;
+    }
+    if (_activeTab == _TaskTab.matrix) {
+      setState(() => _activeTab = _TaskTab.history);
+    }
+  }
+
+  void _goToPreviousTab() {
+    if (_activeTab == _TaskTab.history) {
+      setState(() => _activeTab = _TaskTab.matrix);
+      return;
+    }
+    if (_activeTab == _TaskTab.matrix) {
+      setState(() => _activeTab = _TaskTab.list);
+    }
+  }
+
+  void _handleTabSwipe(DragEndDetails details) {
+    final velocity = details.primaryVelocity ?? 0;
+    if (velocity < -300) {
+      _goToNextTab();
+    } else if (velocity > 300) {
+      _goToPreviousTab();
+    }
+  }
+
+  String _swipeHint() {
+    switch (_activeTab) {
+      case _TaskTab.list:
+        return 'Swipe left for matrix';
+      case _TaskTab.matrix:
+        return 'Swipe left for history • right for list';
+      case _TaskTab.history:
+        return 'Swipe right for matrix';
+    }
+  }
@@
-          return SafeArea(
-            child: ListView(
-              padding: const EdgeInsets.fromLTRB(20, 14, 20, 120),
-              children: [
+          return SafeArea(
+            child: GestureDetector(
+              onHorizontalDragEnd: _handleTabSwipe,
+              child: ListView(
+                padding: const EdgeInsets.fromLTRB(20, 14, 20, 120),
+                children: [
@@
-                const SizedBox(height: 14),
+                const SizedBox(height: 12),
+                Text(
+                  _swipeHint().toUpperCase(),
+                  textAlign: TextAlign.center,
+                  style: AppTypography.mono.copyWith(
+                    fontSize: 9,
+                    color: AppColors.tertiaryLabel,
+                  ),
+                ),
+                const SizedBox(height: 12),
                 if (_activeTab == _TaskTab.list) ...[
@@
-              ],
+                ],
+              ),
             ),
           );
         },
       ),
*** End Patch
