*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
         final scorePercent = totalExpected > 0
             ? ((totalCompleted / totalExpected) * 100).round()
             : 0;
         final isOnTrack = scorePercent >= 90;
         final isAtRisk = scorePercent >= 70 && !isOnTrack;
-        final completedToday = sortedHabits
-            .where((habit) => prov.isHabitCompletedToday(habit.id))
-            .length;
-        final weeklyMomentum = isOnTrack
-            ? 'ON_TRACK'
-            : isAtRisk
-            ? 'RECOVERABLE'
-            : 'CRITICAL';
-        final weeklyTone = isOnTrack
-            ? AppColors.success
-            : isAtRisk
-            ? AppColors.primaryOrange
-            : AppColors.error;
*** End Patch
