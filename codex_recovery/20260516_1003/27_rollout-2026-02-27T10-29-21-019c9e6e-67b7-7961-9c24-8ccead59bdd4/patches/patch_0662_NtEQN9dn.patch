*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/calendar/calendar_screen.dart
@@
           child: Container(
             padding: const EdgeInsets.all(14),
             decoration: BoxDecoration(
               gradient: LinearGradient(
                 begin: Alignment.topCenter,
                 end: Alignment.bottomCenter,
                 colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
               ),
               borderRadius: BorderRadius.circular(18),
               border: Border.all(
-                color: AppColors.border,
+                color: AppColors.isDark
+                    ? AppColors.border
+                    : AppColors.borderStrong.withValues(alpha: 0.68),
                 width: 1,
               ),
               boxShadow: [
                 BoxShadow(
-                  color: AppColors.black.withValues(alpha: 0.5),
-                  blurRadius: 24,
-                  offset: const Offset(0, 10),
+                  color: AppColors.glassShadow.withValues(
+                    alpha: AppColors.isDark ? 0.18 : 0.065,
+                  ),
+                  blurRadius: AppColors.isDark ? 18 : 11,
+                  offset: Offset(0, AppColors.isDark ? 8 : 5),
+                  spreadRadius: AppColors.isDark ? -10 : -8,
                 ),
               ],
             ),
@@
                   child: Container(
                     padding: const EdgeInsets.fromLTRB(8, 8, 8, 8),
                     decoration: BoxDecoration(
                       color: isToday
                           ? AppColors.accent.withValues(alpha: 0.08)
-                          : AppColors.white.withValues(alpha: 0.02),
+                          : (AppColors.isDark
+                              ? AppColors.white.withValues(alpha: 0.02)
+                              : AppColors.cardBackgroundAlt.withValues(alpha: 0.9)),
                       borderRadius: BorderRadius.circular(12),
                       border: Border.all(
                         color: isToday
                             ? AppColors.activeBorder.withValues(alpha: 0.8)
-                            : AppColors.white.withValues(alpha: 0.06),
+                            : (AppColors.isDark
+                                ? AppColors.white.withValues(alpha: 0.06)
+                                : AppColors.borderStrong.withValues(alpha: 0.56)),
                         width: 1,
                       ),
+                      boxShadow: AppColors.isDark
+                          ? const []
+                          : [
+                              BoxShadow(
+                                color: AppColors.glassShadowSoft.withValues(
+                                  alpha: 0.06,
+                                ),
+                                blurRadius: 8,
+                                offset: const Offset(0, 3),
+                                spreadRadius: -6,
+                              ),
+                            ],
                     ),
                     child: Column(
*** End Patch
