*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/rank/rank_screen.dart
@@
 class _RankScreenState extends State<RankScreen> {
   final UserRepository _userRepository = UserRepository();
   final FeatureRepository _featureRepository = FeatureRepository();
 
   UserRank? _rank;
   WinStreak? _streak;
   double _avgDailyScreenMinutes = 0;
+  int? _globalPosition;
+  int _globalTotal = 0;
+  int? _globalPercentile;
   bool _loading = true;
@@
   Future<void> _load() async {
     setState(() => _loading = true);
     try {
       await _userRepository.refreshLeaderboardForMe();
       final rank = await _userRepository.getUserRank();
       final streak = await _userRepository.getWinStreak();
       final logs = await _featureRepository.getScreenTimeLogs(days: 7);
       final avg = _computeDailyAverageMinutes(logs);
+      final entries = await _userRepository.getLeaderboard();
+      final myUserId = await _userRepository
+          .getProfile()
+          .then((value) => value?.id)
+          .catchError((_) => null);
+      final ranked =
+          entries.where((e) => e.optedIn).toList()
+            ..sort((a, b) {
+              final byLevel = (b.rankLevel ?? 0).compareTo(a.rankLevel ?? 0);
+              if (byLevel != 0) return byLevel;
+              final byStreak = (b.winStreak ?? 0).compareTo(a.winStreak ?? 0);
+              if (byStreak != 0) return byStreak;
+              return (b.percentile ?? 0).compareTo(a.percentile ?? 0);
+            });
+
+      int? globalPosition;
+      int? percentile;
+      if (myUserId != null) {
+        final idx = ranked.indexWhere((e) => e.createdBy == myUserId);
+        if (idx >= 0) {
+          globalPosition = idx + 1;
+          percentile = ranked[idx].percentile;
+        }
+      }
 
       if (!mounted) return;
       setState(() {
         _rank = rank;
         _streak = streak;
         _avgDailyScreenMinutes = avg;
+        _globalPosition = globalPosition;
+        _globalPercentile = percentile;
+        _globalTotal = ranked.length;
         _loading = false;
       });
     } catch (_) {
       if (!mounted) return;
       setState(() => _loading = false);
@@
   @override
   Widget build(BuildContext context) {
     final level = (_rank?.rankLevel ?? 1).clamp(1, 99);
     final nextTier = _tiers.where((tier) => tier.level > level).firstOrNull;
+    final currentTier =
+        _tiers.where((tier) => tier.level == level).firstOrNull ??
+        _tiers.first;
+    final topPercent = (100 - (_globalPercentile ?? 50)).clamp(0, 100);
+    final currentTone = _tierTone(level);
 
     return CupertinoPageScaffold(
@@
                   _glowSurface(
                     glowColor: AppColors.primaryOrange.withValues(alpha: 0.16),
                     borderRadius: 18,
                     child: GlassCard(
                       padding: const EdgeInsets.all(20),
                       borderRadius: 18,
+                      gradientColors: [
+                        currentTone.withValues(alpha: 0.2),
+                        AppColors.background.withValues(alpha: 0.88),
+                      ],
                       border: Border.all(
                         color: AppColors.primaryOrange.withValues(alpha: 0.24),
                         width: 0.7,
                       ),
                       child: Column(
                         crossAxisAlignment: CrossAxisAlignment.start,
                         children: [
-                          Text(
-                            (_rank?.rankName ?? 'Bronze').toUpperCase(),
-                            style: AppTypography.mono.copyWith(
-                              fontSize: 20,
-                              fontWeight: FontWeight.w900,
+                          Row(
+                            children: [
+                              Container(
+                                width: 40,
+                                height: 40,
+                                decoration: BoxDecoration(
+                                  borderRadius: BorderRadius.circular(12),
+                                  color: currentTone.withValues(alpha: 0.18),
+                                  border: Border.all(
+                                    color: currentTone.withValues(alpha: 0.32),
+                                    width: 0.6,
+                                  ),
+                                ),
+                                child: Center(
+                                  child: Text(
+                                    currentTier.icon,
+                                    style: AppTypography.mono.copyWith(
+                                      fontSize: 18,
+                                    ),
+                                  ),
+                                ),
+                              ),
+                              const SizedBox(width: 10),
+                              Expanded(
+                                child: Column(
+                                  crossAxisAlignment: CrossAxisAlignment.start,
+                                  children: [
+                                    Text(
+                                      (_rank?.rankName ?? 'Panda').toUpperCase(),
+                                      style: AppTypography.mono.copyWith(
+                                        fontSize: 20,
+                                        fontWeight: FontWeight.w900,
+                                      ),
+                                    ),
+                                    const SizedBox(height: 2),
+                                    Text(
+                                      'LEVEL $level',
+                                      style: AppTypography.mono.copyWith(
+                                        fontSize: 10,
+                                        color: AppColors.tertiaryLabel,
+                                        letterSpacing: 1.2,
+                                      ),
+                                    ),
+                                  ],
+                                ),
+                              ),
+                              if (_globalPosition != null)
+                                Container(
+                                  padding: const EdgeInsets.symmetric(
+                                    horizontal: 8,
+                                    vertical: 6,
+                                  ),
+                                  decoration: BoxDecoration(
+                                    borderRadius: BorderRadius.circular(10),
+                                    color: AppColors.primaryOrange.withValues(
+                                      alpha: 0.14,
+                                    ),
+                                  ),
+                                  child: Text(
+                                    '#$_globalPosition',
+                                    style: AppTypography.mono.copyWith(
+                                      fontSize: 11,
+                                      color: AppColors.primaryOrange,
+                                      fontWeight: FontWeight.bold,
+                                    ),
+                                  ),
+                                ),
+                            ],
+                          ),
+                          const SizedBox(height: 12),
+                          Row(
+                            children: [
+                              _metric('POINTS', '${_rank?.totalRankPoints ?? 0}'),
+                              _metric(
+                                'AT_RANK',
+                                '${_rank?.daysAtCurrentRank ?? 0}d',
+                              ),
+                              _metric(
+                                'PREVIOUS',
+                                (_rank?.previousRankName ?? '-').toUpperCase(),
+                              ),
+                            ],
+                          ),
+                        ],
+                      ),
+                    ),
+                  ),
+                  if (_globalPosition != null) ...[
+                    const SizedBox(height: 12),
+                    GlassCard(
+                      padding: const EdgeInsets.all(14),
+                      borderRadius: 14,
+                      border: Border.all(
+                        color: AppColors.primaryOrange.withValues(alpha: 0.2),
+                        width: 0.55,
+                      ),
+                      child: Row(
+                        children: [
+                          Icon(
+                            CupertinoIcons.globe,
+                            size: 16,
+                            color: AppColors.primaryOrange,
+                          ),
+                          const SizedBox(width: 8),
+                          Expanded(
+                            child: Text(
+                              'Global position #$_globalPosition of $_globalTotal • Top $topPercent%',
+                              style: AppTypography.mono.copyWith(
+                                fontSize: 10,
+                                color: AppColors.secondaryLabel,
+                              ),
                             ),
                           ),
-                          const SizedBox(height: 6),
                           Text(
-                            'LEVEL $level',
+                            'LIVE',
                             style: AppTypography.mono.copyWith(
-                              fontSize: 10,
-                              color: AppColors.tertiaryLabel,
-                              letterSpacing: 1.2,
+                              fontSize: 9,
+                              color: AppColors.primaryOrange,
+                              fontWeight: FontWeight.bold,
                             ),
                           ),
-                          const SizedBox(height: 12),
-                          Row(
-                            children: [
-                              _metric(
-                                'POINTS',
-                                '${_rank?.totalRankPoints ?? 0}',
-                              ),
-                              _metric(
-                                'AT_RANK',
-                                '${_rank?.daysAtCurrentRank ?? 0}d',
-                              ),
-                              _metric(
-                                'PREVIOUS',
-                                (_rank?.previousRankName ?? '-').toUpperCase(),
-                              ),
-                            ],
-                          ),
                         ],
                       ),
                     ),
-                  ),
+                  ],
                   const SizedBox(height: 14),
                   GlassCard(
@@
       border: Border.all(
         color: statusColor.withValues(alpha: isCurrent ? 0.3 : 0.18),
         width: isCurrent ? 0.9 : 0.5,
       ),
@@
           Container(
-            width: 28,
-            height: 28,
+            width: 34,
+            height: 34,
             decoration: BoxDecoration(
-              color: statusColor.withValues(alpha: 0.16),
-              borderRadius: BorderRadius.circular(8),
+              gradient: LinearGradient(
+                colors: [
+                  _tierTone(tier.level).withValues(alpha: 0.3),
+                  AppColors.background.withValues(alpha: 0.75),
+                ],
+              ),
+              borderRadius: BorderRadius.circular(10),
+              border: Border.all(
+                color: _tierTone(tier.level).withValues(alpha: 0.4),
+                width: 0.55,
+              ),
             ),
             child: Center(
               child: Text(
                 tier.icon,
                 style: AppTypography.mono.copyWith(
-                  fontSize: 13,
+                  fontSize: 16,
                   fontWeight: FontWeight.bold,
-                  color: statusColor,
                 ),
               ),
             ),
@@
   Widget _metric(String label, String value) {
@@
   }
+
+  Color _tierTone(int level) {
+    if (level >= 9) return AppColors.warning;
+    if (level >= 8) return AppColors.accentPrimary;
+    if (level >= 6) return AppColors.primaryOrange;
+    if (level >= 4) return AppColors.secondaryLabel;
+    return AppColors.tertiaryLabel;
+  }
 }
*** End Patch
