*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
   Widget _titleBlock() {
+    final isMatrix = _activeTab == _TaskTab.matrix;
+    final titleSize = isMatrix ? 44.0 : 52.0;
     return Column(
       crossAxisAlignment: CrossAxisAlignment.start,
       children: [
         SizedBox(
           width: double.infinity,
           child: Text(
             _titleByTab(),
             style: AppTypography.mono.copyWith(
-              fontSize: 52,
+              fontSize: titleSize,
               color: AppColors.label,
               fontWeight: FontWeight.w900,
               height: 0.95,
             ),
           ),
         ),
-        const SizedBox(height: 6),
+        const SizedBox(height: 4),
         Text(
           _subtitleByTab(),
           style: AppTypography.mono.copyWith(
-            fontSize: 12,
+            fontSize: isMatrix ? 11 : 12,
             color: AppColors.tertiaryLabel,
             letterSpacing: 2.1,
             fontWeight: FontWeight.w800,
@@
   Widget _matrixQuadrant({
@@
   }) {
     return Container(
-      constraints: const BoxConstraints(minHeight: 250),
+      constraints: const BoxConstraints(minHeight: 268),
*** End Patch
