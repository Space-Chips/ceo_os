*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/add_habit_sheet.dart
@@
-                        Text(
-                          'Goal',
-                          style: AppTypography.overline.copyWith(
-                            fontSize: 12,
-                            letterSpacing: 2,
-                            color: AppColors.secondaryLabel.withValues(
-                              alpha: 0.6,
-                            ),
-                            fontWeight: FontWeight.w600,
-                          ),
-                        ),
-                        const SizedBox(height: 10),
                         if (_goals.isEmpty)
@@
-                              Wrap(
-                                spacing: 8,
-                                runSpacing: 8,
-                                children: List.generate(7, (index) {
+                              Row(
+                                mainAxisAlignment:
+                                    MainAxisAlignment.spaceBetween,
+                                children: List.generate(7, (index) {
                                   final day = index + 1;
                                   final selected = _selectedWeekdays.contains(
                                     day,
                                   );
                                   final disabled = _isDaily;
@@
                                       opacity: disabled ? 0.45 : 1,
                                       child: Container(
-                                        width: 36,
-                                        height: 36,
+                                        width: 34,
+                                        height: 34,
                                         alignment: Alignment.center,
                                         decoration: BoxDecoration(
                                           shape: BoxShape.circle,
@@
                                         ),
                                       ),
                                     ),
                                   );
-                                }),
-                              ),
+                                }),
+                              ),
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
                                       color:
                                           prov.isHabitCompletedToday(habit.id)
-                                          ? AppColors.accent
+                                          ? AppColors.habitsCompletionAccent
                                           : Colors.transparent,
                                       border:
                                           prov.isHabitCompletedToday(habit.id)
                                           ? null
                                           : Border.all(
-                                              color: AppColors.white.withValues(
-                                                alpha: 0.25,
-                                              ),
+                                              color:
+                                                  AppColors.habitsOpenRingAccent,
                                               width: 1,
                                             ),
@@
                       Text(
                         '${_weeklyScore?.successPercentage ?? 0}%',
                         style: AppTypography.title2.copyWith(
                           fontSize: 24,
                           fontWeight: FontWeight.w700,
-                          color: AppColors.scoreValue,
+                          color: AppColors.habitsWeekAccent,
                         ),
                       ),
@@
   @override
   Widget build(BuildContext context) {
-    final decoration =
-        widget.decoration ??
-        BoxDecoration(
-          gradient: LinearGradient(
-            begin: Alignment.topCenter,
-            end: Alignment.bottomCenter,
-            colors: AppColors.buttonGradient,
-          ),
-          borderRadius: BorderRadius.circular(widget.borderRadius),
-          border: Border.all(
-            color: AppColors.white.withValues(alpha: 0.14),
-            width: 1,
-          ),
-        );
+    final carbonDecoration = BoxDecoration(
+      gradient: LinearGradient(
+        begin: Alignment.topLeft,
+        end: Alignment.bottomRight,
+        colors: [
+          Color.alphaBlend(
+            AppColors.habitsWeekAccent.withValues(alpha: 0.78),
+            AppColors.cardBackgroundStrong,
+          ),
+          Color.alphaBlend(
+            AppColors.familyCreateAccent.withValues(alpha: 0.56),
+            AppColors.cardBackgroundStrong,
+          ),
+        ],
+      ),
+      borderRadius: BorderRadius.circular(widget.borderRadius),
+      border: Border.all(
+        color: Color.alphaBlend(
+          AppColors.habitsWeekAccent.withValues(alpha: 0.46),
+          AppColors.borderStrong,
+        ),
+        width: 1,
+      ),
+    );
+    final decoration =
+        widget.decoration ??
+        (AppColors.isCarbonSystem
+            ? carbonDecoration
+            : BoxDecoration(
+                gradient: LinearGradient(
+                  begin: Alignment.topCenter,
+                  end: Alignment.bottomCenter,
+                  colors: AppColors.buttonGradient,
+                ),
+                borderRadius: BorderRadius.circular(widget.borderRadius),
+                border: Border.all(
+                  color: AppColors.white.withValues(alpha: 0.14),
+                  width: 1,
+                ),
+              ));
*** Update File: /Users/timo/ceo_os/lib/features/leaderboard/leaderboard_screen.dart
@@
   @override
   Widget build(BuildContext context) {
+    final inviteGradient = AppColors.isCarbonSystem
+        ? [
+            Color.alphaBlend(
+              AppColors.focusControlAccent.withValues(alpha: 0.74),
+              AppColors.cardBackgroundStrong,
+            ),
+            Color.alphaBlend(
+              AppColors.familyCreateAccent.withValues(alpha: 0.54),
+              AppColors.cardBackgroundStrong,
+            ),
+          ]
+        : AppColors.buttonGradient;
     return _LeaderboardPressScale(
       onTap: onTap,
@@
           decoration: BoxDecoration(
             gradient: LinearGradient(
               begin: Alignment.centerLeft,
               end: Alignment.centerRight,
-              colors: AppColors.buttonGradient,
+              colors: inviteGradient,
             ),
             borderRadius: BorderRadius.circular(18),
+            border: Border.all(
+              color: AppColors.isCarbonSystem
+                  ? Color.alphaBlend(
+                      AppColors.focusControlAccent.withValues(alpha: 0.34),
+                      AppColors.borderStrong,
+                    )
+                  : AppColors.white.withValues(alpha: 0.12),
+              width: 1,
+            ),
           ),
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
       child: Container(
         height: 56,
         padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
         decoration: BoxDecoration(
           borderRadius: BorderRadius.circular(16),
-          color: AppColors.cardBackgroundAlt,
-          border: Border.all(color: AppColors.border, width: 0.9),
+          gradient: LinearGradient(
+            begin: Alignment.topCenter,
+            end: Alignment.bottomCenter,
+            colors: AppColors.isCarbonSystem
+                ? [AppColors.cardBackgroundStrong, AppColors.cardBackgroundAlt]
+                : [AppColors.cardBackgroundAlt, AppColors.cardBackgroundAlt],
+          ),
+          border: Border.all(
+            color: AppColors.isCarbonSystem
+                ? AppColors.borderStrong.withValues(alpha: 0.94)
+                : AppColors.border,
+            width: AppColors.isCarbonSystem ? 1 : 0.9,
+          ),
+          boxShadow: [
+            if (AppColors.isCarbonSystem)
+              BoxShadow(
+                color: AppColors.glassShadowSoft.withValues(alpha: 0.14),
+                blurRadius: 10,
+                offset: const Offset(0, 4),
+                spreadRadius: -8,
+              ),
+          ],
         ),
@@
         decoration: BoxDecoration(
           borderRadius: BorderRadius.circular(18),
-          color: enabled
-              ? AppColors.accent.withValues(alpha: 0.06)
-              : AppColors.cardBackgroundAlt,
+          gradient: LinearGradient(
+            begin: Alignment.topCenter,
+            end: Alignment.bottomCenter,
+            colors: enabled
+                ? [
+                    Color.alphaBlend(
+                      AppColors.accent.withValues(alpha: 0.06),
+                      AppColors.cardBackgroundStrong,
+                    ),
+                    AppColors.cardBackgroundAlt,
+                  ]
+                : (AppColors.isCarbonSystem
+                    ? [
+                        AppColors.cardBackgroundStrong,
+                        AppColors.cardBackgroundAlt,
+                      ]
+                    : [
+                        AppColors.cardBackgroundAlt,
+                        AppColors.cardBackgroundAlt,
+                      ]),
+          ),
           border: Border.all(
             color: enabled
                 ? (AppColors.isDark
@@
-                : (AppColors.isDark
-                      ? AppColors.border
-                      : AppColors.borderStrong.withValues(alpha: 0.92)),
+                : (AppColors.isDark
+                      ? (AppColors.isCarbonSystem
+                            ? AppColors.borderStrong.withValues(alpha: 0.88)
+                            : AppColors.border)
+                      : AppColors.borderStrong.withValues(alpha: 0.92)),
             width: AppColors.isDark ? 1 : 1.2,
           ),
@@
                       borderRadius: BorderRadius.circular(999),
                       color: AppColors.glassHighlight.withValues(
-                        alpha: AppColors.isDark ? 0.18 : 0.24,
+                        alpha: AppColors.isCarbonSystem
+                            ? 0.24
+                            : (AppColors.isDark ? 0.18 : 0.24),
                       ),
                     ),
                   ),
*** End Patch
