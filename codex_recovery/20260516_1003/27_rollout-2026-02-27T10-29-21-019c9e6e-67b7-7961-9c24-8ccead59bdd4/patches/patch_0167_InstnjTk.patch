*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/leaderboard/leaderboard_screen.dart
@@
   String? _myUserId;
   bool _loading = true;
   bool _friendsLoading = false;
   _LeaderboardTab _activeTab = _LeaderboardTab.global;
+  int _tabDirection = -1;
@@
   Future<void> _syncAndLoadFriends() async {
     await _repository.syncFriendConnections();
     await _loadFriendsOnly();
   }
+
+  void _setLeaderboardTab(_LeaderboardTab tab) {
+    if (tab == _activeTab) return;
+    final direction = tab.index > _activeTab.index ? -1 : 1;
+    setState(() {
+      _tabDirection = direction;
+      _activeTab = tab;
+      if (tab == _LeaderboardTab.friends) {
+        _friendsLoading = true;
+      }
+    });
+    if (tab == _LeaderboardTab.friends) {
+      _syncAndLoadFriends();
+    }
+  }
@@
       ),
-      child: _loading
-          ? Center(
-              child: CupertinoActivityIndicator(color: AppColors.primaryOrange),
-            )
-          : SafeArea(
-              child: ListView(
-                padding: const EdgeInsets.all(20),
-                children: [
-                  _buildHeader(),
-                  const SizedBox(height: 12),
-                  if (_activeTab == _LeaderboardTab.global)
-                    ..._buildGlobalCards()
-                  else
-                    ..._buildFriendCards(),
-                ],
-              ),
-            ),
+      child: AmbientBackdrop(
+        child: _loading
+            ? Center(
+                child: CupertinoActivityIndicator(
+                  color: AppColors.primaryOrange,
+                ),
+              )
+            : SafeArea(
+                child: ListView(
+                  padding: const EdgeInsets.all(20),
+                  children: [
+                    _buildHeader(),
+                    const SizedBox(height: 12),
+                    AnimatedSwitcher(
+                      duration: const Duration(milliseconds: 260),
+                      switchInCurve: Curves.easeOutCubic,
+                      switchOutCurve: Curves.easeInCubic,
+                      transitionBuilder: (child, animation) {
+                        final begin = _tabDirection < 0
+                            ? const Offset(0.16, 0)
+                            : const Offset(-0.16, 0);
+                        final slide = Tween<Offset>(
+                          begin: begin,
+                          end: Offset.zero,
+                        ).animate(animation);
+                        return FadeTransition(
+                          opacity: animation,
+                          child: SlideTransition(position: slide, child: child),
+                        );
+                      },
+                      child: Container(
+                        key: ValueKey(_activeTab),
+                        child: Column(
+                          children: _activeTab == _LeaderboardTab.global
+                              ? _buildGlobalCards()
+                              : _buildFriendCards(),
+                        ),
+                      ),
+                    ),
+                  ],
+                ),
+              ),
+      ),
     );
   }
@@
             onValueChanged: (value) {
               if (value == null) return;
-              if (value == _LeaderboardTab.friends) {
-                setState(() {
-                  _activeTab = value;
-                  _friendsLoading = true;
-                });
-                _syncAndLoadFriends();
-                return;
-              }
-              setState(() => _activeTab = value);
+              _setLeaderboardTab(value);
             },
           ),
         ),
*** End Patch
