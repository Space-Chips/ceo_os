*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/repositories/family_time_repository.dart
@@
 import '../models/family_time_models.dart';
 import '../services/supabase_service.dart';
 
+class FamilyTimeRepositoryException implements Exception {
+  final String message;
+  const FamilyTimeRepositoryException(this.message);
+
+  @override
+  String toString() => message;
+}
+
 class FamilyTimeRepository {
@@
   String _makeInviteToken() {
@@
   }
+
+  String _humanizeError(Object error) {
+    if (error is PostgrestException) {
+      if (error.code == '42P01') {
+        return 'Family Time backend not deployed. Run supabase_family_time_update.sql in Supabase SQL Editor.';
+      }
+      if (error.code == '42501') {
+        return 'Family Time permissions are not configured. Verify RLS policies.';
+      }
+      if ((error.message).trim().isNotEmpty) {
+        return error.message.trim();
+      }
+    }
+    return 'Unexpected error. Please retry.';
+  }
@@
   Future<FamilyTimeGroup?> createGroup(String name) async {
     final trimmed = name.trim();
     if (trimmed.isEmpty) return null;
 
-    try {
-      final token = _makeInviteToken();
-      final created = await _client
-          .from('family_time_groups')
-          .insert({
-            'created_by': _currentUserId,
-            'name': trimmed,
-            'invite_token': token,
-          })
-          .select()
-          .single();
-      final group = FamilyTimeGroup.fromJson(created);
-      final label = await _resolveMyDisplayName();
-      await _client.from('family_time_members').insert({
-        'group_id': group.id,
-        'user_id': _currentUserId,
-        'invite_email': _currentUserEmail,
-        'display_name': label,
-        'role': 'admin',
-      });
-      return group;
-    } catch (_) {
-      return null;
+    for (var attempt = 0; attempt < 5; attempt++) {
+      try {
+        final token = _makeInviteToken();
+        final created = await _client
+            .from('family_time_groups')
+            .insert({
+              'created_by': _currentUserId,
+              'name': trimmed,
+              'invite_token': token,
+            })
+            .select()
+            .single();
+        final group = FamilyTimeGroup.fromJson(created);
+        final label = await _resolveMyDisplayName();
+        await _client.from('family_time_members').insert({
+          'group_id': group.id,
+          'user_id': _currentUserId,
+          'invite_email': _currentUserEmail,
+          'display_name': label,
+          'role': 'admin',
+        });
+        return group;
+      } catch (error) {
+        if (error is PostgrestException && error.code == '23505') {
+          continue;
+        }
+        throw FamilyTimeRepositoryException(_humanizeError(error));
+      }
     }
+    throw const FamilyTimeRepositoryException(
+      'Unable to create an invite token. Retry in a few seconds.',
+    );
   }
@@
   Future<FamilyTimeGroup?> joinGroupByToken({
@@
-    try {
+    try {
       final groupRow = await _client
           .from('family_time_groups')
           .select()
           .eq('invite_token', cleanedToken)
           .maybeSingle();
@@
-      return group;
-    } catch (_) {
-      return null;
+      return group;
+    } catch (error) {
+      throw FamilyTimeRepositoryException(_humanizeError(error));
     }
   }
*** End Patch
