*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/rank/rank_screen.dart
@@
       child: Center(
         child: Container(
           width: size * 0.62,
           height: size * 0.62,
           decoration: BoxDecoration(
             borderRadius: BorderRadius.circular(size * 0.2),
-            color: const Color(0xFF060B15),
+            color: AppColors.cardBackgroundStrong,
             border: Border.all(
               color: AppColors.glassBorder.withValues(alpha: 0.65),
             ),
           ),
         ),
@@
       height: size,
       decoration: BoxDecoration(
         borderRadius: BorderRadius.circular(size * 0.28),
-        gradient: const LinearGradient(
+        gradient: LinearGradient(
           begin: Alignment.topLeft,
           end: Alignment.bottomRight,
-          colors: [Color(0xFFCFF3FF), Color(0xFF57C3FF), Color(0xFF0A2942)],
+          colors: [
+            AppColors.accentLight,
+            AppColors.accent,
+            AppColors.accentDeep,
+          ],
         ),
         boxShadow: [
           BoxShadow(
-            color: const Color(0x8857C3FF),
+            color: AppColors.themeGlow.withValues(alpha: 0.42),
             blurRadius: size * 0.42,
             spreadRadius: 1,
           ),
         ],
       ),
@@
             Positioned.fill(
               child: CustomPaint(
                 painter: _FacetPainter(
-                  lineColor: const Color(0xDDF2FBFF),
-                  secondaryColor: const Color(0x7757C3FF),
+                  lineColor: AppColors.label.withValues(alpha: 0.86),
+                  secondaryColor: AppColors.accent.withValues(alpha: 0.45),
                   density: 22,
                 ),
               ),
             ),
             Center(
@@
                 decoration: BoxDecoration(
                   borderRadius: BorderRadius.circular(size * 0.2),
-                  gradient: const RadialGradient(
-                    colors: [Color(0xFF061224), Color(0xFF02060F)],
+                  gradient: RadialGradient(
+                    colors: [
+                      AppColors.cardBackgroundStrong,
+                      AppColors.background,
+                    ],
                   ),
                   border: Border.all(
-                    color: const Color(0xAAEAF7FF),
+                    color: AppColors.label.withValues(alpha: 0.78),
                     width: 1.1,
                   ),
                   boxShadow: [
                     BoxShadow(
-                      color: const Color(0x6657C3FF),
+                      color: AppColors.themeGlow.withValues(alpha: 0.32),
                       blurRadius: size * 0.16,
                     ),
                   ],
                 ),
@@
       height: size,
       decoration: BoxDecoration(
         borderRadius: BorderRadius.circular(size * 0.28),
-        gradient: const LinearGradient(
+        gradient: LinearGradient(
           begin: Alignment.topLeft,
           end: Alignment.bottomRight,
-          colors: [Color(0xFFF6FAFF), Color(0xFFD1D9E7), Color(0xFF2A3F58)],
+          colors: [
+            AppColors.label,
+            AppColors.secondaryLabel,
+            AppColors.surfaceMuted,
+          ],
         ),
         boxShadow: [
           BoxShadow(
-            color: const Color(0x66E9F1FF),
+            color: AppColors.label.withValues(alpha: 0.22),
             blurRadius: size * 0.36,
             spreadRadius: 0.5,
           ),
         ],
       ),
@@
             Positioned.fill(
               child: CustomPaint(
                 painter: _FacetPainter(
-                  lineColor: const Color(0xA5FFFFFF),
-                  secondaryColor: const Color(0x666BA7D9),
+                  lineColor: AppColors.label.withValues(alpha: 0.72),
+                  secondaryColor: AppColors.accentLight.withValues(alpha: 0.4),
                   density: 16,
                 ),
               ),
             ),
             Center(
@@
                 decoration: BoxDecoration(
                   borderRadius: BorderRadius.circular(size * 0.2),
-                  gradient: const RadialGradient(
-                    colors: [Color(0xFF1E2D3E), Color(0xFF060B15)],
+                  gradient: RadialGradient(
+                    colors: [
+                      AppColors.cardBackgroundAlt,
+                      AppColors.cardBackgroundStrong,
+                    ],
                   ),
                   border: Border.all(
-                    color: const Color(0xBDF3F6FF),
+                    color: AppColors.label.withValues(alpha: 0.76),
                     width: 1.0,
                   ),
                 ),
               ),
@@
       height: size,
       decoration: BoxDecoration(
         borderRadius: BorderRadius.circular(size * 0.28),
-        color: const Color(0xFF07090C),
-        border: Border.all(color: const Color(0xFF10141A), width: 1.0),
+        color: AppColors.cardBackgroundStrong,
+        border: Border.all(color: AppColors.cardBorderStrong, width: 1.0),
         boxShadow: [
           BoxShadow(
-            color: const Color(0x44000000),
+            color: AppColors.black.withValues(alpha: 0.28),
             blurRadius: size * 0.24,
             spreadRadius: 1,
           ),
         ],
@@
         child: Container(
           width: size * 0.64,
           height: size * 0.64,
           decoration: BoxDecoration(
             borderRadius: BorderRadius.circular(size * 0.2),
-            color: const Color(0xFF0E1218),
-            border: Border.all(color: const Color(0xFF1A2029), width: 0.9),
+            color: AppColors.cardBackgroundAlt,
+            border: Border.all(color: AppColors.cardBorder, width: 0.9),
           ),
         ),
       ),
     );
@@
       height: size,
       decoration: BoxDecoration(
         borderRadius: BorderRadius.circular(size * 0.3),
-        gradient: const LinearGradient(
+        gradient: LinearGradient(
           begin: Alignment.topLeft,
           end: Alignment.bottomRight,
-          colors: [Color(0xFFF8D86A), Color(0xFF8D6A1D), Color(0xFF0B111D)],
+          colors: [
+            AppColors.warning,
+            AppColors.rankAccent,
+            AppColors.cardBackgroundStrong,
+          ],
         ),
         boxShadow: [
           BoxShadow(
-            color: const Color(0x88E9C55E),
+            color: AppColors.warning.withValues(alpha: 0.36),
             blurRadius: size * 0.48,
             spreadRadius: 1,
           ),
         ],
@@
             Positioned.fill(
               child: CustomPaint(
                 painter: _IrisPainter(
-                  primary: const Color(0xFF78D6E8),
-                  secondary: const Color(0xFFF6C945),
-                  shadow: const Color(0xFF1B2F47),
+                  primary: AppColors.rankAccent,
+                  secondary: AppColors.warning,
+                  shadow: AppColors.accentDeep,
                 ),
               ),
             ),
             Center(
               child: Container(
                 width: size * 0.36,
                 height: size * 0.36,
-                decoration: const BoxDecoration(
+                decoration: BoxDecoration(
                   shape: BoxShape.circle,
-                  color: Color(0xFF020305),
+                  color: AppColors.background,
                 ),
               ),
             ),
             Positioned(
@@
                 width: size * 0.2,
                 height: size * 0.08,
                 decoration: BoxDecoration(
                   borderRadius: BorderRadius.circular(size * 0.06),
-                  color: const Color(0x66FFFFFF),
+                  color: AppColors.label.withValues(alpha: 0.4),
                 ),
               ),
             ),
           ],
@@
         colors: [
           shadow.withValues(alpha: 0.95),
           primary.withValues(alpha: 0.78),
           secondary.withValues(alpha: 0.76),
-          const Color(0xFF0B111D),
+          AppColors.cardBackgroundStrong,
         ],
         stops: const [0.0, 0.42, 0.76, 1.0],
       ).createShader(Rect.fromCircle(center: c, radius: r));
@@
     final ringPaint = Paint()
       ..style = PaintingStyle.stroke
       ..strokeWidth = 1.5
-      ..color = const Color(0x88FFFFFF);
+      ..color = AppColors.label.withValues(alpha: 0.5);
*** End Patch
