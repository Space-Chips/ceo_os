*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/main.dart
@@
   @override
   Widget build(BuildContext context) {
     return Consumer<ThemeProvider>(
       builder: (context, theme, _) {
+        final overlayStyle = SystemUiOverlayStyle(
+          statusBarBrightness: theme.currentPreset.isDark
+              ? Brightness.dark
+              : Brightness.light,
+          statusBarIconBrightness: theme.currentPreset.isDark
+              ? Brightness.light
+              : Brightness.dark,
+          systemNavigationBarColor: AppColors.systemBackground,
+          systemNavigationBarIconBrightness: theme.currentPreset.isDark
+              ? Brightness.light
+              : Brightness.dark,
+        );
+        SystemChrome.setSystemUIOverlayStyle(overlayStyle);
         final lightTone = AppThemeTone.light;
         final darkTone = AppThemeTone.dark;
         return AdaptiveApp.router(
+          key: ValueKey(theme.currentPreset.id),
           title: 'CEO OS',
           themeMode: theme.themeMode,
           cupertinoLightTheme: AppTheme.cupertinoForTone(lightTone),
*** Update File: /Users/timo/ceo_os/lib/features/home/stats_screen.dart
@@
   @override
   Widget build(BuildContext context) {
+    final base = AppColors.accentSurfaceSoft;
     final color = switch (cell.intensityLevel) {
-      0 => const Color(0xFF612A2A),
-      1 => const Color(0xFF35563E),
-      2 => const Color(0xFF2E6E44),
-      3 => const Color(0xFF25814B),
-      _ => const Color(0xFF1EA95A),
+      0 => Color.alphaBlend(AppColors.error.withValues(alpha: 0.16), AppColors.surface),
+      1 => Color.alphaBlend(base.withValues(alpha: 0.52), AppColors.surface),
+      2 => Color.alphaBlend(base.withValues(alpha: 0.72), AppColors.surface),
+      3 => Color.alphaBlend(AppColors.accentSurfaceStrong, AppColors.surface),
+      _ => Color.alphaBlend(AppColors.accent.withValues(alpha: 0.26), AppColors.surface),
     };
 
     return Container(
*** Update File: /Users/timo/ceo_os/lib/features/leaderboard/leaderboard_screen.dart
@@
                             Icon(
                               CupertinoIcons.rosette,
                               size: 32,
-                              color: const Color(0xFFFACC15),
+                              color: AppColors.rankAccent,
                             ),
@@
   BoxDecoration _leaderboardRowDecoration({Color? borderColor}) {
     return BoxDecoration(
-      gradient: const LinearGradient(
+      gradient: LinearGradient(
         begin: Alignment.topCenter,
         end: Alignment.bottomCenter,
-        colors: [Color(0xFF1A1A1A), Color(0xFF111111)],
+        colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
       ),
       borderRadius: BorderRadius.circular(16),
       border: Border.all(
-        color: borderColor ?? AppColors.white.withValues(alpha: 0.06),
+        color: borderColor ?? AppColors.border,
         width: 1,
       ),
@@
       padding: const EdgeInsets.fromLTRB(16, 14, 16, 14),
       decoration: BoxDecoration(
-        gradient: const LinearGradient(
+        gradient: LinearGradient(
           begin: Alignment.topCenter,
           end: Alignment.bottomCenter,
-          colors: [Color(0xFF1A1A1A), Color(0xFF111111)],
+          colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
         ),
         border: Border(
           top: BorderSide(
-            color: AppColors.white.withValues(alpha: 0.08),
+            color: AppColors.borderStrong,
             width: 1,
           ),
         ),
@@
         child: Container(
           padding: const EdgeInsets.all(16),
           decoration: _leaderboardRowDecoration(
-            borderColor: const Color(0xFF508CFF).withValues(alpha: 0.35),
+            borderColor: AppColors.selectionOutline,
           ),
*** Update File: /Users/timo/ceo_os/lib/features/ceo_mode/ceo_mode_screen.dart
@@
             decoration: BoxDecoration(
-              color: const Color(0xF314161C),
+              color: AppColors.cardBackgroundAlt.withValues(alpha: 0.96),
               borderRadius: const BorderRadius.vertical(
                 top: Radius.circular(24),
               ),
-              boxShadow: const [
+              boxShadow: [
                 BoxShadow(
-                  color: Color(0x66000000),
+                  color: AppColors.glassShadow.withValues(alpha: 0.4),
                   blurRadius: 28,
                   offset: Offset(0, -6),
                 ),
@@
                     Container(
                       width: 40,
                       height: 4,
                       decoration: BoxDecoration(
-                        color: CupertinoColors.white.withValues(alpha: 0.14),
+                        color: AppColors.borderStrong,
                         borderRadius: BorderRadius.circular(999),
                       ),
                     ),
@@
                             style: AppTypography.callout.copyWith(
                               fontWeight: FontWeight.w700,
-                              color: const Color(0xFF1A1A1A),
+                              color: AppColors.background,
                             ),
                           ),
@@
         ShaderMask(
-          shaderCallback: (bounds) => const LinearGradient(
+          shaderCallback: (bounds) => LinearGradient(
             begin: Alignment.centerLeft,
             end: Alignment.centerRight,
-            colors: [Color(0xFFFFFFFF), Color(0xFFD7DCE5)],
+            colors: [AppColors.label, AppColors.secondaryLabel],
           ).createShader(bounds),
           child: Text(
             'CEO Mode',
@@
               fontWeight: FontWeight.w800,
               letterSpacing: 0.3,
               height: 0.96,
-              color: CupertinoColors.white,
+              color: AppColors.label,
             ),
           ),
         ),
@@
         Container(
           decoration: BoxDecoration(
             borderRadius: BorderRadius.circular(20),
-            color: CupertinoColors.white.withValues(alpha: 0.04),
+            color: AppColors.cardBackgroundAlt.withValues(alpha: 0.94),
             border: Border.all(
-              color: CupertinoColors.white.withValues(alpha: 0.08),
+              color: AppColors.borderStrong,
               width: 0.9,
             ),
@@
                         decoration: BoxDecoration(
                           borderRadius: BorderRadius.circular(16),
-                          color: CupertinoColors.white.withValues(alpha: 0.03),
+                          color: AppColors.sectionBackground.withValues(alpha: 0.88),
                           border: Border.all(
-                            color: CupertinoColors.white.withValues(alpha: 0.06),
+                            color: AppColors.border,
                           ),
                         ),
@@
                               color: CupertinoColors.white.withValues(
-                                alpha: 0.03,
+                                alpha: 0.03,
                               ),
                             ),
                           ),
@@
           showEdgeGlow: false,
-          gradientColors: const [Color(0xFF1A1A1A), Color(0xFF111111)],
+          gradientColors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
           border: Border.all(
-            color: CupertinoColors.white.withValues(alpha: 0.06),
+            color: AppColors.border,
             width: 0.9,
           ),
@@
           gradientColors: [
-            CupertinoColors.white.withValues(alpha: 0.05),
-            CupertinoColors.white.withValues(alpha: 0.04),
+            AppColors.cardBackgroundAlt.withValues(alpha: 0.98),
+            AppColors.cardBase.withValues(alpha: 0.98),
           ],
           border: Border.all(
-            color: CupertinoColors.white.withValues(alpha: 0.08),
+            color: AppColors.borderStrong,
             width: 0.9,
           ),
*** Update File: /Users/timo/ceo_os/lib/features/legacy/notes_screen.dart
@@
-  static const Color _notesGreen = Color(0xFF38C793);
-  static const Color _notesGreenLight = Color(0xFF64E1B2);
+  Color get _notesGreen => AppColors.accent;
+  Color get _notesGreenLight => AppColors.accentLight;
*** End Patch
