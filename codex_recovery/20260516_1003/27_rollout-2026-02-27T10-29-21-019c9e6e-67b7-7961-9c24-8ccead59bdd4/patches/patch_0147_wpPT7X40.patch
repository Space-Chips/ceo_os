*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
 class _HomeHeader extends StatelessWidget {
   final String userLabel;
+  final String welcomeBackLabel;
   final VoidCallback onOpenProfile;
   final VoidCallback onOpenMenu;
 
   const _HomeHeader({
     required this.userLabel,
+    required this.welcomeBackLabel,
     required this.onOpenProfile,
     required this.onOpenMenu,
   });
@@
                 Text(
-                  'Welcome back, $userLabel',
+                  '$welcomeBackLabel, $userLabel',
                   style: AppTypography.mono.copyWith(
                     fontSize: 11,
                     color: AppColors.tertiaryLabel,
@@
   @override
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     return Column(
       children: [
         Row(
           children: [
             Text(
-              'QUICK_ACTIONS',
+              language.t('quick_actions'),
               style: AppTypography.mono.copyWith(
                 fontSize: 10,
                 color: AppColors.tertiaryLabel,
@@
         GlassCard(
@@
           child: Row(
             children: [
-              _action('TASK', CupertinoIcons.checkmark_alt, onTask),
+              _action(language.t('tasks'), CupertinoIcons.checkmark_alt, onTask),
               const SizedBox(width: 8),
-              _action('HABIT', CupertinoIcons.flame_fill, onHabit),
+              _action(language.t('habits'), CupertinoIcons.flame_fill, onHabit),
               const SizedBox(width: 8),
-              _action('EVENT', CupertinoIcons.calendar, onEvent),
+              _action(
+                language.t('calendar'),
+                CupertinoIcons.calendar,
+                onEvent,
+              ),
               const SizedBox(width: 8),
-              _action('FOCUS', CupertinoIcons.timer_fill, onFocus),
+              _action(language.t('focus'), CupertinoIcons.timer_fill, onFocus),
             ],
           ),
         ),
@@
 class _ControlCenterCard extends StatelessWidget {
@@
   @override
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     return GestureDetector(
       onTap: () => context.push('/dashboard'),
@@
                 Text(
-                  'Daily Control Center'.toUpperCase(),
+                  language.t('daily_control_center'),
                   style: AppTypography.mono.copyWith(
                     fontSize: 13,
                     fontWeight: FontWeight.bold,
@@
                 _StatPill(
-                  label: 'TASKS',
+                  label: language.t('tasks'),
                   value: '${data.completedTasks}/${data.totalTasks}',
                 ),
                 const SizedBox(width: 8),
                 _StatPill(
-                  label: 'HABITS',
+                  label: language.t('habits'),
                   value: '${data.habitsCompletedToday}/${data.totalHabits}',
                 ),
                 const SizedBox(width: 8),
-                _StatPill(label: 'RANK', value: 'L${data.rankLevel}'),
+                _StatPill(label: language.t('rank'), value: 'L${data.rankLevel}'),
@@
             Text(
-              'Tap to open full dashboard intelligence',
+              language.t('tap_open_dashboard_intelligence'),
               style: AppTypography.mono.copyWith(
                 fontSize: 9,
                 color: AppColors.tertiaryLabel,
@@
   @override
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     if (modules.isEmpty) {
       return GlassCard(
         padding: const EdgeInsets.all(14),
         borderRadius: 14,
         child: Text(
-          'No active modules. Enable modules from the grid button in the header.',
+          language.t('no_active_modules'),
           style: AppTypography.mono.copyWith(
             fontSize: 11,
             color: AppColors.tertiaryLabel,
@@
 class _MissionHeroCard extends StatelessWidget {
@@
   @override
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     return Stack(
@@
                     child: Text(
-                      'MISSION',
+                      language.t('mission'),
                       style: AppTypography.mono.copyWith(
                         fontSize: 9,
@@
               Text(
-                'Build high-leverage execution blocks today.',
+                language.t('mission_statement'),
                 style: AppTypography.mono.copyWith(
                   fontSize: 13,
@@
               Text(
-                '$userLabel • ${isFocusing ? 'Focus mode active' : 'Ready for first sprint'}',
+                '$userLabel • ${isFocusing ? language.t('focus_mode_active') : language.t('ready_for_first_sprint')}',
                 style: AppTypography.mono.copyWith(
                   fontSize: 10,
@@
                             Text(
-                              'START',
+                              language.t('start'),
                               style: AppTypography.mono.copyWith(
                                 fontSize: 9,
@@
                             Text(
-                              'INSIGHTS',
+                              language.t('insights'),
                               style: AppTypography.mono.copyWith(
                                 fontSize: 9,
@@
   @override
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     return GlassCard(
@@
           Text(
-            'Daily Indexes',
+            language.t('daily_indexes'),
             style: AppTypography.mono.copyWith(
               fontSize: 12,
@@
           _indexRow(
-            'Task index',
+            language.t('task_index'),
             '${(data.taskCompletionRate * 100).round()}%',
           ),
           _indexRow(
-            'Habit index',
+            language.t('habit_index'),
             '${(data.habitCompletionRate * 100).round()}%',
           ),
-          _indexRow('Consistency', '${data.currentHabitStreak} day streak'),
-          _indexRow('Planning', '${data.eventsNext7d} events / 7d'),
+          _indexRow(
+            language.t('consistency'),
+            '${data.currentHabitStreak} day streak',
+          ),
+          _indexRow(
+            language.t('planning'),
+            '${data.eventsNext7d} events / 7d',
+          ),
         ],
       ),
     );
@@
   @override
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     return GestureDetector(
@@
                   Text(
-                    'Tap to manage focus mode',
+                    language.t('tap_manage_focus_mode'),
                     style: AppTypography.mono.copyWith(
                       fontSize: 10,
                       color: AppColors.secondaryLabel,
*** End Patch
