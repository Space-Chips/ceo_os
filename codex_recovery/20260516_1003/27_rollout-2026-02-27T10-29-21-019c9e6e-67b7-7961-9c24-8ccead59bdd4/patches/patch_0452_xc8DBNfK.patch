*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/stats_engine.dart
@@
   Future<void> trackEvent(
     String eventType, {
     DateTime? eventTime,
     Map<String, dynamic> payload = const {},
+    String? sourceKey,
     bool recompute = true,
   }) async {
     if (!StatsEventType.values.contains(eventType)) return;
 
     final now = eventTime ?? DateTime.now();
-    await _repository.logEvent(
-      StatsEventInput(eventType: eventType, eventTime: now, payload: payload),
-    );
+    await _repository.logEvent(
+      StatsEventInput(
+        eventType: eventType,
+        eventTime: now,
+        sourceKey: sourceKey,
+        payload: payload,
+      ),
+    );
*** End Patch
