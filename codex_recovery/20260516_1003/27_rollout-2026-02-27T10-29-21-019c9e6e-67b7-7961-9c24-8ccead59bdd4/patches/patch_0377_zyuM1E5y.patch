*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
         Row(
           children: [
-            Container(width: 2, height: 28, color: Colors.white),
+            Container(
+              width: 2,
+              height: 28,
+              color: AppColors.glassHighlight.withValues(alpha: 0.7),
+            ),
             const SizedBox(width: 10),
             Expanded(
               child: Text(
                 'YOUR PRIORITIES',
@@
-            CupertinoButton(
-              padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 12),
-              color: Colors.white.withValues(alpha: 0.95),
-              borderRadius: BorderRadius.circular(16),
-              onPressed: onAdd,
-              child: Row(
-                children: [
-                  const Icon(CupertinoIcons.add, color: Colors.black, size: 20),
-                  const SizedBox(width: 8),
-                  Text(
-                    'Add',
-                    style: AppTypography.mono.copyWith(
-                      fontSize: 14,
-                      fontWeight: FontWeight.w900,
-                      color: Colors.black,
-                    ),
-                  ),
-                ],
-              ),
+            SizedBox(
+              width: 108,
+              child: LiquidButton(
+                label: 'Add',
+                icon: CupertinoIcons.add,
+                borderRadius: 14,
+                height: 46,
+                onPressed: onAdd,
+              ),
             ),
           ],
         ),
@@
           GlassCard(
             padding: const EdgeInsets.all(20),
             borderRadius: 18,
+            level: GlassCardLevel.standard,
+            showEdgeGlow: true,
             border: Border.all(color: AppColors.glassBorder, width: 0.65),
             child: Column(
@@
               Container(width: 2, height: 24, color: AppColors.tertiaryLabel),
@@
           Center(
-            child: CupertinoButton(
-              padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 12),
-              color: AppColors.backgroundLight.withValues(alpha: 0.65),
-              borderRadius: BorderRadius.circular(14),
-              onPressed: onAdd,
-              child: Row(
-                mainAxisSize: MainAxisSize.min,
-                children: [
-                  Icon(
-                    CupertinoIcons.add,
-                    size: 20,
-                    color: AppColors.secondaryLabel,
-                  ),
-                  const SizedBox(width: 8),
-                  Text(
-                    'Add Task',
-                    style: AppTypography.mono.copyWith(
-                      fontSize: 15,
-                      color: AppColors.secondaryLabel,
-                      fontWeight: FontWeight.w800,
-                    ),
-                  ),
-                ],
-              ),
+            child: SizedBox(
+              width: 170,
+              child: LiquidButton(
+                label: 'Add Task',
+                icon: CupertinoIcons.add,
+                borderRadius: 14,
+                height: 46,
+                onPressed: onAdd,
+              ),
             ),
           ),
         ],
@@
           GlassCard(
             padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 12),
             borderRadius: 20,
+            level: GlassCardLevel.elevated,
+            showEdgeGlow: true,
             border: Border.all(color: palette.border, width: 0.95),
             gradientColors: [palette.start, palette.end],
             child: Row(
@@
       child: GlassCard(
         padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 10),
         borderRadius: 14,
+        level: GlassCardLevel.subtle,
         border: Border.all(
           color: AppColors.glassBorder.withValues(alpha: 0.55),
           width: 0.6,
@@
   }) {
-    return Container(
+    return GlassCard(
       constraints: const BoxConstraints(minHeight: 268),
-      decoration: BoxDecoration(
-        borderRadius: BorderRadius.circular(20),
-        border: Border.all(color: accent.withValues(alpha: 0.72), width: 0.9),
-        gradient: LinearGradient(
-          begin: Alignment.topLeft,
-          end: Alignment.bottomRight,
-          colors: [
-            accent.withValues(alpha: 0.18),
-            AppColors.backgroundLight.withValues(alpha: 0.78),
-          ],
-        ),
-      ),
+      borderRadius: 20,
+      level: GlassCardLevel.standard,
+      showEdgeGlow: true,
+      border: Border.all(color: accent.withValues(alpha: 0.72), width: 0.9),
+      gradientColors: [
+        accent.withValues(alpha: 0.18),
+        AppColors.floatingGlassGradient.last.withValues(alpha: 0.9),
+      ],
       child: Padding(
         padding: const EdgeInsets.fromLTRB(12, 12, 12, 10),
         child: Column(
@@
           GlassCard(
             padding: const EdgeInsets.all(20),
             borderRadius: 18,
+            level: GlassCardLevel.standard,
+            showEdgeGlow: true,
             border: Border.all(color: AppColors.glassBorder, width: 0.65),
             child: Text(
@@
     return GlassCard(
       padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 12),
       borderRadius: 18,
+      level: task.completed ? GlassCardLevel.subtle : GlassCardLevel.standard,
+      showEdgeGlow: !task.completed,
       border: Border.all(
         color: task.completed
             ? AppColors.glassBorder.withValues(alpha: 0.4)
*** End Patch
