*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
   Future<void> setClassicShieldEnabled(bool enabled) async {
@@
     }
   }
+
+  Future<void> syncClassicPauseSchedule(
+    List<String> packageNames,
+    List<String> categories,
+    List<Map<String, dynamic>> periods,
+  ) async {
+    try {
+      await _channel.invokeMethod('syncClassicPauseSchedule', {
+        'packages': packageNames,
+        'categories': categories,
+        'periods': periods,
+      });
+    } on PlatformException catch (e) {
+      AppLogger.error("Failed to sync classic pause schedule: '${e.message}'.");
+    } on MissingPluginException {
+      AppLogger.warning(
+        'Native syncClassicPauseSchedule method missing. Rebuild required.',
+      );
+    }
+  }
*** End Patch
