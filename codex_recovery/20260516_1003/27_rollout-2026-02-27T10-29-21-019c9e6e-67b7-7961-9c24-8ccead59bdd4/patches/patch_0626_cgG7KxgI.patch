*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/components/glass_card.dart
@@
   @override
   Widget build(BuildContext context) {
     return ShaderMask(
       blendMode: BlendMode.softLight,
       shaderCallback: (bounds) {
-        return const LinearGradient(
+        return LinearGradient(
           begin: Alignment.topLeft,
           end: Alignment.bottomRight,
-          colors: [Color(0x11FFFFFF), Color(0x00FFFFFF), Color(0x0D000000)],
+          colors: [
+            AppColors.label.withValues(alpha: AppColors.isDark ? 0.07 : 0.04),
+            AppColors.label.withValues(alpha: 0),
+            AppColors.overlayScrim.withValues(
+              alpha: AppColors.isDark ? 0.08 : 0.04,
+            ),
+          ],
           stops: [0, 0.5, 1],
         ).createShader(bounds);
       },
*** End Patch
