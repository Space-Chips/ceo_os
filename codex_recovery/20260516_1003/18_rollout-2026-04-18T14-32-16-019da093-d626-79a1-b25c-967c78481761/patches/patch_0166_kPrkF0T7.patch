*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/home/home_screen.dart
@@
   @override
   Widget build(BuildContext context) {
     final focusProvider = context.watch<FocusProvider>();
+    final language = context.watch<LanguageProvider>();
     final hasActiveSession = focusProvider.state != FocusState.idle;
@@
                     child: Text(
-                      'Quick Focus Session',
+                      language.t('quick_focus_session'),
                       style: AppTypography.title2.copyWith(
@@
               Text(
                 hasActiveSession
-                    ? 'A focus session is already active.'
-                    : 'Select Duration',
+                    ? language.t('focus_session_already_active')
+                    : language.t('select_duration'),
                 style: AppTypography.body.copyWith(
@@
                             Text(
-                              'min',
+                              language.t('focus_min'),
                               style: AppTypography.callout.copyWith(
@@
                         hasActiveSession
-                            ? 'Open Focus Mode'
-                            : (_starting ? 'Starting...' : 'Start Focus Mode'),
+                            ? language.t('open_focus_mode')
+                            : (_starting
+                                  ? language.t('starting')
+                                  : language.t('start_focus_mode')),
                         style: AppTypography.callout.copyWith(
*** End Patch
