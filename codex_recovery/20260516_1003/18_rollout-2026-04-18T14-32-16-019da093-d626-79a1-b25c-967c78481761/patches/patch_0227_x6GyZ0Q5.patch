*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/calendar/add_event_sheet.dart
@@
 import 'package:flutter/cupertino.dart';
 import 'package:flutter/material.dart' show Colors, TimeOfDay;
 import 'package:go_router/go_router.dart';
 import 'package:intl/intl.dart';
 import 'package:provider/provider.dart';
 
 import '../../core/models/task_models.dart';
+import '../../core/providers/language_provider.dart';
 import '../../core/providers/task_provider.dart';
 import '../../core/repositories/feature_repository.dart';
 import '../../core/theme/app_colors.dart';
 import '../../core/theme/app_typography.dart';
@@
   Future<void> _addEvent() async {
@@
     if (_isFocusSession && timeStr == null) {
       if (mounted) {
-        _toast('Focus session requires a start time.');
+        _toast(_t('calendar_add_event_focus_requires_start_time'));
       }
       return;
     }
@@
   }
 
+  String _t(String key) => context.read<LanguageProvider>().t(key);
+
   void _toast(String text) {
     showCupertinoDialog<void>(
       context: context,
       builder: (ctx) => CupertinoAlertDialog(
-        title: const Text('Cannot create event'),
+        title: Text(_t('calendar_add_event_error_title')),
         content: Text(text),
         actions: [
           CupertinoDialogAction(
             isDefaultAction: true,
             onPressed: () => Navigator.of(ctx).pop(),
-            child: const Text('OK'),
+            child: Text(_t('ok')),
           ),
         ],
       ),
@@
                       onPressed: () => Navigator.pop(context),
                       child: Text(
-                        'CANCEL',
+                        _t('cancel'),
                         style: TextStyle(
                           color: AppColors.secondaryLabel,
                           fontSize: 12,
@@
                       onPressed: () => Navigator.pop(context),
                       child: Text(
-                        'DONE',
+                        _t('done'),
                         style: TextStyle(
                           color: AppColors.primaryOrange,
                           fontWeight: FontWeight.bold,
@@
                       child: Text(
-                        'CLEAR',
+                        _t('tasks_picker_clear'),
                         style: TextStyle(
                           color: AppColors.secondaryLabel,
                           fontSize: 12,
@@
                       onPressed: () => Navigator.pop(context),
                       child: Text(
-                        'DONE',
+                        _t('done'),
                         style: TextStyle(
                           color: AppColors.primaryOrange,
                           fontWeight: FontWeight.bold,
@@
   String _timeLabel() {
     final time = _selectedTime;
-    if (time == null) return 'ALL DAY';
+    if (time == null) return _t('calendar_event_all_day');
     final dt = DateTime(2000, 1, 1, time.hour, time.minute);
     return DateFormat('HH:mm').format(dt);
   }
@@
                   Text(
-                    'Create New Event',
+                    _t('calendar_add_event_title'),
                     style: AppTypography.mono.copyWith(
                       fontSize: 22,
                       fontWeight: FontWeight.w900,
@@
                   const SizedBox(height: 24),
-                  _sectionLabel('EVENT TYPE'),
+                  _sectionLabel(_t('calendar_add_event_section_type')),
                   const SizedBox(height: 16),
                   Wrap(
                     spacing: 10,
@@
                       _selectionChip(
-                        label: 'Standard',
+                        label: _t('calendar_add_event_type_standard'),
                         selected: !_isBirthday && !_isFocusSession,
                         onTap: () {
@@
                       _selectionChip(
-                        label: 'Birthday',
+                        label: _t('calendar_add_event_type_birthday'),
                         selected: _isBirthday,
                         onTap: _toggleBirthday,
                       ),
                       _selectionChip(
-                        label: 'Focus Session',
+                        label: _t('calendar_add_event_type_focus_session'),
                         selected: _isFocusSession,
                         onTap: _toggleFocusSession,
                       ),
@@
                           Text(
-                            'Loading types…',
+                            _t('calendar_add_event_loading_types'),
                             style: AppTypography.body.copyWith(
                               fontSize: 13,
                               color: AppColors.secondaryLabel,
@@
                             return _selectionChip(
-                              label: type.name ?? 'Type',
+                              label: type.name ?? _t('calendar_add_event_type'),
                               selected: selected,
                               accentColor: color,
@@
                         child: Text(
-                          _eventTypes.isEmpty ? 'Create event types' : 'Manage types',
+                          _eventTypes.isEmpty
+                              ? _t('calendar_add_event_create_types')
+                              : _t('calendar_add_event_manage_types'),
                           style: AppTypography.body.copyWith(
                             fontSize: 13,
                             color: AppColors.secondaryLabel.withValues(alpha: 0.78),
@@
                     ],
                   ],
                   const SizedBox(height: 24),
-                  _sectionLabel('TITLE'),
+                  _sectionLabel(_t('calendar_add_event_section_title')),
                   const SizedBox(height: 16),
                   _inputField(
                     controller: _titleCtrl,
                     placeholder: _isBirthday
-                        ? 'Birthday title (e.g. Marie)'
+                        ? _t('calendar_add_event_title_placeholder_birthday')
                         : _isFocusSession
-                        ? 'Focus title'
-                        : 'Title',
+                        ? _t('calendar_add_event_title_placeholder_focus')
+                        : _t('calendar_add_event_title_placeholder_default'),
                     autofocus: true,
                     minHeight: 48,
                   ),
                   const SizedBox(height: 24),
-                  _sectionLabel('DESCRIPTION'),
+                  _sectionLabel(_t('calendar_add_event_section_description')),
                   const SizedBox(height: 16),
                   _inputField(
                     controller: _descCtrl,
                     placeholder: _isBirthday
-                        ? 'Notes (optional)'
+                        ? _t('calendar_add_event_description_placeholder_birthday')
                         : _isFocusSession
-                        ? 'Context (optional)'
-                        : 'Description (optional)',
+                        ? _t('calendar_add_event_description_placeholder_focus')
+                        : _t('calendar_add_event_description_placeholder_default'),
                     maxLines: 3,
                     minHeight: 72,
                     opacity: 0.8,
                   ),
                   const SizedBox(height: 24),
-                  _sectionLabel('DATE'),
+                  _sectionLabel(_t('calendar_add_event_section_date')),
                   const SizedBox(height: 16),
                   Row(
@@
                               child: _pickerField(
-                                label: _isBirthday ? 'All day' : _timeLabel(),
+                                label: _isBirthday
+                                    ? _t('calendar_event_all_day')
+                                    : _timeLabel(),
                                 icon: _isBirthday
                                     ? CupertinoIcons.gift
                                     : CupertinoIcons.clock,
@@
                   if (!_isBirthday) ...[
                     const SizedBox(height: 24),
-                    _sectionLabel('DURATION'),
+                    _sectionLabel(_t('calendar_add_event_section_duration')),
                     const SizedBox(height: 16),
@@
                               Text(
-                                'minutes',
+                                _t('calendar_add_event_minutes'),
                                 style: AppTypography.body.copyWith(
                                   fontSize: 12,
@@
                             child: Text(
-                              'Repeat weekly',
+                              _t('calendar_add_event_repeat_weekly'),
                               style: AppTypography.body.copyWith(
                                 fontSize: 14,
@@
                   ],
                   const SizedBox(height: 24),
-                  _sectionLabel('ACTION BUTTONS'),
+                  _sectionLabel(_t('calendar_add_event_section_actions')),
                   const SizedBox(height: 16),
                   Row(
@@
                           onPressed: () => Navigator.of(context).pop(),
                           child: Text(
-                            'Cancel',
+                            _t('cancel'),
                             style: AppTypography.body.copyWith(
                               fontSize: 14,
@@
                             _isBirthday
-                                ? 'Create Birthday'
+                                ? _t('calendar_add_event_create_birthday')
                                 : _isFocusSession
-                                ? 'Plan Session'
-                                : 'Create',
+                                ? _t('calendar_add_event_plan_session')
+                                : _t('create'),
                             style: AppTypography.body.copyWith(
                               fontSize: 14,
*** End Patch
