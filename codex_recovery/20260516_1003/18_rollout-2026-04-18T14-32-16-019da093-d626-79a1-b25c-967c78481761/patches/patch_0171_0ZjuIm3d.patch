*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/router/app_router.dart
@@
 import '../providers/auth_provider.dart';
 import '../providers/ceo_mode_provider.dart';
 import '../providers/focus_provider.dart';
+import '../providers/language_provider.dart';
@@
   void _showPlanReminderSnackBar() {
     if (!mounted) return;
+    final language = context.read<LanguageProvider>();
     ScaffoldMessenger.of(context).showSnackBar(
       SnackBar(
         behavior: SnackBarBehavior.floating,
         duration: const Duration(seconds: 3),
-        content: const Text(
-          'Focus mode will start automatically in 5 minutes.',
-        ),
+        content: Text(language.t('focus_auto_start_5min_notice')),
       ),
     );
   }
 
   void _showFocusStartedSnackBar(int duration) {
     if (!mounted) return;
+    final language = context.read<LanguageProvider>();
     ScaffoldMessenger.of(context).showSnackBar(
       SnackBar(
         behavior: SnackBarBehavior.floating,
         duration: const Duration(seconds: 3),
-        content: Text('Planned focus started ($duration min).'),
+        content: Text(
+          language.t('planned_focus_started_notice').replaceAll(
+            '{duration}',
+            '$duration',
+          ),
+        ),
       ),
     );
   }
*** End Patch
