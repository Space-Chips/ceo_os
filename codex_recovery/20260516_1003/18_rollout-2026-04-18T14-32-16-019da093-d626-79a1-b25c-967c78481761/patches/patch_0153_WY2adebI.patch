*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
   Future<void> _confirmDeleteGoal(Objective objective) async {
+    final language = context.read<LanguageProvider>();
     final meta = _objectiveMeta(objective);
     final confirmed = await _confirmDeletionTwice(
       context,
-      firstTitle: 'Delete Goal?',
-      firstMessage: '“${meta.name}” will be removed from your active goals.',
-      secondTitle: 'Confirm Goal Deletion',
-      secondMessage:
-          'This will permanently remove “${meta.name}” and detach related habits.',
+      firstTitle: language.t('habits_delete_goal_title'),
+      firstMessage: language
+          .t('habits_delete_goal_message')
+          .replaceAll('{goal}', meta.name),
+      secondTitle: language.t('habits_confirm_goal_deletion_title'),
+      secondMessage: language
+          .t('habits_confirm_goal_deletion_message')
+          .replaceAll('{goal}', meta.name),
     );
@@
   Future<void> _confirmDeleteLinkedHabit(Habit habit) async {
+    final language = context.read<LanguageProvider>();
     final confirmed = await _confirmDeletionTwice(
       context,
-      firstTitle: 'Delete Habit?',
-      firstMessage: '“${habit.title}” will be removed from your active habits.',
-      secondTitle: 'Confirm Habit Deletion',
-      secondMessage:
-          'This will permanently remove “${habit.title}” from this goal.',
+      firstTitle: language.t('habits_delete_habit_title'),
+      firstMessage: language
+          .t('habits_delete_habit_message')
+          .replaceAll('{habit}', habit.title),
+      secondTitle: language.t('habits_confirm_habit_deletion_title'),
+      secondMessage: language
+          .t('habits_confirm_habit_deletion_message')
+          .replaceAll('{habit}', habit.title),
     );
*** End Patch
