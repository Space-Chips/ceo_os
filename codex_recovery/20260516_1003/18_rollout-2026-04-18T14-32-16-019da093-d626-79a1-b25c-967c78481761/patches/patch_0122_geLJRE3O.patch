*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
-  Future<void> _confirmDeleteHabit(Habit habit) async {
-    final confirmed = await _confirmDeletionTwice(
-      context,
-      firstTitle: 'Delete Habit?',
-      firstMessage: '“${habit.title}” will be removed from your active habits.',
-      secondTitle: 'Confirm Habit Deletion',
-      secondMessage:
-          'This will permanently remove “${habit.title}” from this screen.',
-    );
-    if (confirmed != true || !mounted) return;
-    await context.read<HabitProvider>().deleteHabit(habit.id);
-    if (!mounted) return;
-    await _loadWeeklyData();
-  }
-
   int _frequencyScore(Habit habit) {
@@
                   ...sortedHabits.map(
                     (habit) => _HabitGridRow(
                       habit: habit,
                       weekStart: weekStart,
                       weeklyCompletions: _weeklyCompletions[habit.id] ?? {},
                       onTap: (targetDate) =>
                           widget.onOpenHabit(habit, targetDate),
-                      onDelete: () => _confirmDeleteHabit(habit),
                     ),
                   ),
@@
 class _HabitGridRow extends StatelessWidget {
   final Habit habit;
   final DateTime weekStart;
   final Map<String, HabitCompletion> weeklyCompletions;
   final ValueChanged<DateTime?> onTap;
-  final VoidCallback onDelete;
 
   const _HabitGridRow({
     required this.habit,
     required this.weekStart,
     required this.weeklyCompletions,
     required this.onTap,
-    required this.onDelete,
   });
@@
-    return _HoverDeleteRow(
-      onTap: pendingCarryoverDate == null
-          ? null
-          : () => onTap(pendingCarryoverDate),
-      onDelete: onDelete,
-      child: Padding(
-        padding: const EdgeInsets.symmetric(vertical: 4),
-        child: Row(
-          children: [
-            Expanded(
-              flex: 3,
-              child: Text(
-                habit.title,
-                style: AppTypography.callout.copyWith(
-                  fontSize: 12,
-                  color: AppColors.secondaryLabel,
-                  fontWeight: FontWeight.w500,
-                ),
-                maxLines: 1,
-                overflow: TextOverflow.ellipsis,
-              ),
-            ),
-            ...List.generate(7, (d) {
-              final day = weekStart.add(Duration(days: d));
-              final dayStr = DateFormat('yyyy-MM-dd').format(day);
-              final dayNum = day.weekday == DateTime.sunday ? 7 : day.weekday;
-              final beforeHabitCreation = day.isBefore(createdDay);
-
-              final isScheduled =
-                  habit.isDaily ||
-                  (habit.specificDays != null &&
-                      (habit.specificDays?.contains(dayNum) ?? false));
-
-              if (!isScheduled || beforeHabitCreation) {
-                return SizedBox(
-                  width: 32,
-                  child: Center(
-                    child: Text(
-                      '·',
-                      style: AppTypography.mono.copyWith(
-                        fontSize: 10,
-                        color: AppColors.quaternaryLabel,
-                      ),
-                    ),
-                  ),
-                );
-              }
-
-              final comp = weeklyCompletions[dayStr];
-              final hasCompletion = comp != null;
-              final isCompleted = hasCompletion && comp.completed;
-              final isMissed = hasCompletion && !comp.completed;
-              final isToday =
-                  day.year == today.year &&
-                  day.month == today.month &&
-                  day.day == today.day;
-              final isPast = day.isBefore(today);
-              final isMissingPastValidation =
-                  isPast &&
-                  !beforeHabitCreation &&
-                  !hasCompletion &&
-                  !isCompleted &&
-                  !isMissed;
-
-              return SizedBox(
-                width: 32,
-                child: Center(
-                  child: _DayCell(
-                    isCompleted: isCompleted,
-                    isMissed: isMissed,
-                    isLockedMissing: isMissingPastValidation,
-                    isToday: isToday,
-                  ),
-                ),
-              );
-            }),
-          ],
-        ),
-      ),
+    return _HabitPressScale(
+      onTap: pendingCarryoverDate == null
+          ? null
+          : () => onTap(pendingCarryoverDate),
+      child: Padding(
+        padding: const EdgeInsets.symmetric(vertical: 4),
+        child: Row(
+          children: [
+            Expanded(
+              flex: 3,
+              child: Text(
+                habit.title,
+                style: AppTypography.callout.copyWith(
+                  fontSize: 12,
+                  color: AppColors.secondaryLabel,
+                  fontWeight: FontWeight.w500,
+                ),
+                maxLines: 1,
+                overflow: TextOverflow.ellipsis,
+              ),
+            ),
+            ...List.generate(7, (d) {
+              final day = weekStart.add(Duration(days: d));
+              final dayStr = DateFormat('yyyy-MM-dd').format(day);
+              final dayNum = day.weekday == DateTime.sunday ? 7 : day.weekday;
+              final beforeHabitCreation = day.isBefore(createdDay);
+
+              final isScheduled =
+                  habit.isDaily ||
+                  (habit.specificDays != null &&
+                      (habit.specificDays?.contains(dayNum) ?? false));
+
+              if (!isScheduled || beforeHabitCreation) {
+                return SizedBox(
+                  width: 32,
+                  child: Center(
+                    child: Text(
+                      '·',
+                      style: AppTypography.mono.copyWith(
+                        fontSize: 10,
+                        color: AppColors.quaternaryLabel,
+                      ),
+                    ),
+                  ),
+                );
+              }
+
+              final comp = weeklyCompletions[dayStr];
+              final hasCompletion = comp != null;
+              final isCompleted = hasCompletion && comp.completed;
+              final isMissed = hasCompletion && !comp.completed;
+              final isToday =
+                  day.year == today.year &&
+                  day.month == today.month &&
+                  day.day == today.day;
+              final isPast = day.isBefore(today);
+              final isMissingPastValidation =
+                  isPast &&
+                  !beforeHabitCreation &&
+                  !hasCompletion &&
+                  !isCompleted &&
+                  !isMissed;
+
+              return SizedBox(
+                width: 32,
+                child: Center(
+                  child: _DayCell(
+                    isCompleted: isCompleted,
+                    isMissed: isMissed,
+                    isLockedMissing: isMissingPastValidation,
+                    isToday: isToday,
+                  ),
+                ),
+              );
+            }),
+          ],
+        ),
+      ),
     );
   }
 }
*** End Patch
