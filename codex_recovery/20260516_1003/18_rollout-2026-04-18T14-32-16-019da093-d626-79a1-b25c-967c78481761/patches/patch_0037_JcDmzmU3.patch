*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
       final existingBundleIds = <String>{
         for (final binding in _appBindings.values)
-          if (binding.nativeIdentifier?.trim().isNotEmpty ?? false)
-            binding.nativeIdentifier!.trim().toLowerCase(),
+          if ((binding.nativeIdentifier ?? '').trim().isNotEmpty)
+            binding.nativeIdentifier!.trim().toLowerCase(),
       };
@@
       final existingDomains = <String>{
         for (final site in _blockedWebsites)
-          if ((site.urlDomain ?? '').trim().isNotEmpty)
-            site.urlDomain!.trim().toLowerCase(),
+          if ((site.urlDomain ?? '').trim().isNotEmpty) site.urlDomain!.trim().toLowerCase(),
         for (final binding in websiteBindings.values)
-          if ((binding.nativeIdentifier ?? '').trim().isNotEmpty)
-            binding.nativeIdentifier!.trim().toLowerCase(),
+          if ((binding.nativeIdentifier ?? '').trim().isNotEmpty)
+            binding.nativeIdentifier!.trim().toLowerCase(),
       };
@@
-    final title = site.urlDomain?.trim().isNotEmpty == true
-        ? site.urlDomain!.trim()
+    final title = (site.urlDomain ?? '').trim().isNotEmpty
+        ? site.urlDomain!.trim()
         : (bindingDomain != null && bindingDomain.isNotEmpty
               ? bindingDomain
               : 'Blocked website');
*** End Patch
