*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/calendar/calendar_screen.dart
@@
                                                 const SizedBox(height: 2),
                                                 Text(
-                                                  event.eventTime == null
-                                                      ? (_isBirthdayEvent(event)
-                                                            ? 'BIRTHDAY • repeats yearly'
-                                                            : 'ALL DAY')
-                                                      : _isFocusPlanEvent(event)
-                                                      ? '${event.eventTime} • ${_duration(event)}min${(event.recurrenceRule ?? '').toLowerCase().contains('week') ? ' • weekly' : ''}'
-                                                      : '${event.eventTime} • ${_duration(event)}min',
+                                                  _eventMetaLabel(event),
                                                   style: AppTypography.body
                                                       .copyWith(
                                                         fontSize: 11,
*** End Patch
