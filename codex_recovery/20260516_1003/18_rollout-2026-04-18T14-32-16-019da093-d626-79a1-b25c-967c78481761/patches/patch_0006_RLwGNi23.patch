*** Begin Patch
*** Update File: /Users/timo/ceo_os/ios/FocusActivityMonitor/FocusActivityMonitorExtension.swift
@@
     static let ceoAlwaysAllowedBundleIds: Set<String> = [
         "com.apple.mobilephone",
         "com.apple.MobileSMS",
         "com.apple.mobilecal"
     ]
+    static let ceoForceBlockedBundleIds: Set<String> = [
+        "com.withopal.opal",
+        "com.apple.facetime",
+        "com.apple.Maps",
+        "com.apple.Passbook",
+        "com.apple.Fitness",
+        "com.apple.findmy",
+        "com.apple.Home",
+        "com.apple.DocumentsApp",
+        "com.apple.appleseed.FeedbackAssistant",
+        "com.apple.tips"
+    ]
 }
@@
     private func ceoAllowedApplicationTokens() -> Set<ApplicationToken> {
         var tokens = applicationTokens(for: FocusMonitorShared.ceoAlwaysAllowedBundleIds)
         if let selfToken = selfApplicationToken() {
             tokens.insert(selfToken)
         }
         return tokens
     }
+
+    private func ceoForceBlockedApplicationTokens() -> Set<ApplicationToken> {
+        var tokens = applicationTokens(for: FocusMonitorShared.ceoForceBlockedBundleIds)
+        tokens.subtract(ceoAllowedApplicationTokens())
+        return tokens
+    }
@@
         if ceoEnabled {
             let ceoAllowedTokens = ceoAllowedApplicationTokens()
-            store.shield.applications = nil
+            let ceoForcedTokens = ceoForceBlockedApplicationTokens()
+            store.shield.applications = ceoForcedTokens.isEmpty ? nil : ceoForcedTokens
             store.shield.applicationCategories = .all(except: ceoAllowedTokens)
             if #available(iOS 16.0, *) {
                 store.shield.webDomainCategories = .all(except: Set())
*** End Patch
