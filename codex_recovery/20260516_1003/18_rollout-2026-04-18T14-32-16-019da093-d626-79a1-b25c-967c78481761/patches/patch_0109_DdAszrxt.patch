*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/providers/focus_provider.dart
@@
   Future<void> _endSessionEarly() async {
     if (_endingSession) return;
     _endingSession = true;
     _timer?.cancel();
+    _timer = null;
@@
   void reset() {
     _timer?.cancel();
+    _timer = null;
     _state = FocusState.idle;
     _isFocusModeActive = false;
+    _sessionStartTime = null;
+    _sessionEndTime = null;
     _focusExitReadyAt = null;
     _remainingSeconds = focusDurationMinutes * 60;
@@
   void skip() {
     _timer?.cancel();
+    _timer = null;
     unawaited(_onTimerComplete());
   }
@@
   Future<void> _onTimerComplete() async {
     if (_endingSession) return;
     _endingSession = true;
     try {
       if (_state == FocusState.focusing || _state == FocusState.exitPending) {
+        final completedDurationMinutes =
+            (_sessionStartTime != null && _sessionEndTime != null)
+            ? _sessionEndTime!
+                  .difference(_sessionStartTime!)
+                  .inMinutes
+                  .clamp(1, 24 * 60)
+            : focusDurationMinutes;
         await _logSession(completed: true);
         _completedSessions++;
-        _totalFocusMinutesToday += focusDurationMinutes;
+        _totalFocusMinutesToday += completedDurationMinutes;
         unawaited(
           _statsEngine.recordFocusSessionCompleted(
-            durationMinutes: focusDurationMinutes,
+            durationMinutes: completedDurationMinutes,
             linkedTaskId: linkedTaskId,
           ),
         );
         unawaited(
-          _statsEngine.recordProductiveTime(minutes: focusDurationMinutes),
+          _statsEngine.recordProductiveTime(minutes: completedDurationMinutes),
         );
         await _resetAfterSession();
       } else {
*** End Patch
