*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/family_time_screen.dart
@@
-        if (session.startsAt != null) ...[
+        if (session.startsAt != null) ...[
           const SizedBox(height: 4),
+          Builder(
+            builder: (_) {
+              final startedAt = session.startsAt;
+              if (startedAt == null) return const SizedBox.shrink();
+              return Text(
+                'Started ${DateFormat('MMM d HH:mm').format(startedAt.toLocal())}',
+                style: AppTypography.body.copyWith(
+                  fontSize: 12,
+                  color: AppColors.tertiaryLabel,
+                ),
+              );
+            },
+          ),
-          Text(
-            'Started ${DateFormat('MMM d HH:mm').format(session.startsAt!.toLocal())}',
-            style: AppTypography.body.copyWith(
-              fontSize: 12,
-              color: AppColors.tertiaryLabel,
-            ),
-          ),
         ],
*** End Patch
