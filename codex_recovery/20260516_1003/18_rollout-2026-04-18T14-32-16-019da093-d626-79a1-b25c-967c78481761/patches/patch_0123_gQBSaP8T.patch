*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/providers/focus_provider.dart
@@
 import '../models/block_list_model.dart';
 import '../models/premium_models.dart';
 import '../models/task_models.dart';
 import '../repositories/feature_repository.dart';
 import '../repositories/premium_repository.dart';
@@
 import '../services/stats_engine.dart';
 import '../utils/app_logger.dart';
 import '../utils/domain_utils.dart';
+import '../../features/focus/focus_preparation/focus_preparation_models.dart';
@@
 class FocusProvider extends ChangeNotifier with WidgetsBindingObserver {
   FocusProvider() {
     WidgetsBinding.instance.addObserver(this);
-    Future.microtask(_restoreSessionFromPrefs);
+    Future.microtask(_restoreLocalState);
   }
@@
   static const String _prefsSessionTitle = 'focus_session_title_v1';
   static const String _prefsLinkedTaskId = 'focus_linked_task_id_v1';
+  static const String _prefsPreparationStatus = 'focus_preparation_status_v1';
@@
   String? _lastBlockingSyncError;
+  FocusPreparationStatus _preparationStatus = FocusPreparationStatus.notSeen;
@@
   String? get lastBlockingSyncError => _lastBlockingSyncError;
+  FocusPreparationStatus get preparationStatus => _preparationStatus;
+  bool get shouldShowPreparationFlowBeforeFocus =>
+      _preparationStatus == FocusPreparationStatus.notSeen;
@@
   bool get _isPersistableState =>
       _state == FocusState.focusing || _state == FocusState.exitPending;
+
+  Future<void> persistPreparationOutcome(
+    FocusPreparationFlowOutcome outcome,
+  ) async {
+    final next = outcome == FocusPreparationFlowOutcome.completed
+        ? FocusPreparationStatus.completed
+        : FocusPreparationStatus.skipped;
+    if (_preparationStatus == next) return;
+    _preparationStatus = next;
+    final prefs = await SharedPreferences.getInstance();
+    await prefs.setString(_prefsPreparationStatus, next.storageValue);
+    notifyListeners();
+  }
+
+  Future<void> _restoreLocalState() async {
+    await _restorePreparationStatus();
+    await _restoreSessionFromPrefs();
+  }
+
+  Future<void> _restorePreparationStatus() async {
+    final prefs = await SharedPreferences.getInstance();
+    _preparationStatus = FocusPreparationStatus.fromStorage(
+      prefs.getString(_prefsPreparationStatus),
+    );
+  }
*** End Patch
