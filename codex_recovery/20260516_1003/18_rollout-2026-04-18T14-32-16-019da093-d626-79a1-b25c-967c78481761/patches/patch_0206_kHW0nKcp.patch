*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/auth/signup_screen.dart
@@
 import '../../components/components.dart';
 import '../../core/models/onboarding_models.dart';
 import '../../core/providers/auth_provider.dart';
+import '../../core/providers/language_provider.dart';
 import '../../core/providers/theme_provider.dart';
 import '../../core/theme/app_colors.dart';
 import '../../core/theme/app_typography.dart';
 import 'auth_support.dart';
@@
 class _SignupScreenState extends State<SignupScreen> {
@@
   OnboardingSetupData? _setupData;
   bool _didHydrateSetup = false;
+
+  String _t(String key) => context.read<LanguageProvider>().t(key);
@@
   Future<void> _showError(dynamic error) async {
     if (!mounted) return;
     await showAuthDialog(
       context,
-      title: 'Account setup error',
+      title: _t('auth_account_setup_error_title'),
       message: humanizeAuthError(error),
     );
   }
@@
     if (name.length < 2) {
       await showAuthDialog(
         context,
-        title: 'Name required',
-        message: 'Please enter your name to create the account.',
+        title: _t('auth_name_required_title'),
+        message: _t('auth_name_required_message'),
       );
       return;
     }
     if (!isValidEmail(email)) {
       await showAuthDialog(
         context,
-        title: 'Invalid email',
-        message: 'Please enter a valid email address.',
+        title: _t('auth_invalid_email_title'),
+        message: _t('auth_invalid_email_message'),
       );
       return;
     }
     if (password.length < 8) {
       await showAuthDialog(
         context,
-        title: 'Weak password',
-        message: 'Use at least 8 characters for your password.',
+        title: _t('auth_weak_password_title'),
+        message: _t('auth_weak_password_message'),
       );
       return;
     }
     if (password != confirmPassword) {
       await showAuthDialog(
         context,
-        title: 'Password mismatch',
-        message: 'The password confirmation does not match.',
+        title: _t('auth_password_mismatch_title'),
+        message: _t('auth_password_mismatch_message'),
       );
       return;
     }
@@
       if (result.requiresEmailConfirmation) {
         await showAuthDialog(
           context,
-          title: 'Check your email',
-          message:
-              'Your account was created. Please confirm your email address before signing in.',
+          title: _t('auth_check_email_title'),
+          message: _t('auth_check_email_signup_message'),
         );
       }
*** End Patch
