*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/profile/profile_screen.dart
@@
   Future<void> _showLanguagePicker() async {
-    final current = context.read<LanguageProvider>().languageCode.toLowerCase();
+    final languageProvider = context.read<LanguageProvider>();
+    final current = languageProvider.languageCode.toLowerCase();
@@
     if (selected == null || selected == current) return;
     setState(() => _isSettingsSaving = true);
     try {
-      await context.read<LanguageProvider>().setLanguage(
+      await languageProvider.setLanguage(
         selected,
         persistToCloud: true,
       );
*** End Patch
