*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/rank/rank_screen.dart
@@
               child: Text(
-                tier.name,
+                _localizedTierName(tier.name),
                 style: AppTypography.largeTitle.copyWith(
@@
               child: Text(
-                'Unlocked today',
+                _t('rank_unlocked_today'),
                 style: AppTypography.footnote.copyWith(
@@
               child: Text(
-                'Unlocked locally — sync pending',
+                _t('rank_unlocked_local_pending_sync'),
                 style: AppTypography.footnote.copyWith(
@@
               Text(
-                next == null ? 'Top Rank Reached' : 'Path to ${next.name}',
+                next == null
+                    ? _t('rank_top_reached')
+                    : _t('rank_path_to').replaceAll(
+                        '{rank}',
+                        _localizedTierName(next.name),
+                      ),
                 style: AppTypography.callout.copyWith(
@@
                 Text(
-                  'Progress',
+                  _t('rank_progress'),
                   style: AppTypography.caption1.copyWith(
@@
             Text(
-              'You are currently at the highest tier.',
+              _t('rank_highest_tier'),
               style: AppTypography.footnote.copyWith(
@@
               child: Text(
-                'Your next rank requirements are completed. The server rank is still catching up.',
+                _t('rank_requirements_completed_sync_pending'),
                 style: AppTypography.footnote.copyWith(
@@
           Text(
-            'YOUR STATS',
+            _t('rank_your_stats'),
             style: AppTypography.overline.copyWith(
@@
-          _statRow('Days in App', '$_daysInApp'),
+          _statRow(_t('rank_days_in_app'), '$_daysInApp'),
           const SizedBox(height: 10),
-          _statRow('Streak', '${_streak?.currentStreak ?? 0} days'),
+          _statRow(
+            _t('rank_streak'),
+            _t(
+              'rank_days_value',
+            ).replaceAll('{count}', '${_streak?.currentStreak ?? 0}'),
+          ),
@@
-                color: label == 'Streak' ? AppColors.warning : AppColors.label,
+                color: label == _t('rank_streak')
+                    ? AppColors.warning
+                    : AppColors.label,
@@
                     Text(
-                      tier.name,
+                      _localizedTierName(tier.name),
                       style: AppTypography.callout.copyWith(
@@
                     Text(
-                      tier.requirements.join(' + '),
+                      tier.requirements.map(_localizedRequirement).join(' + '),
                       style: AppTypography.footnote.copyWith(
@@
                 Text(
-                  unlocked ? 'Unlocked' : 'Locked',
+                  unlocked ? _t('rank_unlocked') : _t('rank_locked'),
                   style: AppTypography.caption1.copyWith(
*** End Patch
