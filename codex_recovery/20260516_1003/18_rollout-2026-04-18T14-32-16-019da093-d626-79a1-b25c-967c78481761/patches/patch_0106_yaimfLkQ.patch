*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/profile/profile_screen.dart
@@
     try {
       await languageProvider.setLanguage(selected, persistToCloud: true);
       await _refreshSettingsOnly();
-      final cloudLanguage = (_appSettings?.languageCode ?? '').toLowerCase();
-      final savedToCloud = cloudLanguage == selected;
       await _showNotice(
         title: 'LANGUAGE_UPDATED',
-        message: savedToCloud
-            ? 'Preference saved to your profile settings.'
-            : 'Language updated on this device. Cloud profile sync is temporarily unavailable.',
+        message: 'Language preference updated successfully.',
       );
*** End Patch
