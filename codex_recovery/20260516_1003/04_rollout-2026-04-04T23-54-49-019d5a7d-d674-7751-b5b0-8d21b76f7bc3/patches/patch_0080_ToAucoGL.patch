*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/MainActivity.kt
@@
-import android.app.admin.DevicePolicyManager
+import android.app.admin.DevicePolicyManager
 import android.content.ComponentName
 import android.content.Context
 import android.content.Intent
 import android.content.pm.ApplicationInfo
+import android.content.pm.PackageManager
 import android.graphics.Bitmap
 import android.graphics.Canvas
 import android.graphics.drawable.BitmapDrawable
 import android.net.Uri
 import android.os.Build
@@
 import io.flutter.plugin.common.MethodChannel
 import io.flutter.embedding.android.FlutterActivity
 import io.flutter.embedding.engine.FlutterEngine
 import java.io.ByteArrayOutputStream
 import java.util.Locale
 
-class MainActivity: FlutterActivity() {
+open class MainActivity: FlutterActivity() {
     private val CHANNEL = "com.ceoos.app/focus"
@@
     private fun isSystemApp(applicationInfo: ApplicationInfo): Boolean {
         val flags = applicationInfo.flags
         return (flags and ApplicationInfo.FLAG_SYSTEM) != 0 ||
             (flags and ApplicationInfo.FLAG_UPDATED_SYSTEM_APP) != 0
     }
+
+    private fun parsePackageArray(rawJson: String?, key: String): Set<String> {
+        if (rawJson.isNullOrBlank()) return emptySet()
+        return try {
+            val json = org.json.JSONObject(rawJson)
+            val array = json.optJSONArray(key) ?: return emptySet()
+            buildSet {
+                for (index in 0 until array.length()) {
+                    val value = array.optString(index).trim()
+                    if (value.isNotEmpty()) add(value)
+                }
+            }
+        } catch (_: Exception) {
+            emptySet()
+        }
+    }
+
+    private fun parseStringArray(rawJson: String?): Set<String> {
+        if (rawJson.isNullOrBlank()) return emptySet()
+        return try {
+            val array = org.json.JSONArray(rawJson)
+            buildSet {
+                for (index in 0 until array.length()) {
+                    val value = array.optString(index).trim()
+                    if (value.isNotEmpty()) add(value)
+                }
+            }
+        } catch (_: Exception) {
+            emptySet()
+        }
+    }
+
+    private fun currentBlockedPackages(): Set<String> {
+        val prefs = prefs()
+        val blocked = mutableSetOf<String>()
+
+        if (
+            prefs.getBoolean("focus_shield_active", false) ||
+            prefs.getBoolean("ceo_shield_active", false)
+        ) {
+            blocked.addAll(
+                parsePackageArray(
+                    prefs.getString("flutter.active_block_list", null),
+                    "blocked_package_names",
+                ),
+            )
+        }
+
+        if (prefs.getBoolean("classic_shield_active", false)) {
+            blocked.addAll(
+                parsePackageArray(
+                    prefs.getString("classic_block_list", null),
+                    "blocked_package_names",
+                ),
+            )
+        }
+
+        if (prefs.getBoolean("classic_pause_shield_active", false)) {
+            blocked.addAll(
+                parsePackageArray(
+                    prefs.getString("classic_pause_block_list", null),
+                    "blocked_package_names",
+                ),
+            )
+        }
+
+        blocked.addAll(
+            parseStringArray(
+                prefs.getString("classic_daily_limit_exceeded_packages", null),
+            ),
+        )
+
+        return blocked
+    }
+
+    private fun isPackageSuspendedCompat(packageName: String): Boolean {
+        if (Build.VERSION.SDK_INT < Build.VERSION_CODES.N) return false
+        return try {
+            packageManager.isPackageSuspended(packageName)
+        } catch (_: PackageManager.NameNotFoundException) {
+            false
+        } catch (_: Exception) {
+            false
+        }
+    }
@@
     private fun listLaunchableApps(result: MethodChannel.Result) {
         try {
             val launchIntent = Intent(Intent.ACTION_MAIN).apply {
                 addCategory(Intent.CATEGORY_LAUNCHER)
             }
             val packageManager = packageManager
             val activities = packageManager.queryIntentActivities(launchIntent, 0)
             val launchableApps = linkedMapOf<String, Map<String, Any?>>()
+            val blockedPackages = currentBlockedPackages()
 
             for (resolveInfo in activities) {
                 val activityInfo = resolveInfo.activityInfo ?: continue
                 val pkg = activityInfo.packageName?.trim().orEmpty()
                 if (pkg.isEmpty() || pkg == packageName) continue
@@
                     launchableApps[pkg] = mapOf(
                         "name" to if (!label.isNullOrEmpty()) label else pkg,
                         "packageName" to pkg,
                         "icon" to iconBytes,
+                        "isBlocked" to (blockedPackages.contains(pkg) || isPackageSuspendedCompat(pkg)),
                     )
                 }
             }
@@
         }
     }
+
+    private fun launchExternalPackage(packageName: String, result: MethodChannel.Result) {
+        try {
+            val launchIntent = packageManager.getLaunchIntentForPackage(packageName)?.apply {
+                addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
+            }
+            if (launchIntent == null) {
+                result.error("APP_NOT_FOUND", "Launch intent unavailable for $packageName", null)
+                return
+            }
+            startActivity(launchIntent)
+            result.success(true)
+        } catch (error: Exception) {
+            result.error("APP_LAUNCH_FAILED", error.message, null)
+        }
+    }
@@
                 "listLaunchableApps" -> {
                     listLaunchableApps(result)
                 }
+                "launchExternalApp" -> {
+                    val packageName = call.argument<String>("packageName")?.trim().orEmpty()
+                    if (packageName.isEmpty()) {
+                        result.error("INVALID_PACKAGE", "Missing packageName", null)
+                    } else {
+                        launchExternalPackage(packageName, result)
+                    }
+                }
                 "getHardBlockStatus" -> {
                     result.success(HardAppBlockManager.status(this))
                 }
*** End Patch
