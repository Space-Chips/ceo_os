*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/AppBlockingService.kt
@@
     private val usageHandler = Handler(Looper.getMainLooper())
+    private val foregroundMonitorHandler = Handler(Looper.getMainLooper())
     private val usageCheckRunnable = object : Runnable {
         override fun run() {
             evaluateDailyLimitsNow()
             if (dailyLimitMinutesByPackage.isNotEmpty() || dailyLimitMinutesByWebsite.isNotEmpty()) {
                 usageHandler.postDelayed(this, 15000L)
             }
         }
     }
+    private val foregroundMonitorRunnable = object : Runnable {
+        override fun run() {
+            if (!hasActiveProtection()) {
+                foregroundMonitorHandler.removeCallbacks(this)
+                return
+            }
+
+            val rootPackage = rootInActiveWindow?.packageName?.toString()
+            if (!rootPackage.isNullOrBlank()) {
+                currentPackageName = rootPackage
+            }
+            refreshCurrentDomain()
+            enforceCurrentForegroundIfNeeded()
+            foregroundMonitorHandler.postDelayed(this, 350L)
+        }
+    }
@@
     private fun refreshShieldFlagsFromPrefs() {
@@
         }
     }
+
+    private fun hasActiveProtection(): Boolean {
+        return focusShieldActive ||
+            classicShieldActive ||
+            classicPauseShieldActive ||
+            ceoShieldActive ||
+            exceededDailyLimitPackages.isNotEmpty() ||
+            exceededDailyLimitWebsites.isNotEmpty()
+    }
@@
     private fun applyShieldState() {
-        if (
-            !focusShieldActive &&
-            !classicShieldActive &&
-            !classicPauseShieldActive &&
-            !ceoShieldActive &&
-            exceededDailyLimitPackages.isEmpty() &&
-            exceededDailyLimitWebsites.isEmpty()
-        ) {
+        if (!hasActiveProtection()) {
+            foregroundMonitorHandler.removeCallbacks(foregroundMonitorRunnable)
             removeBlockOverlay()
+            return
         }
+        foregroundMonitorHandler.removeCallbacks(foregroundMonitorRunnable)
+        foregroundMonitorHandler.post(foregroundMonitorRunnable)
     }
@@
     override fun onDestroy() {
         super.onDestroy()
         instance = null
         usageHandler.removeCallbacks(usageCheckRunnable)
+        foregroundMonitorHandler.removeCallbacks(foregroundMonitorRunnable)
         removeBlockOverlay()
     }
 }
*** End Patch
