*** Begin Patch
*** Update File: /Users/timo/ceo_os/ios/FocusActivityMonitor/FocusActivityMonitorExtension.swift
@@
     private let store = ManagedSettingsStore()
 
     private func sharedDefaults() -> UserDefaults? {
-        UserDefaults(suiteName: FocusMonitorShared.appGroupId)
+        guard FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: FocusMonitorShared.appGroupId) != nil else {
+            return nil
+        }
+        return UserDefaults(suiteName: FocusMonitorShared.appGroupId)
     }
*** End Patch