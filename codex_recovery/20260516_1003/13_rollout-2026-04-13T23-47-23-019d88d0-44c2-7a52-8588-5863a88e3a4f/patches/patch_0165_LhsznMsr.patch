*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/family_controls_local_store.dart
@@
   Future<BlockedWebsite?> createBlockedWebsiteRecord({
     required String createdBy,
-    required String urlDomain,
+    String? urlDomain,
   }) async {
-    final trimmedDomain = urlDomain.trim();
-    if (trimmedDomain.isEmpty) return null;
+    final trimmedDomain = urlDomain?.trim();
 
     final item = BlockedWebsite(
       id: _newId('blocked_website'),
       createdBy: createdBy,
-      urlDomain: trimmedDomain,
+      urlDomain: (trimmedDomain?.isNotEmpty ?? false) ? trimmedDomain : null,
       timeLimitMinutes: 0,
       createdAt: DateTime.now(),
     );
*** End Patch
