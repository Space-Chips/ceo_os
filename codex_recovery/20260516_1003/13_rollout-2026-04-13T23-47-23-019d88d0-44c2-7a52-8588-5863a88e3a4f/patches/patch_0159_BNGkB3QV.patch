*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
-  Future<IosFamilyActivitySelection?> encodeApplicationBundleSelection(
-    String bundleIdentifier,
-  ) async {
-    const method = 'encodeApplicationBundleSelection';
-    if (_shouldSkipMissingMethod(method)) return null;
-    try {
-      final result = await _channel.invokeMapMethod<String, dynamic>(method, {
-        'bundleIdentifier': bundleIdentifier,
-      });
-      if (result == null) return null;
-      final payload = (result['payload'] as String?)?.trim();
-      if (payload == null || payload.isEmpty) return null;
-      final appName = (result['appName'] as String?)?.trim();
-      final resolvedBundle =
-          (result['bundleIdentifier'] as String?)?.trim() ??
-          bundleIdentifier.trim();
-      return IosFamilyActivitySelection(
-        payload: payload,
-        appName: appName?.isNotEmpty == true ? appName : null,
-        bundleIdentifier: resolvedBundle.isNotEmpty ? resolvedBundle : null,
-      );
-    } on PlatformException catch (e) {
-      AppLogger.error("Failed to encode app bundle selection: '${e.message}'.");
-      return null;
-    } on MissingPluginException {
-      _markMissingMethod(method);
-      return null;
-    }
-  }
-
*** End Patch
