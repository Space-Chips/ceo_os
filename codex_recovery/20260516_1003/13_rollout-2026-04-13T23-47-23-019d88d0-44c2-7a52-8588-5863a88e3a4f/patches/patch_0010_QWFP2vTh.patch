*** Begin Patch
*** Update File: lib/features/setup/setup_flow_screen.dart
@@
 class _SetupFlowScreenState extends State<SetupFlowScreen>
     with WidgetsBindingObserver {
   final ValueNotifier<bool> _showStepSuccess = ValueNotifier(false);
+  final ValueNotifier<bool> _showReturnOverlay = ValueNotifier(false);
   final Set<SetupStep> _autoAdvancedSteps = <SetupStep>{};
   Timer? _successTimer;
+  Timer? _returnOverlayTimer;
@@
   void dispose() {
     WidgetsBinding.instance.removeObserver(this);
     _successTimer?.cancel();
+    _returnOverlayTimer?.cancel();
     _showStepSuccess.dispose();
+    _showReturnOverlay.dispose();
     super.dispose();
   }
@@
     if (!granted) {
       setState(() {});
       return;
     }
     await controller.clearPendingPermissionReturn();
-    _showStepSuccess.value = true;
-    _successTimer?.cancel();
-    _successTimer = Timer(const Duration(milliseconds: 650), () async {
-      if (!mounted) return;
-      _showStepSuccess.value = false;
-      await controller.advance();
-    });
+    _showReturnOverlay.value = true;
+    _returnOverlayTimer?.cancel();
+    _returnOverlayTimer = Timer(const Duration(milliseconds: 180), () {
+      if (!mounted) return;
+      _showReturnOverlay.value = false;
+      _showStepSuccess.value = true;
+      _successTimer?.cancel();
+      _successTimer = Timer(const Duration(milliseconds: 720), () async {
+        if (!mounted) return;
+        _showStepSuccess.value = false;
+        await controller.advance();
+      });
+    });
   }
@@
                 children: [
                   const _SetupDecorations(),
+                  ValueListenableBuilder<bool>(
+                    valueListenable: _showReturnOverlay,
+                    builder: (context, visible, _) {
+                      return AnimatedOpacity(
+                        duration: const Duration(milliseconds: 220),
+                        curve: Curves.easeOutCubic,
+                        opacity: visible ? 1 : 0,
+                        child: IgnorePointer(
+                          child: Container(
+                            decoration: BoxDecoration(
+                              gradient: LinearGradient(
+                                begin: Alignment.topCenter,
+                                end: Alignment.bottomCenter,
+                                colors: [
+                                  AppColors.accent.withValues(alpha: 0.12),
+                                  AppColors.themeGlow.withValues(alpha: 0.0),
+                                ],
+                              ),
+                            ),
+                          ),
+                        ),
+                      );
+                    },
+                  ),
                   AnimatedSwitcher(
-                    duration: const Duration(milliseconds: 240),
+                    duration: const Duration(milliseconds: 320),
+                    switchInCurve: Curves.easeOutCubic,
+                    switchOutCurve: Curves.easeInCubic,
+                    transitionBuilder: (child, animation) {
+                      final fade = CurvedAnimation(
+                        parent: animation,
+                        curve: Curves.easeOutCubic,
+                      );
+                      return FadeTransition(
+                        opacity: fade,
+                        child: SlideTransition(
+                          position: Tween<Offset>(
+                            begin: const Offset(0, 0.02),
+                            end: Offset.zero,
+                          ).animate(fade),
+                          child: child,
+                        ),
+                      );
+                    },
                     child: _buildStep(
                       context,
                       controller,
                       controller.currentStep,
                     ),
                   ),
*** End Patch