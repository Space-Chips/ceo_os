*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
   String _displayNameForBlockedApp(BlockedApp app) {
     final explicit = app.appName?.trim();
     if (explicit != null && explicit.isNotEmpty && !_isGenericAppName(explicit)) {
       return explicit;
     }
@@
     return explicit?.isNotEmpty == true ? explicit! : 'Unknown app';
   }
@@
   String _resolvedAppLabel(
     IosFamilyActivitySelection selection,
     Set<String> existingLabels,
   ) {
     final preferred = selection.preferredLabel?.trim();
-    if (preferred != null && preferred.isNotEmpty) return preferred;
+    if (preferred != null &&
+        preferred.isNotEmpty &&
+        !_isGenericAppName(preferred)) {
+      if (!existingLabels.contains(preferred)) return preferred;
+      var suffix = 2;
+      var candidate = '$preferred $suffix';
+      while (existingLabels.contains(candidate)) {
+        suffix += 1;
+        candidate = '$preferred $suffix';
+      }
+      return candidate;
+    }
     return _nextAppSelectionLabelFrom(existingLabels);
   }
@@
   Future<void> _addBlockedWebsite({String? overrideDomain}) async {
@@
     if (normalizedDomain == null) {
@@
       return;
     }
+
+    final alreadyBlocked = _blockedWebsites.any(
+      (site) => (site.urlDomain ?? '').trim().toLowerCase() == normalizedDomain,
+    );
+    if (alreadyBlocked) {
+      await _showNotice(
+        title: 'Already blocked',
+        message: '$normalizedDomain is already in your blocked websites.',
+      );
+      return;
+    }
@@
-      if (websitePayload == null || websitePayload.isEmpty) {
+      if (websitePayload == null || websitePayload.isEmpty) {
         final picked = await _focusService.openFamilyActivityWebsitePicker();
         final payloads = (picked ?? [])
             .where((item) => item.trim().isNotEmpty)
             .toList(growable: false);
         if (payloads.isEmpty) {
@@
-            message:
-                'Select the website in the Apple picker so WakeApp can enforce blocking on this iPhone.',
+            message:
+                'Open the Apple picker, select this website, then tap Done so WakeApp can enforce blocking on this iPhone.',
           );
           return;
         }
*** End Patch
