*** Begin Patch
*** Update File: lib/features/control_center_setup/control_center_setup_screen.dart
@@
   void _handleThemeSelect(ControlCenterTheme theme) {
-    _viewModel.selectTheme(theme);
-    // Reflect the theme live so the user sees their choice.
-    final themeProvider = context.read<ThemeProvider>();
-    if (themeProvider.currentPreset.id != theme.id) {
-      themeProvider.previewTheme(theme.id);
-    }
+    // Premium themes must never be selectable unless the user has Premium.
+    // Selecting a premium theme should only happen after the paywall flow.
+    if (theme.isPremium) {
+      _pendingPremiumTheme = theme;
+      _viewModel.openPremiumPopup();
+      return;
+    }
+
+    _viewModel.selectTheme(theme);
+    // Reflect the theme live so the user sees their choice.
+    final themeProvider = context.read<ThemeProvider>();
+    if (themeProvider.currentPreset.id != theme.id) {
+      themeProvider.previewTheme(theme.id);
+    }
   }
*** End Patch