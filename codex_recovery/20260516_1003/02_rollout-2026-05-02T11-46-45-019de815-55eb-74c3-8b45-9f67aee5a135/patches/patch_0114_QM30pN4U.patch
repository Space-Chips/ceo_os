*** Begin Patch
*** Update File: lib/core/services/home_widget_service.dart
@@
-class _CeoHabitsTableCard extends StatelessWidget {
-  final String title;
-  final LanguageProvider language;
-  final List<Habit> habits;
-  final List<DateTime> days;
-  final Map<String, Set<String>> completions;
-  final bool large;
-
-  const _CeoHabitsTableCard._({
-    required this.title,
-    required this.language,
-    required this.habits,
-    required this.days,
-    required this.completions,
-    required this.large,
-  });
-
-  factory _CeoHabitsTableCard.small({
-    required String title,
-    required LanguageProvider language,
-    required List<Habit> habits,
-    required List<DateTime> days,
-    required Map<String, Set<String>> completions,
-  }) {
-    return _CeoHabitsTableCard._(
-      title: title,
-      language: language,
-      habits: habits,
-      days: days,
-      completions: completions,
-      large: false,
-    );
-  }
-
-  factory _CeoHabitsTableCard.large({
-    required String title,
-    required LanguageProvider language,
-    required List<Habit> habits,
-    required List<DateTime> days,
-    required Map<String, Set<String>> completions,
-  }) {
-    return _CeoHabitsTableCard._(
-      title: title,
-      language: language,
-      habits: habits,
-      days: days,
-      completions: completions,
-      large: true,
-    );
-  }
-
-  @override
-  Widget build(BuildContext context) {
-    final maxHabits = large ? 5 : 3;
-    final items = habits.take(maxHabits).toList();
-    final isFr = language.languageCode.toLowerCase().trim() == 'fr';
-    final dayLabels = isFr
-        ? const ['L', 'M', 'M', 'J', 'V', 'S', 'D']
-        : const ['M', 'T', 'W', 'T', 'F', 'S', 'S'];
-
-    return _CeoBaseCard(
-      headerTitle: title,
-      headerSubtitle: language.t('widget_last_7_days'),
-      child: items.isEmpty
-          ? Column(
-              crossAxisAlignment: CrossAxisAlignment.start,
-              children: [
-                const SizedBox(height: 10),
-                _dayHeaderRow(dayLabels),
-                const SizedBox(height: 10),
-                _CeoEmptyState(
-                  title: language.t('widget_no_habits'),
-                  subtitle: language.t('widget_habits_table_hint'),
-                ),
-              ],
-            )
-          : Column(
-              crossAxisAlignment: CrossAxisAlignment.start,
-              children: [
-                const SizedBox(height: 10),
-                _dayHeaderRow(dayLabels),
-                const SizedBox(height: 10),
-                for (final habit in items)
-                  Padding(
-                    padding: const EdgeInsets.only(bottom: 10),
-                    child: _habitRow(habit),
-                  ),
-              ],
-            ),
-    );
-  }
-
-  Widget _dayHeaderRow(List<String> labels) {
-    return Row(
-      children: [
-        const SizedBox(width: 64),
-        for (final label in labels)
-          Expanded(
-            child: Center(
-              child: Text(
-                label,
-                style: AppTypography.overline.copyWith(
-                  fontSize: 11,
-                  letterSpacing: 1.6,
-                  color: AppColors.secondaryLabel.withValues(alpha: 0.75),
-                ),
-              ),
-            ),
-          ),
-      ],
-    );
-  }
-
-  Widget _habitRow(Habit habit) {
-    final title = habit.title.trim();
-    final compactTitle = title.length <= 14
-        ? title
-        : '${title.substring(0, 13)}…';
-    final doneDates = completions[habit.id] ?? const <String>{};
-    final today = DateTime.now();
-    final todayKey =
-        '${today.year}-${today.month.toString().padLeft(2, '0')}-${today.day.toString().padLeft(2, '0')}';
-    return Row(
-      children: [
-        SizedBox(
-          width: 64,
-          child: Text(
-            compactTitle,
-            maxLines: 1,
-            overflow: TextOverflow.ellipsis,
-            style: AppTypography.caption1.copyWith(
-              fontSize: 12,
-              fontWeight: FontWeight.w800,
-              color: AppColors.label.withValues(alpha: 0.92),
-            ),
-          ),
-        ),
-        for (final day in days)
-          Expanded(
-            child: Center(
-              child: _cellSymbol(
-                day: day,
-                doneDates: doneDates,
-                todayKey: todayKey,
-              ),
-            ),
-          ),
-      ],
-    );
-  }
-
-  Widget _cellSymbol({
-    required DateTime day,
-    required Set<String> doneDates,
-    required String todayKey,
-  }) {
-    final key =
-        '${day.year}-${day.month.toString().padLeft(2, '0')}-${day.day.toString().padLeft(2, '0')}';
-    final isDone = doneDates.contains(key);
-    if (isDone) {
-      return Text(
-        '✓',
-        style: AppTypography.callout.copyWith(
-          fontSize: 14,
-          fontWeight: FontWeight.w900,
-          color: AppColors.accent,
-          height: 1,
-        ),
-      );
-    }
-    if (key == todayKey) {
-      return Text(
-        '○',
-        style: AppTypography.callout.copyWith(
-          fontSize: 14,
-          fontWeight: FontWeight.w900,
-          color: AppColors.secondaryLabel.withValues(alpha: 0.85),
-          height: 1,
-        ),
-      );
-    }
-    return Text(
-      '✕',
-      style: AppTypography.callout.copyWith(
-        fontSize: 14,
-        fontWeight: FontWeight.w900,
-        color: AppColors.secondaryLabel.withValues(alpha: 0.55),
-        height: 1,
-      ),
-    );
-  }
-}
-
-class _CeoHabitsTodayCard extends StatelessWidget {
-  final String title;
-  final LanguageProvider language;
-  final List<Habit> habits;
-  final bool Function(String habitId) isDone;
-  final int completedHabits;
-  final int totalHabits;
-  final bool large;
-
-  const _CeoHabitsTodayCard._({
-    required this.title,
-    required this.language,
-    required this.habits,
-    required this.isDone,
-    required this.completedHabits,
-    required this.totalHabits,
-    required this.large,
-  });
-
-  factory _CeoHabitsTodayCard.small({
-    required String title,
-    required LanguageProvider language,
-    required List<Habit> habits,
-    required bool Function(String habitId) isDone,
-    required int completedHabits,
-    required int totalHabits,
-  }) {
-    return _CeoHabitsTodayCard._(
-      title: title,
-      language: language,
-      habits: habits,
-      isDone: isDone,
-      completedHabits: completedHabits,
-      totalHabits: totalHabits,
-      large: false,
-    );
-  }
-
-  factory _CeoHabitsTodayCard.large({
-    required String title,
-    required LanguageProvider language,
-    required List<Habit> habits,
-    required bool Function(String habitId) isDone,
-    required int completedHabits,
-    required int totalHabits,
-  }) {
-    return _CeoHabitsTodayCard._(
-      title: title,
-      language: language,
-      habits: habits,
-      isDone: isDone,
-      completedHabits: completedHabits,
-      totalHabits: totalHabits,
-      large: true,
-    );
-  }
-
-  @override
-  Widget build(BuildContext context) {
-    final maxItems = large ? 5 : 3;
-    final items = habits.take(maxItems).toList();
-    final progress = totalHabits == 0
-        ? 0.0
-        : (completedHabits / totalHabits).clamp(0.0, 1.0);
-    return _CeoBaseCard(
-      headerTitle: title,
-      headerSubtitle:
-          '${language.t('today')} • $completedHabits/$totalHabits ${language.t('widget_completed')}',
-      child: items.isEmpty
-          ? _CeoEmptyState(
-              title: language.t('widget_no_habits'),
-              subtitle: language.t('widget_add_habits_hint'),
-            )
-          : Column(
-              crossAxisAlignment: CrossAxisAlignment.start,
-              children: [
-                const SizedBox(height: 10),
-                for (final habit in items)
-                  Padding(
-                    padding: const EdgeInsets.only(bottom: 8),
-                    child: Row(
-                      children: [
-                        Text(
-                          isDone(habit.id) ? '✓' : '○',
-                          style: AppTypography.callout.copyWith(
-                            fontSize: 14,
-                            fontWeight: FontWeight.w900,
-                            color: isDone(habit.id)
-                                ? AppColors.accent
-                                : AppColors.secondaryLabel.withValues(
-                                    alpha: 0.85,
-                                  ),
-                            height: 1,
-                          ),
-                        ),
-                        const SizedBox(width: 8),
-                        Expanded(
-                          child: Text(
-                            habit.title,
-                            maxLines: 1,
-                            overflow: TextOverflow.ellipsis,
-                            style: AppTypography.callout.copyWith(
-                              fontSize: 13,
-                              fontWeight: FontWeight.w700,
-                              color: AppColors.label.withValues(alpha: 0.92),
-                            ),
-                          ),
-                        ),
-                      ],
-                    ),
-                  ),
-                const Spacer(),
-                _progressBar(progress: progress),
-              ],
-            ),
-    );
-  }
-}
-
 class _CeoShortcutCard extends StatelessWidget {
*** End Patch