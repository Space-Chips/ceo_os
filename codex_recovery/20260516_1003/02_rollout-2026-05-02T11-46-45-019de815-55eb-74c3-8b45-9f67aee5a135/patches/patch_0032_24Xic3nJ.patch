*** Begin Patch
*** Add File: ios/CeoWidgets/CeoHomeWidget.swift
+import SwiftUI
+import WidgetKit
+
+@available(iOSApplicationExtension 16.0, *)
+private let appGroupId = "group.com.wakeapp.ceoos"
+
+@available(iOSApplicationExtension 16.0, *)
+private let keyDefaultMode = "ceo_widget_default_mode"
+
+@available(iOSApplicationExtension 16.0, *)
+private let keyTodoSmall = "ceo_widget_todo_small"
+@available(iOSApplicationExtension 16.0, *)
+private let keyTodoLarge = "ceo_widget_todo_large"
+@available(iOSApplicationExtension 16.0, *)
+private let keyDashboardSmall = "ceo_widget_dashboard_small"
+@available(iOSApplicationExtension 16.0, *)
+private let keyDashboardLarge = "ceo_widget_dashboard_large"
+@available(iOSApplicationExtension 16.0, *)
+private let keyHabitsSmall = "ceo_widget_habits_small"
+@available(iOSApplicationExtension 16.0, *)
+private let keyHabitsLarge = "ceo_widget_habits_large"
+
+enum CeoWidgetResolvedMode: String {
+  case todo
+  case dashboard
+  case habits
+}
+
+@available(iOSApplicationExtension 16.0, *)
+struct CeoHomeWidget: Widget {
+  let kind: String = "com.wakeapp.ceoos.widget"
+
+  var body: some WidgetConfiguration {
+    makeWidgetConfiguration()
+  }
+
+  @MainActor
+  private func makeWidgetConfiguration() -> some WidgetConfiguration {
+    if #available(iOSApplicationExtension 17.0, *) {
+      return CeoHomeWidgetAppIntentConfiguration(kind: kind).configuration
+    } else {
+      return StaticConfiguration(kind: kind, provider: CeoHomeWidgetStaticProvider()) { entry in
+        CeoHomeWidgetView(
+          resolvedMode: entry.resolvedMode,
+          widgetFamily: entry.family,
+        )
+      }
+      .configurationDisplayName("WakeApp")
+      .description("To‑do, dashboard, or habits — right on your Home Screen.")
+      .supportedFamilies([.systemSmall, .systemMedium, .systemLarge])
+    }
+  }
+}
+
+// MARK: - iOS 16 Static widget (no configuration UI)
+
+@available(iOSApplicationExtension 16.0, *)
+struct CeoHomeWidgetStaticEntry: TimelineEntry {
+  let date: Date
+  let resolvedMode: CeoWidgetResolvedMode
+  let family: WidgetFamily
+}
+
+@available(iOSApplicationExtension 16.0, *)
+struct CeoHomeWidgetStaticProvider: TimelineProvider {
+  func placeholder(in context: Context) -> CeoHomeWidgetStaticEntry {
+    CeoHomeWidgetStaticEntry(
+      date: Date(),
+      resolvedMode: .dashboard,
+      family: context.family,
+    )
+  }
+
+  func getSnapshot(in context: Context, completion: @escaping (CeoHomeWidgetStaticEntry) -> Void) {
+    completion(
+      CeoHomeWidgetStaticEntry(
+        date: Date(),
+        resolvedMode: resolveAutoModeFromDefaults(),
+        family: context.family,
+      ),
+    )
+  }
+
+  func getTimeline(in context: Context, completion: @escaping (Timeline<CeoHomeWidgetStaticEntry>) -> Void) {
+    let entry = CeoHomeWidgetStaticEntry(
+      date: Date(),
+      resolvedMode: resolveAutoModeFromDefaults(),
+      family: context.family,
+    )
+    completion(Timeline(entries: [entry], policy: .after(Date().addingTimeInterval(60))))
+  }
+
+  private func resolveAutoModeFromDefaults() -> CeoWidgetResolvedMode {
+    let defaults = UserDefaults(suiteName: appGroupId) ?? .standard
+    let raw = defaults.string(forKey: keyDefaultMode) ?? "dashboard"
+    switch raw {
+    case "todo": return .todo
+    case "habits": return .habits
+    default: return .dashboard
+    }
+  }
+}
+
+// MARK: - Shared View
+
+@available(iOSApplicationExtension 16.0, *)
+struct CeoHomeWidgetView: View {
+  let resolvedMode: CeoWidgetResolvedMode
+  let widgetFamily: WidgetFamily
+
+  var body: some View {
+    let large = widgetFamily != .systemSmall
+    let imageKey = imageKeyFor(resolvedMode, large: large)
+    let path = sharedDefaults().string(forKey: imageKey)
+
+    Group {
+      if let path, let image = UIImage(contentsOfFile: path) {
+        Image(uiImage: image)
+          .resizable()
+          .scaledToFill()
+      } else {
+        ZStack {
+          Color.black.opacity(0.6)
+          VStack(spacing: 10) {
+            Text("WakeApp")
+              .font(.headline)
+              .foregroundStyle(.white)
+            Text("Open the app to generate widget previews.")
+              .font(.caption)
+              .foregroundStyle(.white.opacity(0.7))
+              .multilineTextAlignment(.center)
+              .padding(.horizontal, 10)
+          }
+        }
+      }
+    }
+    .applyWidgetBackground()
+    .widgetURL(URL(string: routeUrlFor(resolvedMode)))
+    .clipped()
+  }
+
+  private func imageKeyFor(_ mode: CeoWidgetResolvedMode, large: Bool) -> String {
+    switch mode {
+    case .todo: return large ? keyTodoLarge : keyTodoSmall
+    case .habits: return large ? keyHabitsLarge : keyHabitsSmall
+    case .dashboard: return large ? keyDashboardLarge : keyDashboardSmall
+    }
+  }
+
+  private func routeUrlFor(_ mode: CeoWidgetResolvedMode) -> String {
+    switch mode {
+    case .todo: return "ceoos:///tasks"
+    case .habits: return "ceoos:///habits"
+    case .dashboard: return "ceoos:///home"
+    }
+  }
+
+  private func sharedDefaults() -> UserDefaults {
+    UserDefaults(suiteName: appGroupId) ?? .standard
+  }
+}
+
+@available(iOSApplicationExtension 16.0, *)
+private extension View {
+  @ViewBuilder
+  func applyWidgetBackground() -> some View {
+    if #available(iOSApplicationExtension 17.0, *) {
+      self.containerBackground(.fill.tertiary, for: .widget)
+    } else {
+      self.padding()
+    }
+  }
+}
+
+// MARK: - iOS 17 AppIntent configuration
+
+@available(iOSApplicationExtension 17.0, *)
+private struct CeoHomeWidgetAppIntentConfiguration {
+  let kind: String
+
+  var configuration: some WidgetConfiguration {
+    AppIntentConfiguration(kind: kind, intent: CeoWidgetIntent.self, provider: CeoWidgetProvider()) { entry in
+      CeoHomeWidgetView(
+        resolvedMode: entry.resolvedMode,
+        widgetFamily: entry.family,
+      )
+    }
+    .configurationDisplayName("WakeApp")
+    .description("To‑do, dashboard, or habits — right on your Home Screen.")
+    .supportedFamilies([.systemSmall, .systemMedium, .systemLarge])
+  }
+}
+
+@available(iOSApplicationExtension 17.0, *)
+import AppIntents
+
+@available(iOSApplicationExtension 17.0, *)
+enum CeoWidgetMode: String, AppEnum {
+  case auto
+  case todo
+  case dashboard
+  case habits
+
+  static var typeDisplayRepresentation: TypeDisplayRepresentation = "Widget View"
+  static var caseDisplayRepresentations: [CeoWidgetMode: DisplayRepresentation] = [
+    .auto: "Auto",
+    .todo: "To‑Do",
+    .dashboard: "Dashboard",
+    .habits: "Habits",
+  ]
+}
+
+@available(iOSApplicationExtension 17.0, *)
+struct CeoWidgetIntent: AppIntent, WidgetConfigurationIntent {
+  static var title: LocalizedStringResource = "Widget"
+  static var description = IntentDescription("Choose what the widget shows.")
+
+  @Parameter(title: "View")
+  var mode: CeoWidgetMode
+
+  init() {
+    self.mode = .auto
+  }
+
+  func perform() async throws -> some IntentResult {
+    .result()
+  }
+}
+
+@available(iOSApplicationExtension 17.0, *)
+struct CeoWidgetEntry: TimelineEntry {
+  let date: Date
+  let resolvedMode: CeoWidgetResolvedMode
+  let family: WidgetFamily
+}
+
+@available(iOSApplicationExtension 17.0, *)
+struct CeoWidgetProvider: AppIntentTimelineProvider {
+  func placeholder(in context: Context) -> CeoWidgetEntry {
+    CeoWidgetEntry(date: Date(), resolvedMode: .dashboard, family: context.family)
+  }
+
+  func snapshot(for configuration: CeoWidgetIntent, in context: Context) async -> CeoWidgetEntry {
+    CeoWidgetEntry(
+      date: Date(),
+      resolvedMode: resolve(configuration.mode),
+      family: context.family,
+    )
+  }
+
+  func timeline(for configuration: CeoWidgetIntent, in context: Context) async -> Timeline<CeoWidgetEntry> {
+    let entry = CeoWidgetEntry(
+      date: Date(),
+      resolvedMode: resolve(configuration.mode),
+      family: context.family,
+    )
+    return Timeline(entries: [entry], policy: .after(Date().addingTimeInterval(60)))
+  }
+
+  private func resolve(_ mode: CeoWidgetMode) -> CeoWidgetResolvedMode {
+    switch mode {
+    case .todo: return .todo
+    case .habits: return .habits
+    case .dashboard: return .dashboard
+    case .auto:
+      let defaults = UserDefaults(suiteName: appGroupId) ?? .standard
+      let raw = defaults.string(forKey: keyDefaultMode) ?? "dashboard"
+      switch raw {
+      case "todo": return .todo
+      case "habits": return .habits
+      default: return .dashboard
+      }
+    }
+  }
+}
+
*** End Patch