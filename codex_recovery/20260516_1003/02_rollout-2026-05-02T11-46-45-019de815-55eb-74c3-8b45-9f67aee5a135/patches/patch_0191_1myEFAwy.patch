*** Begin Patch
*** Update File: ios/CeoWidgets/CeoHomeWidget.swift
@@
     case .focus:
       FocusSmallNativeView(
-        minutes: max(5, defaults.integer(forKey: keyFocusDurationMinutes)),
+        minutes: max(5, (defaults.object(forKey: keyFocusDurationMinutes) as? Int) ?? 25),
       )
     case .blackout:
       BlackoutSmallNativeView(
         isOn: defaults.object(forKey: keyBlackoutIsOn) as? Bool ?? false,
-        durationMinutes: max(15, defaults.integer(forKey: keyBlackoutDurationMinutes)),
+        durationMinutes: max(15, (defaults.object(forKey: keyBlackoutDurationMinutes) as? Int) ?? 120),
         untilText: defaults.string(forKey: keyBlackoutUntil),
         blockedApps: max(0, defaults.integer(forKey: keyBlackoutBlockedApps)),
       )
@@
   private func decodeIntGrid(_ raw: String?) -> [[Int]] {
     guard let raw, !raw.isEmpty, let data = raw.data(using: .utf8) else { return [] }
     do {
-      let json = try JSONSerialization.jsonObject(with: data, options: [])
-      return json as? [[Int]] ?? []
+      let json = try JSONSerialization.jsonObject(with: data, options: [])
+      guard let rows = json as? [[Any]] else { return [] }
+      return rows.map { row in
+        row.compactMap { cell in
+          if let i = cell as? Int { return i }
+          if let n = cell as? NSNumber { return n.intValue }
+          return nil
+        }
+      }
     } catch {
       return []
     }
   }
*** End Patch