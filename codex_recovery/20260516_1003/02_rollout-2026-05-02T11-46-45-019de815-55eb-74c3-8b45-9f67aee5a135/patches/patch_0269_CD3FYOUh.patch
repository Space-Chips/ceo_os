*** Begin Patch
*** Update File: lib/features/home/home_screen.dart
@@
   @override
   Widget build(BuildContext context) {
     final taskProvider = context.watch<TaskProvider>();
     final habitProvider = context.watch<HabitProvider>();
     final ceoProvider = context.watch<CeoModeProvider>();
     final language = context.watch<LanguageProvider>();
+    final auth = context.watch<AuthProvider>();
+
+    final shouldForceSetupScreen =
+        !_controlCenterConfiguration.setupCompleted && auth.wasJustSignedUp;
 
     final pendingTasks = taskProvider.tasks.where((t) => !t.completed).length;
     final completedTasks = taskProvider.tasks.where((t) => t.completed).length;
@@
     final dataSignature =
         '${taskProvider.tasks.length}:$completedTasks:${taskProvider.events.length}:'
         '${habitProvider.habits.length}:${habitProvider.completedToday}:'
         '${taskProvider.isLoading}:${habitProvider.isLoading}';
     _scheduleRefreshFromSignature(dataSignature);
 
+    if (shouldForceSetupScreen) {
+      // Spec: for a fresh signup, the first thing the user sees must be the
+      // start of the configuration flow — never a partially hydrated dashboard.
+      return const CupertinoPageScaffold(
+        backgroundColor: AppColors.background,
+        child: SafeArea(
+          child: Center(child: CupertinoActivityIndicator()),
+        ),
+      );
+    }
+
     return CupertinoPageScaffold(
       backgroundColor: AppColors.background,
       child: AmbientBackdrop(
         child: SafeArea(
           child: FutureBuilder<DashboardSnapshot>(
*** End Patch