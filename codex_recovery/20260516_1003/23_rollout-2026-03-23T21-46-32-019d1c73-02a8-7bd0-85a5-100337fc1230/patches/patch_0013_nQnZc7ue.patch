*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/screen_time_manager_screen.dart
@@
 import 'package:flutter/cupertino.dart';
 import 'package:go_router/go_router.dart';
 import 'package:provider/provider.dart';
 
 import '../../components/components.dart';
-import '../../core/models/advanced_stats_models.dart';
 import '../../core/models/settings_models.dart';
-import '../../core/models/user_models.dart';
 import '../../core/providers/focus_provider.dart';
-import '../../core/repositories/advanced_stats_repository.dart';
 import '../../core/repositories/feature_repository.dart';
-import '../../core/repositories/user_repository.dart';
 import '../../core/services/classic_blocking_coordinator.dart';
 import '../../core/theme/app_colors.dart';
 import '../../core/theme/app_typography.dart';
-import '../../core/widgets/rank_art.dart';
@@
 class _ScreenTimeManagerScreenState extends State<ScreenTimeManagerScreen> {
   final FeatureRepository _featureRepository = FeatureRepository();
-  final AdvancedStatsRepository _advancedStatsRepository =
-      AdvancedStatsRepository();
-  final UserRepository _userRepository = UserRepository();
   final ClassicBlockingCoordinator _classicBlockingCoordinator =
       ClassicBlockingCoordinator();
 
-  List<Map<String, dynamic>> _logs = const [];
-  List<DailyStats> _recentDailyStats = const [];
-  UserRank? _rank;
-  WinStreak? _streak;
   int _classicBlockedTargets = 0;
+  int _activePauseCount = 0;
+  int _plannedPauseCount = 0;
   bool _loading = true;
@@
     try {
       final focus = context.read<FocusProvider>();
       await focus.loadInitialData();
-      await focus.refreshScreenTime();
-      await _userRepository.refreshLeaderboardForMe();
 
       final results = await Future.wait<dynamic>([
-        _featureRepository.getScreenTimeLogs(days: 14),
-        _advancedStatsRepository.getRecentDailyStats(days: 7),
-        _userRepository.getUserRank(),
-        _userRepository.getWinStreak(),
         _featureRepository.getBlockedApps(),
         _featureRepository.getBlockedWebsites(),
+        _featureRepository.getRestPeriods(),
       ]);
-      final logs = results[0] as List<Map<String, dynamic>>;
-      final recentDailyStats = results[1] as List<DailyStats>;
-      final rank = results[2] as UserRank?;
-      final streak = results[3] as WinStreak?;
-      final blockedApps = results[4] as List<BlockedApp>;
-      final blockedSites = results[5] as List<BlockedWebsite>;
+      final blockedApps = results[0] as List<BlockedApp>;
+      final blockedSites = results[1] as List<BlockedWebsite>;
+      final restPeriods = results[2] as List<RestPeriod>;
       final adultShieldEnabled = blockedSites.any(
         (site) =>
             (site.urlDomain ?? '').trim() ==
@@
                   FeatureRepository.adultContentShieldMarker,
               )
               .length +
           (adultShieldEnabled ? 1 : 0);
+      final now = DateTime.now();
+      final activePauseCount = restPeriods
+          .where((period) => _isRestActive(period, now))
+          .length;
+      final plannedPauseCount = restPeriods
+          .where((period) => _isScheduledRest(period, now))
+          .length;
       await _classicBlockingCoordinator.syncNativeState(
         apps: blockedApps,
         websites: blockedSites
@@
                   FeatureRepository.adultContentShieldMarker,
               )
               .toList(growable: false),
-        restPeriods: await _featureRepository.getRestPeriods(),
+        restPeriods: restPeriods,
       );
 
       if (!mounted) return;
       setState(() {
-        _logs = logs;
-        _recentDailyStats = recentDailyStats;
-        _rank = rank;
-        _streak = streak;
         _classicBlockedTargets = classicBlockedTargets;
+        _activePauseCount = activePauseCount;
+        _plannedPauseCount = plannedPauseCount;
         _loading = false;
       });
@@
       setState(() => _loading = false);
     }
   }
-
-  int _todayMinutesFromLogs() {
-    final todayKey = DateTime.now().toIso8601String().split('T').first;
-    var seconds = 0;
-    for (final log in _logs) {
-      final date = (log['date'] ?? '').toString().split('T').first;
-      if (date != todayKey) continue;
-      seconds += (log['duration_seconds'] as num?)?.toInt() ?? 0;
-    }
-    return seconds ~/ 60;
-  }
-
-  int _todayMinutesFromDailyStats() {
-    if (_recentDailyStats.isEmpty) return 0;
-    final today = DateTime.now();
-    final todayKey = DateTime(today.year, today.month, today.day);
-    for (final stat in _recentDailyStats) {
-      final statDay = DateTime(stat.date.year, stat.date.month, stat.date.day);
-      if (statDay == todayKey) return stat.screenTimeMinutes;
-    }
-    return 0;
-  }
-
-  int _averageDailyMinutes({int days = 7}) {
-    if (_logs.isEmpty) return 0;
-    final byDay = <String, int>{};
-
-    for (final log in _logs) {
-      final rawDate = (log['date'] ?? '').toString();
-      if (rawDate.isEmpty) continue;
-      final day = rawDate.split('T').first;
-      byDay[day] =
-          (byDay[day] ?? 0) + ((log['duration_seconds'] as num?)?.toInt() ?? 0);
-    }
-
-    if (byDay.isEmpty) return 0;
-    final sortedDays = byDay.keys.toList()..sort((a, b) => b.compareTo(a));
-    final subset = sortedDays.take(days).toList();
-    if (subset.isEmpty) return 0;
-
-    final totalSeconds = subset.fold<int>(
-      0,
-      (sum, day) => sum + (byDay[day] ?? 0),
-    );
-    return (totalSeconds / 60 / subset.length).round();
-  }
-
-  int _averageDailyMinutesFromDailyStats({int days = 7}) {
-    if (_recentDailyStats.isEmpty) return 0;
-    final sorted = [..._recentDailyStats]
-      ..sort((a, b) => b.date.compareTo(a.date));
-    final subset = sorted.take(days).toList(growable: false);
-    if (subset.isEmpty) return 0;
-    final totalMinutes = subset.fold<int>(
-      0,
-      (sum, stat) => sum + stat.screenTimeMinutes,
-    );
-    return (totalMinutes / subset.length).round();
-  }
+
+  bool _isRestActive(RestPeriod period, DateTime now) {
+    final start = period.startTime;
+    final end = period.endTime;
+    if (end != null && !end.isAfter(now)) return false;
+    if (start != null) {
+      return !start.isAfter(now);
+    }
+    return period.active;
+  }
+
+  bool _isScheduledRest(RestPeriod period, DateTime now) {
+    final start = period.startTime;
+    final end = period.endTime;
+    if (start == null || end == null) return false;
+    if (!end.isAfter(start)) return false;
+    return start.isAfter(now);
+  }
*** End Patch
