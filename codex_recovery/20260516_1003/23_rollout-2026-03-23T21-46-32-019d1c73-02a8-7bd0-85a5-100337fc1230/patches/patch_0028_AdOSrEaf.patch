*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/config/apple_review_compliance.dart
@@
   static bool get exposesLocalOnlyFamilyControls =>
       isIosFamilyControlsReviewBuild;
 
+  static bool get allowCloudStatsAndAnalytics =>
+      !isIosFamilyControlsReviewBuild;
+
   static bool get allowAdvancedStats =>
       !isIosFamilyControlsReviewBuild;
 
   static bool get allowSocialScreenTimeSurfaces =>
       !isIosFamilyControlsReviewBuild;
*** Update File: /Users/timo/ceo_os/lib/core/services/stats_engine.dart
@@
   static const Map<String, int> _recoveryMinutesByCategory = {
@@
     'default': 4,
   };
+
+  bool get _cloudStatsDisabled =>
+      !AppleReviewCompliance.allowCloudStatsAndAnalytics;
@@
   Future<void> trackEvent(
     String eventType, {
@@
     String? sourceKey,
     bool recompute = true,
   }) async {
+    if (_cloudStatsDisabled) return;
     if (!StatsEventType.values.contains(eventType)) return;
     if (AppleReviewCompliance.exposesLocalOnlyFamilyControls &&
         _isSensitiveFamilyControlsEvent(eventType)) {
       return;
     }
@@
   Future<void> recomputeForDate(DateTime date) async {
+    if (_cloudStatsDisabled) return;
     final uid = _repository.currentUserId;
     if (uid == null) return;
@@
   Future<void> backfillRecent({int days = 30}) async {
+    if (_cloudStatsDisabled) return;
     final now = DateTime.now();
@@
   Future<void> hydrateHistoricalStats({int days = 90}) async {
+    if (_cloudStatsDisabled) return;
     final uid = _repository.currentUserId;
     if (uid == null) return;
@@
   Future<void> hydrateHistoricalStatsIfNeeded({
     int days = 90,
     Duration minInterval = const Duration(minutes: 20),
   }) {
+    if (_cloudStatsDisabled) return Future.value();
     final uid = _repository.currentUserId;
     if (uid == null) return Future.value();
@@
   Future<void> recordRankChangedFromSnapshot() async {
+    if (_cloudStatsDisabled) return;
     final snapshot = await _repository.getRankSnapshot();
     if (snapshot == null) return;
@@
   Future<AdvancedStatsSnapshot> buildSnapshot({DateTime? now}) async {
+    if (_cloudStatsDisabled) {
+      return const AdvancedStatsSnapshot(
+        daily: null,
+        weekly: null,
+        monthly: null,
+        lifetime: null,
+        insights: [],
+        heatmap: [],
+        todayCard: null,
+        weeklyCard: null,
+        ceoCard: null,
+        milestoneCards: [],
+        heatmapCard: null,
+        highlights: [],
+      );
+    }
     final today = _day(now ?? DateTime.now());
*** Update File: /Users/timo/ceo_os/lib/core/repositories/advanced_stats_repository.dart
@@
 import 'package:supabase_flutter/supabase_flutter.dart';
 
+import '../config/apple_review_compliance.dart';
 import '../models/advanced_stats_models.dart';
 import '../models/stats_event_models.dart';
 import '../services/supabase_service.dart';
@@
   SupabaseClient get _client => _supabaseService.client;
   String? get _currentUserId => _client.auth.currentUser?.id;
   String? get currentUserId => _currentUserId;
+  bool get _cloudStatsDisabled =>
+      !AppleReviewCompliance.allowCloudStatsAndAnalytics;
 
   Future<void> logEvent(StatsEventInput input) async {
+    if (_cloudStatsDisabled) return;
     final uid = _currentUserId;
     if (uid == null) return;
@@
   Future<List<StatsEvent>> getEventsForRange({
     required DateTime start,
     required DateTime end,
   }) async {
+    if (_cloudStatsDisabled) return const [];
     final uid = _currentUserId;
     if (uid == null) return const [];
@@
   Future<void> upsertDailyStats(DailyStats stats) async {
+    if (_cloudStatsDisabled) return;
     await _client.from('advanced_daily_stats').upsert(
       stats.toJson(),
@@
   Future<void> upsertWeeklyStats(WeeklyStats stats) async {
+    if (_cloudStatsDisabled) return;
     await _client.from('advanced_weekly_stats').upsert(
       stats.toJson(),
@@
   Future<void> upsertMonthlyStats(MonthlyStats stats) async {
+    if (_cloudStatsDisabled) return;
     await _client.from('advanced_monthly_stats').upsert(
       stats.toJson(),
@@
   Future<void> upsertLifetimeStats(LifetimeStats stats) async {
+    if (_cloudStatsDisabled) return;
     await _client.from('advanced_lifetime_stats').upsert(
       stats.toJson(),
@@
   Future<DailyStats?> getDailyStats(DateTime date) async {
+    if (_cloudStatsDisabled) return null;
     final uid = _currentUserId;
     if (uid == null) return null;
@@
   Future<List<DailyStats>> getDailyStatsRange({
     required DateTime start,
     required DateTime end,
   }) async {
+    if (_cloudStatsDisabled) return const [];
     final uid = _currentUserId;
     if (uid == null) return const [];
@@
   Future<WeeklyStats?> getWeeklyStats(DateTime weekStart) async {
+    if (_cloudStatsDisabled) return null;
     final uid = _currentUserId;
     if (uid == null) return null;
@@
   Future<MonthlyStats?> getMonthlyStats(String month) async {
+    if (_cloudStatsDisabled) return null;
     final uid = _currentUserId;
     if (uid == null) return null;
@@
   Future<LifetimeStats?> getLifetimeStats() async {
+    if (_cloudStatsDisabled) return null;
     final uid = _currentUserId;
     if (uid == null) return null;
@@
   Future<Map<String, dynamic>?> getRankSnapshot() async {
+    if (_cloudStatsDisabled) return null;
     final uid = _currentUserId;
     if (uid == null) return null;
@@
   Future<double> getLeaderboardPercentile() async {
+    if (_cloudStatsDisabled) return 0;
     final uid = _currentUserId;
     if (uid == null) return 0;
@@
   Future<List<DailyStats>> getRecentDailyStats({int days = 30}) async {
+    if (_cloudStatsDisabled) return const [];
     final end = DateTime.now();
     final start = end.subtract(Duration(days: days - 1));
     return getDailyStatsRange(start: start, end: end);
@@
   Future<List<Map<String, dynamic>>> getSourceFocusSessions({
     required DateTime start,
     required DateTime end,
   }) async {
+    if (_cloudStatsDisabled) return const [];
     final uid = _currentUserId;
     if (uid == null) return const [];
@@
   Future<List<Map<String, dynamic>>> getSourceCeoSessions({
     required DateTime start,
     required DateTime end,
   }) async {
+    if (_cloudStatsDisabled) return const [];
     final uid = _currentUserId;
     if (uid == null) return const [];
@@
   Future<List<Map<String, dynamic>>> getSourceHabits() async {
+    if (_cloudStatsDisabled) return const [];
     final uid = _currentUserId;
     if (uid == null) return const [];
@@
   Future<List<Map<String, dynamic>>> getSourceHabitCompletions({
     required DateTime start,
     required DateTime end,
   }) async {
+    if (_cloudStatsDisabled) return const [];
     final uid = _currentUserId;
     if (uid == null) return const [];
*** Update File: /Users/timo/ceo_os/lib/core/repositories/insights_repository.dart
@@
-      final percentileFuture = _client
-          .from('leaderboard_entries')
-          .select('percentile')
-          .eq('created_by', _currentUserId)
-          .maybeSingle();
+      final Future<dynamic> percentileFuture =
+          AppleReviewCompliance.allowSocialScreenTimeSurfaces
+          ? _client
+                .from('leaderboard_entries')
+                .select('percentile')
+                .eq('created_by', _currentUserId)
+                .maybeSingle()
+          : Future.value(null);
*** Update File: /Users/timo/ceo_os/lib/core/repositories/user_repository.dart
@@
 import 'package:supabase_flutter/supabase_flutter.dart';
+import '../config/apple_review_compliance.dart';
 import '../models/user_models.dart';
 import 'focus_repository.dart';
 import '../services/supabase_service.dart';
@@
   SupabaseClient get _client => _supabaseService.client;
   String get _currentUserId => _client.auth.currentUser!.id;
+  bool get _allowSocialScreenTimeSurfaces =>
+      AppleReviewCompliance.allowSocialScreenTimeSurfaces;
@@
-      await _client.from('leaderboard_entries').upsert({
-        'created_by': _currentUserId,
-        'rank_level': 1,
-        'rank_name': 'Bronze',
-        'win_streak': streak,
-        'last_sync_date': DateTime.now().toIso8601String(),
-      });
+      if (_allowSocialScreenTimeSurfaces) {
+        await _client.from('leaderboard_entries').upsert({
+          'created_by': _currentUserId,
+          'rank_level': 1,
+          'rank_name': 'Bronze',
+          'win_streak': streak,
+          'last_sync_date': DateTime.now().toIso8601String(),
+        });
+      }
@@
   Future<List<LeaderboardEntry>> getLeaderboard() async {
+    if (!_allowSocialScreenTimeSurfaces) return [];
     try {
@@
   Future<void> refreshLeaderboardForMe() async {
+    if (!_allowSocialScreenTimeSurfaces) return;
     try {
@@
   Future<List<FriendConnection>> getFriendConnections() async {
+    if (!_allowSocialScreenTimeSurfaces) return [];
     try {
@@
   Future<void> upsertFriendConnection({
     required String friendEmail,
     String? friendName,
   }) async {
+    if (!_allowSocialScreenTimeSurfaces) return;
     final normalizedEmail = friendEmail.trim().toLowerCase();
@@
   Future<void> deleteFriendConnection(String id) async {
+    if (!_allowSocialScreenTimeSurfaces) return;
     await _client
         .from('friend_connections')
         .delete()
@@
   Future<void> syncFriendConnections() async {
+    if (!_allowSocialScreenTimeSurfaces) return;
     try {
*** Update File: /Users/timo/ceo_os/lib/core/repositories/focus_repository.dart
@@
-        await _client
-            .from('leaderboard_entries')
-            .update({
-              'win_streak': currentStreak,
-              'last_sync_date': DateTime.now().toIso8601String(),
-            })
-            .eq('created_by', _currentUserId);
+        if (AppleReviewCompliance.allowSocialScreenTimeSurfaces) {
+          await _client
+              .from('leaderboard_entries')
+              .update({
+                'win_streak': currentStreak,
+                'last_sync_date': DateTime.now().toIso8601String(),
+              })
+              .eq('created_by', _currentUserId);
+        }
*** End Patch
