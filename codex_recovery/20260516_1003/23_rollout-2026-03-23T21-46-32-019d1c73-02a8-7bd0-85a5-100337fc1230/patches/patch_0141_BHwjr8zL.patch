*** Begin Patch
*** Add File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/blocking/LauncherIconTarget.kt
+package com.wakeapp.ceoos.blocking
+
+import android.graphics.Rect
+
+data class LauncherIconTarget(
+    val packageName: String,
+    val label: String,
+    val bounds: Rect,
+)
+
+data class OverlayKey(
+    val packageName: String,
+    val left: Int,
+    val top: Int,
+    val right: Int,
+    val bottom: Int,
+) {
+    companion object {
+        fun from(target: LauncherIconTarget): OverlayKey = OverlayKey(
+            packageName = target.packageName,
+            left = target.bounds.left,
+            top = target.bounds.top,
+            right = target.bounds.right,
+            bottom = target.bounds.bottom,
+        )
+    }
+}
*** End Patch