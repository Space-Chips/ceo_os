*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/providers/focus_provider.dart
@@
-import 'package:screen_time/screen_time.dart';
 import 'package:shared_preferences/shared_preferences.dart';
@@
   final FocusService _focusService = FocusService();
   final FocusRepository _repository = FocusRepository();
   final PremiumRepository _premiumRepository = PremiumRepository();
-  final ScreenTime _screenTime = ScreenTime();
   final StatsEngine _statsEngine = StatsEngine();
@@
-    await refreshScreenTime();
     notifyListeners();
   }
 
   Future<void> refreshScreenTime() async {
-    try {
-      final now = DateTime.now();
-      final startOfDay = DateTime(now.year, now.month, now.day);
-      final List<AppUsage> usage = await _screenTime.appUsageData(
-        startTime: startOfDay,
-        endTime: now,
-      );
-
-      double totalMinutes = 0;
-      for (var app in usage) {
-        totalMinutes += app.usageTime?.inMinutes ?? 0;
-      }
-      _screenTimeToday = totalMinutes;
-      await _repository.replaceScreenTimeLogsForDay(day: now, usage: usage);
-      unawaited(
-        _statsEngine.recordScreenTime(totalMinutes: totalMinutes.round()),
-      );
-
-      // If we got 0 minutes, maybe it's just a fresh day or restricted,
-      // but if we got data, we use it.
-    } on MissingPluginException {
-      // Native screen-time bridge not available on this runtime (ex: simulator).
-      _screenTimeToday = 0;
-      _hourlyUsage = List.filled(24, 0);
-    } catch (e) {
-      AppLogger.error('Screen Time refresh failed.', e);
-      _screenTimeToday = 0;
-      _hourlyUsage = List.filled(24, 0);
-    }
-    if (_screenTimeToday > 0) {
-      final distracting = (_screenTimeToday.round() - _totalFocusMinutesToday)
-          .clamp(0, 24 * 60);
-      unawaited(
-        _statsEngine.recordDistractingTime(minutes: distracting, isTotal: true),
-      );
-    }
+    _screenTimeToday = 0;
+    _hourlyUsage = List.filled(24, 0);
     notifyListeners();
   }
*** End Patch
