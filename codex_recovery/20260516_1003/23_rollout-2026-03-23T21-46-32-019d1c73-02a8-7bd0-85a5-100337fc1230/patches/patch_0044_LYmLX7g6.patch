*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/focus_service.dart
@@
   Future<bool> openSystemSettings() async {
     const method = 'openSystemSettings';
     if (_shouldSkipMissingMethod(method)) return false;
     try {
       final bool? result = await _channel.invokeMethod<bool>(method);
@@
       _markMissingMethod(method);
       return false;
     }
   }
+
+  Future<bool> supportsPrivateScreenTimeSummaryView() async {
+    const method = 'supportsPrivateScreenTimeSummaryView';
+    if (_shouldSkipMissingMethod(method)) return false;
+    try {
+      final bool? result = await _channel.invokeMethod<bool>(method);
+      return result ?? false;
+    } on PlatformException catch (e) {
+      AppLogger.error(
+        "Failed to check private Screen Time summary support: '${e.message}'.",
+      );
+      return false;
+    } on MissingPluginException {
+      _markMissingMethod(method);
+      return false;
+    }
+  }
 
   // Start shielding specific apps/categories
   Future<void> startShield(
     List<String> packageNames,
     List<String> categories,
*** End Patch
