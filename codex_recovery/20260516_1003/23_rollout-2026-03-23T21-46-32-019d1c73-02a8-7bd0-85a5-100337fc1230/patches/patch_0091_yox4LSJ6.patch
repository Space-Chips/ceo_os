*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
   Future<void> _confirmDeleteGoal(Objective objective) async {
@@
     if (!mounted) return;
     await _load();
   }
+
+  Future<void> _confirmDeleteLinkedHabit(Habit habit) async {
+    final confirmed = await showCupertinoDialog<bool>(
+      context: context,
+      builder: (dialogContext) => CupertinoAlertDialog(
+        title: const Text('Delete Habit?'),
+        content: Text(
+          '“${habit.title}” will be removed from your active habits.',
+        ),
+        actions: [
+          CupertinoDialogAction(
+            onPressed: () => Navigator.of(dialogContext).pop(false),
+            child: const Text('Cancel'),
+          ),
+          CupertinoDialogAction(
+            isDestructiveAction: true,
+            onPressed: () => Navigator.of(dialogContext).pop(true),
+            child: const Text('Delete'),
+          ),
+        ],
+      ),
+    );
+    if (confirmed != true || !mounted) return;
+    await context.read<HabitProvider>().deleteHabit(habit.id);
+    if (!mounted) return;
+    await _load();
+  }
@@
                           else
                             ...linkedHabits.map(
                               (habit) => Padding(
                                 padding: const EdgeInsets.only(bottom: 10),
-                                child: Row(
-                                  children: [
-                                    AnimatedContainer(
-                                      duration: const Duration(milliseconds: 180),
-                                      curve: Curves.easeOutCubic,
-                                      width: 22,
-                                      height: 22,
-                                      decoration: BoxDecoration(
-                                        shape: BoxShape.circle,
-                                        color:
-                                            prov.isHabitCompletedToday(habit.id)
-                                            ? AppColors.habitsCompletionAccent
-                                            : Colors.transparent,
-                                        border:
-                                            prov.isHabitCompletedToday(habit.id)
-                                            ? null
-                                            : Border.all(
-                                                color: AppColors
-                                                    .habitsOpenRingAccent,
-                                                width: 1,
-                                              ),
-                                      ),
-                                      child: prov.isHabitCompletedToday(habit.id)
-                                          ? Icon(
-                                              CupertinoIcons.checkmark,
-                                              size: 12,
-                                              color: AppColors.onAccent,
-                                            )
-                                          : null,
-                                    ),
-                                    const SizedBox(width: 12),
-                                    Expanded(
-                                      child: Text(
-                                        habit.title,
-                                        style: AppTypography.callout.copyWith(
-                                          fontSize: 14,
-                                          color: AppColors.secondaryLabel,
+                                child: _HoverDeleteRow(
+                                  onTap: null,
+                                  onDelete: () => _confirmDeleteLinkedHabit(habit),
+                                  child: Row(
+                                    children: [
+                                      AnimatedContainer(
+                                        duration: const Duration(milliseconds: 180),
+                                        curve: Curves.easeOutCubic,
+                                        width: 22,
+                                        height: 22,
+                                        decoration: BoxDecoration(
+                                          shape: BoxShape.circle,
+                                          color:
+                                              prov.isHabitCompletedToday(habit.id)
+                                              ? AppColors.habitsCompletionAccent
+                                              : Colors.transparent,
+                                          border:
+                                              prov.isHabitCompletedToday(habit.id)
+                                              ? null
+                                              : Border.all(
+                                                  color: AppColors
+                                                      .habitsOpenRingAccent,
+                                                  width: 1,
+                                                ),
+                                        ),
+                                        child: prov.isHabitCompletedToday(habit.id)
+                                            ? Icon(
+                                                CupertinoIcons.checkmark,
+                                                size: 12,
+                                                color: AppColors.onAccent,
+                                              )
+                                            : null,
+                                      ),
+                                      const SizedBox(width: 12),
+                                      Expanded(
+                                        child: Text(
+                                          habit.title,
+                                          style: AppTypography.callout.copyWith(
+                                            fontSize: 14,
+                                            color: AppColors.secondaryLabel,
+                                          ),
                                         ),
                                       ),
-                                    ),
-                                  ],
+                                    ],
+                                  ),
                                 ),
                               ),
                             ),
*** End Patch
