*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/stats_engine.dart
@@
 import 'dart:async';
 import 'dart:math' as math;
 
+import '../config/apple_review_compliance.dart';
 import '../models/advanced_stats_models.dart';
 import '../models/stats_event_models.dart';
 import '../repositories/advanced_stats_repository.dart';
 import 'stats_heatmap_generator.dart';
 import 'stats_insight_generator.dart';
@@
   static const Map<String, int> _recoveryMinutesByCategory = {
@@
     'default': 4,
   };
+
+  bool _isSensitiveFamilyControlsEvent(String eventType) {
+    switch (eventType) {
+      case StatsEventType.blockedAppAttempt:
+      case StatsEventType.blockedSiteAttempt:
+      case StatsEventType.screenTimeRecorded:
+      case StatsEventType.distractingTimeRecorded:
+        return true;
+      default:
+        return false;
+    }
+  }
 
   Future<void> trackEvent(
     String eventType, {
@@
   }) async {
     if (!StatsEventType.values.contains(eventType)) return;
+    if (AppleReviewCompliance.exposesLocalOnlyFamilyControls &&
+        _isSensitiveFamilyControlsEvent(eventType)) {
+      return;
+    }
 
     try {
@@
     await _syncFocusSessions(start: start, end: end);
     await _syncCeoSessions(start: start, end: end);
     await _syncHabitSchedule(start: start, end: end);
-    await _syncScreenTimeLogs(start: start, end: end);
     await recordRankChangedFromSnapshot();
     await backfillRecent(days: days);
   }
@@
-  Future<void> _syncScreenTimeLogs({
-    required DateTime start,
-    required DateTime end,
-  }) async {
-    final logs = await _repository.getSourceScreenTimeLogs(
-      start: start,
-      end: end,
-    );
-    final totalByDay = <DateTime, int>{};
-    final distractingByDay = <DateTime, int>{};
-    final productiveByDay = <DateTime, int>{};
-
-    for (final log in logs) {
-      final date = _parseLooseDate(log['date']) ?? _day(DateTime.now());
-      final day = _day(date);
-      final minutes = ((_int(log['duration_seconds'])) / 60).round();
-      totalByDay[day] = (totalByDay[day] ?? 0) + minutes;
-
-      final entityType =
-          (log['entity_type'] as String?)?.toLowerCase().trim() ?? '';
-      final entityName =
-          (log['entity_name'] as String?)?.toLowerCase().trim() ?? '';
-      if (_looksProductive(entityType, entityName)) {
-        productiveByDay[day] = (productiveByDay[day] ?? 0) + minutes;
-      } else {
-        distractingByDay[day] = (distractingByDay[day] ?? 0) + minutes;
-      }
-    }
-
-    for (final entry in totalByDay.entries) {
-      final day = entry.key;
-      final key = _fmtDay(day);
-      await trackEvent(
-        StatsEventType.screenTimeRecorded,
-        eventTime: day,
-        recompute: false,
-        payload: {'total_minutes': entry.value, 'is_total': true},
-        sourceKey: 'screen_time_total:$key',
-      );
-
-      final productive = productiveByDay[day] ?? 0;
-      if (productive > 0) {
-        await trackEvent(
-          StatsEventType.productiveTimeRecorded,
-          eventTime: day,
-          recompute: false,
-          payload: {'minutes': productive, 'is_total': true},
-          sourceKey: 'screen_time_productive:$key',
-        );
-      }
-
-      final distracting = distractingByDay[day] ?? 0;
-      if (distracting > 0) {
-        await trackEvent(
-          StatsEventType.distractingTimeRecorded,
-          eventTime: day,
-          recompute: false,
-          payload: {'minutes': distracting, 'is_total': true},
-          sourceKey: 'screen_time_distracting:$key',
-        );
-      }
-    }
-  }
-
   DailyStats _computeDailyStats({
@@
         case StatsEventType.blockedAppAttempt:
+          if (AppleReviewCompliance.exposesLocalOnlyFamilyControls) {
+            break;
+          }
           distractionAttempts += 1;
@@
         case StatsEventType.blockedSiteAttempt:
+          if (AppleReviewCompliance.exposesLocalOnlyFamilyControls) {
+            break;
+          }
           distractionAttempts += 1;
@@
         case StatsEventType.screenTimeRecorded:
+          if (AppleReviewCompliance.exposesLocalOnlyFamilyControls) {
+            break;
+          }
           final total = _int(payload['total_minutes']);
           screenTimeMinutes = math.max(screenTimeMinutes, total);
           break;
@@
         case StatsEventType.distractingTimeRecorded:
+          if (AppleReviewCompliance.exposesLocalOnlyFamilyControls) {
+            break;
+          }
           final minutes = _int(payload['minutes']);
@@
-    final events = await _repository.getEventsForRange(
-      start: weekStart,
-      end: weekEnd
-          .add(const Duration(days: 1))
-          .subtract(const Duration(milliseconds: 1)),
-    );
     final distractionByName = <String, int>{};
     final distractionByHour = <int, int>{};
     final focusByHour = <int, int>{};
-
-    for (final event in events) {
-      final payload = event.payload;
-      final hour = event.eventTime.toLocal().hour;
-      if (event.eventType == StatsEventType.blockedAppAttempt ||
-          event.eventType == StatsEventType.blockedSiteAttempt) {
-        final name =
-            (payload['app_name'] as String?) ??
-            (payload['site_domain'] as String?) ??
-            'Unknown';
-        distractionByName[name] = (distractionByName[name] ?? 0) + 1;
-        distractionByHour[hour] = (distractionByHour[hour] ?? 0) + 1;
-      }
-      if (event.eventType == StatsEventType.focusSessionCompleted) {
-        focusByHour[hour] = (focusByHour[hour] ?? 0) + 1;
-      }
-    }
+    final events = await _repository.getEventsForRange(
+      start: weekStart,
+      end: weekEnd
+          .add(const Duration(days: 1))
+          .subtract(const Duration(milliseconds: 1)),
+    );
+
+    for (final event in events) {
+      final payload = event.payload;
+      final hour = event.eventTime.toLocal().hour;
+      if (!AppleReviewCompliance.exposesLocalOnlyFamilyControls &&
+          (event.eventType == StatsEventType.blockedAppAttempt ||
+              event.eventType == StatsEventType.blockedSiteAttempt)) {
+        final name =
+            (payload['app_name'] as String?) ??
+            (payload['site_domain'] as String?) ??
+            'Unknown';
+        distractionByName[name] = (distractionByName[name] ?? 0) + 1;
+        distractionByHour[hour] = (distractionByHour[hour] ?? 0) + 1;
+      }
+      if (event.eventType == StatsEventType.focusSessionCompleted) {
+        focusByHour[hour] = (focusByHour[hour] ?? 0) + 1;
+      }
+    }
*** Update File: /Users/timo/ceo_os/lib/core/repositories/insights_repository.dart
@@
 import 'package:intl/intl.dart';
 import 'package:supabase_flutter/supabase_flutter.dart';
 
+import '../config/apple_review_compliance.dart';
 import '../models/insights_models.dart';
 import '../services/supabase_service.dart';
 import '../utils/app_logger.dart';
@@
   Future<Map<String, dynamic>?> _fetchOptionalAdvancedDaily(
     String today,
   ) async {
+    if (!AppleReviewCompliance.allowAdvancedStats) {
+      return null;
+    }
     try {
@@
   Future<Map<String, dynamic>?> _fetchOptionalAdvancedLifetime() async {
+    if (!AppleReviewCompliance.allowAdvancedStats) {
+      return null;
+    }
     try {
*** Update File: /Users/timo/ceo_os/lib/core/router/app_router.dart
@@
 import '../providers/focus_provider.dart';
+import '../config/apple_review_compliance.dart';
 import '../theme/app_colors.dart';
@@
-            GoRoute(
-              path: '/stats',
-              pageBuilder: (context, state) =>
-                  const NoTransitionPage(child: StatsScreen()),
-            ),
+            if (AppleReviewCompliance.allowAdvancedStats)
+              GoRoute(
+                path: '/stats',
+                pageBuilder: (context, state) =>
+                    const NoTransitionPage(child: StatsScreen()),
+              ),
@@
-            GoRoute(
-              path: '/leaderboard',
-              pageBuilder: (context, state) =>
-                  const NoTransitionPage(child: LeaderboardScreen()),
-            ),
+            if (AppleReviewCompliance.allowSocialScreenTimeSurfaces)
+              GoRoute(
+                path: '/leaderboard',
+                pageBuilder: (context, state) =>
+                    const NoTransitionPage(child: LeaderboardScreen()),
+              ),
@@
-            GoRoute(
-              path: '/family-time',
-              pageBuilder: (context, state) =>
-                  const NoTransitionPage(child: FamilyTimeScreen()),
-            ),
+            if (AppleReviewCompliance.allowSocialScreenTimeSurfaces)
+              GoRoute(
+                path: '/family-time',
+                pageBuilder: (context, state) =>
+                    const NoTransitionPage(child: FamilyTimeScreen()),
+              ),
*** Update File: /Users/timo/ceo_os/lib/core/repositories/focus_repository.dart
@@
 import 'package:supabase_flutter/supabase_flutter.dart';
-import 'package:screen_time/screen_time.dart';
 import '../models/block_list_model.dart';
 import '../models/user_models.dart';
 import '../services/supabase_service.dart';
 import '../utils/app_logger.dart';
@@
-  Future<void> replaceScreenTimeLogsForDay({
-    required DateTime day,
-    required List<AppUsage> usage,
-  }) async {
-    final dateKey =
-        '${day.year.toString().padLeft(4, '0')}-'
-        '${day.month.toString().padLeft(2, '0')}-'
-        '${day.day.toString().padLeft(2, '0')}';
-
-    try {
-      await _client
-          .from('screen_time_logs')
-          .delete()
-          .eq('created_by', _currentUserId)
-          .eq('date', dateKey);
-
-      final rows = usage
-          .where((app) => (app.usageTime?.inSeconds ?? 0) > 0)
-          .map(
-            (app) => {
-              'created_by': _currentUserId,
-              'date': dateKey,
-              'entity_type': 'app',
-              'entity_name': (app.packageName?.trim().isNotEmpty ?? false)
-                  ? app.packageName!.trim()
-                  : ((app.appName?.trim().isNotEmpty ?? false)
-                        ? app.appName!.trim()
-                        : 'Unknown app'),
-              'duration_seconds': app.usageTime!.inSeconds,
-              'start_time': app.firstTime?.toIso8601String(),
-              'end_time': app.lastTime?.toIso8601String(),
-              'awareness_notifications_shown': 0,
-            },
-          )
-          .toList(growable: false);
-
-      if (rows.isEmpty) return;
-      await _client.from('screen_time_logs').insert(rows);
-    } catch (e) {
-      AppLogger.error('Error syncing screen time logs.', e);
-    }
-  }
-
   Future<void> _refreshWinStreakForSession({
*** Update File: /Users/timo/ceo_os/lib/core/repositories/feature_repository.dart
@@
-  Future<List<Map<String, dynamic>>> getScreenTimeLogs({int days = 7}) async {
-    final from = DateTime.now().subtract(Duration(days: days));
-    final date = DateFormat('yyyy-MM-dd').format(from);
-    final response = await _client
-        .from('screen_time_logs')
-        .select()
-        .eq('created_by', _currentUserId)
-        .gte('date', date)
-        .order('date', ascending: false);
-    return (response as List).cast<Map<String, dynamic>>();
-  }
-
   Future<List<BlockedApp>> getBlockedApps() async {
*** Update File: /Users/timo/ceo_os/lib/core/models/user_models.dart
@@
 class UserRank {
   final String id;
   final String createdBy;
   final String? rankName;
   final int? rankLevel;
-  final double? screenTimeAvgMinutes;
   final int? winStreakBonus;
@@
     required this.id,
     required this.createdBy,
     this.rankName,
     this.rankLevel,
-    this.screenTimeAvgMinutes,
     this.winStreakBonus,
@@
       createdBy: json['created_by'],
       rankName: json['rank_name'],
       rankLevel: json['rank_level'],
-      screenTimeAvgMinutes: json['screen_time_avg_minutes'] != null
-          ? (json['screen_time_avg_minutes'] as num).toDouble()
-          : null,
       winStreakBonus: json['win_streak_bonus'],
@@
 class LeaderboardEntry {
   final String id;
   final String createdBy;
   final int? rankLevel;
   final String? rankName;
   final int? winStreak;
-  final double? screenTimeAvgMinutes;
   final int? percentile;
@@
     required this.id,
     required this.createdBy,
     this.rankLevel,
     this.rankName,
     this.winStreak,
-    this.screenTimeAvgMinutes,
     this.percentile,
@@
       createdBy: json['created_by'],
       rankLevel: json['rank_level'],
       rankName: json['rank_name'],
       winStreak: json['win_streak'],
-      screenTimeAvgMinutes: json['screen_time_avg_minutes'] != null
-          ? (json['screen_time_avg_minutes'] as num).toDouble()
-          : null,
       percentile: json['percentile'],
*** Update File: /Users/timo/ceo_os/lib/core/repositories/user_repository.dart
@@
       return UserRank(
         id: current?.id ?? _currentUserId,
         createdBy: _currentUserId,
         rankName: 'Bronze',
         rankLevel: 1,
-        screenTimeAvgMinutes: current?.screenTimeAvgMinutes,
         winStreakBonus: current?.winStreakBonus ?? streak,
         totalRankPoints: current?.totalRankPoints ?? 0,
*** End Patch
