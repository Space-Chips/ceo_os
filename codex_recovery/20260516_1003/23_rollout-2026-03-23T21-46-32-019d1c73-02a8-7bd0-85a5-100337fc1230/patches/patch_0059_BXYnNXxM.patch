*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/build.gradle.kts
@@
+import java.io.FileInputStream
+import java.util.Properties
+
 plugins {
     id("com.android.application")
     id("kotlin-android")
     // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
     id("dev.flutter.flutter-gradle-plugin")
 }
 
+val keystoreProperties = Properties()
+val keystorePropertiesFile = rootProject.file("key.properties")
+if (keystorePropertiesFile.exists()) {
+    FileInputStream(keystorePropertiesFile).use(keystoreProperties::load)
+}
+
+val hasReleaseSigning =
+    keystoreProperties.getProperty("storeFile")?.isNotBlank() == true &&
+    keystoreProperties.getProperty("storePassword")?.isNotBlank() == true &&
+    keystoreProperties.getProperty("keyAlias")?.isNotBlank() == true &&
+    keystoreProperties.getProperty("keyPassword")?.isNotBlank() == true
+
 android {
-    namespace = "com.example.ceo_os"
+    namespace = "com.wakeapp.ceoos"
     compileSdk = flutter.compileSdkVersion
     ndkVersion = "27.0.12077973"
@@
     defaultConfig {
-        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
-        applicationId = "com.example.ceo_os"
-        // You can update the following values to match your application needs.
-        // For more information, see: https://flutter.dev/to/review-gradle-config.
+        applicationId = "com.wakeapp.ceoos"
         minSdk = flutter.minSdkVersion
         targetSdk = flutter.targetSdkVersion
         versionCode = flutter.versionCode
         versionName = flutter.versionName
     }
+
+    signingConfigs {
+        create("release") {
+            val storeFilePath = keystoreProperties.getProperty("storeFile")
+            if (!storeFilePath.isNullOrBlank()) {
+                storeFile = file(storeFilePath)
+            }
+            storePassword = keystoreProperties.getProperty("storePassword")
+            keyAlias = keystoreProperties.getProperty("keyAlias")
+            keyPassword = keystoreProperties.getProperty("keyPassword")
+        }
+    }
 
     buildTypes {
         release {
-            // TODO: Add your own signing config for the release build.
-            // Signing with the debug keys for now, so `flutter run --release` works.
-            signingConfig = signingConfigs.getByName("debug")
+            if (hasReleaseSigning) {
+                signingConfig = signingConfigs.getByName("release")
+            }
         }
     }
 }
*** Update File: /Users/timo/ceo_os/android/app/src/main/AndroidManifest.xml
@@
-    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
     <uses-permission
         android:name="android.permission.QUERY_ALL_PACKAGES"
         tools:node="remove" />
@@
     <application
-        android:label="ceo_os"
+        android:label="@string/app_name"
         android:name="${applicationName}"
         android:icon="@mipmap/ic_launcher">
*** Update File: /Users/timo/ceo_os/android/app/src/main/res/values/strings.xml
@@
 <resources>
+    <string name="app_name">CEO OS</string>
     <string name="accessibility_service_description">CEO OS Focus Mode Blocker</string>
 </resources>
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/example/ceo_os/AppBlockingService.kt
@@
-package com.example.ceo_os
+package com.wakeapp.ceoos
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/example/ceo_os/ClassicDailyLimitTracker.kt
@@
-package com.example.ceo_os
+package com.wakeapp.ceoos
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/example/ceo_os/ClassicPauseAlarmReceiver.kt
@@
-package com.example.ceo_os
+package com.wakeapp.ceoos
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/example/ceo_os/ClassicPauseBootReceiver.kt
@@
-package com.example.ceo_os
+package com.wakeapp.ceoos
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/example/ceo_os/ClassicPauseScheduler.kt
@@
-package com.example.ceo_os
+package com.wakeapp.ceoos
@@
-            action = "com.example.ceo_os.CLASSIC_PAUSE_START"
+            action = "com.wakeapp.ceoos.CLASSIC_PAUSE_START"
@@
-            action = "com.example.ceo_os.CLASSIC_PAUSE_END"
+            action = "com.wakeapp.ceoos.CLASSIC_PAUSE_END"
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/example/ceo_os/MainActivity.kt
@@
-package com.example.ceo_os
+package com.wakeapp.ceoos
*** Add File: /Users/timo/ceo_os/android/key.properties.example
+storePassword=change-me
+keyPassword=change-me
+keyAlias=upload
+storeFile=/absolute/path/to/your/upload-keystore.jks
*** End Patch
