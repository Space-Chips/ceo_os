*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
 class _GridTab extends StatefulWidget {
   final void Function(Habit habit, DateTime? targetDate) onOpenHabit;
 
   const _GridTab({super.key, required this.onOpenHabit});
@@
 class _GridTabState extends State<_GridTab> {
   Map<String, Map<String, HabitCompletion>> _weeklyCompletions = {};
@@
   Future<void> _loadWeeklyData() async {
@@
     if (mounted) setState(() => _weeklyCompletions = mapped);
   }
+
+  Future<void> _confirmDeleteHabit(Habit habit) async {
+    final confirmed = await showCupertinoDialog<bool>(
+      context: context,
+      builder: (dialogContext) => CupertinoAlertDialog(
+        title: const Text('Delete Habit?'),
+        content: Text(
+          '“${habit.title}” will be removed from your active habits.',
+        ),
+        actions: [
+          CupertinoDialogAction(
+            onPressed: () => Navigator.of(dialogContext).pop(false),
+            child: const Text('Cancel'),
+          ),
+          CupertinoDialogAction(
+            isDestructiveAction: true,
+            onPressed: () => Navigator.of(dialogContext).pop(true),
+            child: const Text('Delete'),
+          ),
+        ],
+      ),
+    );
+    if (confirmed != true || !mounted) return;
+    await context.read<HabitProvider>().deleteHabit(habit.id);
+    if (!mounted) return;
+    await _loadWeeklyData();
+  }
@@
                   ...sortedHabits.map(
                     (habit) => _HabitGridRow(
                       habit: habit,
                       weekStart: weekStart,
                       weeklyCompletions: _weeklyCompletions[habit.id] ?? {},
                       onTap: (targetDate) =>
                           widget.onOpenHabit(habit, targetDate),
+                      onDelete: () => _confirmDeleteHabit(habit),
                     ),
                   ),
                 ],
               ),
             ),
@@
 class _HabitGridRow extends StatelessWidget {
   final Habit habit;
   final DateTime weekStart;
   final Map<String, HabitCompletion> weeklyCompletions;
   final ValueChanged<DateTime?> onTap;
+  final VoidCallback onDelete;
 
   const _HabitGridRow({
     required this.habit,
     required this.weekStart,
     required this.weeklyCompletions,
     required this.onTap,
+    required this.onDelete,
   });
@@
-    return _HabitPressScale(
-      onTap: pendingCarryoverDate == null
-          ? null
-          : () => onTap(pendingCarryoverDate),
-      child: Padding(
-        padding: const EdgeInsets.symmetric(vertical: 4),
-        child: Row(
-          children: [
-            Expanded(
-              flex: 3,
-              child: Text(
-                habit.title,
-                style: AppTypography.callout.copyWith(
-                  fontSize: 12,
-                  color: AppColors.secondaryLabel,
-                  fontWeight: FontWeight.w500,
-                ),
-                maxLines: 1,
-                overflow: TextOverflow.ellipsis,
-              ),
-            ),
-            ...List.generate(7, (d) {
-              final day = weekStart.add(Duration(days: d));
-              final dayStr = DateFormat('yyyy-MM-dd').format(day);
-              final dayNum = day.weekday == DateTime.sunday ? 7 : day.weekday;
-              final beforeHabitCreation = day.isBefore(createdDay);
-
-              final isScheduled =
-                  habit.isDaily ||
-                  (habit.specificDays != null &&
-                      habit.specificDays!.contains(dayNum));
-
-              if (!isScheduled || beforeHabitCreation) {
-                return SizedBox(
-                  width: 32,
-                  child: Center(
-                    child: Text(
-                      '·',
-                      style: AppTypography.mono.copyWith(
-                        fontSize: 10,
-                        color: AppColors.quaternaryLabel,
-                      ),
-                    ),
-                  ),
-                );
-              }
-
-              final comp = weeklyCompletions[dayStr];
-              final hasCompletion = comp != null;
-              final isCompleted = hasCompletion && comp.completed;
-              final isMissed = hasCompletion && !comp.completed;
-              final isToday =
-                  day.year == today.year &&
-                  day.month == today.month &&
-                  day.day == today.day;
-              final isPast = day.isBefore(today);
-              final isMissingPastValidation =
-                  isPast &&
-                  !beforeHabitCreation &&
-                  !hasCompletion &&
-                  !isCompleted &&
-                  !isMissed;
-
-              return SizedBox(
-                width: 32,
-                child: Center(
-                  child: _DayCell(
-                    isCompleted: isCompleted,
-                    isMissed: isMissed,
-                    isLockedMissing: isMissingPastValidation,
-                    isToday: isToday,
-                  ),
-                ),
-              );
-            }),
-          ],
-        ),
-      ),
-    );
+    return _HoverDeleteRow(
+      onTap: pendingCarryoverDate == null
+          ? null
+          : () => onTap(pendingCarryoverDate),
+      onDelete: onDelete,
+      child: Padding(
+        padding: const EdgeInsets.symmetric(vertical: 4),
+        child: Row(
+          children: [
+            Expanded(
+              flex: 3,
+              child: Text(
+                habit.title,
+                style: AppTypography.callout.copyWith(
+                  fontSize: 12,
+                  color: AppColors.secondaryLabel,
+                  fontWeight: FontWeight.w500,
+                ),
+                maxLines: 1,
+                overflow: TextOverflow.ellipsis,
+              ),
+            ),
+            ...List.generate(7, (d) {
+              final day = weekStart.add(Duration(days: d));
+              final dayStr = DateFormat('yyyy-MM-dd').format(day);
+              final dayNum = day.weekday == DateTime.sunday ? 7 : day.weekday;
+              final beforeHabitCreation = day.isBefore(createdDay);
+
+              final isScheduled =
+                  habit.isDaily ||
+                  (habit.specificDays != null &&
+                      habit.specificDays!.contains(dayNum));
+
+              if (!isScheduled || beforeHabitCreation) {
+                return SizedBox(
+                  width: 32,
+                  child: Center(
+                    child: Text(
+                      '·',
+                      style: AppTypography.mono.copyWith(
+                        fontSize: 10,
+                        color: AppColors.quaternaryLabel,
+                      ),
+                    ),
+                  ),
+                );
+              }
+
+              final comp = weeklyCompletions[dayStr];
+              final hasCompletion = comp != null;
+              final isCompleted = hasCompletion && comp.completed;
+              final isMissed = hasCompletion && !comp.completed;
+              final isToday =
+                  day.year == today.year &&
+                  day.month == today.month &&
+                  day.day == today.day;
+              final isPast = day.isBefore(today);
+              final isMissingPastValidation =
+                  isPast &&
+                  !beforeHabitCreation &&
+                  !hasCompletion &&
+                  !isCompleted &&
+                  !isMissed;
+
+              return SizedBox(
+                width: 32,
+                child: Center(
+                  child: _DayCell(
+                    isCompleted: isCompleted,
+                    isMissed: isMissed,
+                    isLockedMissing: isMissingPastValidation,
+                    isToday: isToday,
+                  ),
+                ),
+              );
+            }),
+          ],
+        ),
+      ),
+    );
   }
 }
@@
   Future<void> _saveContract({required bool committed}) async {
@@
     }
   }
+
+  Future<void> _confirmDeleteGoal(Objective objective) async {
+    final meta = _objectiveMeta(objective);
+    final confirmed = await showCupertinoDialog<bool>(
+      context: context,
+      builder: (dialogContext) => CupertinoAlertDialog(
+        title: const Text('Delete Goal?'),
+        content: Text('“${meta.name}” will be removed from your active goals.'),
+        actions: [
+          CupertinoDialogAction(
+            onPressed: () => Navigator.of(dialogContext).pop(false),
+            child: const Text('Cancel'),
+          ),
+          CupertinoDialogAction(
+            isDestructiveAction: true,
+            onPressed: () => Navigator.of(dialogContext).pop(true),
+            child: const Text('Delete'),
+          ),
+        ],
+      ),
+    );
+    if (confirmed != true || !mounted) return;
+    await _repo.archiveObjective(objective.id);
+    if (!mounted) return;
+    await _load();
+  }
@@
-                return Padding(
-                  padding: const EdgeInsets.only(bottom: 16),
-                  child: Container(
+                return Padding(
+                  padding: const EdgeInsets.only(bottom: 16),
+                  child: _HoverDeleteCard(
+                    onDelete: () => _confirmDeleteGoal(objective),
+                    child: Container(
                     padding: const EdgeInsets.all(18),
                     decoration: _habitModuleCardDecoration(radius: 20),
                     child: Column(
                       crossAxisAlignment: CrossAxisAlignment.start,
                       children: [
@@
-                          ],
-                        ),
-                        const SizedBox(height: 16),
-                        Container(
-                          height: 1,
-                          color: AppColors.white.withValues(alpha: 0.06),
-                        ),
-                        const SizedBox(height: 14),
-                        if (linkedHabits.isEmpty)
-                          Text(
-                            'No habits attached yet.',
-                            style: AppTypography.footnote.copyWith(
-                              fontSize: 13,
-                              color: AppColors.tertiaryLabel.withValues(
-                                alpha: 0.7,
-                              ),
-                            ),
-                          )
-                        else
-                          ...linkedHabits.map(
-                            (habit) => Padding(
-                              padding: const EdgeInsets.only(bottom: 10),
-                              child: Row(
-                                children: [
-                                  AnimatedContainer(
-                                    duration: const Duration(milliseconds: 180),
-                                    curve: Curves.easeOutCubic,
-                                    width: 22,
-                                    height: 22,
-                                    decoration: BoxDecoration(
-                                      shape: BoxShape.circle,
-                                      color:
-                                          prov.isHabitCompletedToday(habit.id)
-                                          ? AppColors.habitsCompletionAccent
-                                          : Colors.transparent,
-                                      border:
-                                          prov.isHabitCompletedToday(habit.id)
-                                          ? null
-                                          : Border.all(
-                                              color: AppColors
-                                                  .habitsOpenRingAccent,
-                                              width: 1,
-                                            ),
-                                    ),
-                                    child: prov.isHabitCompletedToday(habit.id)
-                                        ? Icon(
-                                            CupertinoIcons.checkmark,
-                                            size: 12,
-                                            color: AppColors.onAccent,
-                                          )
-                                        : null,
-                                  ),
-                                  const SizedBox(width: 12),
-                                  Expanded(
-                                    child: Text(
-                                      habit.title,
-                                      style: AppTypography.callout.copyWith(
-                                        fontSize: 14,
-                                        color: AppColors.secondaryLabel,
-                                      ),
-                                    ),
-                                  ),
-                                ],
-                              ),
-                            ),
-                          ),
-                      ],
-                    ),
+                          ],
+                        ),
+                        const SizedBox(height: 16),
+                        Container(
+                          height: 1,
+                          color: AppColors.white.withValues(alpha: 0.06),
+                        ),
+                        const SizedBox(height: 14),
+                        if (linkedHabits.isEmpty)
+                          Text(
+                            'No habits attached yet.',
+                            style: AppTypography.footnote.copyWith(
+                              fontSize: 13,
+                              color: AppColors.tertiaryLabel.withValues(
+                                alpha: 0.7,
+                              ),
+                            ),
+                          )
+                        else
+                          ...linkedHabits.map(
+                            (habit) => Padding(
+                              padding: const EdgeInsets.only(bottom: 10),
+                              child: Row(
+                                children: [
+                                  AnimatedContainer(
+                                    duration: const Duration(milliseconds: 180),
+                                    curve: Curves.easeOutCubic,
+                                    width: 22,
+                                    height: 22,
+                                    decoration: BoxDecoration(
+                                      shape: BoxShape.circle,
+                                      color:
+                                          prov.isHabitCompletedToday(habit.id)
+                                          ? AppColors.habitsCompletionAccent
+                                          : Colors.transparent,
+                                      border:
+                                          prov.isHabitCompletedToday(habit.id)
+                                          ? null
+                                          : Border.all(
+                                              color: AppColors
+                                                  .habitsOpenRingAccent,
+                                              width: 1,
+                                            ),
+                                    ),
+                                    child: prov.isHabitCompletedToday(habit.id)
+                                        ? Icon(
+                                            CupertinoIcons.checkmark,
+                                            size: 12,
+                                            color: AppColors.onAccent,
+                                          )
+                                        : null,
+                                  ),
+                                  const SizedBox(width: 12),
+                                  Expanded(
+                                    child: Text(
+                                      habit.title,
+                                      style: AppTypography.callout.copyWith(
+                                        fontSize: 14,
+                                        color: AppColors.secondaryLabel,
+                                      ),
+                                    ),
+                                  ),
+                                ],
+                              ),
+                            ),
+                          ),
+                      ],
+                    ),
                   ),
                 );
               }),
@@
 class _HabitPressScale extends StatefulWidget {
@@
 }
+
+class _HoverDeleteRow extends StatefulWidget {
+  final Widget child;
+  final VoidCallback? onTap;
+  final VoidCallback onDelete;
+
+  const _HoverDeleteRow({
+    required this.child,
+    required this.onTap,
+    required this.onDelete,
+  });
+
+  @override
+  State<_HoverDeleteRow> createState() => _HoverDeleteRowState();
+}
+
+class _HoverDeleteRowState extends State<_HoverDeleteRow> {
+  bool _hovering = false;
+
+  @override
+  Widget build(BuildContext context) {
+    return MouseRegion(
+      onEnter: (_) => setState(() => _hovering = true),
+      onExit: (_) => setState(() => _hovering = false),
+      child: Row(
+        children: [
+          Expanded(
+            child: _HabitPressScale(onTap: widget.onTap, child: widget.child),
+          ),
+          AnimatedOpacity(
+            opacity: _hovering ? 1 : 0,
+            duration: const Duration(milliseconds: 140),
+            curve: Curves.easeOutCubic,
+            child: IgnorePointer(
+              ignoring: !_hovering,
+              child: CupertinoButton(
+                padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 6),
+                minSize: 32,
+                onPressed: widget.onDelete,
+                child: Icon(
+                  CupertinoIcons.delete,
+                  size: 18,
+                  color: AppColors.error,
+                ),
+              ),
+            ),
+          ),
+        ],
+      ),
+    );
+  }
+}
+
+class _HoverDeleteCard extends StatefulWidget {
+  final Widget child;
+  final VoidCallback onDelete;
+
+  const _HoverDeleteCard({required this.child, required this.onDelete});
+
+  @override
+  State<_HoverDeleteCard> createState() => _HoverDeleteCardState();
+}
+
+class _HoverDeleteCardState extends State<_HoverDeleteCard> {
+  bool _hovering = false;
+
+  @override
+  Widget build(BuildContext context) {
+    return MouseRegion(
+      onEnter: (_) => setState(() => _hovering = true),
+      onExit: (_) => setState(() => _hovering = false),
+      child: Stack(
+        children: [
+          widget.child,
+          Positioned(
+            top: 10,
+            right: 10,
+            child: AnimatedOpacity(
+              opacity: _hovering ? 1 : 0,
+              duration: const Duration(milliseconds: 140),
+              curve: Curves.easeOutCubic,
+              child: IgnorePointer(
+                ignoring: !_hovering,
+                child: CupertinoButton(
+                  padding: const EdgeInsets.all(6),
+                  minSize: 30,
+                  onPressed: widget.onDelete,
+                  child: Icon(
+                    CupertinoIcons.delete,
+                    size: 18,
+                    color: AppColors.error,
+                  ),
+                ),
+              ),
+            ),
+          ),
+        ],
+      ),
+    );
+  }
+}
*** End Patch
