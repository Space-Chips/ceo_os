*** Begin Patch
*** Update File: android/app/src/main/kotlin/com/wakeapp/ceoos/BlockGateActivity.kt
@@
 import android.app.Activity
 import android.content.BroadcastReceiver
 import android.content.Context
 import android.content.Intent
 import android.content.IntentFilter
 import android.graphics.Color
 import android.graphics.Typeface
 import android.graphics.drawable.GradientDrawable
 import android.os.Build
 import android.os.Bundle
 import android.net.Uri
+import android.util.Log
 import android.util.TypedValue
 import android.view.Gravity
 import android.view.View
 import android.view.ViewGroup
 import android.widget.FrameLayout
@@
     private fun goBrowserHome() {
         val browserPackage = blockedBrowserPackage?.trim().orEmpty()
         if (browserPackage.isEmpty()) {
             goHome()
             return
         }
 
         isVisible = false
         BlockingAccessibilityService.onBrowserHomeRequested(browserPackage, blockedDomain)
 
+        val safeUrlIntent = Intent(Intent.ACTION_VIEW, Uri.parse("https://www.google.com/")).apply {
+            `package` = browserPackage
+            addFlags(
+                Intent.FLAG_ACTIVITY_NEW_TASK or
+                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
+                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
+                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
+            )
+        }
+
         val blankIntent = Intent(Intent.ACTION_VIEW, Uri.parse("about:blank")).apply {
             `package` = browserPackage
             addFlags(
                 Intent.FLAG_ACTIVITY_NEW_TASK or
@@
         val chromeNewTabIntent = Intent(Intent.ACTION_VIEW, Uri.parse("chrome://newtab")).apply {
             `package` = browserPackage
             addFlags(
                 Intent.FLAG_ACTIVITY_NEW_TASK or
@@
-        val homeIntent = Intent(Intent.ACTION_MAIN).apply {
-            addCategory(Intent.CATEGORY_APP_BROWSER)
-            `package` = browserPackage
-            addFlags(
-                Intent.FLAG_ACTIVITY_NEW_TASK or
-                    Intent.FLAG_ACTIVITY_CLEAR_TOP or
-                    Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
-                    Intent.FLAG_ACTIVITY_NO_ANIMATION,
-            )
-        }
-
         val launchIntent = packageManager.getLaunchIntentForPackage(browserPackage)?.apply {
             addFlags(
                 Intent.FLAG_ACTIVITY_NEW_TASK or
                     Intent.FLAG_ACTIVITY_CLEAR_TOP or
                     Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED or
                     Intent.FLAG_ACTIVITY_NO_ANIMATION,
             )
         }
 
         try {
-            // Important: don't use resolveActivity() here; package visibility restrictions can make it lie.
-            // Just try the intents, and fall back on exceptions.
+            Log.d("BlockGateActivity", "goBrowserHome pkg=$browserPackage domain=$blockedDomain")
+
+            // Important: don't use resolveActivity() here; package visibility restrictions can make it lie.
+            // Just try the intents, and fall back on exceptions.
+            try {
+                startActivity(safeUrlIntent)
+            } catch (e: Exception) {
+                Log.w("BlockGateActivity", "safeUrlIntent failed: ${e::class.java.simpleName}")
+            }
+
             if (browserPackage == "com.android.chrome" || browserPackage == "com.brave.browser") {
                 try {
                     startActivity(chromeNewTabIntent)
                 } catch (_: Exception) {
                 }
             }
             try {
                 startActivity(blankIntent)
             } catch (_: Exception) {
-                try {
-                    startActivity(homeIntent)
-                } catch (_: Exception) {
-                    if (launchIntent != null) {
-                        startActivity(launchIntent)
-                    } else {
-                        goHome()
-                        return
-                    }
-                }
+                if (launchIntent != null) {
+                    startActivity(launchIntent)
+                } else {
+                    goHome()
+                    return
+                }
             }
         } catch (_: Exception) {
             goHome()
             return
         }
*** End Patch