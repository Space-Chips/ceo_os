*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/BlockingAccessibilityService.kt
@@
 import android.view.accessibility.AccessibilityEvent
 import com.wakeapp.ceoos.blocking.BlockStateCache
-import com.wakeapp.ceoos.blocking.BlockingNavigator
 import com.wakeapp.ceoos.blocking.CachedBlockDecision
 import com.wakeapp.ceoos.blocking.ForegroundBlockInterceptor
 import com.wakeapp.ceoos.blocking.LauncherIconTracker
 import com.wakeapp.ceoos.blocking.LauncherOverlayController
@@
     private lateinit var foregroundAppDetector: ForegroundAppDetector
     private lateinit var appBlockEngine: AppBlockEngine
     private lateinit var gateLauncher: GateLauncher
-    private lateinit var blockingNavigator: BlockingNavigator
     private lateinit var blockStateCache: BlockStateCache
     private lateinit var foregroundInterceptor: ForegroundBlockInterceptor
     private lateinit var browserBlockingOverlay: BrowserBlockingOverlay
     private lateinit var launcherIconTracker: LauncherIconTracker
@@
         fun onBlockGateHidden() {
-            instance?.gateLauncher?.onGateHidden()
-            instance?.shield?.hide()
+            instance?.onGateHiddenByActivity()
         }
@@
         appBlockEngine = AppBlockEngine(BrowserBlockCoordinator())
         gateLauncher = GateLauncher()
-        blockingNavigator = BlockingNavigator(gateLauncher)
         blockStateCache = BlockStateCache()
         foregroundInterceptor = ForegroundBlockInterceptor(blockStateCache)
         homePackagesCache = BlockPolicyRepository.homePackages(this)
         shield = InterceptionShield(this)
         browserBlockingOverlay = BrowserBlockingOverlay(this)
+        launcherIconTracker = LauncherIconTracker(this).apply { refreshIndex() }
+        launcherOverlayController = LauncherOverlayController(this) { packageName ->
+            ensureGateForBlockedApp(
+                BlockDecision.BlockApp(packageName = packageName, reason = "blocked_app"),
+                fallbackReason = "launcher_tap",
+            )
+        }
         registerPolicyListener()
         evaluateCurrentState()
         maybeReturnToAppFromSettings(SystemClock.elapsedRealtime())
@@
         if (resolvedPackage != null) {
             val lastPackage = lastForegroundPackage
             val lastAt = lastEventAtElapsedMillis
             if (lastPackage == resolvedPackage &&
                 event.eventType != AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED
             ) {
-                val cooldown = when {
-                    isBrowserEvent && isBrowserSignalEvent(event.eventType) -> 40L
-                    isBrowserEvent -> 90L
-                    event.eventType == AccessibilityEvent.TYPE_WINDOW_CONTENT_CHANGED -> 120L
-                    else -> 140L
-                }
+                val cooldown = when {
+                    isBrowserEvent && isBrowserSignalEvent(event.eventType) -> 35L
+                    isBrowserEvent -> 70L
+                    else -> 0L
+                }
                 if (startedAtElapsed - lastAt < cooldown) {
                     return
                 }
             }
@@
         handleSnapshot(
             snapshot = stabilizedSnapshot,
+            root = snapshotRoot,
             startedAt = startedAtWall,
             startedAtElapsed = startedAtElapsed,
             startedAtUptime = startedAtUptime,
             eventUptimeMillis = event.eventTime,
@@
         pendingWebOverlayRunnable = null
         pendingWebOverlayDecision = null
         pendingWebOverlaySnapshot = null
+        pendingGateDismissRunnable = null
+        desiredBlockedAppPackage = null
+        gateShownForPackage = null
         shield?.hide()
         browserBlockingOverlay.hide()
+        launcherOverlayController.clear()
         shield = null
     }
@@
             val snapshot = foregroundAppDetector.createSnapshot(syntheticEvent, root)
             handleSnapshot(
                 snapshot = snapshot,
+                root = root,
                 startedAt = System.currentTimeMillis(),
                 startedAtElapsed = SystemClock.elapsedRealtime(),
                 startedAtUptime = SystemClock.uptimeMillis(),
                 eventUptimeMillis = syntheticEvent.eventTime,
@@
     private fun handleSnapshot(
         snapshot: ForegroundSnapshot,
+        root: android.view.accessibility.AccessibilityNodeInfo?,
         startedAt: Long,
         startedAtElapsed: Long,
         startedAtUptime: Long,
         eventUptimeMillis: Long,
@@
         if (foregroundAppDetector.isGateSnapshot(snapshot)) {
             return
         }
-        if (BlockGateActivity.isVisible) {
-            return
-        }
 
         val policyState = getPolicyStateFast()
         blockStateCache.update(policyState)
+        updateLauncherOverlays(snapshot, root)
 
         val decision = foregroundInterceptor.decisionForSnapshot(
             snapshot = snapshot,
             policyState = policyState,
@@
         var gateFailureReason: String? = null
         val gateLaunched = when (effectiveDecision) {
             BlockDecision.Allow -> {
                 cancelPendingWebOverlay()
-                if (!BlockGateActivity.isVisible) {
-                    blockingNavigator.dismiss(this)
-                }
-                shield?.hide()
+                ensureGateDismissed()
                 browserBlockingOverlay.hide()
                 false
             }
             BlockDecision.IgnoreTransient -> {
                 cancelPendingWebOverlay()
                 if (!snapshot.isBrowser) {
                     browserBlockingOverlay.hide()
+                    ensureGateDismissed()
                 }
                 false
             }
             is BlockDecision.BlockApp -> {
                 cancelPendingWebOverlay()
                 browserBlockingOverlay.hide()
-                shield?.show()
-                val launchResult = gateLauncher.launch(this, effectiveDecision)
+                val launchResult = ensureGateForBlockedApp(effectiveDecision)
                 if (!launchResult.launched) {
                     gateFailureReason = launchResult.reason
                     if (shouldForceFallback(launchResult.reason)) {
                         enforceFallback(effectiveDecision)
                     }
-                    shield?.hide()
                 }
                 launchResult.launched
             }
             is BlockDecision.BlockUrl -> {
-                shield?.hide()
+                ensureGateDismissed()
                 scheduleWebOverlay(snapshot, effectiveDecision)
                 false
             }
@@
     private fun shouldForceFallback(gateFailureReason: String?): Boolean {
-        return gateFailureReason != "throttled_duplicate" &&
-            gateFailureReason != "already_visible_same_target"
+        return gateFailureReason != null
     }
@@
     private fun cancelPendingWebOverlay() {
         pendingWebOverlayRunnable?.let { mainHandler.removeCallbacks(it) }
         pendingWebOverlayRunnable = null
         pendingWebOverlayDecision = null
         pendingWebOverlaySnapshot = null
@@
     private fun decisionLabel(decision: BlockDecision): String {
         return when (decision) {
             BlockDecision.Allow -> "allow"
             BlockDecision.IgnoreTransient -> "ignore_transient"
             is BlockDecision.BlockApp -> "block_app:${decision.reason}"
             is BlockDecision.BlockUrl -> "block_url:${decision.reason}"
         }
     }
+
+    private fun ensureGateForBlockedApp(
+        decision: BlockDecision.BlockApp,
+        fallbackReason: String? = null,
+    ): GateLauncher.GateLaunchResult {
+        desiredBlockedAppPackage = decision.packageName
+        cancelPendingGateDismiss()
+        browserBlockingOverlay.hide()
+        shield?.show()
+
+        if (BlockGateActivity.isVisible && gateShownForPackage == decision.packageName) {
+            return GateLauncher.GateLaunchResult(launched = true, reason = fallbackReason)
+        }
+
+        val result = gateLauncher.launch(this, decision)
+        if (result.launched) {
+            gateShownForPackage = decision.packageName
+        } else if (shouldForceFallback(result.reason)) {
+            shield?.hide()
+        }
+        return result
+    }
+
+    private fun ensureGateDismissed() {
+        desiredBlockedAppPackage = null
+        cancelPendingGateDismiss()
+
+        val dismissRunnable = Runnable {
+            if (desiredBlockedAppPackage != null) return@Runnable
+            gateShownForPackage = null
+            gateLauncher.dismiss(this)
+            shield?.hide()
+            pendingGateDismissRunnable = null
+        }
+        pendingGateDismissRunnable = dismissRunnable
+        mainHandler.postDelayed(dismissRunnable, 120L)
+    }
+
+    private fun cancelPendingGateDismiss() {
+        pendingGateDismissRunnable?.let { mainHandler.removeCallbacks(it) }
+        pendingGateDismissRunnable = null
+    }
+
+    private fun updateLauncherOverlays(
+        snapshot: ForegroundSnapshot,
+        root: android.view.accessibility.AccessibilityNodeInfo?,
+    ) {
+        val packageName = snapshot.packageName?.trim()
+        if (packageName.isNullOrEmpty() || !homePackagesCache.contains(packageName)) {
+            launcherOverlayController.clear()
+            return
+        }
+
+        val effectiveRoot = root ?: rootInActiveWindow
+        val targets = launcherIconTracker.findTargets(effectiveRoot)
+        if (targets.isEmpty()) {
+            launcherOverlayController.clear()
+            return
+        }
+        val blockedPackages = targets
+            .asSequence()
+            .map { it.packageName }
+            .filter { blockStateCache.decisionFor(it).blocked }
+            .toSet()
+        launcherOverlayController.updateOverlays(
+            visibleTargets = targets,
+            blockedPackages = blockedPackages,
+        )
+    }
+
+    private fun onGateHiddenByActivity() {
+        gateLauncher.onGateHidden()
+        gateShownForPackage = null
+        shield?.hide()
+        if (desiredBlockedAppPackage != null) {
+            mainHandler.post {
+                evaluateCurrentState()
+            }
+        }
+    }
 }
*** End Patch
