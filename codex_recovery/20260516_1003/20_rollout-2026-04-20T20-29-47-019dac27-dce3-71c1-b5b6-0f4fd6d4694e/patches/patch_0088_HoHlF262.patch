*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/BlockingAccessibilityService.kt
@@
 import android.os.Process
 import android.provider.Settings
 import android.os.SystemClock
 import android.util.Log
+import android.view.accessibility.AccessibilityNodeInfo
 import android.view.accessibility.AccessibilityEvent
@@
     private fun navigateHomeFromGate(
         blockType: String?,
         browserPackage: String?,
         blockedDomain: String?,
@@
         }
 
         armBrowserExitGrace(browserPkg, blockedDomain)
         mainHandler.postDelayed(
             {
-                performGlobalAction(GLOBAL_ACTION_BACK)
+                if (!attemptCloseBlockedBrowserSurface(browserPkg)) {
+                    performGlobalAction(GLOBAL_ACTION_BACK)
+                }
             },
-            80L,
+            40L,
         )
         mainHandler.postDelayed(
             {
-                performGlobalAction(GLOBAL_ACTION_BACK)
+                if (!attemptCloseBlockedBrowserSurface(browserPkg)) {
+                    performGlobalAction(GLOBAL_ACTION_BACK)
+                }
             },
-            210L,
+            170L,
         )
         mainHandler.postDelayed(
             {
                 performGlobalAction(GLOBAL_ACTION_HOME)
             },
-            360L,
+            330L,
+        )
+        mainHandler.postDelayed(
+            {
+                performGlobalAction(GLOBAL_ACTION_HOME)
+            },
+            520L,
         )
     }
+
+    private fun attemptCloseBlockedBrowserSurface(browserPackage: String): Boolean {
+        val root = rootInActiveWindow ?: return false
+        val rootPkg = root.packageName?.toString()?.trim().orEmpty()
+        if (rootPkg.isEmpty()) return false
+
+        val knownBrowserPkgs = linkedSetOf(
+            browserPackage,
+            "com.android.chrome",
+            "com.brave.browser",
+        )
+        if (rootPkg !in knownBrowserPkgs && rootPkg != "com.google.android.googlequicksearchbox") {
+            return false
+        }
+
+        val candidateViewIds = mutableListOf<String>()
+        for (pkg in knownBrowserPkgs) {
+            candidateViewIds += "$pkg:id/close_button"
+            candidateViewIds += "$pkg:id/toolbar_close_button"
+            candidateViewIds += "$pkg:id/custom_tabs_close_button"
+            candidateViewIds += "$pkg:id/tab_close_button"
+            candidateViewIds += "$pkg:id/close"
+        }
+
+        for (viewId in candidateViewIds) {
+            try {
+                val nodes = root.findAccessibilityNodeInfosByViewId(viewId)
+                if (nodes.isNullOrEmpty()) continue
+                for (node in nodes) {
+                    if (clickNodeOrClickableAncestor(node)) {
+                        return true
+                    }
+                }
+            } catch (_: Exception) {
+            }
+        }
+
+        val closeKeywords = listOf(
+            "close tab",
+            "close",
+            "fermer l'onglet",
+            "fermer",
+            "dismiss",
+        )
+        return clickNodeBySemanticLabel(root, closeKeywords)
+    }
+
+    private fun clickNodeBySemanticLabel(
+        root: AccessibilityNodeInfo,
+        keywords: List<String>,
+    ): Boolean {
+        val stack = ArrayDeque<AccessibilityNodeInfo>()
+        val visited = HashSet<Int>()
+        stack.add(root)
+
+        while (stack.isNotEmpty()) {
+            val node = stack.removeLast()
+            val identity = System.identityHashCode(node)
+            if (!visited.add(identity)) continue
+
+            val text = node.text?.toString()?.trim()?.lowercase().orEmpty()
+            val desc = node.contentDescription?.toString()?.trim()?.lowercase().orEmpty()
+            val labelMatches = keywords.any { keyword ->
+                text.contains(keyword) || desc.contains(keyword)
+            }
+            if (labelMatches && clickNodeOrClickableAncestor(node)) {
+                return true
+            }
+
+            for (i in 0 until node.childCount) {
+                node.getChild(i)?.let { stack.add(it) }
+            }
+        }
+        return false
+    }
+
+    private fun clickNodeOrClickableAncestor(node: AccessibilityNodeInfo?): Boolean {
+        var cursor = node
+        var depth = 0
+        while (cursor != null && depth < 6) {
+            val clicked = cursor.performAction(AccessibilityNodeInfo.ACTION_CLICK)
+            if (clicked) return true
+            cursor = cursor.parent
+            depth++
+        }
+        return false
+    }
*** End Patch
