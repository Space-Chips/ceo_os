*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/blocking/LauncherIconTarget.kt
@@
 data class OverlayKey(
     val packageName: String,
+    val label: String,
     val left: Int,
     val top: Int,
     val right: Int,
     val bottom: Int,
 ) {
     companion object {
-        private fun snap(value: Int): Int = (value / 2) * 2
+        private fun snap(value: Int): Int = value
 
         fun from(target: LauncherIconTarget): OverlayKey = OverlayKey(
             packageName = target.packageName,
+            label = target.label,
             left = snap(target.bounds.left),
             top = snap(target.bounds.top),
             right = snap(target.bounds.right),
*** End Patch
