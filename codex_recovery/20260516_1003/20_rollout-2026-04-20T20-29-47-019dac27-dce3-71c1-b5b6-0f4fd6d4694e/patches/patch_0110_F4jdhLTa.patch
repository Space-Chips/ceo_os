*** Begin Patch
*** Update File: /Users/timo/ceo_os/android/app/src/main/kotlin/com/wakeapp/ceoos/GateLauncher.kt
@@
 import android.content.Context
 import android.content.Intent
 import android.content.pm.PackageManager
-import android.os.SystemClock
 import android.util.Log
@@
-    private var lastLaunchSignature: String? = null
-    private var lastLaunchAtMillis: Long = 0L
     private val appLabelCache = mutableMapOf<String, String>()
@@
         val payload = payloadForDecision(context.packageManager, decision)
             ?: return GateLaunchResult(launched = false, reason = "unsupported_decision")
-        val signature = "${payload.blockType}|${payload.target}|${payload.reason}"
-        val now = SystemClock.elapsedRealtime()
-        if (signature == lastLaunchSignature && now - lastLaunchAtMillis < 240L) {
-            return GateLaunchResult(launched = false, reason = "throttled_duplicate")
-        }
-        if (BlockGateActivity.isVisible && signature == lastLaunchSignature) {
-            return GateLaunchResult(launched = false, reason = "already_visible_same_target")
-        }
 
         return try {
             val intent = Intent(context, BlockGateActivity::class.java).apply {
@@
             }
             context.startActivity(intent)
-            lastLaunchSignature = signature
-            lastLaunchAtMillis = now
             GateLaunchResult(launched = true, reason = null)
         } catch (error: Exception) {
@@
     fun dismiss(context: Context) {
-        lastLaunchSignature = null
         try {
             context.sendBroadcast(
                 Intent(BlockGateActivity.ACTION_DISMISS).apply {
@@
 
     fun onGateHidden() {
-        lastLaunchSignature = null
+        // no-op: lifecycle synchronization is handled in BlockingAccessibilityService
     }
*** End Patch
