*** Begin Patch
*** Update File: android/app/src/main/kotlin/com/wakeapp/ceoos/blocking/LauncherIconTarget.kt
@@
     companion object {
+        private fun snap(value: Int): Int = (value / 2) * 2
+
         fun from(target: LauncherIconTarget): OverlayKey = OverlayKey(
             packageName = target.packageName,
-            left = target.bounds.left,
-            top = target.bounds.top,
-            right = target.bounds.right,
-            bottom = target.bounds.bottom,
+            left = snap(target.bounds.left),
+            top = snap(target.bounds.top),
+            right = snap(target.bounds.right),
+            bottom = snap(target.bounds.bottom),
         )
     }
 }
*** End Patch
