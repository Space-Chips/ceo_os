*** Begin Patch
*** Update File: /Users/timo/ceo_os/ios/FocusActivityMonitor/FocusActivityMonitorExtension.swift
@@
 private enum FocusMonitorShared {
     static let appGroupId = "group.com.wakeapp.ceoos"
     static let selectionStorageKey = "focus_selection_json_v1"
+    static let classicSelectionStorageKey = "classic_selection_payloads_v1"
+    static let classicEnabledStorageKey = "classic_shield_enabled_v1"
 }
@@
     private func readSelection() -> FamilyActivitySelection {
         guard let defaults = UserDefaults(suiteName: FocusMonitorShared.appGroupId),
               let json = defaults.string(forKey: FocusMonitorShared.selectionStorageKey),
               let data = json.data(using: .utf8),
               let selection = try? JSONDecoder().decode(FamilyActivitySelection.self, from: data) else {
@@
         return selection
     }
+
+    private func mergeSelections(encodedPayloads: [String]) -> FamilyActivitySelection {
+        var merged = FamilyActivitySelection()
+        for payload in encodedPayloads {
+            guard let data = payload.data(using: .utf8),
+                  let decoded = try? JSONDecoder().decode(FamilyActivitySelection.self, from: data) else {
+                continue
+            }
+            merged.applicationTokens.formUnion(decoded.applicationTokens)
+            merged.categoryTokens.formUnion(decoded.categoryTokens)
+            merged.webDomainTokens.formUnion(decoded.webDomainTokens)
+        }
+        return merged
+    }
 
     private func applyShieldFromSelection() {
-        let selection = readSelection()
-        let hasSelection = !selection.applicationTokens.isEmpty
-            || !selection.categoryTokens.isEmpty
-            || !selection.webDomainTokens.isEmpty
+        let defaults = UserDefaults(suiteName: FocusMonitorShared.appGroupId)
+        var payloads: [String] = []
+        if let focusPayload = defaults?.string(forKey: FocusMonitorShared.selectionStorageKey),
+           !focusPayload.isEmpty {
+            payloads.append(focusPayload)
+        }
+        if defaults?.bool(forKey: FocusMonitorShared.classicEnabledStorageKey) ?? false {
+            payloads.append(contentsOf: defaults?.stringArray(forKey: FocusMonitorShared.classicSelectionStorageKey) ?? [])
+        }
+
+        let selection = payloads.isEmpty ? readSelection() : mergeSelections(encodedPayloads: payloads)
+        let hasSelection = !selection.applicationTokens.isEmpty
+            || !selection.categoryTokens.isEmpty
+            || !selection.webDomainTokens.isEmpty
 
         if hasSelection {
             store.shield.applications = selection.applicationTokens
             store.shield.applicationCategories = .specific(selection.categoryTokens)
             store.shield.webDomains = selection.webDomainTokens
@@
     override func intervalDidEnd(for activity: DeviceActivityName) {
         super.intervalDidEnd(for: activity)
         guard activity.rawValue.hasPrefix("focus_plan_activity_") || activity == .scheduledFocus else {
             return
         }
-        store.clearAllSettings()
+        let defaults = UserDefaults(suiteName: FocusMonitorShared.appGroupId)
+        if defaults?.bool(forKey: FocusMonitorShared.classicEnabledStorageKey) ?? false {
+            let payloads = defaults?.stringArray(forKey: FocusMonitorShared.classicSelectionStorageKey) ?? []
+            let selection = mergeSelections(encodedPayloads: payloads)
+            let hasSelection = !selection.applicationTokens.isEmpty
+                || !selection.categoryTokens.isEmpty
+                || !selection.webDomainTokens.isEmpty
+            if hasSelection {
+                store.shield.applications = selection.applicationTokens
+                store.shield.applicationCategories = .specific(selection.categoryTokens)
+                store.shield.webDomains = selection.webDomainTokens
+                return
+            }
+        }
+        store.clearAllSettings()
     }
 }
*** End Patch
