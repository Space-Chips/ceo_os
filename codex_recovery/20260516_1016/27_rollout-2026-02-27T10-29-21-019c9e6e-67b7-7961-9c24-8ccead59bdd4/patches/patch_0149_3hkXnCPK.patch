*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/rank/rank_screen.dart
@@
 import 'package:flutter/cupertino.dart';
 import 'package:go_router/go_router.dart';
+import 'package:provider/provider.dart';
 
 import '../../components/components.dart';
 import '../../core/models/user_models.dart';
+import '../../core/providers/language_provider.dart';
 import '../../core/repositories/feature_repository.dart';
 import '../../core/repositories/user_repository.dart';
 import '../../core/theme/app_colors.dart';
@@
   Widget build(BuildContext context) {
+    final language = context.watch<LanguageProvider>();
     final level = (_rank?.rankLevel ?? 1).clamp(1, 99);
     final nextTier = _tiers.where((tier) => tier.level > level).firstOrNull;
@@
-        middle: const NeoMonoText(
-          'RANK',
+        middle: NeoMonoText(
+          language.t('rank_title'),
           fontSize: 18,
           fontWeight: FontWeight.bold,
         ),
@@
                                     Text(
-                                      (_rank?.rankName ?? 'Panda')
+                                      (_rank?.rankName ?? language.t('starter'))
                                           .toUpperCase(),
                                       style: AppTypography.mono.copyWith(
                                         fontSize: 20,
@@
                                     Text(
-                                      'LEVEL $level',
+                                      '${language.t('rank_level')} $level',
                                       style: AppTypography.mono.copyWith(
                                         fontSize: 10,
                                         color: AppColors.tertiaryLabel,
@@
                               _metric(
-                                'POINTS',
+                                language.t('rank_points'),
                                 '${_rank?.totalRankPoints ?? 0}',
                               ),
                               _metric(
-                                'AT_RANK',
+                                language.t('rank_at_rank'),
                                 '${_rank?.daysAtCurrentRank ?? 0}d',
                               ),
                               _metric(
-                                'PREVIOUS',
+                                language.t('rank_previous'),
                                 (_rank?.previousRankName ?? '-').toUpperCase(),
                               ),
@@
                             child: Text(
-                              'Global position #$_globalPosition of $_globalTotal • Top $topPercent%',
+                              language
+                                  .t('rank_global_summary')
+                                  .replaceAll(
+                                    '{position}',
+                                    '$_globalPosition',
+                                  )
+                                  .replaceAll('{total}', '$_globalTotal')
+                                  .replaceAll('{top}', '$topPercent'),
                               style: AppTypography.mono.copyWith(
                                 fontSize: 10,
                                 color: AppColors.secondaryLabel,
@@
                           Text(
-                            'LIVE',
+                            language.t('live'),
                             style: AppTypography.mono.copyWith(
                               fontSize: 9,
                               color: AppColors.primaryOrange,
@@
                     child: Row(
                       children: [
-                        _metric('STREAK', '${_streak?.currentStreak ?? 0}d'),
-                        _metric('BEST', '${_streak?.longestStreak ?? 0}d'),
-                        _metric('SCREEN', '${_avgDailyScreenMinutes.round()}m'),
+                        _metric(
+                          language.t('streak'),
+                          '${_streak?.currentStreak ?? 0}d',
+                        ),
+                        _metric(
+                          language.t('rank_best'),
+                          '${_streak?.longestStreak ?? 0}d',
+                        ),
+                        _metric(
+                          language.t('rank_screen'),
+                          '${_avgDailyScreenMinutes.round()}m',
+                        ),
                       ],
                     ),
                   ),
@@
                           child: Text(
                             nextTier == null
-                                ? 'You are at the highest configured tier.'
-                                : 'Next tier: ${nextTier.name} · ${nextTier.requirement}',
+                                ? language.t('highest_tier')
+                                : '${language.t('next_tier')}: ${nextTier.name} · ${nextTier.requirement}',
                             style: AppTypography.mono.copyWith(
                               fontSize: 11,
                               color: AppColors.secondaryLabel,
@@
     final badge = isCurrent
-        ? 'CURRENT'
+        ? context.watch<LanguageProvider>().t('current')
         : isUnlocked
-        ? 'UNLOCKED'
-        : 'LOCKED';
+        ? context.watch<LanguageProvider>().t('unlocked')
+        : context.watch<LanguageProvider>().t('locked');
*** End Patch
