*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/screen_time_manager/family_time_screen.dart
@@
-import 'package:flutter/material.dart' show Colors;
 import 'package:flutter/services.dart';
@@
   bool _loading = true;
   bool _saving = false;
   int _sessionDuration = 60;
 
   Timer? _pollTimer;
+  bool get _isLightTheme => AppColors.background.computeLuminance() > 0.5;
+  Color get _primaryActionBg =>
+      _isLightTheme ? const Color(0xFF0F172A) : const Color(0xFFF8FAFC);
+  Color get _primaryActionFg =>
+      _isLightTheme ? const Color(0xFFF8FAFC) : const Color(0xFF0F172A);
*** End Patch
