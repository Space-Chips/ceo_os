*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/dashboard/dashboard_screen.dart
@@
 import 'package:flutter/cupertino.dart';
 import 'package:go_router/go_router.dart';
+import 'package:intl/intl.dart';
 import 'package:provider/provider.dart';
@@
   int _todayScreenMinutes = 0;
   int _avgScreenMinutes = 0;
+  bool _isConfirmingYesterday = false;
@@
   int _taskScore(ParetoTask task) {
     return (_importanceWeight(task.importanceLevel) * 10) +
         _timeWeight(task.timeDuration);
   }
+
+  String _formatDeadline(DateTime? deadline) {
+    if (deadline == null) return 'NO DATE';
+    final now = DateTime.now();
+    final today = DateTime(now.year, now.month, now.day);
+    final d = DateTime(deadline.year, deadline.month, deadline.day);
+    final diff = d.difference(today).inDays;
+    if (diff < 0) return 'OVERDUE ${diff.abs()}D';
+    if (diff == 0) return 'TODAY';
+    if (diff == 1) return 'TOMORROW';
+    return DateFormat('d MMM').format(deadline).toUpperCase();
+  }
+
+  Color _sourceColor(CalendarEvent event) {
+    if (event.sourceType == 'task') return AppColors.primaryOrange;
+    if (event.sourceType == 'habit') return AppColors.success;
+    return AppColors.accentSecondary;
+  }
+
+  String _sourceLabel(CalendarEvent event) {
+    if (event.sourceType == 'task') return 'TASK';
+    if (event.sourceType == 'habit') return 'HABIT';
+    return 'EVENT';
+  }
+
+  String _eventSlot(CalendarEvent event) {
+    final dateRaw = event.eventDate;
+    final timeRaw = event.eventTime ?? '--:--';
+    if (dateRaw == null || dateRaw.isEmpty) return timeRaw;
+    final parsed = DateTime.tryParse(dateRaw);
+    if (parsed == null) return '$dateRaw $timeRaw';
+    return '${DateFormat('d MMM').format(parsed)} • $timeRaw'.toUpperCase();
+  }
@@
   Future<void> _confirmYesterday() async {
+    if (_isConfirmingYesterday) return;
     final yesterday = DateTime.now().subtract(const Duration(days: 1));
     final provider = context.read<HabitProvider>();
+    setState(() => _isConfirmingYesterday = true);
 
-    for (final habit in _yesterdayHabits) {
-      await provider.setHabitCompletionForDate(
-        habitId: habit.id,
-        date: yesterday,
-        completed: _yesterdayStates[habit.id] ?? false,
-        state: 'validated',
-      );
+    try {
+      for (final habit in _yesterdayHabits) {
+        await provider.setHabitCompletionForDate(
+          habitId: habit.id,
+          date: yesterday,
+          completed: _yesterdayStates[habit.id] ?? false,
+          state: 'validated',
+        );
+      }
+      await _load();
+    } finally {
+      if (mounted) {
+        setState(() => _isConfirmingYesterday = false);
+      }
     }
-
-    await _load();
   }
*** End Patch
