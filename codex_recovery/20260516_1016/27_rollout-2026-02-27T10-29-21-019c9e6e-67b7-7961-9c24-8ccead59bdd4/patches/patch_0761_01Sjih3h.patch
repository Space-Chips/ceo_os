*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/services/stats_engine.dart
@@
   }) {
     final highlights = <StatsHighlight>[];
     if (daily != null) {
       highlights.add(
-        StatsHighlight(
-          label: 'Today Discipline',
-          value: '${daily.attentionScore}',
-          deltaLabel: 'Today',
-        ),
-      );
-      highlights.add(
         StatsHighlight(
           label: 'Deep Work',
           value: '${daily.deepWorkTimeMinutes}m',
@@
     if (monthly != null) {
       highlights.add(
         StatsHighlight(
-          label: 'Month Discipline',
-          value: '${monthly.monthlyAttentionScore}',
+          label: 'Monthly Sessions',
+          value: '${monthly.totalSessionsCompleted}',
           deltaLabel: monthly.rankChange == 0
               ? 'Rank stable'
               : 'Rank ${monthly.rankChange > 0 ? '+' : ''}${monthly.rankChange}',
*** End Patch
