*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/components/glass_card.dart
@@
     final tone = switch (level) {
       GlassCardLevel.subtle => 0.16,
       GlassCardLevel.standard => 0.24,
       GlassCardLevel.elevated => 0.34,
     };
+    final shadowSpec = switch (level) {
+      GlassCardLevel.subtle => (
+          alpha: AppColors.isDark ? 0.12 : 0.08,
+          blur: 12.0,
+          offsetY: 6.0,
+          spread: -8.0,
+        ),
+      GlassCardLevel.standard => (
+          alpha: AppColors.isDark ? 0.16 : 0.11,
+          blur: 18.0,
+          offsetY: 8.0,
+          spread: -10.0,
+        ),
+      GlassCardLevel.elevated => (
+          alpha: AppColors.isDark ? 0.22 : 0.14,
+          blur: 24.0,
+          offsetY: 12.0,
+          spread: -12.0,
+        ),
+    };
@@
         borderRadius: BorderRadius.circular(borderRadius),
         boxShadow: [
           BoxShadow(
-            color: AppColors.glassShadow.withValues(alpha: 0.34 + tone * 0.14),
-            blurRadius: 24 + (tone * 20),
-            offset: const Offset(0, 12),
-            spreadRadius: -14,
+            color: AppColors.glassShadow.withValues(alpha: shadowSpec.alpha),
+            blurRadius: shadowSpec.blur,
+            offset: Offset(0, shadowSpec.offsetY),
+            spreadRadius: shadowSpec.spread,
           ),
*** End Patch
