*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/components/liquid_button.dart
@@
 import 'package:flutter/cupertino.dart';
 import 'dart:ui';
 import 'package:flutter/material.dart' show Colors;
@@
             decoration: BoxDecoration(
               borderRadius: borderRadius,
               boxShadow: [
+                BoxShadow(
+                  color: AppColors.glassHighlightSoft.withValues(
+                    alpha: isDisabled ? 0.02 : 0.1,
+                  ),
+                  blurRadius: _isPressed ? 10 : 18,
+                  offset: Offset(0, _isPressed ? -2 : -4),
+                  spreadRadius: -14,
+                ),
                 if (!isDisabled)
                   BoxShadow(
                     color: effectiveGradient.first.withValues(
-                      alpha: _isPressed ? 0.14 : 0.34,
+                      alpha: _isPressed ? 0.16 : 0.4,
                     ),
-                    blurRadius: _isPressed ? 16 : 28,
-                    offset: Offset(0, _isPressed ? 5 : 11),
-                    spreadRadius: -8,
+                    blurRadius: _isPressed ? 18 : 32,
+                    offset: Offset(0, _isPressed ? 6 : 12),
+                    spreadRadius: -6,
                   ),
                 BoxShadow(
-                  color: AppColors.glassShadow.withValues(alpha: 0.24),
-                  blurRadius: 16,
-                  offset: const Offset(0, 8),
-                  spreadRadius: -10,
+                  color: AppColors.glassShadow.withValues(alpha: 0.34),
+                  blurRadius: 22,
+                  offset: const Offset(0, 10),
+                  spreadRadius: -8,
                 ),
               ],
             ),
             child: ClipRRect(
               borderRadius: borderRadius,
@@
                         border: Border.all(
                           color: isDisabled
                               ? AppColors.glassBorder
                               : AppColors.glassHighlight.withValues(
-                                  alpha: 0.34,
+                                  alpha: 0.42,
                                 ),
-                          width: 0.7,
+                          width: 0.76,
                         ),
                       ),
                     ),
+                    Positioned.fill(
+                      child: IgnorePointer(
+                        child: DecoratedBox(
+                          decoration: BoxDecoration(
+                            borderRadius: borderRadius,
+                            gradient: LinearGradient(
+                              begin: const Alignment(-0.9, -1),
+                              end: const Alignment(1, 1),
+                              colors: [
+                                AppColors.glassHighlight.withValues(
+                                  alpha: isDisabled ? 0.07 : 0.18,
+                                ),
+                                Colors.transparent,
+                                AppColors.glassShadowSoft.withValues(
+                                  alpha: isDisabled ? 0.04 : 0.13,
+                                ),
+                              ],
+                              stops: const [0, 0.42, 1],
+                            ),
+                          ),
+                        ),
+                      ),
+                    ),
                     Positioned(
                       top: 1.6,
                       left: 8,
                       right: 8,
@@
                           gradient: LinearGradient(
                             begin: Alignment.topCenter,
                             end: Alignment.bottomCenter,
                             colors: [
-                              AppColors.glassHighlight.withValues(alpha: 0.22),
+                              AppColors.glassHighlight.withValues(alpha: 0.28),
                               Colors.transparent,
                             ],
                           ),
                         ),
                       ),
                     ),
+                    Positioned(
+                      left: 1.5,
+                      right: 1.5,
+                      bottom: 1.5,
+                      child: IgnorePointer(
+                        child: Container(
+                          height: 1,
+                          decoration: BoxDecoration(
+                            borderRadius: BorderRadius.circular(999),
+                            color: AppColors.glassShadowSoft.withValues(
+                              alpha: 0.3,
+                            ),
+                          ),
+                        ),
+                      ),
+                    ),
*** End Patch
