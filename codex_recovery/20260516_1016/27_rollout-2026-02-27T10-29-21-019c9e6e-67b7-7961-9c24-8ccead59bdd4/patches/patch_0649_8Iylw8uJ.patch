*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/habits_screen.dart
@@
   return Container(
       padding: const EdgeInsets.all(12),
       decoration: BoxDecoration(
-        color: AppColors.white.withValues(alpha: 0.04),
+        color: AppColors.cardBackgroundStrong.withValues(alpha: 0.9),
         borderRadius: BorderRadius.circular(14),
         border: Border.all(
-          color: AppColors.white.withValues(alpha: 0.08),
+          color: AppColors.borderStrong.withValues(alpha: 0.9),
           width: 1,
         ),
+        boxShadow: [
+          BoxShadow(
+            color: AppColors.glassShadowSoft.withValues(alpha: 0.12),
+            blurRadius: 12,
+            offset: const Offset(0, 4),
+            spreadRadius: -8,
+          ),
+        ],
       ),
       child: Column(
@@
     padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 14),
     decoration: BoxDecoration(
       color: AppColors.inputBackground,
       borderRadius: BorderRadius.circular(14),
       border: Border.all(
         color: AppColors.inputBorder,
         width: 1,
       ),
+      boxShadow: [
+        BoxShadow(
+          color: AppColors.glassShadowSoft.withValues(alpha: 0.1),
+          blurRadius: 10,
+          offset: const Offset(0, 4),
+          spreadRadius: -8,
+        ),
+      ],
     ),
   );
 }
@@
   return BoxDecoration(
     gradient: LinearGradient(
       begin: Alignment.topCenter,
       end: Alignment.bottomCenter,
       colors: [AppColors.cardBackgroundAlt, AppColors.cardBase],
     ),
     borderRadius: BorderRadius.circular(radius),
     border: Border.all(
-      color: borderColor ?? AppColors.white.withValues(alpha: 0.06),
+      color: borderColor ?? AppColors.border,
       width: 1,
     ),
     boxShadow: [
       BoxShadow(
-        color: AppColors.black.withValues(alpha: 0.6),
-        blurRadius: 30,
-        offset: const Offset(0, 10),
+        color: AppColors.glassShadow.withValues(alpha: 0.16),
+        blurRadius: 18,
+        offset: const Offset(0, 8),
       ),
     ],
   );
 }
*** End Patch
