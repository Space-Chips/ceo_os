*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/focus/focus_screen.dart
@@
                             border: Border.all(
                               color: provider.focusDurationMinutes == preset
-                                  ? AppColors.selectionOutline
+                                  ? (AppColors.isDark
+                                      ? AppColors.selectionOutline
+                                      : AppColors.borderStrong.withValues(
+                                          alpha: 0.96,
+                                        ))
                                   : AppColors.isDark
                                       ? AppColors.border
-                                      : AppColors.borderStrong.withValues(
-                                          alpha: 0.9,
+                                      : AppColors.border.withValues(
+                                          alpha: 0.72,
                                         ),
-                              width: AppColors.isDark ? 1 : 1.05,
+                              width: provider.focusDurationMinutes == preset
+                                  ? (AppColors.isDark ? 1 : 1.2)
+                                  : (AppColors.isDark ? 1 : 1.0),
                             ),
*** End Patch
