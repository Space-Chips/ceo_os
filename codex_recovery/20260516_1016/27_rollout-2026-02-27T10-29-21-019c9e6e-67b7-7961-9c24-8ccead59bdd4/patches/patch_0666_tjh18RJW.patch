*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/tasks_screen.dart
@@
 import '../../core/providers/task_provider.dart';
 import '../../core/repositories/premium_repository.dart';
 import '../../core/theme/app_colors.dart';
 import '../../core/theme/app_typography.dart';
 import 'add_task_sheet.dart';
+import 'task_importance_theme.dart';
@@
   _TaskPalette _paletteForImportance(String? importanceRaw) {
-    switch ((importanceRaw ?? '').toLowerCase()) {
-      case 'crucial':
-      case 'critical':
-        return _TaskPalette(
-          border: AppColors.error.withValues(alpha: 0.4),
-          start: Color.alphaBlend(
-            AppColors.error.withValues(alpha: 0.04),
-            AppColors.cardBackgroundAlt,
-          ),
-          end: AppColors.cardBase,
-          badgeBg: AppColors.error.withValues(alpha: 0.12),
-          badgeBorder: AppColors.error.withValues(alpha: 0.35),
-          badgeText: AppColors.error,
-        );
-      case 'essential':
-      case 'high':
-        final isLightTheme = !AppColors.isDark;
-        return _TaskPalette(
-          border: isLightTheme
-              ? AppColors.warning.withValues(alpha: 0.26)
-              : AppColors.warning.withValues(alpha: 0.4),
-          start: isLightTheme
-              ? Color.alphaBlend(
-                  AppColors.warning.withValues(alpha: 0.035),
-                  AppColors.cardBackgroundAlt,
-                )
-              : AppColors.warning.withValues(alpha: 0.12),
-          end: isLightTheme
-              ? AppColors.cardBase
-              : AppColors.warning.withValues(alpha: 0.05),
-          badgeBg: isLightTheme
-              ? AppColors.warning.withValues(alpha: 0.08)
-              : AppColors.warning.withValues(alpha: 0.12),
-          badgeBorder: isLightTheme
-              ? AppColors.warning.withValues(alpha: 0.24)
-              : AppColors.warning.withValues(alpha: 0.35),
-          badgeText: AppColors.warning,
-        );
-      case 'average':
-      case 'medium':
-        return _TaskPalette(
-          border: AppColors.activeBorder,
-          start: AppColors.accentSurfaceSoft,
-          end: AppColors.accentSurfaceSoft.withValues(alpha: 0.35),
-          badgeBg: AppColors.accentSurfaceSoft,
-          badgeBorder: AppColors.activeBorder,
-          badgeText: AppColors.accentText,
-        );
-      case 'low':
-      default:
-        return _TaskPalette(
-          border: AppColors.borderStrong,
-          start: AppColors.cardBackgroundAlt,
-          end: AppColors.cardBase,
-          badgeBg: AppColors.label.withValues(alpha: 0.08),
-          badgeBorder: AppColors.borderStrong,
-          badgeText: AppColors.secondaryLabel,
-        );
-    }
+    final style = TaskImportanceTheme.card(importanceRaw);
+    return _TaskPalette(
+      border: style.border,
+      start: style.start,
+      end: style.end,
+      badgeBg: style.badge.background,
+      badgeBorder: style.badge.border,
+      badgeText: style.badge.text,
+    );
   }
 
   String _importanceLabel(String? raw) {
-    switch ((raw ?? '').toLowerCase()) {
-      case 'crucial':
-      case 'critical':
-        return 'CRUCIAL';
-      case 'essential':
-      case 'high':
-        return 'ESSENTIAL';
-      case 'average':
-      case 'medium':
-        return 'AVERAGE';
-      case 'low':
-        return 'LOW';
-      default:
-        return 'LOW';
-    }
+    return TaskImportanceTheme.label(raw);
   }
@@
 _HistoryBadgeStyle _historyBadgeStyle(String? raw) {
-  switch ((raw ?? '').toLowerCase()) {
-    case 'crucial':
-    case 'critical':
-      return _HistoryBadgeStyle(
-        background: AppColors.error.withValues(alpha: 0.12),
-        border: AppColors.error.withValues(alpha: 0.35),
-        text: AppColors.error,
-      );
-    case 'low':
-      return _HistoryBadgeStyle(
-        background: AppColors.accentSurfaceSoft,
-        border: AppColors.activeBorder,
-        text: AppColors.accentText,
-      );
-    case 'average':
-    case 'medium':
-      return _HistoryBadgeStyle(
-        background: AppColors.label.withValues(alpha: 0.08),
-        border: AppColors.borderStrong,
-        text: AppColors.secondaryLabel,
-      );
-    default:
-      return _HistoryBadgeStyle(
-        background: AppColors.label.withValues(alpha: 0.08),
-        border: AppColors.borderStrong,
-        text: AppColors.secondaryLabel,
-      );
-  }
+  final style = TaskImportanceTheme.badge(raw);
+  return _HistoryBadgeStyle(
+    background: style.background,
+    border: style.border,
+    text: style.text,
+  );
 }
*** End Patch
