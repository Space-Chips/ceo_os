*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/habits/add_habit_sheet.dart
@@
 class _TimePickerRow extends StatelessWidget {
@@
   }
 }
+
+BoxDecoration _solidCardDecoration({
+  double radius = 18,
+  Color? borderColor,
+}) {
+  return BoxDecoration(
+    gradient: const LinearGradient(
+      begin: Alignment.topCenter,
+      end: Alignment.bottomCenter,
+      colors: [Color(0xFF1A1A1A), Color(0xFF111111)],
+    ),
+    borderRadius: BorderRadius.circular(radius),
+    border: Border.all(
+      color: borderColor ?? AppColors.white.withValues(alpha: 0.06),
+      width: 1,
+    ),
+    boxShadow: [
+      BoxShadow(
+        color: AppColors.black.withValues(alpha: 0.5),
+        blurRadius: 20,
+        offset: const Offset(0, 8),
+      ),
+    ],
+  );
+}
+
+class _HabitToggle extends StatelessWidget {
+  final bool value;
+  final ValueChanged<bool> onChanged;
+
+  const _HabitToggle({required this.value, required this.onChanged});
+
+  @override
+  Widget build(BuildContext context) {
+    return GestureDetector(
+      onTap: () => onChanged(!value),
+      child: AnimatedContainer(
+        duration: const Duration(milliseconds: 160),
+        width: 44,
+        height: 24,
+        padding: const EdgeInsets.all(2),
+        decoration: BoxDecoration(
+          borderRadius: BorderRadius.circular(12),
+          color: value
+              ? const Color(0xFF55D9A5)
+              : AppColors.white.withValues(alpha: 0.15),
+        ),
+        child: Align(
+          alignment: value ? Alignment.centerRight : Alignment.centerLeft,
+          child: Container(
+            width: 20,
+            height: 20,
+            decoration: BoxDecoration(
+              shape: BoxShape.circle,
+              color: value ? CupertinoColors.black : CupertinoColors.white,
+            ),
+          ),
+        ),
+      ),
+    );
+  }
+}
+
+class _PrimaryProtocolButton extends StatelessWidget {
+  final String label;
+  final Future<void> Function() onTap;
+
+  const _PrimaryProtocolButton({required this.label, required this.onTap});
+
+  @override
+  Widget build(BuildContext context) {
+    return _ProtocolPressScale(
+      onTap: onTap,
+      child: Container(
+        width: double.infinity,
+        padding: const EdgeInsets.symmetric(horizontal: 18, vertical: 18),
+        decoration: BoxDecoration(
+          gradient: const LinearGradient(
+            begin: Alignment.topCenter,
+            end: Alignment.bottomCenter,
+            colors: [Color(0xFFE5E5E5), Color(0xFFCFCFCF)],
+          ),
+          borderRadius: BorderRadius.circular(18),
+        ),
+        child: Center(
+          child: Text(
+            label,
+            style: AppTypography.callout.copyWith(
+              fontSize: 15,
+              fontWeight: FontWeight.w600,
+              color: AppColors.background,
+            ),
+          ),
+        ),
+      ),
+    );
+  }
+}
+
+class _ProtocolPressScale extends StatefulWidget {
+  final Widget child;
+  final Future<void> Function() onTap;
+
+  const _ProtocolPressScale({required this.child, required this.onTap});
+
+  @override
+  State<_ProtocolPressScale> createState() => _ProtocolPressScaleState();
+}
+
+class _ProtocolPressScaleState extends State<_ProtocolPressScale> {
+  bool _pressed = false;
+
+  void _setPressed(bool value) {
+    if (!mounted || _pressed == value) return;
+    setState(() => _pressed = value);
+  }
+
+  Future<void> _handleTap() async {
+    await widget.onTap();
+  }
+
+  @override
+  Widget build(BuildContext context) {
+    return Listener(
+      onPointerDown: (_) => _setPressed(true),
+      onPointerUp: (_) => _setPressed(false),
+      onPointerCancel: (_) => _setPressed(false),
+      child: AnimatedScale(
+        scale: _pressed ? 1.01 : 1,
+        duration: const Duration(milliseconds: 120),
+        curve: Curves.easeOutCubic,
+        child: GestureDetector(
+          behavior: HitTestBehavior.translucent,
+          onTap: _handleTap,
+          child: widget.child,
+        ),
+      ),
+    );
+  }
+}
*** End Patch
