*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
                           child: _blockedItemCard(
                             title: (site.urlDomain ?? 'Unknown website')
                                 .toUpperCase(),
-                            subtitle:
-                                'Blocked • daily limit ${site.timeLimitMinutes ?? 0}m',
-                            onAdd15: _saving
-                                ? null
-                                : () => _runMutation(
-                                    () => _repo.addBlockedWebsiteTime(
-                                      site.id,
-                                      15,
-                                    ),
-                                  ),
-                            onAdd30: _saving
-                                ? null
-                                : () => _runMutation(
-                                    () => _repo.addBlockedWebsiteTime(
-                                      site.id,
-                                      30,
-                                    ),
-                                  ),
+                            subtitle: (site.timeLimitMinutes ?? 0) > 0
+                                ? 'Daily limit ${(site.timeLimitMinutes ?? 0)}m'
+                                : 'Fully blocked',
+                            onSetLimit: _saving
+                                ? null
+                                : () => _editDailyLimit(
+                                    title: site.urlDomain ?? 'Website',
+                                    currentMinutes: site.timeLimitMinutes ?? 0,
+                                    onSave: (minutes) => _repo
+                                        .setBlockedWebsiteTime(
+                                          site.id,
+                                          minutes,
+                                        ),
+                                  ),
                             onDelete: _saving
                                 ? null
                                 : () => _runMutation(
                                     () => _repo.deleteBlockedWebsite(site.id),
                                   ),
                             onTap: () {
+                              final limit = site.timeLimitMinutes ?? 0;
+                              if (limit <= 0) {
+                                _showNotice(
+                                  title: site.urlDomain ?? 'Website',
+                                  message:
+                                      'This website is fully blocked (no daily allowance).',
+                                );
+                                return;
+                              }
                               final used = _usedMinutesForEntity(
                                 entityName: site.urlDomain ?? '',
                                 isWebsite: true,
                               );
                               _showRemainingDialog(
                                 title: site.urlDomain ?? 'Website',
-                                limitMinutes: site.timeLimitMinutes ?? 0,
+                                limitMinutes: limit,
                                 usedMinutes: used,
                               );
                             },
                           ),
                         ),
                       ),
-                    const SizedBox(height: 10),
-                    _sectionHeader(
-                      title: 'WEBSITE_LIMITS',
-                      subtitle:
-                          '${limitedSites.length} sites with daily screen-time limit',
-                      trailing: null,
-                    ),
-                    if (limitedSites.isEmpty)
-                      _emptyCard(
-                        'No site time limits yet. Use +15 / +30 on sites.',
-                      )
-                    else
-                      ...limitedSites.map(
-                        (site) => Padding(
-                          padding: const EdgeInsets.only(bottom: 8),
-                          child: _limitCard(
-                            title: (site.urlDomain ?? 'Unknown site')
-                                .toUpperCase(),
-                            limitMinutes: site.timeLimitMinutes ?? 0,
-                            usedMinutes: _usedMinutesForEntity(
-                              entityName: site.urlDomain ?? '',
-                              isWebsite: true,
-                            ),
-                            onTap: () => _showRemainingDialog(
-                              title: site.urlDomain ?? 'Website',
-                              limitMinutes: site.timeLimitMinutes ?? 0,
-                              usedMinutes: _usedMinutesForEntity(
-                                entityName: site.urlDomain ?? '',
-                                isWebsite: true,
-                              ),
-                            ),
-                          ),
-                        ),
-                      ),
                     const SizedBox(height: 14),
*** End Patch
