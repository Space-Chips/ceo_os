*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/notes_screen.dart
@@
 class _NotesScreenState extends State<NotesScreen> {
@@
   Timer? _autosaveDebounce;
   String _lastSavedText = '';
   String _lastSavedTitle = '';
+
+  Color get _noteAccent => AppColors.accentSecondary;
+  Color get _selectionBg => _noteAccent.withValues(alpha: 0.24);
+  Color get _selectionBorder => _noteAccent.withValues(alpha: 0.74);
+  Color get _selectionText => _noteAccent.withValues(alpha: 0.96);
@@
           Text(
             'Notes',
             style: AppTypography.mono.copyWith(
               fontSize: 46 / 2,
               fontWeight: FontWeight.w900,
-              color: const Color(0xFF7F6DFF),
+              color: _noteAccent,
             ),
           ),
@@
                       color: active
-                          ? const Color(0xFF44207A).withValues(alpha: 0.78)
+                          ? _selectionBg
                           : AppColors.backgroundLight.withValues(alpha: 0.58),
                       border: Border.all(
                         color: active
-                            ? const Color(0xFF5A4DFF)
+                            ? _selectionBorder
                             : AppColors.glassBorder,
                         width: 0.7,
                       ),
@@
                             color: active
-                                ? const Color(0xFFCBD0FF)
+                                ? _selectionText
                                 : AppColors.secondaryLabel,
                           ),
@@
                 color: selected
-                    ? const Color(0xFF44207A).withValues(alpha: 0.78)
+                    ? _selectionBg
                     : AppColors.background.withValues(alpha: 0),
                 border: Border.all(
                   color: selected
-                      ? const Color(0xFF5A4DFF)
+                      ? _selectionBorder
                       : AppColors.background.withValues(alpha: 0),
                   width: 0.7,
                 ),
@@
                       color: selected
-                          ? const Color(0xFFD1D2FF)
+                          ? _selectionText
                           : AppColors.tertiaryLabel,
                     )
@@
           color: selected
-              ? const Color(0xFF44207A).withValues(alpha: 0.78)
+              ? _selectionBg
               : AppColors.background.withValues(alpha: 0),
           border: Border.all(
             color: selected
-                ? const Color(0xFF5A4DFF)
+                ? _selectionBorder
                 : AppColors.background.withValues(alpha: 0),
             width: 0.7,
           ),
@@
       child: _glowSurface(
-        glowColor: const Color(0xFF6B4CFF).withValues(alpha: 0.33),
+        glowColor: _noteAccent.withValues(alpha: 0.3),
         borderRadius: 24,
         child: Container(
           width: 76,
           height: 76,
           decoration: BoxDecoration(
             borderRadius: BorderRadius.circular(24),
-            gradient: const LinearGradient(
+            gradient: LinearGradient(
               begin: Alignment.topLeft,
               end: Alignment.bottomRight,
-              colors: [Color(0xFF8B6DFF), Color(0xFF6E42F4)],
+              colors: [
+                AppColors.accentSecondary.withValues(alpha: 0.96),
+                AppColors.primaryOrange.withValues(alpha: 0.9),
+              ],
             ),
             border: Border.all(
-              color: const Color(0xFF947CFF).withValues(alpha: 0.9),
+              color: _noteAccent.withValues(alpha: 0.9),
               width: 0.8,
             ),
           ),
*** End Patch
