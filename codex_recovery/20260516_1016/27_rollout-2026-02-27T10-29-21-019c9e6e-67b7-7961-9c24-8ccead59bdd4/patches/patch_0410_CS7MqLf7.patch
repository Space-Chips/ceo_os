*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/components/liquid_button.dart
@@
 class LiquidButton extends StatefulWidget {
   final String label;
   final VoidCallback? onPressed;
   final bool isLoading;
   final IconData? icon;
+  final TextStyle? labelStyle;
   final bool fullWidth;
   final List<Color>? gradient;
   final double height;
   final double borderRadius;
@@
     this.onPressed,
     this.isLoading = false,
     this.icon,
+    this.labelStyle,
     this.fullWidth = false,
     this.gradient,
     this.height = 60,
     this.borderRadius = 20,
@@
   Widget build(BuildContext context) {
@@
     final effectiveGradient =
         widget.gradient ?? [AppColors.primaryOrange, AppColors.accentSecondary];
+    final textBaseStyle = widget.labelStyle ?? AppTypography.headline;
@@
                                 Text(
                                   widget.label.toUpperCase(),
-                                  style: AppTypography.headline.copyWith(
+                                  style: textBaseStyle.copyWith(
                                     color: isDisabled
                                         ? AppColors.secondaryLabel
                                         : AppColors.onAccent,
-                                    fontWeight: FontWeight.w800,
-                                    letterSpacing: 1.1,
+                                    fontWeight:
+                                        textBaseStyle.fontWeight ??
+                                        FontWeight.w800,
+                                    letterSpacing:
+                                        textBaseStyle.letterSpacing ?? 1.1,
                                   ),
                                 ),
*** End Patch
