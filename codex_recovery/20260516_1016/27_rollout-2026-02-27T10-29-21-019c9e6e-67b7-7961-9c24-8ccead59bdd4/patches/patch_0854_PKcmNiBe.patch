*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/repositories/premium_repository.dart
@@
 class PremiumRepository {
+  static DateTime? _lastLaunchSyncAt;
+  static String? _lastLaunchSyncUserId;
+  static bool _launchSyncInFlight = false;
+
   final SupabaseService _supabaseService;
@@
   Future<PremiumRuntime> getRuntime() async {
     final config = await getConfig();
+    await _syncLaunchEntitlementsIfNeeded(config);
     final subscriptionStatus = await _getSubscriptionStatus();
     final entitlements = await getUserEntitlements();
     final resolved = _resolveEntitlements(
@@
         paywallEnabled: defaults.paywallEnabled,
         launchPremiumLabelsEnabled: defaults.launchPremiumLabelsEnabled,
         grandfatheringEnabled: defaults.grandfatheringEnabled,
         earlyDiscountEnabled: defaults.earlyDiscountEnabled,
+        earlyDiscountKind: defaults.earlyDiscountKind,
+        earlyDiscountPercent: defaults.earlyDiscountPercent,
+        earlyDiscountPriceId: defaults.earlyDiscountPriceId,
         tasksFreeLimit: defaults.tasksFreeLimit,
         habitsFreeLimit: defaults.habitsFreeLimit,
         notesFreeLimit: defaults.notesFreeLimit,
@@
   Future<ResolvedEntitlements> getResolvedEntitlements() async {
     final runtime = await getRuntime();
     return runtime.resolved;
   }
+
+  Future<void> syncLaunchEntitlements() async {
+    final uid = _currentUserId;
+    if (uid == null) return;
+
+    try {
+      await _client.rpc('sync_launch_entitlements');
+      _lastLaunchSyncAt = DateTime.now();
+      _lastLaunchSyncUserId = uid;
+    } on PostgrestException catch (e) {
+      if (e.code == 'PGRST202' ||
+          e.message.contains('sync_launch_entitlements')) {
+        return;
+      }
+    } catch (_) {
+      // Best effort only. Runtime resolution will fall back to existing state.
+    }
+  }
@@
   ResolvedEntitlements _resolveEntitlements({
@@
     final isPremiumUser =
         launchPremiumActive ||
         hasPaidSubscription ||
         isTrialing ||
         overrideActive;
     final canUsePremiumThemes =
         isPremiumUser || entitlements.themeBundleGranted;
+    final discountKind = entitlements.discountKind ?? config.earlyDiscountKind;
+    final discountPercent =
+        entitlements.discountPercent ?? config.earlyDiscountPercent;
+    final discountPriceId =
+        entitlements.discountPriceId ?? config.earlyDiscountPriceId;
 
     return ResolvedEntitlements(
       launchPremiumActive: launchPremiumActive,
@@
       canUseExtendedCeo: isPremiumUser,
       canAccessReports: isPremiumUser,
       canAccessAdvancedCalendar: isPremiumUser,
       discountEligible: entitlements.discountEligible,
+      discountKind: entitlements.discountEligible ? discountKind : null,
+      discountPercent: entitlements.discountEligible ? discountPercent : null,
+      discountPriceId: entitlements.discountEligible ? discountPriceId : null,
       badgeCode: entitlements.badgeCode,
+      themeBundleGranted: entitlements.themeBundleGranted,
     );
   }
+
+  Future<void> _syncLaunchEntitlementsIfNeeded(PremiumConfig config) async {
+    final uid = _currentUserId;
+    if (uid == null || _launchSyncInFlight) return;
+
+    final now = DateTime.now();
+    final sameUser = _lastLaunchSyncUserId == uid;
+    final recentlySynced =
+        sameUser &&
+        _lastLaunchSyncAt != null &&
+        now.difference(_lastLaunchSyncAt!) < const Duration(minutes: 3);
+    if (recentlySynced) return;
+
+    _launchSyncInFlight = true;
+    try {
+      await syncLaunchEntitlements();
+    } finally {
+      _launchSyncInFlight = false;
+    }
+  }
*** End Patch
