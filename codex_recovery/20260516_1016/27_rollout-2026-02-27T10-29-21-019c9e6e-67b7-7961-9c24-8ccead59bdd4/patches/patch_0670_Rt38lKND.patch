*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/tasks/task_importance_theme.dart
@@
 class TaskImportanceTheme {
   TaskImportanceTheme._();
+
+  static const Color _essentialOrange = Color(0xFFF08A32);
+  static const Color _averageYellow = Color(0xFFE0B84A);
 
   static String label(String? raw) {
@@
   static String detailLabel(String? raw) {
@@
     }
   }
-
-  static Color _mix(Color a, Color b, double t) => Color.lerp(a, b, t)!;
 
   static TaskImportanceBadgeStyle badge(String? raw) {
     switch ((raw ?? '').toLowerCase()) {
       case 'crucial':
       case 'critical':
@@
           glow: AppColors.error.withValues(alpha: 0.16),
         );
       case 'essential':
       case 'high':
-        final tone = _mix(
-          AppColors.warning,
-          AppColors.accentText,
-          AppColors.isDark ? 0.18 : 0.28,
-        );
         return TaskImportanceBadgeStyle(
-          background: tone.withValues(alpha: AppColors.isDark ? 0.13 : 0.09),
-          border: tone.withValues(alpha: AppColors.isDark ? 0.34 : 0.24),
-          text: tone,
-          glow: tone.withValues(alpha: AppColors.isDark ? 0.14 : 0.08),
+          background: _essentialOrange.withValues(
+            alpha: AppColors.isDark ? 0.13 : 0.09,
+          ),
+          border: _essentialOrange.withValues(
+            alpha: AppColors.isDark ? 0.34 : 0.24,
+          ),
+          text: _essentialOrange,
+          glow: _essentialOrange.withValues(
+            alpha: AppColors.isDark ? 0.14 : 0.08,
+          ),
         );
       case 'average':
       case 'medium':
-        final tone = _mix(
-          AppColors.accentText,
-          AppColors.secondaryLabel,
-          AppColors.isDark ? 0.16 : 0.12,
-        );
         return TaskImportanceBadgeStyle(
-          background: tone.withValues(alpha: AppColors.isDark ? 0.13 : 0.09),
-          border: tone.withValues(alpha: AppColors.isDark ? 0.3 : 0.22),
-          text: tone,
-          glow: tone.withValues(alpha: AppColors.isDark ? 0.12 : 0.07),
+          background: _averageYellow.withValues(
+            alpha: AppColors.isDark ? 0.13 : 0.09,
+          ),
+          border: _averageYellow.withValues(
+            alpha: AppColors.isDark ? 0.3 : 0.22,
+          ),
+          text: _averageYellow,
+          glow: _averageYellow.withValues(
+            alpha: AppColors.isDark ? 0.12 : 0.07,
+          ),
         );
       case 'low':
       default:
-        final tone = _mix(
-          AppColors.tertiaryLabel,
-          AppColors.accentText,
-          AppColors.isDark ? 0.18 : 0.12,
-        );
+        final tone = Color.lerp(
+          AppColors.tertiaryLabel,
+          AppColors.secondaryLabel,
+          AppColors.isDark ? 0.28 : 0.18,
+        )!;
         return TaskImportanceBadgeStyle(
           background: tone.withValues(alpha: AppColors.isDark ? 0.1 : 0.075),
           border: tone.withValues(alpha: AppColors.isDark ? 0.22 : 0.18),
*** End Patch
