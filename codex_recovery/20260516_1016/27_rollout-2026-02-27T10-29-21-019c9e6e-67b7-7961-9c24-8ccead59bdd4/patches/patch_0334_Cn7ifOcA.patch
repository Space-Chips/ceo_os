*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
                           child: _blockedItemCard(
                             title: (app.appName ?? 'Unknown app').toUpperCase(),
-                            subtitle:
-                                'Blocked • daily limit ${app.timeLimitMinutes ?? 0}m',
-                            onAdd15: _saving
-                                ? null
-                                : () => _runMutation(
-                                    () => _repo.addBlockedAppTime(app.id, 15),
-                                  ),
-                            onAdd30: _saving
-                                ? null
-                                : () => _runMutation(
-                                    () => _repo.addBlockedAppTime(app.id, 30),
-                                  ),
+                            subtitle: (app.timeLimitMinutes ?? 0) > 0
+                                ? 'Daily limit ${(app.timeLimitMinutes ?? 0)}m'
+                                : 'Fully blocked',
+                            onSetLimit: _saving
+                                ? null
+                                : () => _editDailyLimit(
+                                    title: app.appName ?? 'App',
+                                    currentMinutes: app.timeLimitMinutes ?? 0,
+                                    onSave: (minutes) => _repo
+                                        .setBlockedAppTime(app.id, minutes),
+                                  ),
                             onDelete: _saving
                                 ? null
                                 : () => _runMutation(
                                     () => _repo.deleteBlockedApp(app.id),
                                   ),
                             onTap: () {
                               final limit = app.timeLimitMinutes ?? 0;
+                              if (limit <= 0) {
+                                _showNotice(
+                                  title: app.appName ?? 'App',
+                                  message:
+                                      'This app is fully blocked (no daily allowance).',
+                                );
+                                return;
+                              }
                               final used = _usedMinutesForEntity(
                                 entityName: app.appName ?? '',
                                 isWebsite: false,
                               );
                               _showRemainingDialog(
@@
                           ),
                         ),
                       ),
-                    const SizedBox(height: 10),
-                    _sectionHeader(
-                      title: 'APP_LIMITS',
-                      subtitle:
-                          '${limitedApps.length} apps with daily screen-time limit',
-                      trailing: null,
-                    ),
-                    if (limitedApps.isEmpty)
-                      _emptyCard(
-                        'No app time limits yet. Use +15 / +30 on apps.',
-                      )
-                    else
-                      ...limitedApps.map(
-                        (app) => Padding(
-                          padding: const EdgeInsets.only(bottom: 8),
-                          child: _limitCard(
-                            title: (app.appName ?? 'Unknown app').toUpperCase(),
-                            limitMinutes: app.timeLimitMinutes ?? 0,
-                            usedMinutes: _usedMinutesForEntity(
-                              entityName: app.appName ?? '',
-                              isWebsite: false,
-                            ),
-                            onTap: () => _showRemainingDialog(
-                              title: app.appName ?? 'App',
-                              limitMinutes: app.timeLimitMinutes ?? 0,
-                              usedMinutes: _usedMinutesForEntity(
-                                entityName: app.appName ?? '',
-                                isWebsite: false,
-                              ),
-                            ),
-                          ),
-                        ),
-                      ),
                     const SizedBox(height: 14),
*** End Patch
