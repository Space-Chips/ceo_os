*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/theme/app_colors.dart
@@
   static Color get glassSurfaceStrong =>
-      isDark ? const Color(0x38FFFFFF) : const Color(0xD9FFFFFF);
+      isDark
+          ? Color.alphaBlend(
+              const Color(0x14FFFFFF),
+              Color.lerp(backgroundLight, surface, 0.55)!,
+            )
+          : const Color(0xD9FFFFFF);
   static Color get glassSurfaceSoft =>
-      isDark ? const Color(0x22FFFFFF) : const Color(0xC2FFFFFF);
+      isDark
+          ? Color.alphaBlend(
+              const Color(0x0AFFFFFF),
+              Color.lerp(background, backgroundLight, 0.4)!,
+            )
+          : const Color(0xC2FFFFFF);
 
   static Color get glassHighlight =>
-      isDark ? const Color(0x66FFFFFF) : const Color(0x73FFFFFF);
+      isDark ? const Color(0x66879AB8) : const Color(0x73FFFFFF);
   static Color get glassHighlightSoft =>
-      isDark ? const Color(0x2EFFFFFF) : const Color(0x59FFFFFF);
+      isDark ? const Color(0x33596884) : const Color(0x59FFFFFF);
@@
   static List<Color> get floatingGlassGradient => [
-    glassSurfaceStrong,
-    glassSurfaceSoft,
+    isDark
+        ? Color.alphaBlend(const Color(0x10FFFFFF), backgroundLight)
+        : glassSurfaceStrong,
+    isDark
+        ? Color.alphaBlend(const Color(0x08FFFFFF), background)
+        : glassSurfaceSoft,
   ];
 
   static List<Color> get inputGlassGradient => [
-    isDark ? const Color(0x2EFFFFFF) : const Color(0xF0FFFFFF),
-    isDark ? const Color(0x14FFFFFF) : const Color(0xD9FFFFFF),
+    isDark
+        ? Color.alphaBlend(const Color(0x12FFFFFF), backgroundLight)
+        : const Color(0xF0FFFFFF),
+    isDark
+        ? Color.alphaBlend(const Color(0x08FFFFFF), background)
+        : const Color(0xD9FFFFFF),
   ];
 }
*** End Patch
