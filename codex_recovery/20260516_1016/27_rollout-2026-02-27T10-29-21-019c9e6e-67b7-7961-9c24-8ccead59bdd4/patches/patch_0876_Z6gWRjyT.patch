*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/profile/profile_screen.dart
@@
   Future<void> _openPrivacyPolicy() async {
     await _showDocumentSheet(
       title: 'PRIVACY_POLICY',
       sections: const [
         _SettingsSection(
           'Information We Collect',
-          'We collect your profile identity and productivity data (habits, tasks, calendar events, focus sessions, screen-time logs) to provide the app functionality.',
+          'We collect your profile identity and limited productivity data, including habits, tasks, calendar events, focus sessions, selected blocked apps and websites, and screen-time or usage logs needed to provide the app functionality.',
         ),
         _SettingsSection(
           'How Data Is Used',
-          'Data is used to run your workflows, compute insights, streaks, and ranking progress. Personal data is not sold to third parties.',
+          'Data is used only to run your workflows, enforce focus protections, sync your account across devices, and compute personal insights, streaks, and history for the same user. Personal data is not sold to third parties.',
         ),
         _SettingsSection(
           'Storage and Security',
-          'Data is stored in Supabase with row-level security policies so users can only access records they own.',
+          'Limited usage and blocking data may be stored in Supabase to support sync, history, and personal productivity insights. Row-level security policies are used so users can only access records they own.',
         ),
         _SettingsSection(
           'Your Rights',
           'You can request access, correction, or deletion of your data through support. Account deletion remains irreversible.',
@@
   Future<void> _openPermissionsPolicy() async {
     await _showDocumentSheet(
       title: 'PERMISSIONS',
       sections: const [
         _SettingsSection(
@@
         ),
         _SettingsSection(
           'Screen Time / Shield',
-          'Used only for focus protection workflows and screen-time analysis features.',
+          'Used only for self-initiated focus sessions on your own device, including blocking selected apps and websites and supporting screen-time analysis features.',
+        ),
+        _SettingsSection(
+          'Data Handling',
+          'Limited blocking and usage data may be stored on our backend to support sync, history, and personal productivity insights for the same user.',
         ),
         _SettingsSection(
           'Control',
           'Permissions can be changed anytime in device settings. Some app modules may degrade when disabled.',
         ),
@@
   Future<void> _openContactSupport() async {
-    const supportEmail = 'support@app.com';
+    const supportEmail = 'timofrmac@gmail.com';
     await Clipboard.setData(const ClipboardData(text: supportEmail));
     await _showNotice(
       title: 'SUPPORT_CONTACT',
       message:
-          'Support email copied: support@app.com. Include device, app version, and reproduction steps.',
+          'Support email copied: timofrmac@gmail.com. Include device, app version, and reproduction steps.',
     );
   }
*** Update File: /Users/timo/ceo_os/lib/features/auth/onboarding_screen.dart
@@
   (
     icon: CupertinoIcons.timer_fill,
     title: 'Protect Deep Work',
-    subtitle: 'Use focus sessions and blocking to reduce context switching.',
+    subtitle:
+        'Use focus sessions and blocking to reduce context switching. On iOS, blocking features require Screen Time / Family Controls permission.',
   ),
 ];
*** Update File: /Users/timo/ceo_os/lib/features/legacy/screen_time_screen.dart
@@
       await _showNotice(
         title: 'Permissions required',
         message:
-            'Allow Family Controls first so blocked apps can be enforced on device.',
+            'Allow Screen Time / Family Controls first so selected apps can be blocked during your focus sessions on this device.',
       );
       return;
     }
@@
       await _showNotice(
         title: 'Permissions required',
         message:
-            'Allow Family Controls first so blocked websites can be enforced on device.',
+            'Allow Screen Time / Family Controls first so selected websites can be blocked during your focus sessions on this device.',
       );
       return;
     }
*** End Patch
