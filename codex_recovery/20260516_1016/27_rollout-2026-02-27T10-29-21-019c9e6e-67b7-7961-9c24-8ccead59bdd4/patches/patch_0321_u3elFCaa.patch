*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/repositories/family_time_repository.dart
@@
   Future<FamilyTimeSession?> startSession({
     required String groupId,
     required int durationMinutes,
   }) async {
     if (!await _ensureAdmin(groupId)) return null;
 
-    final members = await getMembers(groupId);
-    if (members.isEmpty) return null;
+    final members = (await getMembers(groupId))
+        .where((member) => (member.userId ?? '').trim().isNotEmpty)
+        .toList(growable: false);
+    if (members.isEmpty) return null;
@@
       await _postSessionEvent(
         sessionId: session.id,
         eventType: 'session_pending',
-        message: 'A group session was launched. Confirm to start together.',
+        message:
+            'A group session was launched. Connected members must confirm to start together.',
       );
*** End Patch
