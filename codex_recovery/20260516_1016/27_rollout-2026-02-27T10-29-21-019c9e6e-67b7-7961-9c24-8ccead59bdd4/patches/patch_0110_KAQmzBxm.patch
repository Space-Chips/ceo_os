*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/features/leaderboard/leaderboard_screen.dart
@@
 class _LeaderboardScreenState extends State<LeaderboardScreen> {
   final UserRepository _repository = UserRepository();
   List<_RankedEntry> _entries = [];
+  List<_FriendRankedEntry> _friendEntries = [];
   String? _myUserId;
   bool _loading = true;
+  bool _friendsLoading = false;
   _LeaderboardTab _activeTab = _LeaderboardTab.global;
@@
   int _computeScore(LeaderboardEntry e) {
@@
     return progression + consistency + standing + focusDiscipline;
   }
+
+  int _computeFriendScore(FriendConnection connection) {
+    final rankLevel = (connection.friendRankLevel ?? 1).clamp(1, 10);
+    final streak = (connection.friendWinStreak ?? 0).clamp(0, 365);
+    final progression = rankLevel * 120;
+    final consistency = streak * 14;
+    const baseline = 120;
+    return progression + consistency + baseline;
+  }
 
   Future<void> _load() async {
     setState(() => _loading = true);
     await _repository.refreshLeaderboardForMe();
 
     final entries = await _repository.getLeaderboard();
+    final friends = await _repository.getFriendConnections();
     final me = await _repository
         .getProfile()
         .then((value) => value?.id)
         .catchError((_) => null);
 
@@
             .map((e) => _RankedEntry(entry: e, score: _computeScore(e)))
             .toList()
           ..sort((a, b) => b.score.compareTo(a.score));
 
+    final friendRanked =
+        friends
+            .map(
+              (friend) => _FriendRankedEntry(
+                connection: friend,
+                score: _computeFriendScore(friend),
+              ),
+            )
+            .toList()
+          ..sort((a, b) => b.score.compareTo(a.score));
+
     if (!mounted) return;
     setState(() {
       _entries = ranked;
+      _friendEntries = friendRanked;
       _myUserId = me;
       _loading = false;
+      _friendsLoading = false;
     });
   }
+
+  Future<void> _loadFriendsOnly() async {
+    final friends = await _repository.getFriendConnections();
+    final ranked =
+        friends
+            .map(
+              (friend) => _FriendRankedEntry(
+                connection: friend,
+                score: _computeFriendScore(friend),
+              ),
+            )
+            .toList()
+          ..sort((a, b) => b.score.compareTo(a.score));
+
+    if (!mounted) return;
+    setState(() {
+      _friendEntries = ranked;
+      _friendsLoading = false;
+    });
+  }
+
+  bool _isValidEmail(String value) {
+    final trimmed = value.trim();
+    if (trimmed.isEmpty) return false;
+    final exp = RegExp(r'^[^\s@]+@[^\s@]+\.[^\s@]+$');
+    return exp.hasMatch(trimmed);
+  }
+
+  String _friendLabel(FriendConnection friend) {
+    final name = (friend.friendName ?? '').trim();
+    if (name.isNotEmpty) return name.toUpperCase();
+    final email = (friend.friendEmail ?? '').trim();
+    if (email.isNotEmpty) return email.split('@').first.toUpperCase();
+    return 'FRIEND';
+  }
+
+  String _friendErrorMessage(Object error) {
+    if (error is ArgumentError) {
+      final message = error.message?.toString();
+      if (message != null && message.isNotEmpty) return message;
+    }
+    return 'Please try again in a moment.';
+  }
+
+  Future<void> _showNotice(String title, String message) async {
+    if (!mounted) return;
+    await showCupertinoDialog<void>(
+      context: context,
+      builder: (ctx) => CupertinoAlertDialog(
+        title: Text(title),
+        content: Padding(
+          padding: const EdgeInsets.only(top: 8),
+          child: Text(message),
+        ),
+        actions: [
+          CupertinoDialogAction(
+            isDefaultAction: true,
+            onPressed: () => Navigator.pop(ctx),
+            child: const Text('OK'),
+          ),
+        ],
+      ),
+    );
+  }
+
+  Future<void> _saveFriend(String email, String? name) async {
+    setState(() => _friendsLoading = true);
+    try {
+      await _repository.upsertFriendConnection(
+        friendEmail: email,
+        friendName: name,
+      );
+      await _loadFriendsOnly();
+      await _showNotice(
+        'FRIEND_CONNECTED',
+        'Friend connection is now visible in your private ranking tab.',
+      );
+    } catch (e) {
+      if (!mounted) return;
+      setState(() => _friendsLoading = false);
+      await _showNotice('CONNECTION_FAILED', _friendErrorMessage(e));
+    }
+  }
+
+  Future<void> _openAddFriendDialog() async {
+    final email = TextEditingController();
+    final name = TextEditingController();
+
+    await showCupertinoDialog<void>(
+      context: context,
+      builder: (ctx) => CupertinoAlertDialog(
+        title: const Text('Connect Friend'),
+        content: Column(
+          mainAxisSize: MainAxisSize.min,
+          children: [
+            const SizedBox(height: 8),
+            CupertinoTextField(
+              controller: email,
+              placeholder: 'Friend email',
+              keyboardType: TextInputType.emailAddress,
+              autocorrect: false,
+            ),
+            const SizedBox(height: 8),
+            CupertinoTextField(
+              controller: name,
+              placeholder: 'Display name (optional)',
+            ),
+          ],
+        ),
+        actions: [
+          CupertinoDialogAction(
+            onPressed: () => Navigator.pop(ctx),
+            child: const Text('Cancel'),
+          ),
+          CupertinoDialogAction(
+            isDefaultAction: true,
+            onPressed: () async {
+              final rawEmail = email.text.trim().toLowerCase();
+              final rawName = name.text.trim();
+              Navigator.pop(ctx);
+
+              if (!_isValidEmail(rawEmail)) {
+                await _showNotice(
+                  'INVALID_EMAIL',
+                  'Please provide a valid email address.',
+                );
+                return;
+              }
+
+              await _saveFriend(rawEmail, rawName.isEmpty ? null : rawName);
+            },
+            child: const Text('Connect'),
+          ),
+        ],
+      ),
+    );
+
+    email.dispose();
+    name.dispose();
+  }
+
+  Future<void> _removeFriend(_FriendRankedEntry row) async {
+    setState(() => _friendsLoading = true);
+    try {
+      await _repository.deleteFriendConnection(row.connection.id);
+      await _loadFriendsOnly();
+    } catch (_) {
+      if (!mounted) return;
+      setState(() => _friendsLoading = false);
+      await _showNotice('DELETE_FAILED', 'Unable to remove this friend now.');
+    }
+  }
+
+  Future<void> _confirmRemoveFriend(_FriendRankedEntry row) async {
+    await showCupertinoDialog<void>(
+      context: context,
+      builder: (ctx) => CupertinoAlertDialog(
+        title: const Text('Remove Friend'),
+        content: Text('Remove ${_friendLabel(row.connection)} from this tab?'),
+        actions: [
+          CupertinoDialogAction(
+            onPressed: () => Navigator.pop(ctx),
+            child: const Text('Cancel'),
+          ),
+          CupertinoDialogAction(
+            isDestructiveAction: true,
+            onPressed: () async {
+              Navigator.pop(ctx);
+              await _removeFriend(row);
+            },
+            child: const Text('Remove'),
+          ),
+        ],
+      ),
+    );
+  }
@@
       child: _loading
           ? Center(
               child: CupertinoActivityIndicator(color: AppColors.primaryOrange),
             )
           : SafeArea(
-              child: _entries.isEmpty
-                  ? Center(
-                      child: GlassCard(
-                        padding: const EdgeInsets.all(16),
-                        borderRadius: 16,
-                        child: Text(
-                          'No leaderboard data yet. Complete habits/tasks/focus to generate rankings.',
-                          textAlign: TextAlign.center,
-                          style: AppTypography.mono.copyWith(
-                            fontSize: 10,
-                            color: AppColors.tertiaryLabel,
-                          ),
-                        ),
-                      ),
-                    )
-                  : ListView.separated(
-                      padding: const EdgeInsets.all(20),
-                      itemCount: _activeTab == _LeaderboardTab.global
-                          ? _entries.length + 1
-                          : 3,
-                      separatorBuilder: (_, __) => const SizedBox(height: 10),
-                      itemBuilder: (context, index) {
-                        if (index == 0) {
-                          return Column(
-                            children: [
-                              _glowSurface(
-                                glowColor: AppColors.primaryOrange.withValues(
-                                  alpha: 0.14,
-                                ),
-                                borderRadius: 16,
-                                child: GlassCard(
-                                  padding: const EdgeInsets.all(14),
-                                  borderRadius: 16,
-                                  border: Border.all(
-                                    color: AppColors.primaryOrange.withValues(
-                                      alpha: 0.24,
-                                    ),
-                                    width: 0.7,
-                                  ),
-                                  child: Row(
-                                    children: [
-                                      Icon(
-                                        CupertinoIcons.star_fill,
-                                        size: 16,
-                                        color: AppColors.primaryOrange,
-                                      ),
-                                      const SizedBox(width: 8),
-                                      Expanded(
-                                        child: Text(
-                                          'Global ranking based on streak, level, and discipline index.',
-                                          style: AppTypography.mono.copyWith(
-                                            fontSize: 10,
-                                            color: AppColors.secondaryLabel,
-                                          ),
-                                        ),
-                                      ),
-                                    ],
-                                  ),
-                                ),
-                              ),
-                              const SizedBox(height: 10),
-                              GlassCard(
-                                padding: const EdgeInsets.symmetric(
-                                  horizontal: 10,
-                                  vertical: 8,
-                                ),
-                                borderRadius: 12,
-                                child:
-                                    CupertinoSlidingSegmentedControl<
-                                      _LeaderboardTab
-                                    >(
-                                      groupValue: _activeTab,
-                                      thumbColor: AppColors.primaryOrange
-                                          .withValues(alpha: 0.2),
-                                      children: {
-                                        _LeaderboardTab.global: Padding(
-                                          padding: const EdgeInsets.symmetric(
-                                            horizontal: 14,
-                                            vertical: 6,
-                                          ),
-                                          child: Text(
-                                            'GLOBAL',
-                                            style: AppTypography.mono.copyWith(
-                                              fontSize: 10,
-                                              fontWeight: FontWeight.bold,
-                                              color:
-                                                  _activeTab ==
-                                                      _LeaderboardTab.global
-                                                  ? AppColors.primaryOrange
-                                                  : AppColors.secondaryLabel,
-                                            ),
-                                          ),
-                                        ),
-                                        _LeaderboardTab.friends: Padding(
-                                          padding: const EdgeInsets.symmetric(
-                                            horizontal: 14,
-                                            vertical: 6,
-                                          ),
-                                          child: Text(
-                                            'FRIENDS',
-                                            style: AppTypography.mono.copyWith(
-                                              fontSize: 10,
-                                              fontWeight: FontWeight.bold,
-                                              color:
-                                                  _activeTab ==
-                                                      _LeaderboardTab.friends
-                                                  ? AppColors.primaryOrange
-                                                  : AppColors.secondaryLabel,
-                                            ),
-                                          ),
-                                        ),
-                                      },
-                                      onValueChanged: (value) {
-                                        if (value == null) return;
-                                        setState(() => _activeTab = value);
-                                      },
-                                    ),
-                              ),
-                              const SizedBox(height: 12),
-                              if (_activeTab == _LeaderboardTab.friends)
-                                GlassCard(
-                                  padding: const EdgeInsets.all(16),
-                                  borderRadius: 14,
-                                  child: Column(
-                                    children: [
-                                      Text(
-                                        'NO_FRIENDS_CONNECTED',
-                                        style: AppTypography.mono.copyWith(
-                                          fontSize: 10,
-                                          color: AppColors.tertiaryLabel,
-                                          fontWeight: FontWeight.bold,
-                                        ),
-                                      ),
-                                      const SizedBox(height: 8),
-                                      Text(
-                                        'Invite peers to compare streak and rank progression.',
-                                        textAlign: TextAlign.center,
-                                        style: AppTypography.mono.copyWith(
-                                          fontSize: 10,
-                                          color: AppColors.secondaryLabel,
-                                        ),
-                                      ),
-                                      const SizedBox(height: 12),
-                                      LiquidButton(
-                                        label: 'INVITE_FRIENDS',
-                                        fullWidth: true,
-                                        onPressed: () {},
-                                      ),
-                                    ],
-                                  ),
-                                ),
-                            ],
-                          );
-                        }
-
-                        if (_activeTab == _LeaderboardTab.friends) {
-                          return const SizedBox.shrink();
-                        }
-
-                        final row = _entries[index - 1];
-                        final entry = row.entry;
-                        final rank = index;
-                        final mine =
-                            _myUserId != null && entry.createdBy == _myUserId;
-                        final rankAccent = _rankAccentColor(rank);
-                        return GlassCard(
-                          padding: const EdgeInsets.symmetric(
-                            horizontal: 16,
-                            vertical: 14,
-                          ),
-                          borderRadius: 16,
-                          border: mine
-                              ? Border.all(
-                                  color: AppColors.primaryOrange.withValues(
-                                    alpha: 0.35,
-                                  ),
-                                  width: 0.8,
-                                )
-                              : Border.all(
-                                  color: rankAccent.withValues(
-                                    alpha: rank <= 3 ? 0.3 : 0.22,
-                                  ),
-                                  width: 0.6,
-                                ),
-                          child: Row(
-                            children: [
-                              SizedBox(
-                                width: 42,
-                                child: Text(
-                                  rank == 1
-                                      ? '🥇'
-                                      : rank == 2
-                                      ? '🥈'
-                                      : rank == 3
-                                      ? '🥉'
-                                      : '#$rank',
-                                  style: AppTypography.mono.copyWith(
-                                    fontSize: 13,
-                                    fontWeight: FontWeight.bold,
-                                    color: rankAccent,
-                                  ),
-                                ),
-                              ),
-                              Expanded(
-                                child: Column(
-                                  crossAxisAlignment: CrossAxisAlignment.start,
-                                  children: [
-                                    Text(
-                                      entry.rankName ?? 'Starter',
-                                      style: AppTypography.mono.copyWith(
-                                        fontSize: 13,
-                                        fontWeight: FontWeight.bold,
-                                      ),
-                                    ),
-                                    const SizedBox(height: 3),
-                                    Text(
-                                      mine
-                                          ? 'YOU'
-                                          : entry.createdBy.substring(0, 8),
-                                      style: AppTypography.mono.copyWith(
-                                        fontSize: 10,
-                                        color: AppColors.tertiaryLabel,
-                                      ),
-                                    ),
-                                  ],
-                                ),
-                              ),
-                              _pill('IDX ${row.score}'),
-                              const SizedBox(width: 6),
-                              _pill('L${entry.rankLevel ?? 1}', rankAccent),
-                              const SizedBox(width: 6),
-                              _pill(
-                                '${entry.winStreak ?? 0}d',
-                                AppColors.primaryOrange,
-                              ),
-                            ],
-                          ),
-                        );
-                      },
-                    ),
+              child: ListView(
+                padding: const EdgeInsets.all(20),
+                children: [
+                  _buildHeader(),
+                  const SizedBox(height: 12),
+                  if (_activeTab == _LeaderboardTab.global)
+                    ..._buildGlobalCards()
+                  else
+                    ..._buildFriendCards(),
+                ],
+              ),
             ),
     );
   }
+
+  Widget _buildHeader() {
+    return Column(
+      children: [
+        _glowSurface(
+          glowColor: AppColors.primaryOrange.withValues(alpha: 0.14),
+          borderRadius: 16,
+          child: GlassCard(
+            padding: const EdgeInsets.all(14),
+            borderRadius: 16,
+            border: Border.all(
+              color: AppColors.primaryOrange.withValues(alpha: 0.24),
+              width: 0.7,
+            ),
+            child: Row(
+              children: [
+                Icon(
+                  CupertinoIcons.star_fill,
+                  size: 16,
+                  color: AppColors.primaryOrange,
+                ),
+                const SizedBox(width: 8),
+                Expanded(
+                  child: Text(
+                    _activeTab == _LeaderboardTab.global
+                        ? 'Global ranking based on streak, level, and discipline index.'
+                        : 'Private friend ranking synced from your connection list.',
+                    style: AppTypography.mono.copyWith(
+                      fontSize: 10,
+                      color: AppColors.secondaryLabel,
+                    ),
+                  ),
+                ),
+                Container(
+                  padding: const EdgeInsets.symmetric(horizontal: 7, vertical: 3),
+                  decoration: BoxDecoration(
+                    borderRadius: BorderRadius.circular(8),
+                    color: AppColors.primaryOrange.withValues(alpha: 0.12),
+                  ),
+                  child: Text(
+                    _activeTab == _LeaderboardTab.global
+                        ? '${_entries.length}'
+                        : '${_friendEntries.length}',
+                    style: AppTypography.mono.copyWith(
+                      fontSize: 9,
+                      color: AppColors.primaryOrange,
+                      fontWeight: FontWeight.bold,
+                    ),
+                  ),
+                ),
+              ],
+            ),
+          ),
+        ),
+        const SizedBox(height: 10),
+        GlassCard(
+          padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 8),
+          borderRadius: 12,
+          child: CupertinoSlidingSegmentedControl<_LeaderboardTab>(
+            groupValue: _activeTab,
+            thumbColor: AppColors.primaryOrange.withValues(alpha: 0.2),
+            children: {
+              _LeaderboardTab.global: Padding(
+                padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 6),
+                child: Text(
+                  'GLOBAL',
+                  style: AppTypography.mono.copyWith(
+                    fontSize: 10,
+                    fontWeight: FontWeight.bold,
+                    color: _activeTab == _LeaderboardTab.global
+                        ? AppColors.primaryOrange
+                        : AppColors.secondaryLabel,
+                  ),
+                ),
+              ),
+              _LeaderboardTab.friends: Padding(
+                padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 6),
+                child: Text(
+                  'FRIENDS',
+                  style: AppTypography.mono.copyWith(
+                    fontSize: 10,
+                    fontWeight: FontWeight.bold,
+                    color: _activeTab == _LeaderboardTab.friends
+                        ? AppColors.primaryOrange
+                        : AppColors.secondaryLabel,
+                  ),
+                ),
+              ),
+            },
+            onValueChanged: (value) {
+              if (value == null) return;
+              setState(() => _activeTab = value);
+            },
+          ),
+        ),
+      ],
+    );
+  }
+
+  List<Widget> _buildGlobalCards() {
+    if (_entries.isEmpty) {
+      return [
+        GlassCard(
+          padding: const EdgeInsets.all(16),
+          borderRadius: 16,
+          child: Text(
+            'No leaderboard data yet. Complete habits/tasks/focus to generate rankings.',
+            textAlign: TextAlign.center,
+            style: AppTypography.mono.copyWith(
+              fontSize: 10,
+              color: AppColors.tertiaryLabel,
+            ),
+          ),
+        ),
+      ];
+    }
+
+    final widgets = <Widget>[];
+    for (var i = 0; i < _entries.length; i++) {
+      widgets.add(_buildGlobalRow(_entries[i], i + 1));
+      if (i != _entries.length - 1) {
+        widgets.add(const SizedBox(height: 10));
+      }
+    }
+    return widgets;
+  }
+
+  Widget _buildGlobalRow(_RankedEntry row, int rank) {
+    final entry = row.entry;
+    final mine = _myUserId != null && entry.createdBy == _myUserId;
+    final rankAccent = _rankAccentColor(rank);
+
+    return GlassCard(
+      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 14),
+      borderRadius: 16,
+      border: mine
+          ? Border.all(
+              color: AppColors.primaryOrange.withValues(alpha: 0.35),
+              width: 0.8,
+            )
+          : Border.all(
+              color: rankAccent.withValues(alpha: rank <= 3 ? 0.3 : 0.22),
+              width: 0.6,
+            ),
+      child: Row(
+        children: [
+          SizedBox(
+            width: 42,
+            child: Text(
+              rank == 1
+                  ? '🥇'
+                  : rank == 2
+                  ? '🥈'
+                  : rank == 3
+                  ? '🥉'
+                  : '#$rank',
+              style: AppTypography.mono.copyWith(
+                fontSize: 13,
+                fontWeight: FontWeight.bold,
+                color: rankAccent,
+              ),
+            ),
+          ),
+          Expanded(
+            child: Column(
+              crossAxisAlignment: CrossAxisAlignment.start,
+              children: [
+                Text(
+                  entry.rankName ?? 'Starter',
+                  style: AppTypography.mono.copyWith(
+                    fontSize: 13,
+                    fontWeight: FontWeight.bold,
+                  ),
+                ),
+                const SizedBox(height: 3),
+                Text(
+                  mine ? 'YOU' : entry.createdBy.substring(0, 8),
+                  style: AppTypography.mono.copyWith(
+                    fontSize: 10,
+                    color: AppColors.tertiaryLabel,
+                  ),
+                ),
+              ],
+            ),
+          ),
+          _pill('IDX ${row.score}'),
+          const SizedBox(width: 6),
+          _pill('L${entry.rankLevel ?? 1}', rankAccent),
+          const SizedBox(width: 6),
+          _pill('${entry.winStreak ?? 0}d', AppColors.primaryOrange),
+        ],
+      ),
+    );
+  }
+
+  List<Widget> _buildFriendCards() {
+    final widgets = <Widget>[
+      GlassCard(
+        padding: const EdgeInsets.all(14),
+        borderRadius: 14,
+        border: Border.all(
+          color: AppColors.primaryOrange.withValues(alpha: 0.2),
+          width: 0.6,
+        ),
+        child: Row(
+          children: [
+            Icon(
+              CupertinoIcons.person_2_fill,
+              size: 16,
+              color: AppColors.primaryOrange,
+            ),
+            const SizedBox(width: 8),
+            Expanded(
+              child: Text(
+                'Connect friends by email to track their rank progression here.',
+                style: AppTypography.mono.copyWith(
+                  fontSize: 10,
+                  color: AppColors.secondaryLabel,
+                ),
+              ),
+            ),
+            CupertinoButton(
+              padding: EdgeInsets.zero,
+              minSize: 24,
+              onPressed: _friendsLoading
+                  ? null
+                  : () {
+                      setState(() => _friendsLoading = true);
+                      _loadFriendsOnly();
+                    },
+              child: Icon(
+                CupertinoIcons.refresh,
+                size: 16,
+                color: _friendsLoading
+                    ? AppColors.tertiaryLabel
+                    : AppColors.primaryOrange,
+              ),
+            ),
+          ],
+        ),
+      ),
+      const SizedBox(height: 10),
+      LiquidButton(
+        label: 'CONNECT_FRIEND',
+        fullWidth: true,
+        onPressed: _friendsLoading ? null : _openAddFriendDialog,
+      ),
+      const SizedBox(height: 10),
+    ];
+
+    if (_friendsLoading) {
+      widgets.add(
+        GlassCard(
+          padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 18),
+          borderRadius: 14,
+          child: Row(
+            children: [
+              CupertinoActivityIndicator(color: AppColors.primaryOrange),
+              const SizedBox(width: 10),
+              Text(
+                'SYNCING_FRIEND_CONNECTIONS',
+                style: AppTypography.mono.copyWith(
+                  fontSize: 10,
+                  color: AppColors.secondaryLabel,
+                ),
+              ),
+            ],
+          ),
+        ),
+      );
+      return widgets;
+    }
+
+    if (_friendEntries.isEmpty) {
+      widgets.add(
+        GlassCard(
+          padding: const EdgeInsets.all(16),
+          borderRadius: 14,
+          child: Column(
+            children: [
+              Text(
+                'NO_FRIENDS_CONNECTED',
+                style: AppTypography.mono.copyWith(
+                  fontSize: 10,
+                  color: AppColors.tertiaryLabel,
+                  fontWeight: FontWeight.bold,
+                ),
+              ),
+              const SizedBox(height: 8),
+              Text(
+                'Invite peers to compare streak and rank progression.',
+                textAlign: TextAlign.center,
+                style: AppTypography.mono.copyWith(
+                  fontSize: 10,
+                  color: AppColors.secondaryLabel,
+                ),
+              ),
+            ],
+          ),
+        ),
+      );
+      return widgets;
+    }
+
+    for (var i = 0; i < _friendEntries.length; i++) {
+      widgets.add(_buildFriendRow(_friendEntries[i], i + 1));
+      if (i != _friendEntries.length - 1) {
+        widgets.add(const SizedBox(height: 10));
+      }
+    }
+    return widgets;
+  }
+
+  Widget _buildFriendRow(_FriendRankedEntry row, int rank) {
+    final friend = row.connection;
+    final rankAccent = _rankAccentColor(rank);
+    final email = (friend.friendEmail ?? '').trim().toUpperCase();
+
+    return GlassCard(
+      padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 12),
+      borderRadius: 16,
+      border: Border.all(
+        color: rankAccent.withValues(alpha: rank <= 3 ? 0.3 : 0.22),
+        width: 0.6,
+      ),
+      child: Row(
+        crossAxisAlignment: CrossAxisAlignment.start,
+        children: [
+          SizedBox(
+            width: 34,
+            child: Text(
+              rank == 1
+                  ? '🥇'
+                  : rank == 2
+                  ? '🥈'
+                  : rank == 3
+                  ? '🥉'
+                  : '#$rank',
+              style: AppTypography.mono.copyWith(
+                fontSize: 12,
+                fontWeight: FontWeight.bold,
+                color: rankAccent,
+              ),
+            ),
+          ),
+          Expanded(
+            child: Padding(
+              padding: const EdgeInsets.only(top: 1),
+              child: Column(
+                crossAxisAlignment: CrossAxisAlignment.start,
+                children: [
+                  Text(
+                    _friendLabel(friend),
+                    maxLines: 1,
+                    overflow: TextOverflow.ellipsis,
+                    style: AppTypography.mono.copyWith(
+                      fontSize: 12,
+                      fontWeight: FontWeight.bold,
+                    ),
+                  ),
+                  const SizedBox(height: 3),
+                  Text(
+                    email.isEmpty ? 'MANUAL_CONNECTION' : email,
+                    maxLines: 1,
+                    overflow: TextOverflow.ellipsis,
+                    style: AppTypography.mono.copyWith(
+                      fontSize: 9,
+                      color: AppColors.tertiaryLabel,
+                    ),
+                  ),
+                  const SizedBox(height: 6),
+                  Text(
+                    (friend.friendRankName ?? 'Starter').toUpperCase(),
+                    style: AppTypography.mono.copyWith(
+                      fontSize: 9,
+                      color: AppColors.secondaryLabel,
+                    ),
+                  ),
+                ],
+              ),
+            ),
+          ),
+          const SizedBox(width: 8),
+          Column(
+            crossAxisAlignment: CrossAxisAlignment.end,
+            children: [
+              _pill('IDX ${row.score}'),
+              const SizedBox(height: 6),
+              Row(
+                mainAxisSize: MainAxisSize.min,
+                children: [
+                  _pill('L${friend.friendRankLevel ?? 1}', rankAccent),
+                  const SizedBox(width: 6),
+                  _pill(
+                    '${friend.friendWinStreak ?? 0}d',
+                    AppColors.primaryOrange,
+                  ),
+                ],
+              ),
+              const SizedBox(height: 4),
+              CupertinoButton(
+                padding: EdgeInsets.zero,
+                minSize: 24,
+                onPressed: () => _confirmRemoveFriend(row),
+                child: Icon(
+                  CupertinoIcons.delete,
+                  size: 15,
+                  color: AppColors.error,
+                ),
+              ),
+            ],
+          ),
+        ],
+      ),
+    );
+  }
@@
 class _RankedEntry {
   final LeaderboardEntry entry;
   final int score;
 
   const _RankedEntry({required this.entry, required this.score});
 }
 
+class _FriendRankedEntry {
+  final FriendConnection connection;
+  final int score;
+
+  const _FriendRankedEntry({required this.connection, required this.score});
+}
+
 enum _LeaderboardTab { global, friends }
*** End Patch
