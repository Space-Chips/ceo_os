*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/repositories/user_repository.dart
@@
 import 'package:supabase_flutter/supabase_flutter.dart';
 import '../models/user_models.dart';
 import '../services/supabase_service.dart';
+import '../utils/app_logger.dart';
@@
     } catch (e) {
-      // TODO: Handle error properly
-      print('Error getting profile: $e');
+      AppLogger.error('Error getting profile.', e);
       return null;
     }
   }
@@
     } catch (e) {
-      print('Error getting rank: $e');
+      AppLogger.error('Error getting rank.', e);
       return null;
     }
   }
@@
     } catch (e) {
-      print('Error getting streak: $e');
+      AppLogger.error('Error getting streak.', e);
       return null;
     }
   }
@@
     } catch (e) {
-      print('Error getting leaderboard: $e');
+      AppLogger.error('Error getting leaderboard.', e);
       return [];
     }
   }
@@
-      print('Error refreshing leaderboard metrics: $e');
+      AppLogger.error('Error refreshing leaderboard metrics.', e);
     } catch (e) {
-      print('Error refreshing leaderboard metrics: $e');
+      AppLogger.error('Error refreshing leaderboard metrics.', e);
     }
   }
@@
     } catch (e) {
-      print('Error getting friend connections: $e');
+      AppLogger.error('Error getting friend connections.', e);
       return [];
     }
   }
@@
     } catch (e) {
-      print('Unable to resolve friend profile for $normalizedEmail: $e');
+      AppLogger.error(
+        'Unable to resolve friend profile for $normalizedEmail.',
+        e,
+      );
     }
@@
       } catch (e) {
-        print('Unable to resolve friend leaderboard for $normalizedEmail: $e');
+        AppLogger.error(
+          'Unable to resolve friend leaderboard for $normalizedEmail.',
+          e,
+        );
       }
     }
@@
     await _client
         .from('friend_connections')
         .update(payload)
-        .eq('id', existing['id']);
+        .eq('id', existing['id'])
+        .eq('created_by', _currentUserId);
   }
@@
     } catch (e) {
-      print('Error getting public profile labels: $e');
+      AppLogger.error('Error getting public profile labels.', e);
       return {};
     }
   }
@@
     } catch (e) {
-      print('Error syncing friend connections: $e');
+      AppLogger.error('Error syncing friend connections.', e);
     }
   }
 }
*** End Patch
