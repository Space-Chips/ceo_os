*** Begin Patch
*** Update File: /Users/timo/ceo_os/lib/core/models/family_time_models.dart
@@
 class FamilyTimeSession {
   final String id;
   final String groupId;
   final String startedBy;
   final String status;
+  final int durationMinutes;
   final DateTime? startsAt;
   final DateTime? endsAt;
   final DateTime createdAt;
@@
     required this.groupId,
     required this.startedBy,
     required this.status,
+    required this.durationMinutes,
     this.startsAt,
     this.endsAt,
     required this.createdAt,
   });
@@
       groupId: json['group_id'] as String,
       startedBy: json['started_by'] as String,
       status: (json['status'] as String?)?.trim() ?? 'pending',
+      durationMinutes: (json['duration_minutes'] as num?)?.toInt() ?? 60,
       startsAt: json['starts_at'] != null
           ? DateTime.tryParse(json['starts_at'].toString())
           : null,
*** End Patch
