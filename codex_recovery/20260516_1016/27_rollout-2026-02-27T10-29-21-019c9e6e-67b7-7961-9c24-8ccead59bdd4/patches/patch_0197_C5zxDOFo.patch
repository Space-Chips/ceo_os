*** Begin Patch
*** Update File: lib/core/repositories/premium_repository.dart
@@
   Future<int> _countRows({
     required String table,
     dynamic Function(dynamic query)? filters,
   }) async {
@@
     try {
-      var query = _client
-          .from(table)
-          .select(
-            'id',
-            const FetchOptions(count: CountOption.exact, head: true),
-          )
-          .eq('created_by', uid);
+      var query = _client.from(table).select('id').eq('created_by', uid);
 
       if (filters != null) {
         query = filters(query);
       }
 
-      await query;
-      return query.count ?? 0;
+      final response = await query;
+      return (response as List).length;
     } catch (_) {
       // Fallback to permissive mode when schema isn't migrated yet.
       return 0;
*** End Patch
